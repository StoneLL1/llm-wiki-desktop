# Exports Feature

HTML reading pages, cards, reports, export previews, and output-folder actions live here.

External Agent/Skill/Provider generation requires a trusted project. Writing
to the layout-resolved export root (native mapping `exports/html/`) requires
writable access; overwriting an existing artifact also requires backend
checkpoint and confirmation policy. All generation entries open the shared
Workflows preparation flow; Exports owns result history and preview.
