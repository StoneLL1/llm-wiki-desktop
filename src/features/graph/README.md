# Graph Feature

Knowledge graph rendering, filtering, selected-node details, and layout controls
live here. Input is the readable Source/Wiki Markdown allowed by the backend
project layout; Graph does not require compile. Restricted/read-only projects
use in-memory results, trusted writable projects may cache layouts, and an
unfinished deep scan must be labeled partial.
