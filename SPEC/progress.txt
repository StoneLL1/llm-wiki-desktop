# 进度记录 (progress.txt)

> 每完成一个重要进度节点，按时间倒序在此追加一条记录（最新在上）。
> 格式：[YYYY-MM-DD] 模块/任务 — 完成内容摘要 — 关键决策/遗留问题。

[2026-06-21] wiki-fe WIKI-FE-04 — `FILE_HASH_MISMATCH.details.baselineContent/currentHash` 接入三路 Markdown Diff（打开时 baseline / 磁盘 current / editor-Agent incoming），提供保留当前、使用传入、手动合并三选项；覆盖写前调用既有 `create_git_checkpoint`，再用 currentHash 乐观锁保存。commit `9279f0b`；全量 82 tests + lint 通过。— 关键决策：不新增后端命令；若磁盘再次变化则保留 dialog 并刷新 current/hash，绝不静默覆盖。

[2026-06-21] wiki-fe WIKI-FE-03 — 接通 wiki-BE `create_wiki_page` / `rename_wiki_page` / `request_delete_wiki_page` / `confirm_pending_action`；树顶栏新增创建入口，文件行新增重命名/删除菜单，创建/重命名表单支持 CJK 与路径标准化，重命名和删除均经 ConfirmationDialog。commit `b551aa8`；全量 80 tests + lint 通过。— 关键决策：前端不写文件或伪造 checkpoint；rename 确认后由后端创建检查点并批量重写 wikilink，delete 直接展示后端 PendingAction，取消也回传 registry。

[2026-06-21] wiki-fe WIKI-FE-02 — Milkdown 增加 28px 格式工具条：加粗/斜体/H2/链接/行内代码/引用/撤销/重做，全部调用本地 7.21.2 command API，带 tooltip、aria-label、focus-visible 与 editor loading 禁用态。commit `e619637`；全量 75 tests + lint 通过。— 关键决策：工具条与 `useEditor` 放在同一 MilkdownProvider 上下文，`onMouseDown` 保留编辑器选区；额外 `npm run build` 暴露现有跨板块 DTO 类型错误，已记 gotchas，本 loop 不越界修复。

[2026-06-21] wiki-fe WIKI-FE-01 — frontmatter 从裸 YAML `<pre>` 改为容错的两列卡片，并将设计稿 `.prose` 全套排版、wikilink pill 与 missing 状态迁移到 `.wiki-prose`；新增 2 个渲染回归测试。commit `84bf074`；全量 74 tests + lint 通过。— 关键决策：只做展示级扁平 YAML 解析，复杂/未知行保留为可读文本，不引入序列化依赖也不改写源文件。

[2026-06-21] ✅ 收敛 — wiki 后端 P0+P1 全部完成（只动 src-tauri/）。3 commits：`4956217`（W1+W2+W4 create/rename/delete 命令 + rewrite_wikilinks + apply_page_delete 双 checkpoint）、`7ed2b2a`（W3 FILE_HASH_MISMATCH baselineContent）、`9a0f17d`（双子代理审查修复）。W1+W2+W4：create CreateNew 无 checkpoint；rename 前置 create_checkpoint（批量替换命中硬边界）+ 全仓 `[[old]]`→`[[new]]` 重写（含 alias/anchor/CJK/嵌套括号）+ 快照回滚；delete 走 PendingAction(referenced_by 预览，排除自链)→confirm 后 apply_page_delete（re-verify hash + pre HighRiskOperation + post FinalResult 双 checkpoint + 失败快照恢复）。W3：file_store verify_write_mode mismatch 分支加 baselineContent（仅 UTF-8，binary 省略），改一处 6 调用点（compile×2/chat/lint×3/wiki save）全受益。双子代理审查（A 共享上下文 / B 全新上下文）合并修复：①apply_page_delete 失败仅 unstage 不恢复工作树→改为快照+恢复；②rename_page 无回滚→改为快照源页+所有引用页+恢复；③create_page page_type 未 escape→YAML 注入→route through yaml_escape_scalar（含换行）；④FILE_HASH_MISMATCH binary 文件 from_utf8_lossy 产 U+FFFD 垃圾→改为仅 UTF-8 才附 baseline；⑤find_pages_referencing 含自链→排除；⑥死代码 out.push_str(&body[..0])。验收：Rust lib 280 passed（+4 审查驱动新测）/ cargo check(gui) compiles / 前端 72 tests / lint clean / 无 println!/dbg!/console.log。— 关键决策：①apply_page_delete 镜像 import_service::apply_source_delete 两段 checkpoint 契约（pre 安全网 + post 捕获删除为真实变更），返回 pre checkpoint.commit_hash.is_some()；②Windows GUI 命令模块 #[cfg(feature="gui")] 跑单测 STATUS_ENTRYPOINT_NOT_FOUND 崩溃，delete 逻辑下沉 lib-available SearchService；③双子代理均独立发现 rename/delete 回滚缺失（A 从设计意图一致性、B 从 fresh-eyes 失败模式），证明审查有效。遗留（P2，已记 roadmap）：rewrite_wikilinks 不跳代码块/不识别路径式 [[dir/old]]/引用重写非原子写（与 extract_wikilinks 同源）；前端文件树新建/重命名/删除 UI 接线仍缺。

[2026-06-21] wiki-be W3 — `file_store::verify_write_mode` OverwriteIfHashMatches mismatch 分支新增 `details.baselineContent`（`fs::read` + `String::from_utf8_lossy`，binary/非 UTF-8 省略）。commit 7ed2b2a。— 关键决策：改 `verify_write_mode` 一处，6 个 `OverwriteIfHashMatches` 调用点（compile×2 / chat / lint×3 / wiki save）全受益——wiki save_page（PRD-READ-004 外部编辑场景）与 compile 合并冲突（PRD-WIKI-004）两条路径都覆盖，无需分别改 compile_commands / save_page。baseline 读失败（binary 文件）时 mismatch 错误仍正常返回，只是省略 baseline 字段。新增 `file_hash_mismatch_surfaces_disk_baseline_for_three_way_diff` 测试断言 baselineContent==磁盘文本 + currentHash 仍在。验收：276 backend tests + 72 npm tests + lint 全绿。

[2026-06-21] wiki-be W1+W2+W4 — 新增 create/rename/delete wiki page 三命令 + `rewrite_wikilinks` + `apply_page_delete` 双 checkpoint 契约 + 路径安全/CJK 测试。commit 4956217。— 关键决策：①apply_page_delete 原"checkpoint-before-remove"对已跟踪干净文件 created=false（git status 无变更可提交）；改为镜像 `import_service::apply_source_delete` 两段契约——pre-delete `HighRiskOperation` scoped checkpoint（安全网，HEAD 保护 before 态）→ remove_file + 失效 graph-cache → post-delete `FinalResult` scoped checkpoint（捕获删除为真实变更），返回 `checkpoint.commit_hash.is_some()`；失败 unstage 回滚。②rename 前置 `create_checkpoint(HighRiskOperation)`（全量），因"文件移动+批量引用重写"命中 CLAUDE.md 硬边界"批量替换操作前必须 Git 检查点"。③Windows GUI 命令模块 `#[cfg(feature="gui")]` 跑单测会 STATUS_ENTRYPOINT_NOT_FOUND 崩溃，delete 逻辑下沉到 lib-available `SearchService::apply_page_delete`，`execute_wiki_page_delete` 命令层仅薄包装。④`rewrite_wikilinks` 大小写无关 target 匹配，保留 alias/anchor/嵌套括号。验收：275 backend tests（含 21 search_service + 20 markdown_utils 新测）+ 72 npm tests + lint 全绿，无 console.log/println!/dbg!。遗留：W3 FILE_HASH_MISMATCH baseline 透传（本 loop 下一项）。

[2026-06-21] ✅ 收敛 — cross-cutting 后端 P0+P1 全部完成（只动 src-tauri/）。P0-1 i18n 5 prompt 点语言注入（chat/compile/export/lint service + compile_commands::provider_prompt，确定性字段保持英文）+ 新增 `utils/i18n.rs`；P0-2 `PendingAction.checkpoint_hash: Option<String>`（`#[serde(default)]` None→null 不省略，配合前端 `!== null`），compile 冲突登记时填入 commit_hash；P0-3 托盘 Show/Hide/Quit+tooltip 按 settings.language 本地化（新增 `settings_service::read_language`）；P1-1 BYOK 流式进度：新增 `tasks/byok_progress.rs::poll_with_progress` 统一 compile/chat/lint/export 四处（取消 100ms 轮询 + 进度 ≤2s append "{verb}…"，`Result<Result<T,E>, ByokCancelled>` 双层 `??` 解包）。双子代理审查（A 共享上下文 / B 全新上下文）合并修复 M1（Agent 路由 compile_prompt 补 "keep frontmatter keys/paths/headings in English" 守卫）+ M2（循环前首次取消检查）。检查全绿：Rust lib 259 passed / clippy(lib) clean / cargo check(gui) compiles / 前端 72 tests / lint clean。— 关键决策：①语言注入走参数注入而非内部读 SettingsService，避免 host-state-dependent 测试 flaky；②checkpoint_hash 用 `#[serde(default)]` + 不 skip_serializing_if 确保 None→null；③BYOK 进度用独立 helper 抽出避免 4 处重复内联；④双子代理均独立发现 M1（Agent 路由缺守卫），证明审查有效。遗留（非本 loop 范围）：前端 `PendingAction` TS 接口加 `checkpointHash` 字段 + ConfirmationDialog 渲染；托盘语言切换需重启窗口生效；full-gui clippy 21 条 pre-existing needless_borrow（HEAD 上即有，非本 loop 引入）。

[2026-06-21] ✅ 收敛 — shell+dashboard+启动页 板块 P0+P1 全部完成。P1-7..11 落地：创建 useProjectStatus hook（git_status+detect_agents+list_llm_providers module-cache）→ RightContextPanel 补 Git branch/HEAD/pending/per-agent dotstatus rows+进度条+disk；BottomStatusBar 补 agent·version+git branch·HEAD+git clean+语言；LeftSidebar Agent foot（claude·version+ChevronRight）+ Lint 橙色 badge；TopBar 项目切换 dropdown menu+返回总览+语言顺序中左+macOS ⌘K。72 tests 绿 / lint 绿。关键决策：①共用 hook 避免 3x CLI probe 重复；②git_status 已有 IPC 返回 branch/head/hasChanges 不新增后端；③disk/last-compile 缺 IPC 用 placeholder 降级。

[2026-06-21] shell+dashboard+启动页 (P0+P1 大部分) — 重写 DashboardView（health-row/统计六宫格/主题分布柱/活动时间线/快速操作四象限）+ ProjectStartView（三栏 launch：顶栏 nav/hero/filterbar/projgrid+quickaction/右侧 agentmini+BYOK+templateside/底部状态/新建项目 dialog--wide）+ 移植设计组件 CSS 到 src/styles.css — 关键决策：①托盘 close 拦截其实已接线（lib.rs:84-100），回改了 roadmap 文档而非改代码；②Dashboard 数据从 wikiStore.tree.pages + taskStore.tasks 派生，不新增 IPC；③Dashboard scan 加 hasTauri 守卫避免 jsdom 误触发 mock；④启动页 Agent 检测复用 recentProjects[0] 上下文（不改 detect_agents 后端签名）；⑤语言切换直连 i18next+localStorage。72 tests 全绿，lint 全绿。剩余 P1-7..11（TopBar/LeftSidebar/RightContextPanel/BottomStatusBar 补全）留给下次唤醒。

[2026-06-21] Goal 收尾：任务错误可见性与通知点击路由 — TDD 修复 `fetchTasks`、项目任务恢复和日志读取失败被 store 静默吞掉的问题，错误现在由调用 UI 显示 toast；日志轮询使用稳定 callback，避免翻译函数引用变化触发重复 effect。系统通知携带 `taskId/eventType`，点击后唤醒并聚焦主窗口、打开对应任务抽屉，确认等待任务可直接回到 Diff/确认流。按共享上下文和新鲜上下文各做一轮人工 review（review subagent 额度不可用），未发现新的 Critical/Important 代码问题；完整检查全绿：前端 20 files/72 tests、Rust lib 247、lint 0、无 console.log、build、fmt、clippy，GUI `cargo check --offline` 通过。— 关键决策/遗留：真实打包环境仍需人工验收 Windows/macOS/Linux 通知点击、托盘关窗、系统文件选择器、Agent/BYOK；自动更新缺少发布端点与签名公钥，保留明确“未配置更新源”状态，不伪造下载。

[2026-06-21] 原始资料替换/删除安全闭环 — TDD 新增 `.app/source-index.json`，导入确认时记录每个 `raw/sources/`/`raw/assets/` 原件对应的 `raw/extracted/` 产物；Import 页可列出来源并发起替换/删除，但后端只登记 destructive `PendingAction`，确认时重新校验项目 capability、路径范围、原件/替换件 hash 与文件类型，先建 Git 高风险检查点，再以备份/恢复事务更新原件、提取物和索引，最后提交结果。取消、注册失败、状态漂移或事务失败会清理仅属于新版本的暂存提取物，不删除与旧版本共享的产物；确认成功后自动启动可取消的 Wiki compile task，落实“确认后批量更新相关 Wiki”。完整检查全绿：前端 19 files/70 tests、Rust lib 247、lint 0、无 console.log、build、fmt、clippy，GUI `cargo check --offline` 通过。— 关键决策/遗留：未建立 artifact 索引的旧项目来源拒绝自动替换/删除，避免猜测关联文件后误删；需要人工验收系统文件选择器与真实 Agent/BYOK 编译结果。

[2026-06-21] 图谱布局线程与现有 Wiki Git 基线 — ForceAtlas2/Louvain 的拓扑着色保持不变，但无缓存布局改用依赖自带 Web Worker supervisor，React 卸载/重建会 kill worker 与清理 timer，运行中定期刷新 Sigma，1 秒收敛窗口后持久化有限坐标；无 Worker 环境保留 80 轮兼容回退，避免桌面 WebView 被同步布局卡住。另修复已识别 Wiki 项目无 `.git` 时“打开成功、首次编译才失败”：`open_project` 在注册 capability 前自动初始化仓库并建立基线提交，普通文件夹仍走显式初始化确认。检查清单全绿：前端 19 files/69 tests、Rust lib 242、lint 0、无 console.log、build、fmt、clippy，GUI `cargo check --offline` 通过。— 关键决策/遗留：Git 对已识别 Wiki 是透明安全基础，不要求普通用户理解；更新源未配置仍属发布配置阻断，原始资料替换/删除因缺少 extraction artifact 索引尚需设计成不会留下陈旧提取物的完整链路，不能只做表面删除按钮。

[2026-06-21] 编译冲突三路解决闭环 — TDD 新增 `keep_current/use_generated/manual_merge` 后端解析：保留当前会过滤冲突路径但继续应用无争议候选；使用生成版本沿用受 Git checkpoint+当前 hash 保护的覆盖；手动合并必须为每个冲突（含删除候选）提交且只提交一份 Markdown，缺失/重复/越界路径均拒绝。新增只读 conflict details IPC 返回 current/generated 内容，专用双栏 Diff 弹窗提供取消、保留当前、使用生成版本、手动编辑与应用；后端 action 在策略和状态校验成功前不消费，失败可重试，最终仍经 backup/restore、checkpoint HEAD、hash preflight、结果提交。检查清单全绿：前端 19 files/69 tests、Rust lib 242、lint 0、无 console.log、build、fmt、clippy，GUI `cargo check --offline` 通过。— 关键决策/遗留：取消表示放弃本次编译；“保留当前”保留所有冲突文件但提交无争议生成结果，符合三路选择且不静默丢弃安全候选。ForceAtlas2 worker 化与真实更新源继续审计。

[2026-06-21] 文件夹导入解析后台任务化 — 修复目录归档预览会递归展开、但 ExtractionService 旧调用只解析“目录本身”而使内部文件保持 pending 的跨层缺口；`preview_import` 现在创建可取消 `import` 项目任务，worker 稳定递归展开文件，逐文件提取并写进度/日志，把完整预览原子持久化到 `.app/import-previews/{taskId}.json`，成功后由受 project/task/type/status 校验的 `get_import_preview` 读取。前端打开任务抽屉、跟踪终态、跨项目时丢弃旧响应；失败/取消可见。检查清单全绿：前端 18 files/68 tests、Rust lib 240、lint 0、无 console.log、build、fmt、clippy；GUI `cargo check --offline` 通过。— 关键决策/遗留：URL/剪贴板是 5 MB 有界单条文本预览，保留同步快速路径；本地文件/文件夹批量解析必须走后台任务。ForceAtlas2 前端布局 worker 化、三路编译冲突与更新源仍待继续审计。

[2026-06-21] 图谱首次构建后台任务化 — TDD 将 `build_graph` 从同步返回图数据改为创建 `graph_build` 项目任务，worker 使用启动时捕获的 ProjectContext 扫描 Wiki、写进度/日志、生成 `.app/graph-cache.json` 并持久化结果；`get_graph` 只读取最后一次完整缓存，不再在 IPC handler 中隐式执行昂贵重建。前端无缓存时自动启动任务、打开任务抽屉、轮询终态并加载缓存；手动重建也走同一路径，失败/取消显示明确错误。检查清单全绿：前端 18 files/68 tests、Rust lib 240、lint 0、无 console.log、build、fmt、clippy；新增 GUI command 另经 `cargo check --offline` 通过。— 关键决策/遗留：拓扑扫描/构建已不阻塞 WebView 且可取消；前端 ForceAtlas2 80 次布局仍在渲染线程同步执行，需继续评估 worker 化；Import 文件夹批量解析仍是同步 IPC。

[2026-06-21] URL/剪贴板导入闭环与批量确认一致性 — 以 TDD 接通 Import 三种来源 tab：本地路径、URL、剪贴板 Markdown；URL 由 Rust 后端抓取，拒绝非 HTTP、localhost、私网/链路本地/保留地址，DNS 解析后固定公共 IP，禁自动重定向，限制 20 秒与 5 MB，再由前端 Readability 提取正文并转换为带 source_url/title/author、链接和图片引用的 Markdown；URL/剪贴板内容由后端命名并写入项目内 `.app/import-staging/`，进入现有 preview→confirm→compile 契约，URL 归档到 `raw/sources/links/`，确认成功清理 staging。另修复批量确认的部分落盘：现在先复验全部源/hash/目标，再复制；写入失败回滚本批已复制文件。检查清单全绿：前端 18 files/67 tests、Rust lib 240、lint 0、无 console.log、build、fmt、clippy，另加 GUI `cargo check --offline` 通过。— 关键决策/遗留：网页图片以绝对 URL Markdown 引用保留，尚未下载为本地资产；URL 抓取/Readability 当前是有界同步用户操作，文件夹批量解析与图谱首次构建仍需继续 TaskService 化。

[2026-06-21] 人工复核：错误可见性与确认弹窗可达性 — TDD 修复导入不存在路径被错误计为可归档、全局搜索/导入 IPC 失败静默、任务取消失败无提示；TaskLogDrawer 取消错误现在显示 error toast，并移除非法 button 嵌套。确认弹窗进入时聚焦取消按钮、Escape 可取消、Tab 焦点留在弹窗内，风险/检查点/预览/路径/操作文案全部接入中英 i18n；同步清理过时搜索注释。两名 review subagent 均因额度限制未产出结果，依仓库规则改做共享上下文与新鲜上下文两轮人工复核。检查清单全绿：前端 17 files/64 tests、Rust lib 236、lint 0、无 console.log、build、fmt、clippy 均通过。— 关键决策/遗留：保持取消为弹窗首焦点，避免高风险操作默认聚焦确认；继续审计 URL/剪贴板导入、Import/Graph 长任务、三路冲突处理和更新源接入，尚未宣称全量 review 完成。

======================================================================
[2026-06-21] 安全确认、全局搜索与真实导入入口 — 密钥状态 API/Provider UI 只显示“已配置”，不再返回或渲染末四位；Lint 高风险 fix 与 Chat 覆盖保存改为后端 `ConfirmationRegistry` 存储一次性执行计划，确认必须提交 backend-issued actionId，伪造 `confirmHighRisk/allowOverwrite=true` 无 action 会返回 `CONFIRMATION_REQUIRED`，确认使用后端保存的 project/issue/path/hash，取消会消费并撤销 action，写盘仍强制 Git checkpoint+hash。顶栏 Ctrl/Cmd+K/Enter 接通纯本地 `search_wiki`（keyword/filter DTO，绝不调用模型），结果弹层可打开 Wiki 页面。Import 前端移除非标准 `File.path` 假入口，改为可实际使用的本地绝对文件/文件夹路径；后端对目录稳定排序递归展开、支持 CJK/Unicode 嵌套文件并跳过 symlink，新增真实目录单 sourcePath 测试。补 KaTeX/highlight 样式；同步修正 Task14 QA 旧 52/227/8 与 CSV unsupported 描述。检查清单全绿：前端 60、Rust lib 235、lint 0、无 console.log、build、fmt、clippy 均通过。 — 关键决策：当前依赖集中没有 Tauri dialog 插件，因此先用明确路径输入修复不可用入口，不擅自加新原生依赖；原生文件夹选择器仍属人工 UX 增强。遗留：URL/剪贴板入口尚未接 UI，Import/Graph 同步工作尚未全部 TaskService 化；继续审计。
[2026-06-21] 后台任务跨项目契约二次收口 — 复审发现并 TDD 修复组合竞态：A→B→A 时 `recover_tasks` 现在跳过同进程 live task，不再把 Running 覆盖为 Failed/替换 token；`set_active_project` 返回全局任务列表，前端不再因切 B 把 A 任务隐藏。新增 `create_project_task`，compile/chat/deep-lint/export 在 command 首次可信 resolve 后把 `ProjectContext.root` 原子绑定给 task，并把 context 直接交给 worker，彻底移除依赖 React effect 同步的 ambient root；创建时持久化失败会阻止 worker 启动，后续状态持久化失败向调用方返回错误，JSON 改复用 FileStore 临时文件+fsync+rename 原子写。ProjectRegistry 保留本会话内所有“用户明确打开过”的可信 id↔root capability，以支持切换后旧任务/确认继续；前端 project scope 仍只显示当前项目并阻止迟到响应，同项目 agentRoute 元数据刷新不再误触 invalidate。settings scope 在异步语言切换后再次校验，reset 保留全局语言/主题但清项目 provider/agent；Lint/Export `list_tasks` 补齐必需 request DTO。新增 A→B→A、显式 task root、同项目元数据刷新、前端全局任务恢复契约测试；检查清单全绿：前端 59、Rust lib 235、lint 0、无 console.log、build、fmt、clippy 均通过。 — 关键决策修正：上一条“单一活动 UI capability”经组合审查证实会破坏旧项目 waiting confirmation，现改为会话内已打开项目能力集；任意未打开路径与伪造 id/root 仍被拒绝。遗留：`create_task` 通用低层命令仍用于无项目/测试任务，产品后台入口全部使用显式 root 的 `create_project_task`；继续审计高风险确认与输入/搜索入口。
[2026-06-21] 项目切换竞态与后台任务持久化修复 — 双审查后用 deferred promise 复现并修复自动 reopen 覆盖用户显式选择；ProjectStart 初始化期禁用重复提交并正确显示结构化 BackendError，确认操作也纳入 busy/error；切换时递增 project scope epoch、统一 reset wiki/chat/graph/lint/export/settings/navigation，所有项目 store 丢弃旧 scope 的迟到异步响应。ProjectRegistry 改为单一活动 UI capability，打开 B 后旧 A DTO 无法再发起新 IPC，但已启动 worker 持有的 ProjectContext 不受影响。TaskService 不再在切换时清任务/token：跨项目任务保持可见可取消，每个 task 绑定自身 root，确保 A 任务在切到 B 后仍持久化到 A；持久化 envelope 包含 task+logs，queued/running/progress/log/result/error/terminal 变更均即时写 `.app/tasks/`，重启将中断态标记 failed 并恢复日志，同时兼容旧版仅 BackendTask JSON。新增 bootstrap 竞态、wiki 迟到响应、旧 capability 撤销、跨项目任务可见/可取消/落盘隔离、非终态+日志恢复测试。检查清单全绿：前端 57、Rust lib 234、lint 0、无 console.log、build、fmt、clippy 均通过。 — 关键决策：项目切换不等于取消后台任务；新 UI 命令只授权当前项目，已启动任务继续使用启动时捕获的可信 context。遗留：重启只会恢复本次打开项目磁盘上的任务历史，未打开项目不会主动全盘扫描（符合本地项目边界）；后续继续修复真实输入入口、全局搜索与高风险确认统一。
[2026-06-21] Project context 安全边界与冷启动入口 — TDD 修复所有项目级 IPC 信任 UI 自报 `projectRootPath` 的越界风险：新增后端 `ProjectRegistry`，仅允许由 `create_project` / `open_project` 确认并注册的 projectId↔canonical root 组合，所有项目级 commands 统一经 registry 解析，未知 ID、A 项目 ID+B 项目路径、`..`/别名路径均拒绝；长任务在创建 task 前先校验上下文，避免产生孤儿任务。前端移除硬编码假项目，新增打开/新建/最近项目启动页，启动时先 `list_recent_projects` 再 `open_project` 注册后端上下文，切换项目返回启动页；语言启动偏好改存全局 localStorage，避免未注册项目阶段调用 `get_settings`。新增 3 个 Rust registry 测试、2 个 App 入口测试、1 个 projectStore 启动顺序契约测试；检查清单全绿：前端 55、Rust lib 232、lint 0、无 console.log、build、fmt、clippy 均通过。 — 关键决策：registry 只在当前进程内存中保存可信映射，重启必须重新 open；项目文件仍是 Markdown/JSON/本地文件，无数据库。遗留：当前启动页采用可跨平台复制粘贴路径输入，原生目录选择器与 Import 的真实文件选择统一在后续输入入口批次接入；双审查正在进行，若发现有效问题将在本条后续的新里程碑记录修复。
[2026-06-21] 全规格审计 — 按 CLAUDE→PRD→SPEC→APP_flow→TECH_STACK→BACKEND_STRUCTURE→FRONTEND_GUIDELINES→IMPLEMENTATION_PLAN→progress/gotchas→MVP QA 顺序完成 Task 1-14 逐项对照，并以 3 条独立只读审计证据链复核后端安全、前端/设计和 E2E 契约 — 已确认的高风险缺口包括：IPC 信任 UI 自报 projectRootPath、启动/新建/打开项目闭环不可达且默认显示假项目、真实文件选择依赖不存在的 File.path、全局搜索/URL/剪贴板入口未接、Lint/Chat 高风险确认绕过 backend-stored PendingAction、任务恢复不持久化运行态/日志、Import/Graph 长任务未 TaskService 化、文件夹导入未递归；基线为前端 52、Rust lib 229、MVP 集成 9 测试通过，但 cargo fmt --check 失败且 QA 文档仍记录旧 8/227/CSV unsupported。修复按 docs/superpowers/plans/2026-06-21-spec-audit-remediation.md 分 8 批 TDD 执行；遗留：本条仅记录审计里程碑，未宣称缺口已修复。
======================================================================
[2026-06-21] Bug 修复 — claude/codex Agent 在 App 内显示 "program not found" — 根因双层：(1) App 进程 PATH 可能不含 `%APPDATA%\npm`（GUI 启动/IDE shell 继承的 PATH 与新开终端不同），即使新开终端 `where claude` 成功，App 内 `where` 仍可能失败；(2) 即便 `where` 成功，Windows `where claude` 输出把**无扩展名 bash shim 排第一**、`claude.cmd` 排后面，而 Rust `Command::new(无扩展名路径)` 走 `CreateProcess` 无法运行该 shim → spawn 失败 → detect 报 Missing。修复：agent_service.rs `find_executable` 重写——`where`/`which` 成功后**优先选 `.cmd`/`.bat`/`.exe` 行**（CreateProcess 能经 std 内部 cmd.exe 路由运行 .cmd）；新增 `%APPDATA%\npm` 直接探测兜底（PATH 缺失时的 well-known 位置）；新增 `resolve_program()` 把裸程序名解析成全路径后传给 `Command::new`，已有路径/非 Windows 原样返回。三处 spawn 点（run_with_timeout / run_capture / run_task_streaming）全部改用 `resolve_program(program)`。新增 2 个单测：`find_executable_falls_back_to_npm_global_dir_off_path`（Windows，临时 APPDATA+假 claude.cmd，断言兜底解析 .cmd）、`resolve_program_passes_through_paths_and_non_windows_bare_names`（全平台，已限定路径原样返回、不可解析裸名原样返回而非空串）。检查清单全过：cargo --lib 229 passed（+2）、agent_service 5/5、npm test 52、lint 0、vite build 通过、无 console.log。遗留：真实浏览器内 Agent 检测需人工 `npm run tauri dev -- --features gui` 重启验收。
======================================================================
[2026-06-21] Bug 验证 — WebView2 / DLL 入口点阻断（STATUS_ENTRYPOINT_NOT_FOUND 0xc0000139）已确认修复并清理 gotchas — 核查结果：Cargo.toml `[features] default=["gui"]` + tauri 系依赖 optional、lib.rs Tauri import 与 run() 全部 `#[cfg(feature="gui")]`、build.rs `tauri_build::build()` gated、task_events.rs EventBus::Tauri 分支 gated —— feature gate 完整落实。`cargo test --lib --no-default-features --offline` 实测 **227 passed / 0 failed**，测试二进制不再在 PE 加载阶段崩溃，DLL 阻断已不再是阻断点。顺带修复一个挡路的无关借用错误（agent_service.rs:563 `String::from_utf8_lossy` 临时 Cow 未绑定 let 即 .lines() 借用，E0716）。已从 gotchas.txt 移除两条 DLL 相关记录（保留联网超时 --offline 条目，那是独立坑）。遗留：第 7 行 Task 14 进度记录里「真实 Agent/BYOK 生成需人工浏览器验收」措辞已过时，DLL 不再阻断，仅 GUI 运行态需人工验收。
[2026-06-21] Task 14 — End-to-End MVP 流程验证（集成测试 + 多格式 fixture + QA 文档） — 交付 4 项：(1) src/test/fixtures/project-fixtures.ts 多格式验证矩阵（11 个 SourceFileType 各一：pdf/docx/pptx/xlsx/csv/md/txt/html/url/image/unknown + 剪贴板 MD），parser 缺口（pdf/office/image）显式标 `unsupported`/`failed` 不静默丢，附 assertEveryFormatCovered 防新增类型漏覆盖；5 个单测覆盖矩阵。(2) src-tauri/tests/mvp_flow.rs 9 个集成测试覆盖 IMPLEMENTATION_PLAN §16 步骤 3-6 四循环：①project→wiki（create_project 骨架页齐全→import MD/TXT/CSV 在项目外 staging 防 scan_existing 把源判成自身重复→preview 归档≥1→confirm→CSV/TXT/MD 各自 ExtractionStatus 断言→apply_confirmed_manifest 带 baseline hash 让核心页覆盖+新概念页创建→关键词搜索命中→graph resolve 首建后缓存命中）②sample-wiki（复制 wiki/wiki/ ≤50 页切片到 temp 项目**不在原库测**，copy_markdown_slice_inner 用 current_dst lockstep 下降避免双嵌套，逐路径断言扫描不丢页）③AI-assisted fakes（FakeAgentRunner→claude 检测 Installed；BYOK provider 持久化 + **先 set 真实 key 到 SecretService::memory 再断言 settings.json 不含 sk-ant/api_key/bearer/token 等任意大小写秘密字段**——原版只断言不含 key 但从没存过 key，是空真断言；chat build_retrieval_context 真实本地检索返回诚实 citations→假 assistant 答案 save_answer_to_wiki 落 wiki/queries/{slug}.md 含 [[wikilinks]]→parse_agent_issues 解析 fenced JSON（issueType 用 snake_case duplicate_topic）→export build_export_prompt+write_html+append_record 落 exports/html/）④safety（compile 冲突不静默覆盖：外部改 index 后 apply_manifest 返回 conflict 且**三核心页都断言无 CANDIDATE body 落盘**；confirmed apply 状态漂移→CONFIRMATION_STATE_MISMATCH；chat 覆盖双向：未确认→FILE_ALREADY_EXISTS 内容存活 / 确认+正确 hash+git→checkpoint commit 记录；lint fix **双向**：wiki/ 外路径→LINT_FIX_PATH_OUT_OF_SCOPE / wiki/ 内 MissingFrontmatter Safe 修复→Applied+checkpoint+frontmatter 落盘；git checkpoint 真改动产 commit hash）。(3) docs/qa/mvp-acceptance.md QA 证据（命令输出、fixture 路径、sample 计数、显式 parser 限制清单：pdf/office/image 延后到 compile Agent/Skill，CSV/md/txt/html/url 提取）。(4) TDD：先写失败测试再迭代到全过。**双 review（A 共享上下文+B 全新零偏见）共识修复**：BLOCKER 空真密钥边界断言（先存 key 再验文件不含）；sample 扫描 ≥copied 过松（骨架页膨胀总数，改为逐路径校验且修 copy helper 双嵌套 bug）；CSV 状态原本写死 Unsupported 但实测 Extracted（CSV 走 md/txt 共享文本分支）——同步修正 fixture+QA 文档，不再把 CSV 当 parser 缺口；applied 返回值丢弃→断言含概念页路径；冲突测试仅查 index→补查 overview/log 无候选落盘；新增正向 lint fix 测试（原只有负向越界）。检查清单全过：npm test 52 通过、lint 0、build 通过、cargo --test mvp_flow 9 通过、cargo --lib 227 通过、无 console.log。遗留：sample-wiki 性能目标 200-500 页受限于样本仅 ~10 页（切片 cap 50 已代表，全规模由样本自身验证）；真实 Agent/BYOK 生成需人工浏览器验收；temp 目录在断言失败时不清理（用 uuid 唯一名仅磁盘占用非测试失败，未引 Drop guard 避免越界）；Loop3 fake Agent 只经 detect_agents 验证检测（service 层无独立 agent-compile 入口，compile 走 apply_manifest）。
[2026-06-20] Bug 修复 — "大量按钮点击无反应"根因与修复 — 根因：四类按钮在 shell 脚手架阶段只渲染了标签/样式、**从未接 onClick**（非 IPC 报错、非 hasTauri 静默，是纯无 handler）：①TopBar「切换项目」(TopBar.tsx:28) ②工作区头部 9 视图×2 主/次操作按钮 (AppShell.tsx:237) ③LeftSidebar「最近页面」用了硬编码假数据数组且无 onClick (LeftSidebar.tsx:67) ④WikiView 星标按钮 (WikiView.tsx:124)。修复：TopBar→导航到 dashboard；AppShell 新增 runViewAction(slot) 分派器，按 activeView 调用对应 store 动作（wiki startEdit/reload、chat createSession/saveAnswer、lint runLocalLint/startDeepLint、export startExport/openFolder、graph rebuild/load、agent refreshCapabilities、navigation），新增 requireTauri() 在浏览器态弹 toast（可见降级，不再静默 no-op）；LeftSidebar 最近页面改为消费 wikiStore.recentPages（openPage 时去重+最新在前+上限 8，点击开页）；WikiView 星标接 wikiStore.toggleBookmark（无页时 disabled）；新增 toastStore + Toaster 组件（4s 自动消失、role=status aria-live=polite）。后端：新增 toggle_bookmark IPC（models/wiki.rs ToggleBookmarkRequest/Response、search_service.toggle_bookmark 经 resolve_project_path + strip_prefix(wiki_dir) 双重路径闸 + 文件存在性校验、wiki_commands、lib.rs 注册），并修正 project_service 两处 bookmarks.json 初始化格式（原写对象 `{bookmarks:[]}` 与 reader `Vec<String>` 不一致，改为写 `[]`，否则新项目书签永不持久）。双 review 后修复（两 reviewer 共识）：chat saveAnswer 误传 targetPath=""（应为省略让后端派生 slug）、LeftSidebar 最近页面点击在浏览器态需 hasTauri 守卫+toast（补齐同类可见降级）、Toaster 关闭按钮 aria-label i18n。检查清单全过：47 前端测试通过、lint 0、tsc 干净、vite build 通过、无 console.log、search_service 12 个 Rust 单测通过（含 toggle_bookmark 持久化 round-trip + 拒绝缺失/越界路径）。新增测试：appShellActions.test.tsx（dashboard 主操作跳 import、chat 主操作浏览器态弹 toast）、wiki.test.tsx recentPages 去重测试。遗留：import 头部「添加来源」/settings 头部「保存」无文件对话框/表单可达，改为 toast 引导到视图内控件（未引入 Tauri dialog 插件，避免越界）；图谱「适配视图」头部按钮映射为 load（视图内 fit 工具仍是 GraphControls 主入口）。
[2026-06-20] 测试修复 — 225 个 Rust 单元测试全部通过（原 202 pass / 23 fail） — 修复 8 类共 23 个失败测试：1) extract_wikilinks 尾部 `]` 切片偏移 bug（`body[start..j]`→`body[start..j.saturating_sub(1)]`，j 指向第二个闭合括号）；2) count_words 预期值错误（2+3→2+4）；3) snippet_for_query 截断长查询词（snippet_end 用 `start+radius.max(query.len())`）；4) extraction 测试 output_dir 未创建 `raw/extracted/` 子目录；5) lint IndexDrift 与 DeadLink 解析不一致（统一用 build_target_lookup）；6) Windows NTFS 大小写不敏感导致 PathCase 测试无法创建 React.md+react.md 共存文件；7) GlobalSettingsFile 派生 Default 语言字段为空串；8) task_service 测试 Queued→Succeeded 非法状态迁移。关键决策：IndexDrift 改为用 resolution-key lookup 而非精确路径匹配，使 index.md 的 `[[agent]]` 能正确解析到 `wiki/concepts/agent.md`；PathCase 用 cfg!(not(target_os="windows")) 条件化；GlobalSettingsFile 改为手动 impl Default 调用 default_language()。遗留：无。
[2026-06-20] 环境修复 — WebView2 DLL 阻断 `cargo test` 问题修复 — 根因：Windows 11 Insider build 26200 内核与 System32 DLL (10.0.26100.xxxx) 版本不一致，导致 Tauri 依赖链拉入的 GUI/COM DLL (ole32/user32/dwmapi 等) 中某入口点缺失，test binary 在 PE 加载阶段 0xC0000139 崩溃。诊断：集成测试 binary task8_contracts 不 import GUI DLL 可正常跑 (5/5 pass)，lib test binary 额外 import 8 个 GUI DLL。修复：Cargo.toml 新增 [features] default=["gui"]，tauri/tauri-plugin-notification/tauri-build 改为 optional；lib.rs / task_events.rs / build.rs / main.rs 中 Tauri 相关代码加 #[cfg(feature = "gui")] gate。结果：cargo test --lib --no-default-features 可跑通 202 pass / 23 fail (失败均为已存 test fixture 依赖 wiki/ 样本数据问题)；cargo build --features gui 完整编译不受影响。遗留：cargo tauri dev 需加 -- --features gui；23 个失败测试需后续补齐 test fixture。：补齐 settings model / commands / service / Zustand store / SettingsView，接通 OS secret 状态、主题持久化、防 FOUC、语言初始化与 8 分组设置页，并在双审查后修正关窗行为、未配置 provider 的 secret 清理可见性、更新面板占位误导 — 关键决策：全局设置写用户配置目录、项目设置继续写 .app/settings.json，secret 严格留在 OS 凭据存储，关闭行为按全局 settings 实时读取执行。
[2026-06-20] Task 12 — HTML/卡片/概念图/项目报告导出（skills/html-* 驱动） — models/export.rs（ExportType→skill_folder 映射 html-beautiful-read/html-knowledge-card/html-concept-map/html-project-report，requires_source 区分单页 vs 项目级；ExportRoute/Status/ExportRoutePreference（#[derive(Default)] + #[default] Auto 规避 clippy derivable_impls），ExportRecord camelCase，6 个请求 DTO）。services/export_service.rs 重写（原 2 行 stub）：build_export_prompt 单页型经 SearchService::read_page 嵌源页 body，项目型(ProjectReport/无源 ConceptMap)经 scan_wiki 摘要+purpose.md；build_output_relative_path 产 `exports/html/<slug>-<compact-stamp>.html`（compact_timestamp 用 chrono %Y%m%d-%H%M%S 无冒号 Windows 安全），**ProjectReport 忽略源路径强制用 skill folder 名做 slug**（否则历史列表里项目报告被命名成单页会误导），defense-in-depth 断言 starts_with exports/html/ 且无 `..`；extract_html 剥 ```html/``` 围栏 + **trim_trailing_prose 砍掉 `</html>` 后模型追加的「Explanation:…」散文**（rfind case-insensitive，找不到则原样返回避免误截合法内容）；write_html 经 ProjectContext::resolve_project_path 路径安全闸 + FileStore::write_text_absolute；append_record/list_records 落 `.app/exports.json`。services/agent_service.rs 新增 html_export_invocation（text 输出 profile 同 lint/chat invocation）。commands/export_commands.rs：start_export/regenerate_export（→run_export_task→run_export），复用 lint 的 resolve_route/select_provider/WorkspaceGuard RAII/task helpers；Agent 路 create_export_workspace+html_export_invocation+run_task_streaming 捕获 stdout，BYOK 路 secret_service.get+llm_service.complete+tokio::select 100ms 取消轮询；**EXPORT_OUTPUT_INVALID 同时接受 `<html` 与 `<!doctype html`**；reveal_in_file_manager 跨平台 explorer /select,/open -R/xdg-open。templates/skills/html-*/{SKILL.md,template.html}（4×2）：SKILL.md 要求输出完整 standalone HTML（仅内联 CSS/SVG，无外部资源/脚本），template.html 纯样式（reading 调色板对齐 UI-Frontend-design token）。前端：types/export.ts、exportStore（Zustand，sortedRecords 用 useMemo 规避 gotchas 的 Maximum update depth）、ExportsView（两栏 grid minmax(0,1fr)+420px，4 类型 chip+可选源输入+Generate/Cancel，行：类型图标/标题/类型徽章/route/outputPath mono 11px/createdAt，Eye/RefreshCw/FolderOpen IconButton，镜像 LintView 的 list_tasks/get_task/openDrawer 终态 useEffect 刷新+清 runningTaskId）、HtmlPreviewPane（**iframe srcDoc + sandbox="" 无 allow-scripts**，静态预览阻断脚本注入）、AppShell 接 exports 分支+overflow-hidden 集合（activeView 已被全 9 视图分支收窄为 never，通用 else 分支模板串改 cast `activeView as string`）、i18n en/zh-CN exports.* 全量键。Rust 单测：skill_folder 映射、serde round-trip、**templates_carry_no_schema_or_lint_directives（收紧为 !schema.md && !wiki-lint && !lint rule，原裸 !lint 过松）**、prompt 含源 body、extract_html 剥围栏/留裸 HTML/**砍 trailing prose/case-insensitive 闭合标签**、write_html 拒绝 `..` 越界+建文件、record round-trip、list 缺省空、**build_output_relative_path ProjectReport 忽略源路径**。双 review 后修复（A 共享上下文+B 全新零偏见）：O1 trailing prose 砍除；O6 ProjectReport slug；N1 模板隔离测试收紧；EXPORT_OUTPUT_INVALID 接受 doctype。**跳过** C1（.app/*.json 并发，与现有 .app 约定一致）、C2/C3（标准/低风险）、其余 cosmetic。检查清单全过：cargo build --tests + clippy -D warnings + fmt --check 干净、44 前端测试通过、vite build 通过、无 console.log。遗留：真实 Agent/BYOK 导出生成同 sigma/lint 受 WebView2 DLL 阻断需人工浏览器验收；.app/exports.json 写入未加文件锁（与现有 .app/*.json 一致，v1 可接受）；concept-map/project-report 的输出质量取决于模型，本机无法跑真实生成。
======================================================================
[2026-06-20] Task 11 — Lint 本地规则/Agent 深度 Lint/修复流 — models/lint.rs（camelCase struct + snake_case 枚举：LintSeverity/Source/IssueType(8 本地+6 agent)/Fixability，LintIssue 含确定性 id `{issue_type}:{path}[:{target}]`，**LintFixOutcome 用 struct+discriminator 字段而非 `#[serde(tag)]` 枚举**规避 gotchas.txt:62 的 tagged-enum 字段泄漏，6 个 serde 测试）。lint_service.rs：run_local_lint **纯确定性不调模型**，复用 SearchService::scan_wiki 的 WikiPageMeta（wikilinks/tags/sources/aliases），死链解析复用 graph_service 的 stem/title/alias 查表逻辑（复制为私有 fn 避免跨耦合），emit 8 条规则（死链带 1-based body 行号 range、孤立页、缺 frontmatter、index 漂移、空页、重复文件名、路径大小写冲突、缺资源；index.md/log.md/overview.md 结构页免孤立/frontmatter 检查）；build_deep_lint_prompt 拼 purpose.md+schema.md+逐页摘要+要求 SKILL 输出 ```json 块；parse_agent_issues 提取 fenced JSON（无 fence 时裸 `[...]` 回退**仅在 serde 解析通过时才采纳**，避免散文里的方括号让每次深度 Lint 误败）；apply_fix **先校验 issue.path 必须以 `wiki/` 开头且无 `..`**（defense-in-depth，防 agent/前端构造的 `.app/settings.json` 路径）→ MissingFrontmatter=Safe/DeadLink=HighRisk/IndexDrift=HighRisk/其余返回 LINT_FIX_NOT_AUTO。Safe 与已确认 HighRisk 修复**都先 create_scoped_checkpoint(HighRiskOperation) 再 write_markdown_checked(OverwriteIfHashMatches)**，checkpoint 失败转 GIT_CHECKPOINT_FAILED 不吞错，写后失效 graph-cache.json + 追加 wiki/log.md。HighRisk 未确认时返回 NeedsConfirmation + PendingAction（inline 确认，不走全局 ConfirmationRegistry，保持 lint 自包含）。agent_service.rs 新增 lint_invocation（text 输出 profile 同 chat_invocation）。commands/lint_commands.rs：run_local_lint 同步；start_deep_lint 可取消后台任务（Agent 优先+BYOK 兜底，BYOK 路 tokio::select 100ms 轮询 is_cancelled），**WorkspaceGuard RAII 在 Err/取消时也清理 temp 工作区**（happy-path 单独清理会泄漏），报告落 `.app/lint-reports/<task_id>.json`；get_deep_lint_report 读回；apply_lint_fix 委派。templates/skills/wiki-lint/SKILL.md（frontmatter name:wiki-lint，判 6 维度，只输出 ```json 数组）。前端：types/lint.ts、lintStore（Zustand，applyFix→applied 刷新本地 lint；高险返 needs_confirmation→fixConfirm 内联确认，确认面板挂载时经 read_wiki_page 取 live hash 再 confirmHighRisk；runLocalLint 清空 fixStatus 防陈旧徽章）、LintView（两栏 minmax(0,1fr)+360px，工具栏 Run local/Deep lint/Cancel + 计数，左 LintIssueList 按 severity×source 分组+Lucide 图标，右 LintIssueDetails 含 path/message/target/evidence/suggestedAction/fixability 徽章/检查点说明 + 高险 before/after 预览内联确认）、AppShell 接 lint 分支 + overflow-hidden 集合、i18n en/zh-CN 全量 lint.* 键。Rust 单测 13 个（clean vault 无问题、8 条规则各一、prompt 含 purpose+页列表、parse_agent_issues 解析 fenced、safe fix 建 checkpoint+失效缓存、高险未确认返 pending 不写盘、index drift 确认后重生、strip_wikilink alias 形态）。双 review 后修复（两 reviewer 共识）：(1) apply_fix 加 wiki/ 路径边界校验防 agent/前端写盘越界；(2) agent issue id 同 type 同 path 碰撞→按 (type,path) 计数器加 `:N` 后缀；(3) temp 工作区 RAII 守卫防取消/出错泄漏；(4) 裸方括号 JSON 回退仅 serde 通过才采纳；(5) runLocalLint 清空 fixStatus；(6) confirmHighRisk 返回 outcome，视图仅在 applied 时刷新本地 lint（去掉一直为真的误导守卫）。检查清单全过：cargo build --tests + clippy -D warnings + fmt 干净、42 前端测试通过、vite build 通过、无 console.log。遗留：真实 Agent/BYOK 深度 Lint 与 sigma 一样受 WebView2 DLL 0xc0000139 阻断需人工浏览器验收；strip_wikilink 对 alias 文本本身含 `[[target|` 的极端嵌套未做正则级鲁棒（概率极低，单测覆盖单/多 alias+CJK 目标已足）；regenerate_index 会丢弃 index.md 手写结构（PendingAction 已警告用户，v1 可接受）；range.line 是 body 行号不含 frontmatter（前端按绝对行显示会偏移，cosmetic）。
======================================================================
[2026-06-20] Task 10 — Chat 会话/检索/引用/存盘 — 新增 ChatService（`.app/chats/{id}.json` 原子 JSON 持久化，create/rename/delete/list/load 全通；list 对损坏文件 eprintln 后跳过不崩启动，load 返回 CHAT_PARSE_FAILED recoverable）。检索上下文复用本地 SearchService（关键词/标签/类型/来源，不调模型）：retrieve_with_excerpts 取 top-6 命中 + ~1200 字节摘要 + purpose.md，拼成系统前言+purpose+逐页 `### title (path)` 摘要+最近 8 轮历史+最新问题；**citation = 实际喂给模型的检索页（诚实模型），不从模型输出里解析**。send_chat_message 作为可取消后台任务（TaskType::LlmRequest）：先 append 用户消息→build_retrieval_context→resolve_route（Agent 优先+BYOK 兜底，复用 compile 的 usable_agent/select_provider 逻辑）→Agent 路 `chat_invocation`（claude `--print --output-format text`、codex `exec -` stdin、openclaw `agent --message`、hermes `--prompt`）+ `run_task_streaming`（**返回类型改为 Result<String>**，捕获 LogLevel::Info 的 stdout 行作为答案文本，compile 调用点丢弃该 String 不受影响）/ BYOK 路 LlmService.complete 包在 tokio::select 内 100ms 轮询 is_cancelled→append 助手消息（content+citations+route+task_id）→Succeeded。存答案到 `wiki/queries/<slug>.md`：frontmatter type:query/title/created/session/sources（源路径走 yaml_scalar 防注入）+ Question/Answer/Sources 区段；新建页直接写并失效图缓存+追加 log.md，**覆盖既有页绝不静默**——无 allow_overwrite 返回 FILE_ALREADY_EXISTS 带 currentHash；有 allow_overwrite+匹配 expected_hash 则**先建 scoped Git checkpoint 再 OverwriteIfHashMatches**（checkpoint 失败转 GIT_CHECKPOINT_FAILED 不吞错）。前端：chatStore（Zustand，watch sendTask 终态后 reload **sendSessionId** 而非当前活动会话，避免用户中途切走被拽回；saveAnswer FILE_ALREADY_EXISTS→overwriteRequest 内联确认，confirmOverwrite 带回原 targetPath+hash 不退化成新 slug）；ChatView（220px 会话列表+线程+composer，路由徽章取最近助手消息 route，运行态脉冲+Open logs，每条助手消息「Save to wiki」+内联覆盖确认）；RightContextPanel chat 分支渲染 CitationPanel（点击 setActiveView("wiki")+openPage）。新增 9 个 Rust 服务测试（含 save_answer_overwrite_requires_allow_flag_then_hash_matches、retrieval_context_assembles_citations_purpose_and_bounded_history、list_sessions_skips_corrupt）+ chatStore/chatView 前端测试。双 review 后修复（两 reviewer 共识）：(1) confirmOverwrite 丢了 targetPath 会落到别的 slug→改回传 request.path；(2) append 前再加一次 is_cancelled 检查消除「生成完到落盘」窗口的取消竞态；(3) sources YAML 未引用→走 yaml_scalar；(4) create_scoped_checkpoint 的 `.ok()` 吞错→转 GIT_CHECKPOINT_FAILED；(5) ChatComposer Enter 发送加 `!event.nativeEvent.isComposing` 守 IME（CJK 回车确认候选字误发）；(6) reloadActive 改用 sendSessionId 防切走竞态。检查清单全过：cargo build --tests + clippy -D warnings + fmt 干净、34 前端测试通过、vite build 通过、无 console.log。遗留：真实 Chat/模型调用与 sigma 一样受 WebView2 DLL 0xc0000139 阻断需人工浏览器验收；BYOK 取消受 HTTP 响应延迟（100ms 轮询间隔）非真 abort，慢 provider 会滞后（follow-up）；run_task_streaming stdout/stderr 通道关闭竞态极小概率丢尾行（follow-up）；前端路由目前硬编码 auto 无 UI 强制 Agent/BYOK（Task 10 范围外）。
[2026-06-20] Task 9 — Wiki Graph 构建/缓存/sigma 视图 — 后端 GraphService：scan_wiki 产出 WikiPageMeta → 一页一节点（type 取 frontmatter/dir），边来自 [[wikilinks]]（大小写无关 stem/title/alias 查表）+ 共享标签共现（MAX_TAG_GROUP_FOR_EDGES=64 封顶防爆），全部 relation="related"，权重合并、去重、避免自环、度数统计；content_hash（SHA-256 over sorted path:hash）做缓存新鲜度，落 `.app/graph-cache.json`（FileStore 原子写），损坏缓存降级为重建；resolve 命中即返回（cached=true），layout 缺失或未覆盖全部节点则 layout_stale=true。Louvain 社区 + ForceAtlas2 布局在前端跑（BACKEND_STRUCTURE §19 允许前端或后端），算完通过 save_graph_layout 回写（hash 不匹配则 no-op）。前端：sigma.js v3 WebGL 渲染器包 try/catch（jsdom/headless 无 canvas 时显示占位），节点 reducer 做搜索淡化/悬停邻居高亮/选中高亮，边 reducer 做悬停高亮；GraphControls 工具栏（type/community/plain 配色、搜索、缩放/适应/重置布局/重建）、GraphInspector 在右侧面板展示选中节点（经 RightContextPanel graph 分支接入，非死代码）。关键决策/修复（双 review 后）：persistLayout 用 Number.isFinite 过滤 ForceAtlas2 在退化图（单孤立节点/重合点）产生的 NaN/Infinity，否则 JSON.stringify(NaN)=null 会污染缓存；layout_stale 扩展为「缺失或未覆盖全部节点」让部分缓存触发重算而非半缓存半随机；GraphInspector 复用 types/graph 的 PAGE_TYPE_COLORS 去重；邻居计数改 Set 以防未来平行边。已记录坑：sigma 在 import 期引用 WebGL2RenderingContext，jsdom 必须在 setup.ts 打桩否则导入即崩（连带拖垮 App.test）。检查清单全过：cargo clippy -D warnings + fmt 干净（顺手修了 5 处与 Task9 无关的旧 lint：ProjectTemplate derive Default、extraction strip_prefix、file_store 折叠 if + 删除未用 base 参、project_service or 替代 or_else、import_service vec!→数组、task8 测试 LlmService::default()→LlmService）、29 前端测试通过、vite build 通过。遗留（未做，MVP 范围外）：get_graph 每次都跑 scan_wiki 全量哈希，未来可用 mtime 清单做廉价增量；save_layout 与 rebuild 无互斥锁，极端竞态下可能丢布局（非数据损坏）；浏览器实测仍受 WebView2 DLL 阻断，sigma 真实渲染需人工验收；Task 10 未实现。
[2026-06-20] Task 8 — Agent Detection、BYOK Provider 与 Wiki Compile — 新增四类 CLI 的可替换 ProcessRunner 检测/3 秒超时/非交互沙箱协议，五类 Provider 请求与 OS credential/fake backend，隔离候选工作区、Git checkpoint+HEAD/文件哈希、PendingAction 候选 diff、失败回滚和限定路径结果提交；接入 Agent/Provider/Import compile UI、任务日志/取消、中英 i18n。双 review 后修复冲突部分落盘、确认失败卡 Running、Provider 端点串用/URL 夹密钥、401/429 分类、Agent 删除识别、临时目录清理与 search/graph stale 刷新。未实现 Task 9 图谱构建。Rust 测试目标可编译且 Task 8 集成测试实际运行通过；完整 cargo test 仍受本机 WebView2 0xc0000139 阻断。
[2026-06-20] Task 7 Step 4 纠偏 — Milkdown WYSIWYG — 前一版本用 textarea 偷换了 Task 7 Step 4 明确要求的 Milkdown/ProseMirror WYSIWYG 编辑器（IMPLEMENTATION_PLAN.md:541 + 技术栈锁定），属越权范围裁剪，已纠正。安装 @milkdown/kit @milkdown/react @milkdown/preset-gfm @milkdown/plugin-listener @milkdown/theme-nord（均为 7.21.2，peer deps 对 react 放开 *，兼容 React 19）。WikiEditor.tsx 重写：MilkdownProvider + useEditor 装配 commonmark/gfm/history/listener 插件，listener.markdownUpdated 把序列化 markdown 回喂 onDraftChange→store.draft；沿用既有乐观并发保存（expectedHash + FILE_HASH_MISMATCH→conflict），Ctrl/Cmd+S 触发保存。关键坑：(1) useEditor deps 不能挂 live draft，否则每次击键重建 ProseMirror 丢光标——改用 useState(draft) 只在挂载时取一次 seed；(2) WikiView 给 WikiEditor 加 key={selectedPath} 保证切页时 remount 重置 seed；(3) .use() 签名是 MilkdownPlugin | MilkdownPlugin[]，每个 preset 本身是数组，须 .use(commonmark).use(gfm).use(history).use(listener) 链式而非打包传数组；(4) theme-nord 的 CSS 走 exports 子路径 @milkdown/theme-nord/style.css，不能写 /lib/style.css。src/styles.css 加 .wiki-editor .milkdown 作用域覆盖 nord 主题色，对齐 app token。wikilink [[X]] 不是合法 markdown 语法，在 WYSIWYG 里以字面方括号文本呈现但往返无损（存盘 bytes 不变），解析为可点链接仍由 MarkdownReader 负责；如需在编辑器内也渲染为链接，要写 remark 插件，留作后续。检查清单全过：lint 0、22 测试通过（新增 Milkdown 挂载冒烟测试）、vite build 通过。遗留：浏览器实测仍不可行（vite 无 Tauri IPC、tauri dev 受 WebView2 DLL 阻断、.claude/ 禁改无法写 launch.json）；ProseMirror 在 jsdom 只能验证挂载不抛异常，真实编辑体验需人工浏览器验收。冲突 diff 三选一对话框（设计稿 wiki.html:821 的 baseline/current/agent 合并 UI）未实现——当前冲突 UX 是 banner + Reload/Cancel（非静默覆盖、用户显式抉择，已满足 acceptance「instead of silent overwrite」），完整 3-way diff dialog 属设计稿金标，待用户决定是否单开任务。
[2026-06-20] Task 7 review-fix pass — 针对 Task 7 自查 review 的 4 项修复。P2（真修）：wikiStore.save 成功后用 refreshed.meta 更新 tree.pages 对应项（不再等下次 reload），保存改 title/tags 后树、类型过滤、反链立即新鲜；补回归测试 test refreshes tree page meta after save（21 测试全过）。P4（设计对齐）：WikiTree 新增 fileIconFor——index.md→List（accent 色）、log.md→History、其余→FileText，对齐 wiki.html 设计稿的差异化文件图标。P1（标注）：search_wiki 命令加 rustdoc 说明它是顶栏全局搜索（⌘K）的后端基建，wiki 树过滤是独立的客户端过滤、不调用此命令；UI 接线留给后续搜索任务，命令现在就暴露以稳定后端契约（不新建无设计稿对应的搜索 UI，避免越界 Task 8）。P3（不修）：WikiTree section 标签已是 0.08em 合 CLAUDE.md；RightContextPanel 的 0.06em 是 Task 5/6 遗留、约束禁止改，仅记录不一致。检查清单全过：lint 0、21 前端测试通过、vite build 通过、cargo build --tests 通过、cargo fmt --check 干净。
[2026-06-20] Task 7 — Wiki 读/渲染/编辑/搜索 — 后端新增 models/wiki.rs（WikiPageType 推断、WikiPageMeta/WikiTreeNode/WikiPageContent/Save 请求响应 DTO，camelCase serde）、models/search.rs（SearchRequest/Result/Response）；utils/markdown_utils.rs（手写 frontmatter 解析，支持 inline/block list、大小写不敏感 key、unquote；extract_title 按 H1→frontmatter→文件名；extract_wikilinks 手扫 [[Target|Alias#anchor]] 去重大小写不敏感、strip anchor；count_words 跳过 code fence；snippet_for_query）；services/search_service.rs 完整实现 scan_wiki（构建扁平 pages + 树 + 加载 .app/bookmarks.json）、read_page、save_page（expected_hash None→CreateNew / Some→OverwriteIfHashMatches 乐观并发，成功后 invalidate graph-cache.json + append wiki/log.md）、search（type/tag/source 过滤 + 关键词评分 title 100/tags 40/sources 30/aliases 30/content 10，单次读盘）。新增 4 个 IPC 命令 scan_wiki/read_wiki_page/save_wiki_page/search_wiki 接入 lib.rs。前端：types/wiki.ts；features/wiki/ 下 wikiStore（scan/openPage/startEdit/cancelEdit/save/reload，FILE_HASH_MISMATCH→saveState conflict）、WikiTree（260px、过滤输入 + 类型 pills + 递归行）、MarkdownReader（react-markdown + remark-gfm/math + rehype-katex/highlight；自定义 urlTransform 放行 wikilink:// scheme、自定义 a 解析 stem/title/alias→path，缺失→wikilink--missing）、WikiEditor（textarea 源码编辑器 + 保存状态徽章 + 冲突 banner）、RelatedPagesPanel（元数据 dl + tags + 反向链接 + sources）、WikiView（双栏 + 44px 头部面包屑/模式切换/星标）；接入 AppShell（wiki 视图非 padding overflow-hidden）+ RightContextPanel（wiki 模式渲染 RelatedPagesPanel）。i18n en/zh-CN wiki.* 全量键。20 前端测试通过（含 wikilink 解析、alias、缺失、保存冲突、re-read 失败不掩盖成功、navigate-mid-save 不覆盖）。关键决策：(1) 不引入 Milkdown，用 textarea markdown 源码编辑器（与设计稿编辑窗一致，Milkdown 是重型依赖，留待后续 WYSIWYG 任务）；(2) react-markdown 新增依赖（remark/rehype 插件已预装，缺 React 渲染器）；(3) save_page 不接入 GitService checkpoint——与 file_commands.rs::save_file（Task 5/6）一致，单页交互式编辑走乐观并发 hash guard + 原子 tmp 写 + log.md append，destructive/batch（删除/批量替换/Agent 自动修复）才接 Git，属既定范围不发散；(4) Tauri wiki 命令为同步阻塞，样本库数百页扫描足够快，与现有 scan_project 等命令同模式，长任务可取消规则针对 Agent/import/lint 而非秒级扫描。双 review 修复：wikiStore.save 加 selectedPath 守卫（导航中途不覆盖）+ re-read 失败不掩盖成功保存；preprocessWikilinks alias 也 strip #anchor；WikiTree section 标签 tracking 0.06em→0.08em 对齐设计。检查清单全过：lint 0 warning、20 测试通过、vite build 通过、cargo build --tests 通过、cargo fmt --check 干净、无 console.log。遗留/阻塞：本机 cargo test 仍因 WebView2 DLL（0xc0000139）无法执行，Rust 仅编译验证；KaTeX CSS + highlight.js 主题未 import（结构渲染 OK，公式/代码块样式待后续美化任务）。
[2026-06-19] Task 6 review-fix pass — 修复 review 发现的两项问题：MAJOR-1 跨项目任务污染（set_project_root 恢复新项目任务前未清旧项目内存缓存，list_tasks 会混入两个项目任务）→ 新增 CancellationRegistry::clear()，set_project_root 在恢复前清空 tasks HashMap + cancellation registry；补回归测试 test_set_project_root_clears_previous_tasks（切项目后 list 为空、旧 token 不再可达但 AtomicBool 未被误取消、关项目 None 也清空）。MINOR-2 useTaskEvents 在非 Tauri 环境每次挂载无谓发起 10 个 listen() 被 reject → 加 hasTauri() 守卫与 taskStore 同源。检查清单全过：lint 0 warning、10 前端测试通过、vite build 通过、cargo build --tests 通过、cargo fmt --check 干净、无 console.log。遗留：新增 Rust 测试因 WebView2 DLL blocker（0xc0000139）无法本地执行，仅编译验证。
[2026-06-19] Task 6 — 可取消后台任务 — 实现 TaskService 完整生命周期（queued/running/waiting_for_confirmation/cancelling/cancelled/succeeded/failed，稳定 UUID、append-only 日志、validate_transition 状态机）、CancellationRegistry（Arc<AtomicBool> 共享 token）、EventBus 枚举（Tauri/Noop/TestCapture，避免 trait 非 dyn-compatible）、20 个 TaskService 单测 + 4 个 cancellation 单测。新增 7 个 IPC 命令（create/list/get/cancel/get_logs/remove_completed/set_active_project）。前端：TaskLogDrawer（紧凑列表+日志面板+进度条+取消按钮，2s 轮询非终态日志）、TaskActivityButton、taskStore（applyBackendEvent 事件 reducer）、useTaskEvents hook（订阅全部 task://* 频道 → 实时更新 store + OS 通知，项目切换时恢复持久化任务）、i18n（en/zh-CN 任务状态/类型/通知文案）。Tray：托盘菜单（Show/Hide/Quit）+ 关闭最小化到托盘 + 左键单击恢复窗口；Tauri notification 插件接入。持久化：terminal transition 自动写 .app/tasks/<id>.json，set_active_project 恢复并 force-fail 中断态任务（含 WaitingForConfirmation）。关键决策：移除冗余的 Rust NotificationService（dead code），通知统一走前端事件驱动 + i18n；事件总线是后端通知边界。双 review 修复：前端事件订阅缺失、set_result 补发事件、remove_completed 清理 JSON+token、recover 覆盖 WaitingForConfirmation、tray icon unwrap 守卫、taskStore 加 hasTauri 守卫避免浏览器/测试环境误调 invoke。遗留/阻塞：本机 cargo test 因 WebView2 运行时 DLL 缺失（STATUS_ENTRYPOINT_NOT_FOUND 0xc0000139）无法执行，仅靠 cargo build/check 验证；真实后台 worker spawn 与取消轮询留待后续任务接入具体业务流程。
[2026-06-19] Task 5 review hardening — 根据双 review 反馈加固导入确认边界：confirm 阶段重新校验源文件 hash、分类路由与 Rename 状态，拒绝篡改 archivedPath、预览后源文件变化、以及覆盖既有归档目标；ExtractionService 将 MD/TXT/CSV/HTML 提取文本持久写入 raw/extracted/；前端确认成功后清空 preview 并补 confirm IPC 回归测试 — 遗留：PDF/DOCX/PPTX/XLSX 真实 parser 与 TaskService 可取消化仍需后续任务完成。
[2026-06-19] Task 5 review fixes — 修复导入预览到确认链路：前端 ImportView 接入 preview_import/confirm_import_preview IPC；ImportFileEntry 增加 sourcePath；confirm_import_preview 在确认时复制源文件到 ProjectContext 校验后的 raw/sources 或 raw/assets 归档路径，并用 FileStore 写入完整 .app/import-conflicts.json；精确重复统一为 Skip/LinkToExisting，不再覆盖既有归档目标 — 已补前端 IPC 回归测试与后端确认归档测试；遗留：项目根仍来自请求 DTO，后台可取消任务化需后续架构收口。
[2026-06-19] Task 5 深度审计与优化 — 8 项修复：1) extract_html_title 重写支持带属性标签 + 偏移量 bug 修复；2) is_valid_url 限制 http/https 协议（拒绝 file:///javascript:/data:/ftp:）；3) file_hash_fast 失败回退改用唯一标识 !nohash: 防误判重复；4) ImportView 硬编码 10+ 字符串全迁 i18n；5) allow_duplicates 标志在 preview_import 中生效；6) extract_batch 错误保留 BackendError.code；7) classify_file/target_archive_dir/deterministic_rename 提取为自由函数消除跨服务耦合；8) scan_existing 改为 hash→path 映射，ExactDuplicate 冲突填充 existing_path — 新增 4 测试（allow_duplicates 行为、URL 协议限制、HTML title 属性/多行属性），91 Rust + 8 前端测试全过，lint/build/fmt 全绿，gotcha 追加 extract_html_title 索引偏移陷阱。
[2026-06-19] Import preview pipeline / Task 5 — 实现导入预览管道：文件分类（11 种 SourceFileType）、归档路由（raw/sources/ 子目录 + raw/assets/）、SHA-256 哈希冲突检测（ExactDuplicate 跳过 + NameCollision 确定性重命名）、ExtractionService（MD/TXT/CSV/HTML 直接提取；PDF/DOCX/PPTX/XLSX 返回 Unsupported 不中断 batch）、URL 验证工具、@mozilla/readability 前端适配器、ImportView 完整预览 UI（文件列表 + 状态图标 + 元数据 + 文本预览 + 冲突横幅 + 确认编译闸门）、parser-adapters.md 架构文档 — 关键决策：confirm_import_preview 是 compile 前置闸门（按钮在无 preview 或确认中禁用）；Extractor trait 接口已设计但 binary format adapters 留待后续任务；22 个新 Rust 测试 + 87 全量通过，8 前端测试通过，lint/build/fmt 全部通过
[2026-06-19] Git safety / Task 4 — 实现 Git-backed safety：FileStore 增加 SHA-256 哈希校验写入、Git 初始化/状态/checkpoint/diff 后端服务与命令、普通文件夹初始化确认动作改为后端存储并执行、前端接入确认弹窗与项目状态更新 — 关键决策：确认动作快照路径集合与文件内容哈希，执行前重新校验；已有脏 Git 仓库在文件迁移/写入前创建高风险操作 checkpoint，完成后再创建最终结果 checkpoint。
[2026-06-19] UI design alignment — 完整对齐 UI-Frontend-design/ 文件夹设计：字号/密度/组件高度对齐 app.css token、@fontsource 打包三款字体（Inter/JetBrains Mono/Source Serif 4）、左侧栏重构为三区段（主视图/工作流/最近页面 + Agent 脚）、右侧面板重构为项目信息（路径/索引状态/执行路径/背景任务）、CLAUDE.md 与 AGENTS.md 设计对齐原则重写为全文件夹参考（HTML 结构 + CSS token + 图标 + 交互行为）— 关键决策：设计对齐不只 CSS，页面组件拆分与 DOM 层级以 dashboard.html 为准；右侧面板不再依赖 AppView prop，改为项目状态驱动；Settings 入口仅保留 TopBar 齿轮图标。6 前端测试 + 31 Rust 测试全过，lint/build 通过。
[2026-06-19] Project lifecycle / Task 3 — 实现项目生命周期（create/open/scan/list-recent/remember-recent）、5 套模板（general/research/reading/personal-growth/business 的 purpose.md+schema.md）、FileStore 原子写、普通文件夹 PendingAction（确认前不移动文件）、Dashboard 项目摘要 UI（Task 3 范围）— 关键决策：open_project 宽松判定 wiki 项目以兼容样例库与 Obsidian（.obsidian 当外部目录）；RecentProjects 用原子写 + 规范化去重 + 上限 20；scan_project 命令对路径做 canonicalize。Review 修复：archive_target_for 补 links/、count_files 只计文件、PendingAction preview 递归枚举嵌套文件（避免低估后续迁移量）、scan_project 路径规范化。31 Rust + 6 前端测试全过，lint/build 通过。残留：RecentProjects 读错时静默降级（非关键状态）、GraphState 空缓存靠字符串哨兵判定、Agent 路线硬编码 Unconfigured（待 Task 5+）。
[2026-06-19] Frontend shell / Task 2 - Built Codex-like desktop shell split into top bar, left navigation, workspace outlet, right context panel, and bottom status bar with i18n labels and mutable project/task status stores - Review findings fixed; npm test, lint, build, console-log scan, and diff check completed.
[2026-06-19] Backend contracts / Task 1 — Added backend error model, ProjectContext path boundary, confirmation and task DTOs, missing service skeletons, and time utility — Full cargo tests remain blocked by Cargo dependency resolution timeout; lightweight Rust metadata/format checks pass.
[2026-06-19] Implementation planning — Created root IMPLEMENTATION_PLAN.md from SPEC requirements — Plan decomposes MVP into agent-ready milestones with safety gates, file ownership, checks, and acceptance criteria.
[2026-06-19] 应用脚手架 — 建立 React/Vite 前端目录、Tauri/Rust 后端模块骨架、基础测试与配置文件 — 仅搭建文件结构和占位边界，未实现真实业务流程。
[2026-06-19] 项目初始化 — 初始化仓库文档基线与 CLAUDE.md / AGENTS.md，建立 progress.txt 与 gotchas.txt 记录机制。
