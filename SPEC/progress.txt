# 进度记录 (progress.txt)

[2026-08-12] Decision Gate H Batch H1 pinned Skill and typed lint repair contract — Replaced project-overridable wiki-lint guidance with the built-in `builtin.wiki-lint@2026-08-12.1` dual analyze/repair Skill, mirrored its SHA-256 and strict DTOs in Rust/TypeScript, preserved the legacy BYOK analysis-array adapter, and added bounded prompt/output, selection-revision, portable path, and backend Finding-correlation validation under the existing LintService facade — Key decision or open issue: both reviews are clean and full `npm run check` passed; Agent lint routing and project mutation remain disabled until later batches, with H2 next.

[2026-08-12] Decision Gate H Batch H0 authority and fail-closed freeze — Recorded approved 1A/2A/3A/4A across the sole-authority Workflows spec and current PRD/SPEC/backend/app-flow contracts, marked the old broker/no-tools Path B prerequisites as superseded while preserving repository Hard Rules, and added regressions proving Agent Health and repair remain unreachable with a fixed-three Overview — Key decision or open issue: WF-D01's product choice is closed, but runtime remains disabled until later H batches; H1 is the next allowed dependency.

[2026-08-12] Decision Gate H planning graph closure — Refreshed the repository Graphify output after the Agent Deep Lint / repair plan to 13,211 nodes, 36,875 edges, and 600 communities — Key decision or open issue: the sandbox reproduced the already-recorded WinError 5 and the identical approved local update succeeded; Graphify retained the known 756 fail-closed nodes from 82 still-present excluded files and reported stale community labels, neither of which changes the implementation plan.

[2026-08-12] Decision Gate H Agent Deep Lint / repair implementation plan — Completed a current-code, Graphify-backed executable plan with an explicit reuse inventory, product-decision alignment, typed Skill/Agent contracts, candidate-workspace allowlist, end-to-end state machine, eight dependency-ordered H batches, negative/performance matrices, and Batch 7 unblock gates — Key decision or open issue: no production code changed and no further product decision is required; implementation must reuse the existing Agent, Lint, Compile candidate/Diff, Git, confirmation, Workflow queue/task/history and UI-2/UI-3 boundaries.

[2026-08-10] Workflows remediation Batch UI-6 graph sync — Refreshed the final UI-6 AST graph after executable, evidence, and logging closure to 13,125 nodes, 36,788 edges, and 597 communities — Key decision or open issue: the update required the documented Windows sandbox escape after `WinError 5`; graphify retained the known fail-closed 756 nodes from 82 still-present excluded files and reported community labels pending a future explicit semantic relabel, neither of which changes UI-6 acceptance.

[2026-08-10] Workflows remediation Batch UI-6 closure — Completed UI-6 without a standalone Finding ID: shared icon-and-label status treatment across Overview/History/Task detail, duplicate-detail and Attention status removal, stable five-row loading skeleton, 88px desktop/History geometry with content-safe Overview growth, 760px Workflows container reflow, short scoped state/overlay/result transitions, and explicit reduced-motion disablement — Key decision or open issue: both independent final reviews are CLEAN after closing responsive, scope, semantic-color, skeleton, high-content, and virtualizer-contract findings; focused tests pass 100/100, real Tauri WebView2 evidence passes at 1440×900 English/Chinese, 820×900, and 1181×900 with maximum persisted panes, and the final from-scratch `npm run check` passes in 511.5 seconds including 1,020 Rust unit tests; one unrelated Import V2 large-queue timeout reproduced only under an earlier loaded full run and passed its isolated rerun before the green gate, while Decision Gate H remains open and Health route availability/copy is unchanged.

[2026-08-10] Workflows remediation Batch UI-5 History closure — Completed the UI-5 acceptance surface (no standalone Finding ID; consumes the bounded WF-P06 history contract) with current-identity server filters/cursors, linked but individually numbered retry attempts, compact typed outcomes for all three workflow kinds, locale-aware date/duration, distinct empty/loading/load-more/error/stale recovery, unique retry names, isolated History state, and removal of the duplicate header entry — Key decision or open issue: two independent reviews completed and all ten findings were fixed; focused frontend/performance tests pass 140/140, the bounded Rust summary contract passes 1/1, real Tauri WebView2 evidence passes at 1440×900 and 820×900 with no horizontal overflow, and the final from-scratch `npm run check` passes in 493.1 seconds with 122 frontend files / 1,011 tests plus all Rust unit/integration, doc, capability, lint, and build checks; graphify is current at 13,113 nodes / 36,769 edges / 589 communities, while Decision Gate H remains open and Health route availability is unchanged.

[2026-08-10] Workflows remediation Batch UI-3 closure — Closed WF-U03 with semantic pipeline stages, ordered and fail-closed confirmation review, per-file two-way/three-way diffs, typed Update Wiki/Health/Generate result presenters, explicit failed/interrupted/rollback mutation facts, stale-review blocking, bounded lazy pagination, and real Tauri WebView2 evidence at 1440×900 plus 820×900 stress reflow — Key decision or open issue: both final reviews are CLEAN for safety/correctness; focused frontend tests pass 52/52, focused Rust UI-3/lazy-diff tests pass 1+7, and the final from-scratch `npm run check` passes in 603.7 seconds with 122 frontend files / 984 tests, 1,016 Rust unit tests plus all integration/doc/capability/lint/build checks; graphify is current at 13,065 nodes / 36,653 edges / 596 communities, Decision Gate H remains open and unchanged, and repeated full Git-blob/current-file reads during multi-page diff review remain a non-blocking performance follow-up.

[2026-08-10] Workflows remediation Batch UI-2 real-app evidence — Captured the final non-Health Update Wiki Preparation in a real Tauri v2 WebView2 at 1440×900 and an 820×900 stress-reflow viewport, with top/bottom views proving the ordered eight-step surface, collapsed technical details, bounded scope list, single vertical work-surface scroll, no horizontal overflow, and a reachable primary action — Key decision or open issue: the shipping 1120px minimum window remains unchanged outside UI-2; the evidence intentionally shows truthful Git-dirty prerequisite blocking, and Health route copy/availability remains excluded under Decision Gate H.

[2026-08-10] Workflows remediation Batch UI-2 non-Health closure — Closed WF-U02 outside the Decision Gate H route-copy/availability scope with the required eight-step Preparation decision sequence, bounded searchable scopes, truthful empty/invalid/no-change and generated-output states, first-run confirmation, one-run route overrides, identity-guarded Settings/Import draft return, and duplicate-start prevention — Key decision or open issue: both final reviews are CLEAN; focused tests pass 124/124 before final review and 122/122 after review closure, the final from-scratch `npm run check` passes in 512.1 seconds with 122 frontend files / 976 tests, 1,006 Rust unit tests plus all integration/doc/capability/lint/build checks; Health Agent choices remain filtered and concrete Agent detail remains hidden as an interim fail-closed boundary, while Decision Gate H remains open for all Health route product copy/availability decisions.

[2026-08-10] Workflows remediation Batch UI-1 overview closure — Closed WF-U01 with the required attention-first / fixed-three / recent-five hierarchy, single-primary action ownership, stable row-owned queue continuation and completed-task targets, same-kind semantic attention priority, locale-aware dates, and real Tauri WebView2 evidence at 1440 and an 820 stress-reflow viewport — Key decision or open issue: both independent final reviews are CLEAN; focused frontend tests pass 36/36, focused Rust overview tests pass 3/3, and the final from-scratch `npm run check` passes in 649.2 seconds with 122 frontend files / 963 tests, 1,006 Rust unit tests plus all integration/doc/capability/lint/build checks; the shipping `minWidth: 1120` remains unchanged because global window behavior belongs to UI-4, while Decision Gate H still blocks later remediation batches.

[2026-08-10] Workflows remediation Batch 6 large-data surface closure — Closed WF-P06 with bounded and fully reachable Preparation/History rendering, server-filtered summary pagination with cached ordering, stale-request and identity guards, incremental progress upserts, and per-file lazy Diff paging whose final serialized response stays within 256 KiB — Key decision or open issue: both independent reviews were fully addressed, focused frontend tests pass 104/104 plus targeted Rust index/summary tests, the final from-scratch `npm run check` passes in 528.7 seconds with 122 frontend files / 951 tests and 1,004 Rust unit tests plus all integration/doc/capability/lint/build checks, and graphify is current at 12,982 nodes / 36,424 edges / 606 communities with the known fail-closed retained-node observation.

[2026-08-10] Workflows remediation Batch 5C bounded progress persistence closure — Closed the Batch 5C stop/go persistence hot path with per-task revisioned lanes, 250 ms leading/trailing observational snapshots, persist-before-event Barriers, recoverable write failure, durable authority upgrade, path-safe rollback, and deterministic stale-writer/cross-project isolation tests while leaving Batch 6 untouched — Key decision or open issue: Batch 5A explicitly marked GO; TaskService 59 tests, queue 24, recovery 9, compatible layout 4, and both final reviews are CLEAN; the final release reference is 177.451 ms mean / 187.483 ms p95 / 3.65% CV for 500 updates (about 90.0% below the 5A mean), the from-scratch `npm run check` passes in 668.5 seconds, and graphify is current at 12,927 nodes / 36,234 edges / 581 communities with only the known fail-closed retained-node observation.

[2026-08-10] Workflows remediation Batch 5B final graph hash sync — Re-ran `graphify update .` after rustfmt-equivalent formatting of the already gated Batch 5B Rust file; final graph state is 12,865 nodes, 35,939 edges, and 585 communities — Key decision or open issue: executable semantics and the passed 661.1-second full gate are unchanged; targeted rustfmt check passes for every Batch 5B Rust file, while the repository-wide fmt check still reports only a pre-existing untouched import-test formatting difference.

[2026-08-10] Workflows remediation Batch 5B graph and final-gate closure — Refreshed graphify after the final code/test state to 12,864 nodes, 35,938 edges, and 581 communities, then passed the required from-scratch `npm run check` in 661.1 seconds with 122 frontend files / 946 tests, the 988-test Rust unit set (984 passed, 4 ignored), all Rust integration/doc tests, and every import/capability/lint/build check — Key decision or open issue: the first gate exposed only a stale cold-parallel fixture assumption about the newly required post-probe target re-resolution; the corrected fixture preserves the four-before-any-release assertion, its exact test and full gate pass, both reviewers reconfirmed CLEAN, and graphify's fail-closed retention of 756 nodes from 82 still-present files remains a non-blocking graph-maintenance observation.

[2026-08-10] Workflows remediation Batch 5B cross-request route-cache closure — Closed the Batch 5B portion of WF-P05 with a bounded 30-second Agent-only cache keyed by exact spawn target/file identities, PATH generation, settings and project identity; added same-key single-flight, fail-fresh epoch/target revalidation, explicit-only manual refresh, parallel cold probes, and separate Agent/inventory/hash measurement while leaving provider secrets and all authority/content/apply facts request-fresh — Key decision or open issue: Batch 5A explicitly marked GO; both shared- and fresh-context final reviews are CLEAN after fixing automatic-read invalidation, shim/actual-target mismatches, duplicate concurrent probes, stale in-flight returns, and resolver-switch races; focused Rust/frontend suites pass and the final 1,000-Markdown release reference records warm p95 930.893 ms, CV 2.24%, zero warm Agent subprocesses, and every cold Agent phase within slowest probe +500 ms; Batch 5C and Batch 6 remain unimplemented.

[2026-08-10] Workflows remediation Batch 5A request-cost closure — Closed WF-P05 overview amplification with one request-scoped source/Markdown/route evaluation, one read/hash per unique Markdown file, Overview reuse, and four parallel cold Agent probes; preserved independent prepare/start freshness, DTO/IPC/persistence and trust/write/Git/path/confirmation contracts — Key decision or open issue: both independent reviews are CLEAN after fixing real root-walk accounting, streaming resource parsing, mutually exclusive performance phases, and Agent metadata; focused suites pass 34 tests plus release references, final from-scratch `npm run check` passes in 501.6 seconds with 982 Rust unit tests plus all integration/doc tests, and stable release measurements make Batch 5B GO (Agent ~59.6%, overview p95 1,181.474 ms) and Batch 5C GO (persistence 66.27%) without implementing either follow-up batch.

[2026-08-10] Workflows remediation Batch 4 graph closure — Ran `graphify update .` after code, tests, reviews, and documentation; the incremental AST graph now contains 12,756 nodes, 35,535 edges, and 578 communities with updated graph.json, graph.html, and GRAPH_REPORT.md — Key decision or open issue: graphify completed successfully without LLM/API use; it fail-closed preserved 756 nodes from 82 still-present files that left the scan corpus and reported community labels from the prior 585-community set, so a future graph-maintenance pass may deliberately full-reextract/relabel if those exclusions are confirmed, but no Batch 4 controller/service relation is left stale.

[2026-08-10] Workflows remediation Batch 4 event, drawer, notification, and route-local hot-path closure — Closed WF-P01–WF-P04 with 100ms identity-filtered ordinary-progress batching, terminal/queue boundary reconciliation that is single-flight with one trailing invoke per logical wave, current-filter History reloads, terminal-monotonic run merging, closed-drawer heavy-body unmount plus selected/bounded log derivation, safe localized notification eligibility before epoch-scoped permission checks, explicit Settings permission actions, and project-generation-bound inactive route ownership — Key decision or open issue: DTO/IPC, backend authority/trust/write/Git/confirmation contracts, and product decisions remain unchanged; 50 ordinary events add 0 overview/history invokes and one short-window commit, 200 events/2s stay at <=25 workflow commits with local progress p95 <=150ms and terminal <=250ms, closed drawer 1,000 events and non-eligible notification 1,000 events do 0 heavy work/permission work, Workflows main/right panel stay at 0 commits for 100 unrelated updates, both independent reviews are CLEAN after all findings were fixed, focused suites pass 196 tests across 13 files, full frontend passes 122 files/945 tests, and final from-scratch `npm run check` passes in 468.0 seconds with 978 Rust unit tests plus all integration/doc tests.

[2026-08-10] Workflows remediation Batch 3 frontend identity, live confirmation, surface, and operation-state closure — Closed WF-C07, WF-C08, WF-C09 and the Batch 3 portion of WF-U02 with atomic overview/history snapshots, current-project plus authority-revision guards on every success/error/pending/navigation commit, action-keyed selected-waiting hydration, bounded pre-access event buffering, surface-derived right-panel truth, and operation-keyed pending/error recovery — Key decision or open issue: backend authority/trust/write/Git/confirmation contracts and IPC DTOs remain unchanged; same-root stale commits are 0, selected waiting detail is 1 and rapid duplicates coalesce to 1 while non-selected detail is 0, pending events cap at 256, per-progress refresh amplification remains intentionally deferred to Batch 4, both independent reviews are CLEAN after all findings were fixed, focused suites pass 117 tests across 9 files, final `npm run check` passes from scratch in 589.3 seconds, and `graphify update .` rebuilt 12,736 nodes / 35,476 edges / 585 communities.

[2026-08-09] Workflows remediation Batch 2 cancellation, trust, dispatch, and confirmation closure — Closed WF-C06 and WF-C10 with cancellation-first shared finalization, owner-wide queue freeze, generation-bound external/report publication barriers, fail-closed trust revocation, and full-tuple digest-bound confirmation recovery using single validated candidate snapshots — Key decision or open issue: readable Local Quick work may continue memory-only after revocation while mutating/external work stops and re-grant never auto-runs parked work; both independent reviews are CLEAN after all valid findings were fixed, focused suites pass 57 workflow integration tests plus launch/confirmation/AppState unit coverage, and the final from-scratch `npm run check` passes with 120 frontend files/891 tests and 978 Rust unit tests plus all integration/doc tests.

[2026-08-09] Workflows remediation Batch 1 layout and backend correctness closure — Closed WF-C01–WF-C05 with layout-root-derived and owner-serialized preferences, current-runtime retry IDs plus locked canonical-owner rechecks, readable-root Health prerequisites, and bounded preparation records with deterministic idempotent recovery after tombstone pressure — Key decision or open issue: historical misplaced compatible preferences remain preserved and ignored, no-root state stays identity-isolated memory-only, both independent reviews are CLEAN after all valid findings were fixed, focused suites pass 72 integration + 3 Batch 1 unit tests with the Batch 0 cost guard preserved, and the final from-scratch `npm run check` passes with 120 frontend files/891 tests, 65 capability-tool tests, and 970 Rust unit tests plus all integration/doc tests.

[2026-08-09] Workflows remediation Batch 0 contract and measurement baseline — Added deterministic 200/1,000-event, 1,000-Markdown, 500-progress, 10,000-option/history, and 500×20KB Diff fixtures; froze pre-fix witnesses for WF-C01, WF-C06, WF-C07, WF-P01, WF-P02, WF-P03, WF-P05, and WF-P06 through real controller, WorkflowPreferences, AppState revoke/dispatch, coordinator claim/first-stage, preparation, TaskService persistence, and EventBus paths — Key decision or open issue: Batch 0 intentionally changes no product behavior and closes no production Finding; both independent reviews are CLEAN, focused suites pass, and the final full `npm run check` passes with 120 frontend files/891 tests, 65 capability-tool tests, and 967 Rust unit tests plus all integration/doc tests.

[2026-08-09] Workflows correctness, performance, and UI remediation plan — Added a detailed Agent-facing execution plan that maps 21 audit findings into contract baselines, layout/persistence fixes, cancel/trust/dispatch and confirmation-recovery hardening, identity-safe live detail, event/drawer/notification hot-path repair, mandatory request-scoped overview de-duplication plus evidence-gated cross-request optimization, bounded large-data handling, and a separate six-part UI/UX program — Key decision or open issue: correctness and safety precede performance and visual shaping; Health Agent remains an unresolved product/security decision gate that requires either authoritative BYOK-only alignment or a separately approved credential/no-tools security project, while every high-risk executable batch requires two reviews and a full `npm run check`; this milestone is documentation-only and changes no application code.

[2026-08-07] Import URL operation-bound network authority closure — Closed second-pass review findings across builtin, installed-pack, browser-fallback, collection, and QualityGate paths: split page-navigation from asset-CDN allowlists, upgraded reviewed HTTP platform links to HTTPS, consumed private-target confirmation once per operation while sharing it only inside that fallback chain, passed exact private authority to the browser runtime, and recorded formal validation failures — Key decision or open issue: `npm run check` passed from the beginning (118 frontend files / 881 tests, 65 capability-tool tests, 964 Rust unit tests plus all integration/doc tests); prior real probes remain WeChat success and Bilibili subtitle/ASR attention rather than URL rejection.

[2026-08-07] Import URL Fake-IP/TUN security closure — Replaced the initial broad Fake-IP exception after two independent reviews with reviewed HTTPS platform-host allowlists plus the existing item/origin/DNS-bound private-target authorization flow, wired that grant into native fallback engines, canonicalized trailing-dot hosts before DNS pinning, persisted per-route failure codes, and verified the two real failed URLs with application engines — Key decision or open issue: the WeChat article now parses at full text coverage with 20 localized assets; the Bilibili URL reaches its media policy and correctly waits for subtitle/ASR capability instead of failing URL validation, while arbitrary Fake-IP hosts and cross-host redirects never receive silent trust.

[2026-08-07] Import URL Fake-IP/TUN compatibility repair — Audited the URL input-to-batch-to-engine chain, reproduced two recent failures from persisted Import evidence, proved both public hosts resolved to Mihomo-style `198.18.0.0/15` Fake-IP addresses and were rejected before HTTP, then added a TLS/domain/all-Fake-IP-only SSRF exception, safe network-error visibility, and unique recovery actions — Key decision or open issue: Batch E/F changed operation ownership and hid per-item task logs but did not introduce the July URL policy; real application-engine probes now parse the WeChat article at full coverage and route the subtitle-less Bilibili video to media capability/ASR attention instead of a URL failure.

[2026-08-06] Import URL operation-contract repair — Replaced display-string identity with a typed unique ImportBatch operation, restored source-aware singleton URL titles, moved cohort preparation off the command path with cancellation-safe atomic writes, persisted tasks under the resolved native/compatible layout, and unified login, exact-duplicate, cancellation, and terminal aggregation paths — Key decision or open issue: the operation reports source preparation rather than user confirmation/commit; two independent reviews were closed, Graphify updated, targeted frontend/Rust/scale/Web regressions passed, and the final canonical `npm run check` passed from the beginning.

[2026-08-06] External knowledge-base Source binding repair — Repaired six broken legacy Source bindings in the user-selected knowledge base with two path-scoped Git checkpoints, collision-free Wiki paths, current frontmatter, synchronized version baselines, and preserved raw evidence — Key decision or open issue: no application code or raw evidence changed; all 10 current Sources passed binding/hash/index verification and the knowledge-base repair is committed as `52db7a649333524eb7203064d51b5eda631dd79a`.

[2026-08-06] Project compatibility / Import backpressure Batch F — Closed F-12/F-13 with one-commit frontend item/task patches, operation-task ownership, guarded batch-event consumption, saved-scan aggregate confirmation, separate spreadsheet acknowledgement, a fail-closed discovery hard limit, and worksheet-aware output estimates — Key decision or open issue: legacy `start_import_items_v2` remains the `<=200` compatibility path while the primary UI consumes `start_import_batch_v2`; two independent reviews were closed, Graphify updated, focused frontend tests passed 99/99, no-default scan confirmation tests passed 8/8, and the final canonical `npm run check` passed from the beginning (118 frontend files / 879 tests plus 941 Rust unit tests and all integration/doc tests).

[2026-08-06] Project compatibility / Import backpressure Batch E — Closed F-09/F-10/F-11 with 128-entry discovery coalescing, one persisted batch operation task and shared cancellation, incremental item/session persistence, compatible import-state roots, stale-snapshot CAS commits, and bounded cohort journal/parent-sync behavior — Key decision or open issue: item JSON remains authoritative over operation task state; the final canonical `npm run check` passed from the beginning after full Rust/frontend lanes.

[2026-08-05] Project compatibility remediation Batch D — Added a fail-closed compatible layout mapping under `.app/compat/layout.json`, mapping-aware legacy path resolution, scoped Git checkpoint enforcement for mapping updates, portable case/Unicode/ancestor collision rejection, and layout-aware export/workflow recovery — Key decision or open issue: compatible content writes require confirmed existing mapped roots and a clean tracked Git mapping checkpoint; unmapped compatible projects remain readable and report prerequisites without creating native roots. Two independent reviews were closed, focused regressions passed, Graphify updated, and the final `npm run check` passed from the beginning (118 frontend files / 865 tests plus Rust unit, integration, and doc tests).

[2026-08-05] Batch C compatible app-state and Workflows — Added `.app/compat` task/workflow persistence roots without content write roots, layout-aware workflow prerequisites and output paths, root-specific backend write guards, and current expected-red-to-green contracts — Key decision or open issue: compatible state persistence never grants Markdown write authority; trust revocation cancels active workflows and fails closed if durable revocation cannot complete.

[2026-08-05] Batch B corrective dependency — Made workflow persistence rebinding validate every target before mutation, cancel active root-scoped workflows before trust revocation, and aligned repair-plan backup assertions with optional DTO paths — Key decision or open issue: queued runs remain subject to fresh backend authority checks at dispatch; this corrective change is committed separately from Batch C.

[2026-08-05] Project compatibility remediation Batch B — Unified native-layout inspection with explicit Current/RepairableLegacy/IncompleteLegacy states, directory-only confirmed repair, post-repair authority refresh, and no-follow repair creation — Key decision or open issue: compatible/third-party markers and unsafe native markers fail closed; full `npm run check` passed and Graphify AST update completed.

[2026-08-05] Project compatibility / Import backpressure Batch A completion — Re-ran the complete gate from the beginning after freezing expected-red contracts: frontend and Rust lanes passed, including 100/1,000/10,000 real discovery and BackendTask count witnesses; Graphify AST update also completed — Key decision or open issue: the 10,000-item Vitest witness has an explicit 15-second execution budget because its current intentionally unbatched path exceeds Vitest’s default 5 seconds; its 10,000-publication assertion and future <=100 green target remain unchanged for Batch F.

[2026-08-05] Project compatibility / Import backpressure Batch A — Frozen native-layout, legacy-repair, compatible-readability, Import discovery/task/session, and frontend store expected-red contracts with 100/1,000/10,000 synthetic scale fixtures and versioned baseline evidence — Key decision or open issue: witnesses pass only by proving the known defect, contain an anti-skip integrity guard, and must be converted to positive green assertions by B/C/E/F; no production DTO, IPC, persistence, trust, Git, or user-content mutation was introduced.

[2026-08-05] Legacy project compatibility, Workflows, and Import backpressure remediation plan — Added a first-principles, minimum-intrusion implementation plan covering one native-layout authority contract, explicit legacy repair transitions, compatible `.app/compat` state and write-root mapping, layout-aware service cutover, operation-level Import tasks, incremental session persistence, coalesced events, frontend batch patches, aggregate-size confirmation, quantitative gates, rollback, and full scenario coverage — Key decision or open issue: preserve Markdown/JSON storage and existing IPC as compatibility shims, never move compatible Markdown or silently create native roots, and defer Import pagination unless the 10,000-item post-batching evidence still requires it; this milestone is documentation-only and no executable checks were required.

[2026-08-03] First-run authority and Workflows Batch 8 closure Batch G — Completed the final Batch 8 cutover audit, scenario/evidence matrix, detector-hash disclosure, legacy Agent UI retirement verification, and repository-wide validation; fixed both reviewers' valid findings covering explicit right-panel selection, same-root identity replacement, recovered task-id path safety, Workflow-owned confirmation routing, dirty Git coverage, and QA overclaims — Key decision or open issue: the authorized rustfmt exceptions remain formatting-only, the Settings exception is a one-line `initReactI18next` test mock, the detector was not rerun, both independent reviews are closed, focused regressions passed, and the final `npm run check` passed from the beginning in 6m03.1s with 116 frontend files / 828 tests plus all Rust/build/lint/evidence lanes.

[2026-08-03] First-run authority and Workflows Batch 8 closure Batch F elevated final gate — Re-ran the repository-wide `npm run check` outside the sandbox: GUI Rust compilation and all 876 Rust unit/integration/doc tests passed, and all 116 frontend files / 824 tests passed — Key decision or open issue: the wrapper remains non-green solely because the pre-existing out-of-scope `ImportCompatibilitySettings.test.tsx` react-i18next mock omits `initReactI18next` and produces one unhandled rejection; Batch F does not modify the frozen Settings-owned test to mask this baseline issue.

[2026-08-03] First-run authority and Workflows Batch 8 closure Batch F — Made task activation and Workflow replay consume one backend-derived persistence binding, added typed persistent/memory-only transitions across revoke/grant/retry/continue, synchronized authority-sensitive start/replay/confirmation with project trust transitions, and mirrored task persistence state into frontend stores without changing frozen Workflows UI — Key decision or open issue: restricted, read-only, and compatible layouts without a task-state root remain MemoryOnly and never create `.app`; retry never reuses an old task root, downgrade preserves but stops updating old snapshots, upgrade writes only the new attempt, both required reviewers' valid findings were fixed, 876 Rust tests plus 116 frontend files / 824 tests passed, while the repository-wide `npm run check` wrapper remains non-green only because the sandbox blocks Vite's nonessential `net use` spawn and an unrelated existing settings test leaves an unhandled `react-i18next` mock rejection.

[2026-08-02] First-run authority and Workflows Batch 8 closure Batch E — Added typed short-lived project-open assessment, conservative cancellable folder/Git inspection, assessed open/trust/revoke/compatible-enable/Git remediation commands, unified confirmations, and minimal first-run/Settings authority hosts — Key decision or open issue: restricted folders remain read-only, compatible writes are limited to `.app/compat`, Git initialization freezes and revalidates the approved file set, project-bound actions are cancelled on scope changes, both required reviewers report no remaining issues after all valid fixes, focused regressions passed, and the final `npm run check` passed from the beginning in 5m31.1s with 871 Rust tests; §6 full First-run redesign and Workflows-side authority consumption remain deferred.

[2026-08-02] First-run authority and Workflows Batch 8 closure Batch D — Added backend-owned native/compatible trust authority, identity-bound global trust persistence with bounded cross-process locking, real root/task-state writability probes, durable/runtime transition revalidation, and Workflows access snapshots carrying trust kind and authority revision — Key decision or open issue: untrusted projects stay read-only without write probes, compatible grants remain gated on the later Batch E confirmation host, Persistent requires trusted + writable + an existing safe writable task-state root, project open invents neither Git nor `.app`, both required reviewers found no remaining issues after valid fixes, focused regressions passed, and the final `npm run check` passed from the beginning in 4m39.4s; assessment/UI and task/retry consumption remain deferred to Batches E/F.

[2026-08-02] First-run authority and Workflows Batch 8 closure Batch C — Added backend-derived typed ProjectLayout contracts, pure native defaults, bounded compatible-vault Markdown-root discovery with source/wiki/mixed roles, safe confidence warnings, and layout-aware Wiki index, Health Check, and Workflow preparation reads — Key decision or open issue: compatible restricted/read-only layouts expose read roots only and never invent write/state paths; native index results remain legacy-equivalent, both required reviewers completed, all valid findings were fixed, focused regressions passed, and the final `npm run check` passed from the beginning in 5m37.6s; trust/authority and assessment remain deferred to Batches D/E.

[2026-08-02] First-run authority and Workflows Batch 8 closure Batch B — Added one project-contained path-safety helper for TaskService persistence, recovery, and deletion; buffered pre-overview Workflow events with canonical identity/revision revalidation, newest-event merging, project-switch clearing, and bounded failed/concurrent refresh recovery — Key decision or open issue: `app_state.rs` adoption of the shared helper is intentionally deferred to the later trust-baseline batch because its current private helper is inseparable from uncommitted Batch D/F authority work; Batch B does not weaken A3/A4, both required reviewers completed, all valid findings were fixed, focused regressions passed, and the final `npm run check` passed from the beginning in 4m41.3s.

[2026-08-02] First-run authority and Workflows Batch 8 closure Batch A — Committed the confirmed First-run / Project-open and Workflows authorities, the batched Workflows plan, product brief, and closure plan while correcting roadmap and implementation-plan progress signals — Key decision or open issue: Batches 0–7 are committed, but Batch 8 remains uncommitted and unreviewed; First-run / Project-open dependencies B1–B4 remain pending, so no implementation-complete claim is made; out-of-scope closure-plan errata remain recorded for its incomplete ProjectFormat list and incorrect §10 delivery-template cross-reference, while the two confirmed specs stay authoritative.

[2026-08-02] Workflows Batch 8 backend review hardening — Closed queued-cancel Undo claim/IPC replay races, constrained confirmation restoration to the same canonical root/task/execution variant with runtime-project-id-only rebinding, and added explicit wrapper plus nested workflow schema validation without unsafe raw fallback — Key decision or open issue: persisted wrapper v1 migrates to v2, current schemas recover, future or malformed wrappers are skipped without rewriting their bytes; 806 Rust library tests and 42 focused Workflow integration tests passed, with `cargo fmt --check` clean.

[2026-08-02] Workflows Batch 7 shared entries, safety, recovery, and result ownership — Unified Dashboard/Import/Wiki/Lint/Exports/Workflows launches behind one project-bound preparation intent; added non-modal hydrated decision review with exact mixed-change counts and per-file diffs, restart restoration, cancellation/Undo/retry/queue recovery UX, exact Wiki/Lint/Exports result navigation, safe project-scoped notifications, and versioned global remote-provider disclosure — Key decision or open issue: both required reviewers completed and valid findings were closed, including decision evidence refresh loss, on-demand confirmation restoration, notification project-switch races, deleted-result navigation, expiring Undo, and prepare-only next-workflow recommendations; focused frontend tests passed 23 files / 176 tests, focused Rust workflow suites passed 42 tests, and the final `npm run check` passed from the beginning in 4m23.4s; legacy Agent UI retirement remains intentionally assigned to Batch 8.

[2026-08-01] Workflows Batch 6 React surface and IPC closure — Added the Knowledge Processing / Workflows navigation, fixed three-row overview, structured preparation for all three workflows, observable pipeline/detail/history/right-panel modes, project-scoped stale-event protection, pagination, queue/cancel/retry/confirmation actions, bilingual dense styling, and the missing typed backend IPC/Update Wiki confirmation bridge required by the surface — Key decision or open issue: both required reviewers are CLEAN after fixing queue continuation dispatch/revalidation, waiting-candidate terminalization, confirmation claim lifetime, immutable history cursors, dirty preparation tokens, option-catalog DTOs, unknown progress, and undo visibility; focused frontend tests passed 5 files / 15 tests, focused Rust workflow suites passed 47 tests, and final `npm run check` passed from the beginning in 5m33.3s with 114 frontend files / 793 tests plus all Rust unit/integration/doc tests; legacy Agent launch paths remain intentionally present for Batch 7, and per-file decision Diff/domain result navigation remain in Batch 7 ownership.

[2026-08-01] Workflows Batch 5 Generate Content runner — Added the exact nine-stage pipeline for four fixed artifact types with layout-scoped HTML output, stable page/resource/target baselines, independent restricted-content and remote-provider acknowledgement, no-checkpoint atomic create-new publishing, checkpointed hash-bound overwrite confirmation/recovery, safe progress, linked retry, and Exports-owned records/previews — Key decision or open issue: both required reviewers completed and valid findings were closed, including HTML attribute bypasses, candidate permission/hash binding, queue continuation cleanup, Exports index locking, case-collision validation, local-provider disclosure, and Claude stdin privacy; focused tests passed 8 Generate Content integrations, 32 Exports service tests, 18 frontend Exports tests, and the final `npm run check` passed from the beginning in 5m03.4s with 111 frontend files / 781 tests, 804 Rust unit tests, and all integrations; legacy production project access remains intentionally fail-closed until the project-open trust authority lands in its assigned later batch.

[2026-08-01] Workflows Batch 4 read-only Health Check runner — Composed reusable deterministic Source/Wiki checks and optional exact-route BYOK deep analysis into one cancellable eight-stage workflow, with stable baselines, source-aware bounded prompt coverage, deterministic provenance-preserving merge, persistent or identity-isolated memory reports, and existing Lint history/detail ownership — Key decision or open issue: both required reviewers completed after nine valid findings were fixed; Agent deep lint remains intentionally unavailable because the current backend lint Agent safety profile explicitly rejects it, while configured BYOK routes support Complete mode without silent fallback; focused Rust tests passed 40 Lint units plus 4 Health Check integrations, focused frontend tests passed 31, and final `npm run check` passed from the beginning in 4m19.6s with 111 frontend files / 781 tests, 797 Rust unit tests, and all integrations; legacy Lint commands remain the rollback adapter and no Workflows UI cutover was introduced.

[2026-08-01] Workflows Batch 3 observable Update Wiki runner — Extracted one reusable compile generation body behind the legacy adapter and added the nine-stage changed-source/full-recompile runner with clean-head Git checkpointing, typed route/scope baselines, policy-scoped reviewable deletion, hash-bound atomic apply/rollback, persisted waiting and committed-reconciliation candidates, index/graph refresh, truthful result commits, and task-bound recovery — Key decision or open issue: both required reviewers are CLEAN after closing checkpoint races, identity/access rebinding, delete/rollback TOCTOU, final-commit rollback, and post-commit task-state reconciliation; final `npm run check` passed from the beginning in 3m45.0s with 111 frontend files / 778 tests, 793 Rust unit tests, and all integrations including 10 Update Wiki scenarios; confirmation IPC/registry reconstruction remains Batch 7, and legacy production access stays fail-closed until project-open trust authority lands.

[2026-08-01] Workflows Batch 2 overview, preparation, and route contract — Added fixed three-row overview, fail-closed project access summaries, opaque revalidated preparation tokens, stable baselines, typed prerequisites, deterministic route resolution, structured preference/quick-rerun handling, runner dispatch boundaries, and registered overview/prepare/start IPC without exposing the new UI or workflow-specific runners — Key decision or open issue: legacy production IPC remains intentionally untrusted/memory-only until the project-open access authority lands in its assigned scope; both required reviewers are CLEAN after closing dedupe, queued dispatch, project isolation, preference staleness, overview truthfulness, and token replay findings; focused Workflows tests and final `npm run check` passed in 4m31.1s with 111 frontend files / 778 tests, 790 Rust unit tests, and all integrations.

[2026-08-01] Workflows Batch 1 persisted project queue and recovery foundation — Added schema-v2 workflow task snapshots, canonical fingerprint dedupe, one-running-per-project FIFO coordination, atomic stage/terminal handoff, cancel/undo/retry linkage, explicit restart continuation, validated waiting recovery, project-scoped task IPC/events, typed non-secret retry options, layout-derived or memory-only storage, and hardened atomic writes — Key decision or open issue: both required reviewers are CLEAN after closing identity rebinding, stage/cancel races, completion invariants, scoped caller, symlink/reparse, and temporary-link findings; focused queue/recovery tests pass 15 + 5, focused frontend tests pass 45, and final `npm run check` passed from the beginning in 3m39.4s with 111 frontend files / 777 tests, 789 Rust unit tests, and all integrations; no Workflows route/UI or runner work was introduced and the legacy Agent page remains active.

[2026-08-01] Workflows Batch 0 contract freeze — Added mirrored schema-v1 Rust/TypeScript workflow contracts, exact 12-command request APIs, Interrupted/display mapping, real legacy task recovery fixtures, and recursive architecture regressions without exposing a new route or changing visible Agent behavior — Key decision or open issue: both required reviews are CLEAN after closing generic IPC/export-list and union/nullability gaps; 9 frontend files / 63 tests, 8 Rust workflow contract tests, cargo fmt, and final npm run check:quick pass; Interrupted lifecycle/recovery remains Batch 1 and Workflows UI remains Batch 6/7.

[2026-07-29] Import workbench compact entry polish — Removed the visible Import identity copy, left-aligned the three section tabs, unified the public-URL icon/input/action into one field, and added a collapsed-by-default supported-sources summary that expands into the existing accessible capability tiles — Key decision or open issue: the summary exposes localized file/platform/ability availability counts, its state survives tab changes and resets on project change, both required reviews returned CLEAN after fixes, and the final `npm run check` passed in 4m33.3s with 109 frontend files / 749 tests, 65 capability tests, lint/build/console gates, GUI Rust compilation, 784 Rust unit tests, and all integrations; no backend flow changed.

[2026-07-28] Source AI organize full-gate closure — Closed the two real failure paths with schema-bound Agent output, long-running structured BYOK fallback, a Source/project-bound floating review workbench, scoped error handling, accessibility fixes, and stale capability-fixture updates — Key decision or open issue: both required reviews are closed and the final `npm run check` passed from the beginning in 5m23.9s, covering the frontend lane, 784 Rust unit tests, all Rust integration suites, and the Import / Source Batch 9 evidence gate; no live paid-provider request was repeated during the final verification.

[2026-07-28] Source AI structured generation and floating workbench — Reproduced the two latest real failures, replaced free-form Agent handoff with Claude/Codex schema-bound output, moved BYOK generation to cancellable long-running structured streaming, parsed Claude `structured_output`, and implemented a draggable/resizable/minimizable non-modal workbench with real activities, final-draft-first review, optional Diff/process tabs, explicit cancel, retry, apply, discard, and regenerate paths — Key decision or open issue: closing hides without cancelling, the generic task drawer is explicit fallback only, and final dual review plus the full quality gate remain pending.

[2026-07-28] Source AI BYOK/Agent execution closure — Relaxed semantic candidate guards so factual rewrites reach Diff review, unified the source-rewrite contract, enabled Claude Code/Codex/OpenClaw/Hermes with selected local credentials and isolated one-shot Source profiles, added task-visible recoverable retry, normalized Agent model/version audit metadata, and closed both required reviews — Key decision or open issue: BYOK retry pins provider/model; Agent retry pins Agent kind and uses its current Source execution profile; local CLI credential/config reuse is an explicitly accepted relaxed-isolation boundary; final `npm run check` passed in 5m08.6s with 108 frontend files / 741 tests, 65 Node capability tests, 9 Python tests, GUI Rust compilation, 781 Rust unit tests, and all Rust integration suites.

[2026-07-28] Import / Source / media remediation Batch 9 — Closed P2-09, validated P3-02, and completed P3-03 by removing the writable legacy Import surface, retaining only explicitly read-only compatibility/migration adapters, qualifying the production capability/process boundary, hardening Office/PDF/OCR/ASR/media/path/language/accessibility behavior, and adding an executable §19 evidence gate for 32 scenarios / 26 contracts / 14 real-fixture categories / 9 forbidden closures — Key decision or open issue: supported local formats must cross their real built-in parser or `PackProcessEngine` JSON-RPC boundary and preserve every fixture’s original bytes/hash/size; OOXML logical relationships and media coverage fail closed; Source language remains independent of UI language; exact-case paths remain distinct; shared-context and fresh-context reviewers found and closed every valid issue and both returned no valid open findings after final follow-up. The required pre-review and post-review full `npm run check` runs both passed from the beginning; the final 5m21.1s run covered 108 frontend files / 735 tests, 770 Rust core tests plus all integrations, 64 Node capability tests, 5 OCR Python tests, 2 legacy Office Python tests, evidence/lint/build/console gates, and GUI Rust compilation.

[2026-07-28] Import / Source / media remediation Batch 8 — Completed the Source-scoped AI 整理 workflow through isolated Agent or BYOK routing, bounded current-Source input, protected-fact and overview validation, staged candidate preview/discard/apply, external-edit-aware two/three-way review, checkpointed versioning, cancellation cleanup, exact recovery identity, and editor-draft race protection — Key decision or open issue: unconfirmed AI output never writes the current Source, retry is bound to the original project root plus actual resolved engine/model, and Source AI never remounts host Agent credential directories; Claude/Codex therefore fail closed on this path until a credential broker exists, while supported isolated local Agents and BYOK remain available. Shared-context and fresh-context reviewers completed their passes and follow-up reviews; every valid Batch 8 finding was fixed. The required pre-review and final full `npm run check` runs passed from the beginning (with one additional passing post-review interim run); the final 3m08.5s run covered 109 frontend files / 740 tests, GUI Rust compile, 838 Rust core tests plus all integrations, 64 Node capability tests, 5 Python tests, and lint/build/console gates (the pre-existing `FileTransaction` dead-code warning remains out of scope).

[2026-07-27] Import / Source / media remediation Batch 7 — Completed the registry-bound Source reader and exact eight-section right panel, truthful reprocess/refresh candidates backed by the production OCR/ASR/web routes, reliable two-way/explicit three-way review, immutable evidence and version restoration, dedicated atomic move/delete lifecycle, generic-operation guards, CJK SourcePackage coverage, and the AI-organize placeholder without starting Batch 8 — Key decision or open issue: Source mutations are `sourceId`-scoped checkpointed transactions, historical package paths never reclaim or delete a later Source, actual processing evidence is hash-bound through candidate apply, and only reliable package snapshots are restorable; shared-context and fresh-context reviews found 10 independent issues and every valid finding was fixed; both required full `npm run check` runs passed from the beginning, with the final run covering 109 frontend files / 736 tests, 64 Node capability tests, 5 Python tests, lint/build/console gates, GUI Rust compile, and 822 Rust core tests plus all integrations (the pre-existing `FileTransaction` dead-code warning remains out of scope).

[2026-07-27] Import / Source / media remediation Batch 6 — Completed the Import workbench, seven user-facing states, action-based grouping, single-primary-action rules, stable preview targets, restart-safe sessions, partial commit/cancel, completion/history re-entry, and the Agent exact-duplicate lifecycle — Key decision or open issue: every preview producer reuses the production resolution/target planner and exact-duplicate commit path; Agent candidate actions are serialized and exact-duplicate finalization performs a task ID plus full-preview CAS under the mutation lock before task/history writes; both required reviewers and both incremental re-reviews returned FINAL CLEAN; the final `npm run check` passed from the beginning in 4m01.1s with 108 frontend files / 729 tests, 813 Rust core tests, 64 Node capability tests, lint/build/console gates, and GUI Rust compile (the pre-existing Batch 9 `FileTransaction` dead-code warning remains out of scope).

[2026-07-27] Import / Source / media remediation Batch 5 — Completed platform login-group recovery, reusable and sanitized connector identity, explicit restricted-content propagation and export warnings, X/Twitter recovery routing, collection discovery/selection/relations with opaque pagination and cross-session changed-item fingerprints, required image-post localization/OCR ordering, measured remote-media retention confirmation with a disk hard stop, and the embedded sandboxed `import-recovery` Skill — Key decision or open issue: authentication alone never marks content restricted; cookies/profile paths remain outside React/project/log/export; collection items become independent Sources only after selection; remote media stays extract-only unless a verified size and sufficient disk are confirmed; both required reviews were merged and every valid finding was fixed; the final `npm run check` passed from the beginning in 3m21.6s with 106 frontend files / 705 tests, 789 Rust unit tests plus all integrations, 64 Node capability tests, 5 Python tests, lint/build/console gates, and GUI Rust compile (pre-existing transaction dead-code warning only).

[2026-07-26] Import / Source / media remediation Batch 4 — Unified the production OCR, subtitle, ASR, and media fail-closed gate across standalone images, selective PDF pages, local/platform media, and conditional video-frame OCR; added reliable subtitle provenance and all-track evidence, resumable download/OCR/ASR shards, paused restart recovery, and a typed device/resource/dependency-aware ASR setup plan — Key decision or open issue: embedded/original-language subtitles precede companion files and local ASR, machine translations never count as a reliable original, OCR/ASR authorization remains explicit and session-scoped, and no valid finding remains after shared-context plus fresh-context review; the final full `npm run check` passed from the beginning with 105 frontend files / 699 tests, capability/Python checks, lint/build/console gates, GUI Rust compile, and all no-default-features Rust tests.

[2026-07-26] Import / Source / media remediation Batch 3 — Completed the Batch 3 local-file route matrix, content-first classification, clipboard evidence lifecycle, local Markdown/HTML resource archival, CSV SourcePackage output, modern Office/PDF handling, subtitle-first local media routing, atomic package member deletion, and the frozen schemaVersion 3 manifest; kept Gate A in one data-only table and added a genuine PPTX whose only image is on slide 2 — Key decision or open issue: shared-context and fresh-context reviews were merged and their valid findings were fixed (attachment archival, language-suffixed/ambiguous subtitles, package shape updates, real media fixtures, PPTX relationship mapping, and partial-staging cleanup); Gate A, focused Rust suites, TypeScript, and 28 focused frontend tests pass, while both full `npm run check` runs were explicitly skipped at the user’s direction, so the unified-check exit gate remains open.

[2026-07-26] Import / Source / media remediation Batch 2 — Added typed and persisted `ImportCompletion`, exact completion summary/actions and History re-entry; changed Compile to accept hash-bound V2 Source versions, resolve only registry-bound Source-specific workspaces, persist `.app/compile/` consumption, isolate the read-only legacy adapter, reject all `wiki/sources/**` mutations, and keep Import and Compile as explicit independent tasks — Key decision or open issue: the manifest remains frozen at schemaVersion 3 and Gate A remains data-only/table-driven; shared-context and fresh-context reviews closed Source-guard case aliases/additions, request/index/manifest/frontmatter/disk-hash binding, legacy duplicate detection, and durable Compile finalization ordering; restart/cancel restoration of the active completion card is recorded for its assigned Batch 6 scope; both full `npm run check` runs passed, with the final run covering 104 frontend files / 690 tests, 60 Node capability tests, 5 Python tests, lint/build/console checks, Tauri GUI Rust compile, and all no-default-features Rust tests.

[2026-07-26] Import / Source / media remediation Batch 1 — Landed the complete schemaVersion 3 Source manifest contract, immutable raw/evidence/assets plus final Source Markdown in one recoverable transaction, portable `assets/...` resolution, derived new/exact/update/three-way resolution bindings, staged hash-bound manual merge, one-time atomic v2→v3 registry migration, and a data-only table-driven Gate A that runs the production local/generic/Bilibili/Xiaohongshu routes with deterministic network/OCR fixtures — Key decision or open issue: manifest structure is frozen for later batches; future Gate A inputs add rows without changing the runner; shared-context and fresh-context reviews found and closed five valid Batch 1 issues (portable assets, production-route coverage, merged-candidate commit, backend-derived four-tuple bindings, and in-place legacy migration), plus the real Windows OCR `\\?\` path mismatch exposed by the production Gate; `npm run check` passed from the beginning with 103 frontend files / 680 tests, 60 Node capability tests, 5 Python tests, lint/build/console checks, Tauri GUI Rust compile, and all no-default-features Rust tests.

[2026-07-25] Import / Source / media remediation Batch 0 — Removed the Import-only BYOK command/API/service/UI chain, ordinary-failure automatic Agent scheduling, metadata-only media preview, remote-media preselection, global conflict controls, and dead public Agent-policy controls; defaulted remote media to extract-only; moved legacy review into Settings compatibility with backend-bound confirmation and resumable background-task handling; froze stale-safe Rust↔TypeScript contracts through one shared JSON fixture; and rewrote the anti-baseline regressions — Key decision or open issue: local Agent candidate recovery, general Chat/Compile/Export/Lint BYOK, migration backend safety/checkpoints, task/session infrastructure, and old explicit preserve-original sessions remain intact; Batch 1+ Source finalization, clipboard ingestion, per-item conflict application, and broader workbench UX remain out of scope; `npm run check` passed with 103 frontend files / 679 tests, 60 Node capability tests, 5 Python tests, lint/build/console checks, Tauri GUI Rust compile, and all Rust tests after shared-context plus fresh-context review.

[2026-07-25] Import / Source / media 整改执行计划 — 以 `2026-07-24-import-source-media-flow-design.md` 为唯一产品基准、以 `2026-07-25-import-source-media-flow-implementation-review.md` 的 36 条 finding（7 P0 / 13 P1 / 12 P2 / 4 P3）为问题清单，写出 `docs/superpowers/plans/2026-07-25-import-source-media-flow-remediation.md`：全局约束、九条禁止关闭方式对应的反向断言、Finding→Batch 归属总表、Gate A–G 到 batch 的映射、十项强制交付模板、Batch 0–9 各自的 finding/基准章节/调用链与 DTO/写入与 checkpoint/必须保留能力/要删的反基准测试/新增测试与 fixture/中英文与 CJK 覆盖/退出门槛，以及依赖图、每批收尾固定动作和风险表 — 关键决策：Batch 0 先冻结合同并删除 Import 侧 BYOK（Chat/Compile/Export/Lint/Settings/Task 的 BYOK 保留不动），manifest schema 在 Batch 1 一次定完只填值不改结构，Gate A 契约测试必须表驱动以便 Batch 3/4/5 每加输入只加数据行；本次只产出计划文档，未改动任何应用代码。

[2026-07-25] Import / Source / media remediation conversation prompts — Created a main-workspace, no-worktree prompt pack for ten sequential implementation conversations, merging the simple contract cleanup into Source finalization, separating the complex session-state and workbench tasks, and requiring every new conversation to complete a read-only detailed plan and wait for “开始” before editing — Key decision or open issue: conversations must run serially in the shared dirty worktree, preserve unrelated changes, and pass the canonical-design/review, unified-check, and dual-review gates; `npm run check` passed with 103 frontend files / 680 tests, 60 Node capability tests, 5 Python tests, lint/build/console checks, Tauri GUI Rust compile, and all Rust tests.
[2026-07-25] Import / Source / media implementation review — Audited the current dirty worktree against `2026-07-24-import-source-media-flow-design.md`, merged shared-context frontend and fresh-context backend/media reviews, and produced an Agent-facing evidence report with 7 P0, 13 P1, 12 P2, 4 P3 findings, target contracts, a production coverage matrix, acceptance tests, and a dependency-ordered 10-batch implementation sequence — Key decision or open issue: no application code was changed; Source finalization, explicit Agent/BYOK boundaries, and the V2 Compile change set must be corrected before expanding formats or UI; `npm run check` passed with 103 frontend files / 680 tests, capability-tool tests, lint/build/console checks, Tauri GUI Rust compile, and all Rust tests (existing dead-code warning only).
[2026-07-23] Bilibili download and ASR progress runtime closure — Traced the real `builtin.web-bilibili` task, added immediate and byte-level media progress, accepted the observed HTTPS `*.edge.mountaintoys.cn` playback CDN with exact-boundary checks, enforced the same policy on browser/pack routes and every redirect, and made DNS/header/body waits promptly cancellable without misreporting reporter failures as user cancellation — Key decision or open issue: the user verified the live task reached `preview_ready` with visible recognition progress; `npm run check` passed with 101 frontend files / 670 tests, 57 Node tests, 5 Python tests, lint/build/console checks, GUI Rust compile, and all Rust tests.
[2026-07-23] Local ASR visible progress review closure — Merged shared-context and fresh-context reviews; added live Bilibili media-download progress, mapped download/ASR/validation onto one monotonic 0–100 task scale, kept measured bars visibly pulsing during long blocking decode batches, restored progress listeners after restart, rejected stale task events and malformed progress notifications, cleared inherited retry progress, and drained fast-exit process output safely — Key decision or open issue: download uses 5–20%, ASR uses 20–90%, validation uses 95%, and preview readiness alone reaches 100%; unified check passed with 101 frontend files / 670 tests, 57 Node tests, 5 Python tests, lint/build/console checks, GUI Rust compile, and all Rust tests.
[2026-07-23] Local ASR live progress implementation — Added bounded `import.progress` JSON-RPC notifications for subtitle checks, audio decode, segmented recognition, and transcript merge; bridged them through Rust task events into the live Import item; added accessible measured progress UI, bilingual stage labels, batch deduplication, and real multi-chunk qualification assertions — Key decision or open issue: progress reflects completed ASR work and stops at 99% until validation; unified check and required dual review are next.
[2026-07-23] Bilibili long-video ASR review closure — Merged shared-context and fresh-context reviews, added a real 27-second multi-chunk qualification, made development-pack reuse require the complete prior inventory while allowing an atomic runner refresh, preserved the old manifest until qualification succeeds, rebuilt/re-signed the pack, and passed the unified check — Key decision or open issue: target 25e8dbb1's failure is fixed at the engine boundary; the historical failed attempt remains immutable and the source URL must be retried after restarting the development app.
[2026-07-23] Bilibili long-video ASR root-cause repair — Traced target 25e8dbb1 through its persisted import session, reproduced SenseVoice requesting 24.2 GB for one 38:54 attention sequence, replaced whole-file inference with bounded 20-second/24-file batch transcription and timeline merging, and made development packs refresh on runner fingerprints without redownloading native payloads — Key decision or open issue: the exact 35.7 MB video now produces 117 timestamped segments and 13,617 text characters in 69.3 seconds; unified check and dual review are next.
[2026-07-23] Bilibili-to-ASR handoff review closure — Merged both independent reviews, added a Windows namespaced-locator versus relative `chainedInput` Node contract test, and accepted both declared SenseVoice and Whisper temporary output layouts under canonical staging containment — Key decision or open issue: focused Node, orchestrator, Web ingestion, installed-pack transcription, and public Bilibili checks pass; final unified check is next.
[2026-07-23] Bilibili-to-SenseVoice Windows handoff repair — Reproduced `IMPORT_ASR_POLICY_BLOCKED` against the installed SenseVoice 1.13.4 pack, passed the validated staging-relative media path through `chainedInput`, and added an orchestration regression assertion — Key decision or open issue: the installed pack now resolves the same temporary media and completes its real Chinese qualification transcription; unified check and dual review are next.
[2026-07-23] Bilibili video-to-Markdown review closure — Closed both independent reviews with target-bound HTML/XHR evidence, bounded player-response retention, human-Chinese subtitle priority, progressive-versus-DASH media separation, real Local ASR continuations, and explicit zero-asset metadata-only handling — Key decision or open issue: the reported public URL and focused browser/Rust regression suites pass; the unified check is the final verification.
[2026-07-23] Bilibili video-to-Markdown fallback repair — Merged target-bound Bilibili player XHR/page evidence with separate video metadata, recognized `subtitle_url` JSON tracks and DASH/progressive media, rejected recommended-video evidence, failed closed on unparseable subtitle-only results, and marked metadata-only Markdown explicitly — Key decision or open issue: the reported public link now passes both subtitle-or-ASR-media and final Markdown staging contracts; unified check and required dual review are next.
[2026-07-23] Xiaohongshu note-to-Markdown review closure — Completed public-page and isolated-browser extraction for image/video notes, target-ID anchoring, ordered 100-image localization, canonical source identity, privacy-safe evidence, extract-only notices, platform/embedded-subtitle/local-ASR priority, partial failures, and distinct auth/removal/structure errors — Key decision or open issue: both required reviews were merged and all valid findings fixed; `npm run check` passed with 100 frontend files/661 tests, 47 Node capability tests, 5 Python tests, lint/build/console/GUI and all Rust suites.
[2026-07-23] Xiaohongshu note-to-Markdown implementation — Added target-ID-bound INITIAL_STATE parsing for image/video notes, ordered image deduplication, title provenance, hashtag/time metadata, dedicated captcha/login/removed/structure errors, browser and built-in Markdown parity, redacted provider evidence, bounded 100-image localization, and partial-asset cleanup — Key decision or open issue: preserve-original and extract-only remain explicit per import, OCR/ASR remain opt-in capability continuations, targeted Rust/Node/frontend tests pass; unified check and required dual review are next.
[2026-07-22] Bilibili video-to-Markdown review closure — Completed provider fallback, gzip subtitle handling, install-to-authorization recovery, metadata-only preview, development-pack overlay, live readiness refresh, route-level capability UI, and backend-driven file/platform/ability matrix — Key decision or open issue: `npm run check` passed with 100 frontend files/661 tests, 37 Node capability tests, 5 Python tests, lint/build/console/GUI and all Rust suites; real SenseVoice integrity and public Bilibili API validations also passed.
[2026-07-22] Bilibili video-to-Markdown implementation — Added public metadata/player/subtitle extraction, subtitle-first normalized evidence, explicit per-item local ASR authorization, signed one-command SenseVoice development runtime, dynamic frontend readiness, and separate workbench/history/capability-pack sections — Key decision or open issue: imports commit only raw source/evidence artifacts and never auto-compile Wiki pages; Release remains fail-closed until maintainers publish signed capability artifacts.
[2026-07-22] 导入工作台设计稿 (UI-Frontend-design/import-workbench.html) — 落地单文件 HTML 交付物，复现 Codex Desktop 外壳并叠加 v2 导入状态机：入口区（紧凑拖拽 + URL 输入 + 剪贴板 + 11 个来源/能力状态 tile，区分 支持/需登录/需能力包/暂不可用）、8 列队列表（12 行覆盖 queued→completed 全部状态）、右侧 master-detail 详情面板（状态专属提示卡 + 提取预览/来源/媒体/质量门禁/归档规则/冲突）、底部确认栏（原始文件不被修改徽章 + Git 检查点 chip + 导入后编译开关 + 影响路径）、4 个弹窗（登录/能力包/冲突/历史）— 关键决策：以 app.css 为唯一视觉权威（Inter/JetBrains Mono/Source Serif Pro、紧凑 13px、单色 #10a37f），把 import-v2-reference.html 仅作概念输入；自包含内联 SVG sprite 避免与 app.js 冲突；浏览器验证通过（行选择切状态、弹窗开关、Esc、筛选、响应式折叠、token 取值均确认）。

[2026-07-20] Automation report review closure — Incorporated two independent reviews: separated current TaskService recovery from future Run reconcile, fixed Import Discovery/Commit and LocalLint TaskType boundaries, defined time/DST/misfire semantics, cross-file crash recovery, lifecycle scope, confirmation gates, and manifest single-source ownership — Key decision: keep the report implementation-ready without changing application source code.
[2026-07-20] Automation research — Completed a context-guided research report for scheduled Compile/Lint/Import automation, covering typed schedules and runs, misfire/overlap/recovery, Git safety, Import confirmation boundaries, and a staged implementation plan — Key decision: reuse TaskService and file-backed JSON, keep automatic high-risk changes behind explicit confirmation, and change no application source code.
[2026-07-20] Unified verification — Frontend tests (97 files/625 tests), lint, build, console scan, GUI Rust check, and 638 Rust unit tests passed; the unified command remains blocked only by the pre-existing missing `turndown` package in two browser-runtime-lite integration tests — Key decision: keep that environment/dependency gap explicit rather than installing an unrelated package.

[2026-07-20] WeChat image rendering closure — Added HTML image-to-Markdown conversion, source-version asset resolution, a project-scoped Wiki asset IPC command, and reader-side Blob loading; focused frontend/Rust checks passed — Key decision: keep Markdown references portable as `assets/...` while resolving immutable raw assets only in the backend.

[2026-07-19] Lint confirmation UI closure — Added immediate effective-choice rendering so an active high-risk confirmation cannot be hidden by a prior Ignore selection; final check reached 97 frontend files/624 tests and 633 Rust tests, with only the known missing `turndown` browser-runtime-lite dependency failing — Key decision: an active confirmation exposes only Confirm/Cancel and remains the source of truth until explicitly resolved.

[2026-07-19] Lint final verification — Removed the last lint-only initialization warning and reran the unified check from the beginning: frontend tests, lint, build, console scan, GUI Rust check, and 633 Rust unit tests passed; only two browser-runtime-lite tests remain blocked by the missing `turndown` package — Key decision: keep the dependency failure explicit rather than altering production code or installing packages silently.

[2026-07-19] Lint mutation race closure — Added store/UI guards for local-scan and batch mutation overlap, and deferred confirmation cancellation replay for in-flight claims; final unified check reached 97 frontend files/623 tests and 633 Rust tests — Key decision: operation epochs never supersede active work silently, and teardown cancellation is remembered until the backend claim releases.

[2026-07-19] Lint review fix closure — Hardened confirmation claim cleanup across validation failures, blocked ignore/restore actions during batch execution, and passed final TypeScript/Rust checks; full suite reached 97 frontend files/623 tests and 632 Rust tests — Key decision: stale operation results are ignored after history/project scope changes; users can rescan to refresh the active report.


[2026-07-19] Lint confirmation lifecycle closure — Added teardown cancellation for project switches, terminal expiry handling, per-action confirmation IDs/expiry, duplicate-ID rejection, and partial registration reporting while preserving safe batch results — Key decision: confirmation state is an app-global capability and must be cancelled, expired, or surfaced as a typed skip rather than overwritten or silently discarded.

[2026-07-19] Lint confirmation and batch rollback hardening — Preserved pending batch confirmations across post-fix rescans, cancelled every backend pending action before history navigation, prevented issue/fix races from hiding confirmations, narrowed Agent probing to the selected CLI, and carried post-write hashes through shared-checkpoint failures — Key decision: a destructive lint action remains reviewable until its backend confirmation is explicitly cancelled or applied.

[2026-07-19] Lint ignore/index/frontmatter UX closure — Added an in-view restore action for persisted ignores with local rescan, reported a missing wiki/index.md as an explicit error and de-duplicated index dead links, made unknown-folder frontmatter findings manual-only, and cleared stale selection/confirmation state before local reruns — Key decision: a lint result must never silently look healthy or offer a fix that can immediately invalidate its own schema.

[2026-07-18] Lint review repair final verification — Backend/frontend fixes were rechecked: TypeScript, focused Lint tests, ESLint, production build, console scan, GUI Rust check, and all 626 Rust unit tests pass; the unified check remains blocked only by two existing browser-runtime-lite tests missing the `turndown` package — Key issue: do not claim the repository-wide check is green while that dependency is absent.

# 进度记录 (progress.txt)

[2026-07-18] Lint concurrent-write guard — Serialized in-process Lint mutations, added a post-atomic-write hash check that preserves external editor content on detected races, and re-enumerated the final local scan/deep prompt snapshot immediately before accepting results — Key issue: optimistic hash checks cannot be a cross-process compare-and-swap, so the backend now fails closed on observed write drift and never rolls it back to HEAD.

[2026-07-18] Lint stale-card UX closure — Lint issue cards now hide Fix actions for historical findings without a scan baseline while keeping them selectable for the rescan explanation — Key decision: the frontend must not offer an action that the backend is guaranteed to reject as stale.

[2026-07-18] Lint trust-boundary and resolution closure — Deep BYOK Auto selection now rejects arbitrary project-local endpoints, deep findings share persisted ignore rules, prompt input read failures are explicit, path-form wikilinks resolve through canonical and project-relative keys, and import Agent authentication remains isolated separately from the disabled Deep Lint CLI route — Key issue: project-local configuration is untrusted input and must not silently choose a credential destination.

[2026-07-18] Lint deep-route and snapshot safety closure — Disabled unverified CLI Agent execution for Deep Lint, removed unsupported Codex no-tools claims and real credential-directory exposure, included optional prompt guidance files in deep snapshot checks, refreshed local page metadata before rules, and made the issue list use sibling buttons instead of nested interactive controls — Key decision: BYOK is the only verified Deep Lint route until a credential broker proves no-tools isolation.

[2026-07-18] Lint rollback and metadata consistency hardening — Added rollback-to-HEAD preservation for failed single/batch fixes, held report body/history writes under one metadata lock, distinguished unreadable metadata from missing metadata, stabilized deep prompt/page hash snapshots across scans, and propagated page-read failures — Key decision: a failed Lint mutation must return to the pre-fix Git state before exposing an error.

[2026-07-18] Lint verification and test-root hardening — Excluded untracked `.worktrees` and generated target directories from Vitest/ESLint discovery, updated Rust Lint fixtures for scan hashes, and completed frontend tests, TypeScript, ESLint, production build, console scan, GUI Rust check, and 626 Rust unit tests — Key issue: the unified check now reaches browser-runtime-lite tests but stops because the existing `turndown` package is absent.

[2026-07-18] Lint snapshot and Agent safety closure — Local Lint now compares before/after hashes for every scanned page and rejects a changing scan; single-fix cache/log failures roll back through the pre-fix checkpoint; Claude/Codex deep profiles disable model tools while retaining only explicit CLI auth directories; old reports without scan hashes are excluded from batch auto-fix — Key decision: untrusted wiki content must never receive filesystem tools or turn a moving scan into a writable baseline.

[2026-07-18] Lint repair regression closure — Fixed dead-link anchor/alias/case handling with title/alias target recheck, moved deep scan hashes before Agent execution, added cancellation checkpoints and local post-fix verification, rejected unsupported deep Agent routes, isolated timeout/error codes, bounded/redacted persisted Agent output, enforced ignore path scope, and renamed the Lint splitter state to the right details pane — Key decision: stale reports cannot offer a write action without a scan hash; focused frontend tests and Rust compile checks pass, while the full unified check still needs the known worktree-test cleanup.

[2026-07-18] Lint repair safety pass — Added scan-time hashes, deterministic fix-plan validation, scoped pre-fix checkpoints plus final-result commits, retryable high-risk confirmations, affected-path tracking, report pruning, metadata write serialization, strict Agent JSON failure handling, skill-contract loading, prompt budget/inert data markers, read-only Agent profiles, and frontend scan-snapshot/UI layout updates — Key decision: a Lint fix may only write against the report snapshot and must expose the resulting commit; full unified check is still pending environment/worktree cleanup.

[2026-07-18] Lint first-principles review — Reviewed frontend, Rust backend, Agent/Skill, Git safety, task lifecycle, persistence, tests, and authoritative UI design on local master `89ecf77`; documented 2 P0, 20 P1, and 5 P2 issue groups in `docs/reviews/2026-07-18-lint-first-principles-code-review.md`, then merged shared-context and fresh-context independent reviews — Key decision: close scan-bound repair plans, pre/post Git safety, Agent isolation, and task/state truth before expanding lint rules; focused Lint frontend tests pass, while unified check remains blocked by existing `.worktrees` test collection.

[2026-07-15] Import V2 UX-focused review supplement — Re-reviewed perceived performance, batch feedback, action pending states, failure recovery, preview fidelity, history, keyboard semantics, and task-context switching with local-tool UX prioritized — Key decision: prioritize inline/progressive feedback and one coherent batch workflow; defer non-critical hardening while keeping user-visible state truth and recovery intact; supplement added to `docs/reviews/2026-07-15-import-v2-review.md`.

[2026-07-15] Import V2 implementation review — Completed a first-principles review of the final-integration worktree against the 2026-07-11 design, covering capability sandboxing, web authorization, session state, batch scheduling, quality gates, commit safety, frontend interaction, and release evidence — Key decision: do not recommend release until the P0 capability sandbox and the P1 user/state/recovery blockers are closed; review written to `docs/reviews/2026-07-15-import-v2-review.md`.

[2026-07-14] Import V2 gate removal review complete — Two independent reviews found the unified V2 path consistent with typed IPC, immutable legacy evidence, project-scoped safety, and Agent/BYOK credential rules; final npm check passed — Remaining release evidence is platform/external-capability certification, not a runtime import prerequisite.

[2026-07-14] Import V2 unified write path — Current import bootstrap treats migration metadata as advisory, skips stale safe session evidence, and uses V2 as the sole import writer — Activation remains release evidence and is not a user import gate.

[2026-07-14] Import V2 final integration — Merged `codex/import-v2-frontend` into `codex/import-v2-final-integration`, preserved master shell/theme/document history, and resolved the two-file merge conflict without broad ours/theirs replacement — Key decision: keep V2 as the only active staging/commit path; final GUI, signed capability, platform, Agent, BYOK, and migration-token gates remain release-environment work.

[2026-07-14] Import V2 review fixes — Closed review findings for fail-closed connector presentation, text staging order, Markdown image symlink containment, and local Agent credential environment isolation — Key decision: capability readiness must be explicit and host API-key environment variables are never inherited by Agent subprocesses.

[2026-07-14] Import V2 frontend Task 11 — Added end-to-end presentation evidence for partial commit, retry/cancel, migration blocking, and bounded 2,000-item queue rendering; completed focused gates, final unified check, and two manual independent reviews — Key decision: frontend certification records only the Windows/automated evidence actually run; GUI, macOS/Linux, real external-platform, and migration-token gates remain explicit release conditions.

[2026-07-14] Import V2 frontend Task 10 — Replaced the staged V1 ImportView with the composed V2 header, source methods, queue, inspector/dialog surface, migration notice, history, sticky commit bar, and bilingual responsive tokens; removed obsolete import dialogs and legacy skipped App tests — Key decision: the active frontend exposes only typed workflow intents, keeps ambiguous/history actions fail-closed, and commits only selected preview-ready items.

[2026-07-13] Import V2 frontend Task 9 — Added backend-read migration notice, dry-run/report review, explicit fingerprint/no-Git confirmation, resumable apply intents, and read-only legacy history — Key decision: migration writes remain disabled without a backend-bound confirmation token; the UI never infers activation or exposes a V1/V2 switch.

[2026-07-13] Import V2 frontend Task 8 — Added dedicated connector login, captcha/private-target authorization, and capability-pack requirement/install gates with bilingual accessible dialogs; extended workflow routing for begin/complete/revoke login, one-target grants, capability facts, and cancellable backend install tasks — Key decision: credentials, cookies, signed URLs, and pack lifecycle remain backend-owned; unsupported capability installation is rendered as a fallback instead of a fabricated UI action.

[2026-07-13] Import V2 frontend Task 7 — Added project-scoped Agent policy controls, explicit local assistance and BYOK scope/approval UI, deterministic/current/Agent candidate evidence, and typed workflow routing for candidate actions and backend tasks — Key decision: Agent/BYOK controls never hold secrets or write results; only the backend task/candidate contracts can authorize work, and all UI review choices remain intents until normal Import confirmation.

[2026-07-13] Import V2 frontend Task 6 — Replaced the import right panel with source/route/provenance, quality metrics, attempts, artifact facts, and status-aware preview actions; added identity-bound bounded Markdown preview with safe rendering, stale-response suppression, truncation/hash/size/copy, errors, and modal focus handling — Key decision: preview components receive only session/item/candidate identity and typed bounded content; they never receive filesystem paths or fetch URLs.

[2026-07-13] Import V2 frontend Task 5 — Rebuilt workflow orchestration around typed V2 readiness/session/task APIs, project-epoch guards, task-event reconciliation, nonblocking add/start, cancellation, retry, and explicit commit decisions — Key decision: active workflow code contains no V1 mutation calls; temporary legacy UI props adapt to V2 or fail closed until the final ImportView replacement.

[2026-07-13] Import Core TOCTOU hardening — Added cross-platform same-file identity checks around artifact open/read so a same-size replacement between validation and open is rejected — Key decision: reuse the existing Core quality gate and journal boundary; no second transaction protocol.

[2026-07-13] Import V2 frontend Task 4 — Added the persistent queue presentation with full Core status matrix, typed action derivation, filters/counts, stable row selection, commit eligibility, partial-success visibility, and measured/indeterminate progress — Key decision: JSX receives presentation actions from a typed status adapter and never switches on raw backend status codes to invoke APIs.

[2026-07-13] Import V2 frontend Task 3 — Added the two-pane file/folder and public URL entry component with native picker, authorized Tauri drag-drop, URL retry preservation, local-target rejection, and readiness-gated connector chips — Key decision: browser drag payloads are never treated as filesystem paths; only the backend/native bridge may authorize paths.

[2026-07-13] Import V2 frontend Task 2 — Added recoverable project-scoped session state, epoch-guarded refresh/append/replace transitions, queue selectors, progress counts, and identity-keyed dialogs — Key decision: task facts remain in the global task store while import presentation state clears on project changes.

[2026-07-13] Import V2 frontend Task 1 — Added identity-bound preview/readiness/history/capability presentation DTOs and one typed Tauri IPC adapter — Key decision: preview reads are capped and hash-checked; capability installation remains fail-closed until the signed-pack installer exists.

[2026-07-13] Import V2 conflict safety review — Compatibility confirmation no longer guesses `CreateNew` for V2 `NeedsMerge`; unresolved V2 conflicts now return Core’s explicit conflict error — Key decision: the unchanged legacy UI cannot authorize a V2 conflict action on the user’s behalf.

[2026-07-13] Import V2 cutover cancellation hardening — Parent-routed preview now links cancellation to the active V2 child and completes the parent through TaskService’s cancellation-aware atomic result boundary — Key decision: a cancelled parent cannot be promoted to a successful preview by a set-result/status race.

[2026-07-13] Import V2 cutover routing fix — Activation-aware legacy import facade now creates and executes V2 sessions, projects previews read-only, carries the V2 session through confirmation, and stages clipboard text inside the V2 session workspace — Key decision: preserve the existing import UI contract while post-activation import writes remain V2-only.

[2026-07-13] Import V2 Agent Assistance release gate — Final unified check passed from the beginning after all review fixes (71 frontend files / 461 tests, lint, build, console scan, GUI Rust check, 597 Rust unit tests plus integration suites); product/integration review A and attacker review B both passed with no remaining findings — Key decision: packaged parent-crash process semantics remain an explicit operational qualification item, not an overstated repository certification.

[2026-07-13] Import V2 Agent workspace crash lease — Added a pre-copy atomic lease bound to process instance, optional task, expiry and exact workspace UUID; restart recovery preserves active/succeeded tasks, removes abandoned preview or terminal task roots, and safely cleans partial atomic temporaries — Key decision: recovery never deletes an item-wide Agent directory and cannot be poisoned by a truncated lease temp.

[2026-07-13] Import V2 Agent Assistance release hardening — Added exact audit reconciliation, fail-closed route/template/hash provenance, handle-bound source copying, response-stage duplicate-charge protection, suspended Windows Job assignment, Unix process-group watchdog cleanup, and task-exact workspace cleanup that preserves concurrent siblings — Key decision: only a validated output_staged audit may reconcile to succeeded after durable Task completion; packaged parent-crash behavior remains explicit operational evidence.

[2026-07-13] Import V2 Agent fallback orchestration — Added policy-approved deterministic hard-failure fallback, explicit manual low-quality assistance, local/BYOK candidate actions, typed identity envelopes, staging-relative candidate selection/discard/three-way merge, and crash-safe revalidation without direct raw/wiki writes — Key decision: ordinary polling preserves live tasks, restarts never resubmit cloud calls, and incomplete candidate storage is containment-checked then rebuilt from validated staged output.

[2026-07-13] Import V2 Agent candidate validation — Added exact-hash strict manifests, single-read immutable candidate sealing, deterministic input revalidation, hardened text/image asset checks, Source Registry baseline/current/agent Git Diff, idempotent recovery, and Core PreviewReady/NeedsMerge registration — Key decision: unmeasured Agent fidelity stays an explicit warning and neither deterministic staging nor raw/wiki is ever overwritten.

[2026-07-13] Import V2 BYOK assistance — Added exact one-shot send-scope approval bound to project/item/provider/model/destination, canonical secret-redacted payload hashing, context/cost disclosure, cancellable shared LLM streaming, staging-only output, durable send-phase audit, and duplicate-charge acknowledgement after ambiguous recovery — Key decision: cloud assistance is never automatic and a send-started call is recovered as charge-unknown rather than silently retried.

[2026-07-13] Import V2 local Agent invocation — Added policy-gated automatic hard-failure and explicit manual starts, isolated stdin-only Claude/Codex profiles, durable AgentRun task binding, redacted output capture, process-tree cancellation, hard-failure source recovery, and fail-closed unsupported CLI profiles — Key decision: low-quality optimization stays manual-only, unavailable agents are never installed, and successful output remains staging-only for later candidate validation.

[2026-07-13] Import V2 structured Agent tool broker — Added a typed, grant-checked, item-bound tool protocol with route/model limits, cancellation checks, output containment, and a redacted per-workspace ledger — Key decision: shell, Git, installers, arbitrary network, credential access, and arbitrary file writes are unrepresentable in the protocol.

[2026-07-13] Import V2 Agent Assistance isolated workspace — Added item-bound sanitized workspaces, immutable verified copies, untrusted-source labeling, output-only contract, reparse containment checks, terminal hash preservation, and the wiki-ingest-assist Skill — Key decision: task.json contains only public source identity, hashes, grants, and relative material references; live project paths and credentials never enter the Agent bundle.

[2026-07-13] Import V2 Agent Assistance policy persistence — Added version-safe project policy persistence and thin registered commands through SettingsService, reusing agentDefault and keeping provider keys exclusively in SecretService — Key decision: auto-BYOK is rejected at persistence time and attempt budgets are constrained to one through three.

[2026-07-13] Import V2 Agent Assistance contracts — Froze Rust/TypeScript policy, trigger, tool grant, candidate, Diff, send-scope, audit, and recovery action DTOs while preserving legacy ImportIssue recoveryActions with a defaulted availableActions extension — Key decision: balanced policy never auto-invokes BYOK or low-quality optimization; hard-failure local automation remains opt-in.

[2026-07-13] Import V2 Web authorized Bilibili ASR and final security closure — Added exact per-item local-ASR authorization, reusable media.asr continuation, whisper.cpp 1.8.3/ggml-small qualification contracts, offline Bilibili embedded metadata parsing, crash-recoverable temporary workspaces, and exact single-use login profiles; closed both independent review sets and passed the full check — Key decision: yt-dlp direct networking, unsigned payloads, and OS sandbox claims remain fail-closed release gates; only Rust-pinned fetches may reach the network.

[2026-07-13] Import V2 Web login retry closure — Requeued verified waiting-login items, required platform auth cookies instead of public DOM selectors, and made authenticated profiles single-use with deletion after the browser retry — Key decision: profile credentials authorize exactly one matching item attempt and are never retained as a general browser session.

[2026-07-13] Import V2 Web adversarial re-review hardening — Pinned the only browser-connected host, required positive platform login evidence, authenticated every capability runtime sibling, wired single-use private grants and exact redirect state, rebound dedicated login profiles to items, preserved WeChat public identity, and added startup/shutdown profile cleanup — Key decision: Bilibili subtitle absence is a typed failure rather than a metadata-only success; unsigned release payloads and unimplemented authorized ASR remain fail-closed release blockers.

[2026-07-13] Import V2 Web production-path review fixes — Connected SSRF-pinned HTTP prefetch, real Readability and isolated Playwright packs, opaque keyring URL targets, authenticated connector profiles, remote-asset localization, restricted yt-dlp metadata/subtitle fallback, and Windows/Unix process-tree cleanup — Key decision: Bilibili without a verified subtitle fails closed; local ASR requires both an installed signed capability and explicit per-item authorization, while phase-two connectors remain release-gated.

[2026-07-12] Import V2 final execution-boundary hardening — Rebuilt authorized staging without static links/reparse points, revalidated hash-verified pack entrypoints immediately before spawn, killed Unix process groups even after parent exit, and made accurate CJK OCR independently addressable — Key decision: preserve signed pack-relative runtime layout; immutable install-root enforcement and handle-bound cross-platform spawn remain release-hardening gates.

[2026-07-12] Import V2 dual-review runtime and security hardening — Connected signed app-local capability loading, background cancellable discovery, planner-owned typed fallbacks and chained legacy conversion, added source identity revalidation, handle-bound quality reads, bounded pack framing, reader cleanup, and process-tree termination — Key decision: the embedded development trust store remains empty and every placeholder pack stays fail-closed; only release-signed, hash-verified, qualified app-local payloads can register executable routes.

[2026-07-12] Import V2 ingestion race closure — Persisted canonical/size/hash/magic source identity from discovery into session inputs, revalidated it before native extraction, hardened Quality Gate same-file reads, and bounded/joined capability stdout handling — Key decision: legacy sessions deserialize without identity, but newly discovered native inputs fail closed on any source replacement; Windows process-tree cancellation remains the absolute taskkill fallback.

[2026-07-12] Import V2 File Ingestion release gates — Added executable all-format route/manifest/performance contracts and a fail-closed QA evidence matrix covering golden, security, recovery, package, license, and platform requirements — Key decision: local source-level gates pass, but absent signed pack payloads, actual size/RSS data, frozen binary Golden Corpus, real process-tree tests, and macOS/Linux/reference-device evidence remain explicit one-time-cutover release blockers.

[2026-07-12] Import V2 deterministic file orchestration — Integrated explicit route-ordered engine execution, typed fallback attempts, stable file error recovery actions, cancellation, and persisted attempt outcomes while retaining Core preflight, partial commit, and backend-derived targets — Key decision: registry insertion order never selects file parsers; typed execution failure, invalid staging output, or attempt-level quality rejection may advance to the next declared route, while the selected candidate passes the formal Quality Gate exactly once.

[2026-07-12] Import V2 Optional Media and Local ASR — Added deterministic human/local/platform → automatic → embedded → whisper subtitle routing, bounded streaming/backpressure contracts, staging-only subtitle/timestamped Markdown/cover/metadata artifacts, crash-safe temporary media cleanup, strict-LGPL FFmpeg provenance, and separately signed resumable whisper model contracts — Key decision: public FileFormat remains the approved eight formats; unqualified binaries advertise no platform payload and missing whisper reports waiting_capability without runtime install or download.

[2026-07-12] Import V2 Office output quality contracts — Added identity-preserving three-tier workbook plans, explicit chunk warnings with complete CSV fallbacks, XFD bounds, formula/value and hidden-sheet preservation, and one ordered presentation candidate with slide-bound notes and deterministic meaningful-image filtering; extended EngineResult/QualityReport with five backward-compatible Office metrics without weakening Core safety checks — Key decision: any range omitted from Markdown must remain available through an explicit warning and complete staged CSV attachment.

[2026-07-12] Import V2 Tiered Local OCR — Added staging-confined OCR request/result contracts, deterministic basic-versus-accurate routing, explicit waiting-capability requirements, pinned model hash/language validation, complete cache identity, confidence and Golden Corpus accuracy helpers, and honest non-installable Tesseract/PaddleOCR source manifests — Key decision: PaddleOCR advertises no target triples until reproducible CI qualification, missing local models never fall back to cloud or runtime installation, and execution remains behind the existing JSON-RPC staging-only pack engine.

[2026-07-12] Import V2 PDF Routing — Added passive lopdf inspection, page-exact text/image/encryption/action reporting, fail-closed active-content and password handling, deterministic text/layout/selective-OCR/Agent/waiting page plans, 98% clean-text quality gating, and an honest pinned Docling JSON-RPC capability contract — Key decision: never execute PDF actions or install Docling at runtime; absent verified payloads remain waiting_capability and all engine output stays in item staging behind Import Core.

[2026-07-12] Import V2 Native File Engine — Added deterministic UTF-8 Markdown/text and RFC CSV normalization plus conservative local HTML sanitization, byte-exact source staging, safe relative-image copies, typed loss warnings, metadata provenance, cancellation cleanup, and default Import Core registration — Key decision: keep the public eight-format FileFormat contract stable while routing lightweight identity extensions internally through builtin.native-file and keeping formal writes behind Import Core.

[2026-07-12] Import V2 File Discovery — Added deterministic bounded breadth-first folder and multi-file discovery, portable path collision handling, extension/magic/MIME and bounded OOXML validation, repeated-append deduplication, and a thin registered Tauri command — Key decision: discovery only creates Import Core inputs; skipped paths never become placeholder items and formal writes remain owned by Import Core.

[2026-07-12] Import V2 Capability Packs — Added signed/hash-pinned/versioned platform and license selection, health rollback, traversal-safe immutable entrypoints, and a no-shell JSON-RPC process engine with cancellation, timeout, process-tree cleanup, and staging-only artifact validation — Key decision: formal raw/wiki/index writes remain exclusively behind Import Core; pack manifests and processes can only return item-staging artifacts.

[2026-07-12] Import V2 File Contracts — Frozen Rust/TypeScript file formats, discovery skip reasons, scan policy/results, capability requirements, and Golden Corpus assertion schema with RED→GREEN contract coverage — Key decision: preserve the eight approved release formats and keep capability/runtime details additive behind Import Core.

[2026-07-11] Import 2.0 Core — Added versioned DTOs, resumable sessions, engine protocol, quality gating, immutable source registry, atomic per-item commits, task integration, and V2 IPC without changing the legacy importer — Key decision: keep real file/web/Agent engines behind the stable ImportEngine boundary and defer command cutover until full regression.

[2026-07-12] Import V2 Frontend implementation planning — Wrote the sixth independent TDD plan for the reference-aligned file/folder and URL entry panes, recoverable queue, right-side Markdown/quality inspector, Agent/BYOK/Diff actions, login/capability gates, migration/history surfaces, accessibility, localization, and release verification — Key decision: execute only after all five backend packages stabilize; React remains presentation-only and any missing preview/readiness/history/capability IPC is completed as thin typed Rust delegation rather than frontend filesystem or process logic.

[2026-07-12] Import V2 downstream implementation plans — Wrote independent File Ingestion, Web Ingestion, Agent Assistance, and Migration & Cutover TDD plans plus an official-source open-source decision record, adapted to Import Core HEAD `3bd282c` and its stable service/engine/command contracts — Plans and design references only; no application code or UI implementation, and final cutover preserves the legacy index/content as immutable rollback evidence.

[2026-07-11] Sidebar scroll and appearance stability — Unified the upper left sidebar scroll region, refined left/right panel scrollbars, and removed settings save-status reflow — Key decision: the Agent footer remains fixed while save feedback is absolutely positioned outside normal layout flow.

[2026-07-11] Import 2.0 Core implementation planning — Wrote a TDD implementation plan for versioned DTOs, resumable sessions, engine protocol, immutable source registry, quality gating, atomic per-item commits, task integration, IPC, and end-to-end review — Key decision: keep the legacy importer untouched during Core, isolate temporary metadata in `.app/source-index-v2.json`, and defer real file/web/Agent engines plus final command replacement to their own plans.
[2026-07-11] LLM Wiki Desktop context skill evolution — Updated the project skill with current AppShell/workflow ownership, project-switch async commit guards, stable Rust facade contracts, context authority rules, exact code routing, and the unified `npm run check` gate — Key decision: the skill is an anti-drift map, not a duplicate architecture spec; focused checks accelerate iteration but never replace the full check.

[2026-07-11] Architecture documentation alignment — Aligned living SPEC, flow, technical, backend, frontend, and roadmap documentation with the AppShell workflow controllers and Rust service use-case modules while preserving product contracts and historical evidence — Key decision: current code is the implementation source of truth; DESIGN.md and historical audits remain unchanged.

[2026-07-11] SPEC and application-flow alignment — Updated the canonical current-state docs for AppShell/WorkspaceController/WorkspaceRouter orchestration, project-key/epoch workflow guards, global task/confirmation flows, and AppState-backed Import/Search/Lint/Chat stable service facades while keeping ChatConvenienceService and WikiIndex independent — Key decision: record implemented workflow and facade facts without changing product scope, local Markdown/JSON storage, Git checkpoint/confirmation, search, BYOK/Agent, or other safety contracts.

[2026-07-11] Integration dual-review cancel guard — Prevented a delayed old-project task-cancellation failure from toasting into the newly active project, backed by a deferred RED→GREEN hook regression from both independent reviewer findings — Key decision: task records remain globally trackable, but cancellation error UI commits only when the initiating projectId + rootPath is still active.

[2026-07-11] Integration preflight stale-toast fix — Guarded the source-action follow-up compile error path so a rejection settling after project switch cannot toast into the newly active project, with a deferred RED→GREEN controller regression — Key decision: every post-await UI effect, including error reporting, must revalidate both projectId and rootPath; old-project task registry updates remain allowed.

[2026-07-11] Final service modularization review fixes — Restored the planned Lint report-directory visibility, narrowed Chat retrieval-only candidates, and centralized Import artifact validation/removal helpers without changing facade APIs or write safety ordering — Key decision: keep `LINT_REPORTS_DIR` at the exact planned `pub(crate)` scope, keep rules helpers private to the Lint module tree, and route Import confirmation/promotion/source actions one-way through a private artifacts module.

[2026-07-11] Import 2.0 architecture design — Consolidated the approved file/folder, exact-URL, capability-pack, OCR/ASR, legacy Office conversion, balanced Agent, versioned-source, security, migration, and hard acceptance decisions into a formal design spec — Key decision: implement five independently validated packages, then replace the legacy importer once after complete regression; UI visual implementation remains deferred for a separate design.

[2026-07-10] Service use-case modularization — Split Import, Search, Lint, and Chat internals into facade-preserving Rust modules with unchanged IPC/DTO/persistence contracts — Key decision: keep AppState and command call sites stable; chat convenience audit and WikiIndex remain separate security/cache boundaries.

[2026-07-10] Task 9 Chat full modularization history separator fix — Preserved a newline between a budget-truncated older history chunk and the next newer role label without exceeding the history character budget, backed by a real multi-message RED→GREEN boundary test — Key decision: spend one already-budgeted trailing character on the separator; an extremely small budget that cannot preserve meaningful text omits the history section.

[2026-07-10] Task 9 Chat full modularization review fixes — Added explicit Index/Pinned/KeywordHit retrieval reasons to diagnostics and made bounded history retain the newest suffix while rendering it chronologically, with real RED→GREEN regressions — Key decision: diagnostics may list multiple primary reasons for one path while graph/source expansion keeps its existing path dedupe and precedence; prompt source selection, citations, DTOs, commands, AppState, and convenience auditing remain unchanged.

[2026-07-10] Task 9 Chat full modularization — Moved retrieval planning/prompts/budgets/graph and source-overlap expansion plus saved-answer Markdown/persistence into focused modules while preserving the ChatService facade and all 32 baseline Chat tests — Key decision: retain required purpose/index → full pinned page → keyword → bounded graph → bounded source overlap → prompt/history budget ordering with complete diagnostics, keep model-marker-only saved citations, and keep saved answers separate from Agent convenience-write auditing.

[2026-07-10] Task 8 Chat sessions/citations modularization — Split session CRUD/path validation and model citation parsing/answer Markdown into focused modules while preserving the ChatService facade and all 32 baseline Chat tests — Key decision: keep retrieval and saved-answer orchestration in the facade for Task 9, preserve recoverable corrupt-session handling and model-marker-only persisted citations, and adjust the moved wiki-query include path for the deeper Rust module directory.

[2026-07-10] Task 7 Lint full modularization — Moved deep-lint prompt/parsing/normalization and checkpoint-protected single/batch fix orchestration with their tests into focused modules, leaving `mod.rs` as the 19-line LintService facade while preserving all 30 baseline Lint tests — Key decision: keep rules helpers at the narrowest module visibility and preserve the planned `pub(crate)` visibility of `LINT_REPORTS_DIR`, reject unknown/out-of-scope paths before writes, and preserve evidence-free error downgrades plus hash/PendingAction/checkpoint/graph invalidation/fix-log contracts.

[2026-07-10] Task 6 Lint rules/ignores/reports modularization — Split deterministic local rules, ignore CRUD persistence, and report/history persistence into focused modules while preserving the LintService facade and all 30 baseline Lint tests — Key decision: keep SearchService read-only catalog semantics and existing ignore/report corruption tolerance, while leaving deep analysis and fix orchestration in the facade for Task 7.

[2026-07-10] Task 5 Search full modularization — Moved local query tokenization, field scoring, and deterministic sorting into `query.rs`, and cached retrieval, first-body excerpt, and truncation into `excerpts.rs`, leaving `mod.rs` as the SearchService facade with shared fields — Key decision: keep one shared WikiIndex snapshot/cache with bookmark-neutral entries, and derive retrieval excerpts from cached bodies without Markdown rereads.

[2026-07-10] Search catalog and page use-case modularization — Split the SearchService catalog/read and page mutation use cases into focused directory modules while preserving the public facade and existing characterization coverage — Key decision: keep one shared WikiIndex and live bookmark overlay; preserve optimistic hash/checkpoint/reference rollback contracts.

[2026-07-10] Import service modularization — Split read-only preview, confirmation/rollback, and source delete/replace use cases from the ImportService facade into focused modules with their existing regression tests — Key decision: keep the public facade and command/DTO contracts unchanged while preserving confirm preflight-before-write/rollback/staging cleanup and source-action revalidation/checkpoint/mutation/commit-or-restore safety order.

[2026-07-10] Workflow controller review hardening — Added post-await project/epoch guards for confirmation, task drawer, Agent, Provider, capability probes, and Import previews; isolated Settings lazy failures and removed duplicate secret refreshes — Key decision: old-project tasks remain in the unified task registry, but stale requests cannot change the active project, open current UI, navigate, toast, replace staging, or start follow-on compile work.

[2026-07-10] AppShell reduced to layout wiring — Replaced the 768-line mixed shell/workflow component with a 154-line layout shell that retains pane sizing, responsive panel collapse, global shortcuts, frame components, confirmation controller, task drawer, and toaster — Key decision: AppShell imports no Tauri invoke, feature DTO/store, Readability, terminal waiter, lazy view, or business command; WorkspaceController owns workspace composition.

[2026-07-10] Workspace router and controller — Added a static-Dashboard/lazy-feature router with colocated Suspense + ViewErrorBoundary and a composition controller that wires capability, task, Import, Provider, and Agent workflows into existing views/dialogs — Key decision: routing performs no IPC and controller composition stays feature-separated rather than becoming a monolithic workspace hook.

[2026-07-10] Project confirmation controller — Moved source delete/replace confirmation, post-confirm compile launch, waiting compile actions, and merge-conflict task updates into a global controller — Key decision: AppShell will render one controller only; confirmation semantics and checkpoint display stay in the existing dialogs while all returned tasks remain in taskStore.

[2026-07-10] Agent workflow extraction — Added a focused Agent hook for dialog presets, installed-default derivation, default Agent persistence, and explicit wiki-ingest/lint/query/single-page export/project-report routing — Key decision: task-producing skills call only the typed shared launcher; single-page exports navigate to their picker and never invent a source path.

[2026-07-10] Provider workflow extraction — Added a focused provider hook for config save, OS-credential secret save/delete, provider tests, and shared capability refresh — Key decision: secret material is an ephemeral IPC argument only; it is never returned from the workflow or written to React/Zustand state, logs, snapshots, or toast messages.

[2026-07-10] Import workflow extraction — Added a focused import hook covering project-scoped source lists, tracked file preview tasks, clipboard/URL previews with on-demand Readability, guarded source actions, and confirm → Wiki scan → optional compile sequencing — Key decision: every awaited UI-state commit checks projectId + rootPath; old project tasks remain globally trackable but cannot mutate the new project's Import staging.

[2026-07-10] Typed task launcher workflow — Centralized compile, deep-lint, export, tracking, shared drawer selection, and cancellation error handling in useTaskLauncher — Key decision: expose only typed task methods, never an arbitrary command launcher, so all new long tasks continue through the existing taskStore and TaskLogDrawer.

[2026-07-10] Guarded AI capability workflow — Added atomic project-scoped agentRoute updates and a shared capability probe hook with initial/visibility refresh, Agent-first routing, BYOK fallback, and stale-project result rejection — Key decision: compare projectId + rootPath both in the hook ref guard and the store action so delayed project A probes cannot mutate project B.

[2026-07-10] AppShell workflow boundary baseline — Added a static architecture contract that fails while Tauri/import/task/provider orchestration remains in AppShell and reverified the existing eight shell interaction regressions before extraction — Key decision: preserve current DOM, Settings modal, focus, panel, checkpoint, and dashboard capability behavior while moving workflows behind controllers.

[2026-07-10] Runtime cleanup — After explicit confirmation, restored tracked `.app` files to HEAD and moved untracked runtime state out to `D:\Users\Aletta\Desktop\Works\llm-wiki-desktop-runtime-backup-2026-07-10` — Key decision: retain all runtime bytes in the external backup while keeping the shared baseline free of task/settings/graph-cache state.

[2026-07-10] Git baseline audit — Created and switched to `codex/p1-base` at `bc0dabba`, preserved `task1-backend-contracts`, audited tracked/untracked work, and backed up runtime state outside the repository — Key decision: source/docs/validation/icon assets can be committed in scoped commits; `.app` runtime files remain pending explicit user confirmation.

[2026-07-10] Architecture improvement planning — Reviewed audit section 4.1 against current Rust services, AppShell orchestration, SPEC constraints, progress history, and gotchas; wrote separate implementation plans for facade-preserving service use-case modules and feature workflow controllers — Key decision: both P1 recommendations are valid, but implementation must be incremental, keep IPC/AppState contracts stable, preserve chat convenience and WikiIndex as separate boundaries, and prevent AppShell refactoring from becoming one new monolithic hook.

[2026-07-09] Settings brand icon refresh — Replaced OpenClaw, Hermes, Custom BYOK, and future fallback text placeholders with local inline SVG glyphs; aligned SVG fill/stroke CSS so filled official marks do not inherit accidental outlines; strengthened BrandMark tests to assert per-brand glyph structure and no `.settings-brand__letter` fallback for the updated cases — Key decision: use Hermes official `acp_registry/icon.svg` line mark, a compact OpenClaw mascot glyph suitable for 36px cards, and a neutral generic glyph for custom/unknown providers instead of monograms.

[2026-07-09] Settings AI configuration polish — Combined Agent CLI and BYOK into one AI settings surface with segmented tabs, brand-mark cards, safer provider/editor state sync, split provider/key save status, and shared card styling across the remaining Settings sections — Key decision: keep React on existing Settings callbacks only; render OpenAI, Claude, Google, and Ollama as local inline SVG marks from verified public/official sources, while OpenClaw/Hermes remain monogram fallbacks until stable official SVG assets are available.

[2026-07-09] Settings → floating modal dialog — Converted Settings from a full workspace view into a floating modal (preserves the workspace behind it); added ⌘/Ctrl+, shortcut (guarded against inputs and already-open modals), grouped nav (Application/AI/System) with Lucide icons, lazy-loaded SettingsView, and reinforced `useModalDialog` with body-scroll-lock + topmost-modal Escape guard (benefits all dialogs). — Key decision: model Settings as a control surface, not a workspace destination; ran 5 rounds of 5-dimension scoring (A/B/C/D/E) converging at 9/10 each with no actionable findings. — Left out (low value): collapsed-nav breakpoint tuning, AppearanceSettings Tailwind→token spacing.

[2026-07-09] App icon generation - Created a new LLM Wiki Desktop icon set from the approved book-page + knowledge-graph direction, including SVG/PNG source assets, Tauri desktop icons, Windows Appx icons, and explicit bundle icon config - Key decision: use the user-selected generated raster image unchanged as the source, preserving the monochrome book-page + knowledge-graph mark.

[2026-07-09] Verification instructions — Updated `AGENTS.md` and `CLAUDE.md` so completed tasks and review closeout use the unified `npm run check` command by default — Key decision: make the newly added CI/local validation wrapper the single documented verification path for future agents.

[2026-07-09] CI/CD automatic validation — Added a three-platform GitHub Actions workflow, unified local `npm run check` script, default-feature Tauri Rust compile check, Rust no-default-features test script, console-log scanner, and CI contract test for the P0 review item "建立 CI/CD 自动验证" — Key decision: run GUI Rust as `cargo check` and service/model Rust tests via `--no-default-features` to catch Tauri integration drift without hitting the Windows GUI-linked test-runner issue.

[2026-07-09] Review improvement explainer — Added `docs/audits/2026-07-09-review-improvement-explainer.md`, expanding the review report's improvement items by P0/P1/P2 priority with plain-language and professional explanations, rationale, and implementation direction — Key decision: turn audit findings into an execution-oriented explanation document without changing product scope or source code.

[2026-07-09] Comprehensive review report — Produced `REVIEW-REPORT.md` with project architecture summary, competitor research matrix, six-dimension code/UX/security/engineering audit, P0/P1/P2 roadmap, self-check, adversarial review supplement, and verification results; added `LOOP-STATE.md` for phase tracking — Key decision: prioritize CSP/CI/release trust chain first, then service/AppShell decomposition and first-run/current-page UX.

[2026-07-08] Project context skill — Added a project-level Codex skill at `skills/llm-wiki-desktop-context/` with progressive disclosure for project rules, docs, current progress, gotchas, code routing, and verification commands — Key decision: keep the skill in repo-root `skills/` for versioned project onboarding, with detailed routing in a reference file instead of duplicating the SPEC.

[2026-07-08] Page chat review fixes — Fixed reviewer-found stale page chat race by clearing old active session immediately during page-session resolution and by making PageChatPanel ignore/send around mismatched contextPagePath sessions; added empty search result state for ChatSessionList — Key decision: protect both store state and UI send path, because effect timing can briefly expose old session state after navigation.

[2026-07-08] SPEC implementation alignment — Added a scoped current-implementation alignment section to SPEC/SPEC.md covering initialized Tauri/React structure, Codex-like shell constraints, page-scoped Chat sessions, retrieval diagnostics vs persisted citations, indexed local search, graph visual scale, export path display, theme presets, and task/log audit rules — Key decision: document landed constraints without changing core product direction or expanding scope.

[2026-07-08] Sidebar icon sizing fix - Fixed favorite and recent page row icons shrinking inconsistently beside long page titles by pinning LeftSidebar Lucide icons with shrink-0 - Key decision: align React icons with UI-Frontend-design `.ico { flex: none; }` instead of changing visual size tokens.

[2026-07-08] Graph edge visibility fix - Increased default graph edge contrast and minimum rendered edge thickness after user feedback that links were nearly invisible - Key decision: preserve compact nodes while keeping normal edges readable on the quiet canvas.

[2026-07-08] Export list & sidebar icon fixes - Export page: filename and action buttons overlapped because the 4 action buttons lived inside the file <td> (.export-file-cell grid) competing with a full-outputPath .secondary line that had no truncation. Moved success-row action buttons into the existing trailing <td> as a dedicated shrink-to-fit .col-export-actions column, replaced the full path with pathBasename() (cross-platform helper in src/lib/pathDisplay.ts), and added title={outputPath} on the file cell so the full path is still discoverable on hover. Sidebar: HTML-export favorite icon looked too small because it used text-[var(--text-muted)] while wiki favorites used text-[var(--accent)] (both size=14 - perceptual, not literal). Changed export-favorite icon to text-[var(--accent)] (matches UI-Frontend-design/dashboard.html:115) and recent-page non-first icons to text-[var(--text-secondary)]. Updated exportsView.test.tsx (full path -> basename + title assertion) and ui-css-contracts.test.ts (.export-file-cell -> .col-export-actions). - Key decision: secondary line shows basename per the user's explicit "filename only" request (not the design's directory); full path is NOT displayed inline but is preserved via a title tooltip.

[2026-07-08] Graph visual polish - Reduced graph node and edge scale, centralized visual sizing rules, softened edge/dim colors, tuned Sigma label density and z-index, and quieted the canvas grid/overlays - Key decision: keep beauty work rule-based and testable through graphVisualScale plus CSS contracts instead of ad hoc per-node styling.

[2026-07-07] Graph rebuild UX implementation - Fixed Graph rebuild to keep prior graph data visible, show an in-view rebuilding overlay with task progress, prevent duplicate rebuild tasks, and make view retry local instead of full app reload - Key decision: Graph rebuild recovery stays in graphStore/ViewErrorBoundary and does not force project bootstrap or task drawer navigation.

[2026-07-07] P0/P1 non-import Batch 5-7 closeout — Added read-only wiki-query Skill, graph/source-overlap retrieval diagnostics, local schema/source lint checks, deep-lint baseline normalization, integration API drift fixes, and closeout notes — Key decision: keep API/MCP/vector/import work deferred; expanded retrieval pages are citeable prompt sources but not persisted citations unless model-used.

[2026-07-07] Graph rebuild UX planning — Added approved design spec and implementation plan for in-view Graph rebuild overlay, local error-boundary retry, task-state handling, backend build semantics audit, and first visual polish pass — Key decision: preserve the previous graph during rebuild and replace it in place after task success.

[2026-07-07] Chat 根因修复后续 — 用户从审查遗留风险里选中并修掉 M3/S2/S3 三条（TDD，全套 356 测试绿，lint/build 通过，无 console.log，本轮无 Rust 改动）。M3（删除确认）：ChatView 工具栏删除 + 会话列表删除两处都包 `window.confirm(t("chat.sessions.deleteConfirm"))`，新增 en/zh i18n key。S2（懒创建会话）：ensurePageSession(forceNew=false) 从"无匹配就建会话"改为"只复用；无匹配就清空 activeSessionId/activeSession 并返回 null 不创建"——首次提问由 PageChatPanel.handleSend 创建，New Chat 按钮走 forceNew=true。无匹配清空 active 是防 bug #4（不同 wiki 页面共用一条会话）复发的关键。S3（切页竞态守卫）：chatStore 加 pageSessionEpoch 计数，ensurePageSession 起始 bump 并在每次 await 后检查 superseded 退出；loadSessions 加可选 `{ autoSelect?: boolean }`（默认 true），ensurePageSession 传 `{ autoSelect: false }` 防 loadSessions 的 newest-auto-select 在 epoch 检查前抢跑。双子代理审查又揪出更深一层的同类漏洞（M1/M2）：selectSession 内部同步 set activeSessionId + await 后 set activeSession 都无 epoch 校验，createSession 内部也调 selectSession——被 supersede 的 ensurePageSession 仍可能把 A 页会话提交到 B 页。修法：ensurePageSession 复用/新建两条路径全部内联 invoke（load_chat_session / create_chat_session），只在 epoch 仍 current 时才 set 提交，不再调 selectSession/createSession。新增两个失败测试（mid-load supersede / mid-create supersede）钉住这两条窗口。已知遗留（用户明确未选）：S1 单 store 共享 activeSessionId 导致页面会话带到全局 Chat 视图——仍仅在 plan 范围外。

[2026-07-07] Compile planning and semantic validation �� Landed shared compile instruction rules, CompilePlan DTOs, plan validation, semantic manifest validation, and no-write compile rejection tests �� BYOK now plans before manifest generation; Agent candidate workspaces must provide compile-plan.json before manifest apply.

[2026-07-07] Chat root-cause repair �?�?docs/superpowers/plans/2026-07-07-chat-root-cause-repair.md 完成全部 7 个任务（TDD，前�?348 测试 + Rust chat 40 测试全绿，lint/build 通过，无 console.log）。根因修复点�?1) 滚动边界——给 chat-stream-wrap/chat-scroll-region/chat-conversation/page-chat-shell �?`min-height:0; overflow:hidden/y-auto`，把 SessionToolbar 移出滚动区，解决 grid/flex 子项 min-height:auto 撑高导致输入框被挤出�?2) Composer 发送契约——onSend 改为 `Promise<boolean>` + submitting 态，只在后端确认接受后才清空草稿，发送失�?�?session 不丢字�?3) 重开会话——loadSessions �?active session 时自动选最新一条，恢复可用 composer�?4) Wiki 页面作用域——ChatSession/Summary/CreateRequest 新增 `contextPagePath`（Rust serde `#[serde(default)]` 兼容�?JSON），新增 ensurePageSession 复用/新建页面会话，PageChatPanel �?page.meta.path/title 变化时绑定会话并提供 New Chat 按钮。设计对齐：ChatSessionList 加搜索框/消息�?meta/底部存储说明，Chat 按钮统一�?Lucide (Plus/Pencil/Trash2/Search/SendHorizontal/CircleStop)。侧边栏 citation/save：PageChatPanel �?onOpenCitation �?saveAnswer，RightContextPanel �?selector �?saveStatus/saveAnswer（原�?getState 不触发重渲染）。关键决策：页面 AI 会话�?`.app/chats/{id}.json` �?typed metadata，不是数据库；effect deps �?page.meta.path/title 而非整个 page 对象（plan 指定，避免无关重渲染）。已知遗留（plan 范围外）：单 store 共享 activeSessionId 导致 Wiki 页面会话会带到全局 Chat 视图（S1）；�?plan 设计每个访问过的页面会创建一条会话（去重、随页面数有界，S2）；快速切页存在自纠正�?selectSession 竞态（S3），M1 修复已显著降低其触发频率�?
[2026-07-07] Non-import audit Chinese companion �?新增 docs/audits/2026-07-07-non-import-code-audit-readable-cn.md，把 non-import code audit �?P0/P1/P2、已改善项、不要做事项、导入专项边界和 Phase 0-6 执行计划整理成中文白话对照版。关键判断：该文档不新增技术结论，只帮助逐条理解“为什么要修”和“修什么”，便于后续把原审计交给 Agent 分批实现�?
[2026-07-07] Chat root-cause audit plan �?新增 docs/superpowers/plans/2026-07-07-chat-root-cause-repair.md，对 Chat 页面滚动/按钮风格/重开会话 composer 消失/Wiki 页面 AI 侧边栏会话串用做第一性原理彻查，并拆成布局滚动边界、composer 发送契约、会话自动选择、页面作用域 session、按钮设计对齐、侧边栏 citation/save 六组修复任务。关键判断：根因不是单一 UI 样式，而是滚动容器 min-height/overflow 边界、ChatComposer 无确认清空、ChatStore �?active session 恢复、PageChatPanel 复用全局 activeSession 四者叠加；页面 AI 会话应作�?`.app/chats` JSON �?typed metadata，而不是数据库或标题约定�?
[2026-07-07] Non-import code audit �?新增 docs/audits/2026-07-07-non-import-code-audit-and-plan.md，对导入以外�?Compile/Skill/Prompt、Chat citations/retrieval、Lint/schema、Search/Graph/Index、Agent/BYOK/API 边界做代码级外部对照审计，并�?P0/P1/P2 给出 Phase 0-6 执行计划。关键判断：WikiIndex、wiki/sources compile 保护、Agent Chat read-only、BYOK 编译提示词已有改善；当前最高优先级转向 CompilePlan/manifest 语义校验、真实模型引用闭环、BYOK/Agent prompt 拆分、deep lint 证据质量�?Codex lint sandbox profile�?
[2026-07-07] Audit follow-up todo �?新增 docs/audits/2026-07-07-audit-followup-todo.md，把 docs/audits 三份审计复核后的状态整理成待办表：已修掉的 P0 性能/测试项、还没完全收口的 Chat/Import/Compile/Lint 信任链条、剩下值得继续修的 P0/P1/P2 项目，并用大白话补全为什么要修。关键判断：PERF-001/002/003/004/005 已有实质修复，后续最�?ROI 转向 Chat 引用真实性、Agent/BYOK 能力边界、URL/批量导入可靠性、Compile manifest 语义校验、Deep lint 上下文质量�?
[2026-07-06] P0 Batch 5/Graph reducer render snapshot �?消除 GraphView �?O(E*N) 渲染热路径（PERF-005）。改动：(1) 新增 src/features/graph/graphRenderModel.ts �?�?utility，buildRenderSnapshot(graphData, input) 一次性算�?{ options: GraphRenderOptions, hiddenNodeIds: Set<string> }：communityByNodeId Map �?graphData.layout.communities 一次性构建（之前 currentRenderOptions() �?edge reducer 里每条边 new 一�?Map）；hiddenNodeIds 通过一�?nodes 扫描 + hiddenReasonForNode 构建（之�?hiddenNodeIds(options) �?edge reducer 里每条边扫一遍全�?nodes）；visibleTypeFilterFromHidden �?store �?hidden types"转成 GraphRenderOptions.typeFilter 期待�?visible types"（逐字符等价于�?GraphView.visibleTypeFilter）。RenderSnapshotInput 字段�?RenderState + useGraphStore.colorMode 对齐�?2) 新增 src/features/graph/graphRenderModel.test.ts �?19 �?focused 测试钉契约：community map �?layout.communities 一次性构�?/ layout �?null 时空 map / hiddenNodeIds 覆盖 type/degree/search / �?filter 时空�?/ visualForNode 与改前逐字段一致（color/highlight/borderColor/opacity�? edge 端点 hidden �?edge hidden / 两端可见�?edge 可见 / selected dims non-neighbors / selected-neighbor edge 高亮 / hoveredType dimming / focused-node highlight parity（sizeDelta 1、forceLabel、dims far�? search-hit forceLabel / �?focus root �?edge 全不透明 / 快照不跨调用 memoize / 空图不抛 / 输入字段逐字传播�?3) �?src/features/graph/GraphView.tsx �?RenderRefs �?snapshot/dataRef/stateRef 三个字段；stateRef �?dataRef 提到 refs 之前并传入；dataRef.current = data 每次渲染同步；新 refresh(refs, renderer) �?updateRenderSnapshot(refs) �?renderer.refresh({ skipIndexation: false })；新 refreshVisuals(refs, renderer) 同样 updateRenderSnapshot �?skipIndexation: true（仅视觉变化的场景）；新 updateRenderSnapshot(refs) �?dataRef.current + stateRef.current + useGraphStore.getState().colorMode �?buildRenderSnapshot �?refs.snapshot；createRenderer 改签名收 refs，nodeReducer/edgeReducer �?refs.snapshot（配 emptySnapshot 兜底 sigma 构造期�?reducer 调用），不再�?currentRenderOptions()/hiddenNodeIds(options)——O(E*N) 路径消除。skipIndexation 安全性�?call site 评估：skipIndexation: true 用于 selectedNodeId/focusedNodeId/hoveredType/enterNode/leaveNode/colorMode（hidden set 与位置都不变，仅 opacity/highlight/color 变；visualForNode �?dimmed non-neighbor 返回 hidden:false opacity:0.16，不�?hidden:true，hidden 集合稳定）；保持 skipIndexation: false 用于 search/typeFilter/degreeThreshold（hidden set 变）、data rebuild、layout refresh timer、reset/recompute layout（位置变）。删�?dead �?visibleTypeFilter/currentRenderOptions/hiddenNodeIds 闭包与未�?import（hiddenReasonForNode/GraphRenderOptions/GraphNode）。审查：跑了两个子代理（A 共享上下文、B 全新上下文），无 blocker。A：无缺陷，设计意�?快照新鲜�?skipIndexation 安全�?dataRef 模式/export 一致性全部确认；建议�?focused-node parity + search-hit forceLabel + no-focus-root edge opacity 三个测试（已补）。B 发现一�?minor 真实缺陷已修：B#2 disposeRenderer 没清 refs.snapshot �?第二次建 renderer �?sigma 构造期 reducer 读到�?graph �?stale snapshot（而非 emptySnapshot 兜底），修法�?disposeRenderer 里加 refs.snapshot = null；同步修�?emptySnapshot 注释（B#3）。B#1 观察：visibleTypeFilterFromHidden �?empty = all visible"等价性依�?hiddenReasonForNode �?size>0 guard——加了注释说明这�?load-bearing 依赖。验收标准逐项：hiddenNodeIds/communityByNodeId 不在 per-edge reducer 重建 ✓；search/filter/hover/selected/focused/layout/export 行为一致（visualForNode/visualForEdge 未改，options 等价；export 仍走 visibleNodeIdsForExport 独立重算，与 canvas 同源 store）✓；focused tests 覆盖 render visibility/visual logic�?9 测试）✓；npm run test 337 通过�?18 基线 + 16 + 3 review 补）、lint 0 警告、cargo check 干净、无 console.log ✓。无 UI 改动、无 Rust 改动、无用户 wiki 内容改动。本轮未推进后续批次�?
[2026-07-06] P0 Batch 4/最�?wiki index/cache �?新增 src-tauri/src/services/wiki_index.rs：per-project in-memory index（Mutex<HashMap<project_id, IndexSnapshot>>，每�?entry �?path/mtime_secs/nanos/size/hash/完整 WikiPageMeta/完整 body_markdown/content_reads 计数器）。refresh() 一次走 FileStore::list_markdown_files，对每个文件�?mtime+size 与缓存比对——命中就复用（零 fs.read），未命中就一�?fs.read 拿到 bytes，从同一 buffer 派生 frontmatter+body+meta+SHA256 hash（不再二�?fs.read �?hash，消�?PERF-004 的双读）；删除文件通过 next.retain(live_paths) 从快照里清掉。SearchService �?index: WikiIndex 字段，scan_wiki/search/retrieve_with_excerpts 全部走索引：scan_wiki 从缓�?metas 构建 WikiTree（bookmark �?scan �?overlay，不进缓存——bookmark toggle 只改 bookmarks.json 不动 page mtime/size，缓�?bookmarked �?stale）；search 用缓�?body_markdown 评分（与改前 scoring/snippet/sort 逐字符一致）；retrieve_with_excerpts 从缓�?body �?excerpt（不�?read_page 二次读盘）。read_page（单文件）保持原样。services/mod.rs 注册+导出 wiki_index。关键决策：(1) 缓存**完整 body** 而非 plan 写的"bounded excerpt"——search 评分需要完整正文，缓存 excerpt 等于�?search 留在全量扫描热路径上，正�?PERF-004 相反�?00-500 页目标下内存可接受，chat excerpt �?retrieval 时从缓存 body 截�?2) 索引不持久化（MVP in-memory only，plan 明确允许�?app/index.json 持久化是后续可选）�?3) hash 派生路径�?FileStore::file_hash 都是 SHA-256(raw bytes)，graph content_hash_for 消费 page.hash 不变——graph cache hit/miss 行为不变（有 index_hash_matches_file_store_file_hash 测试钉契约）。审查：跑了两个子代理（A 共享上下文、B 全新上下文）。A：无缺陷，全部验收标准满足，full-body 缓存决策正确，对 valid UTF-8 与改前逐字节一致。B 发现两个真实问题已修�?B#1) evict 从无生产调用�?+ 无项目关闭命�?�?长会话开多项目内存无界。修法：�?MAX_CACHED_PROJECTS=8 上限 + LRU 顺序（order: Mutex<Vec<String>>，每�?refresh 把当前项目提升为 most-recent，超限丢最老），evict 同步�?order；新�?cap_drops_oldest_project_snapshot_when_limit_exceeded + refreshing_an_old_project_promotes_it_and_evicts_a_different_one 两个测试。B#2 content_reads �?pub 与文�?不外�?矛盾 �?�?pub(crate) + #[allow(dead_code)]（lib build 不读、test build 读）。B#3 �?mtime+�?size 的内容编辑：read_page �?FileStore::file_hash（新�?bytes）与 scan_wiki 走索引缓�?hash 可能分歧——这�?plan 明确接受�?mtime/size/hash"风险包络内（hash �?fallback 而非每次重算触发器，否则 PERF-004 要消除的 fs.read 又回来了），记入 gotchas。补的测试：refresh_on_empty_wiki_returns_no_entries_without_error、index_hash_matches_file_store_file_hash_for_the_same_bytes。验收标准逐项：Search/Chat/Graph 共享一个当�?index 快照（同 SearchService 实例，命令层都走它）✓；unchanged Markdown 不再�?metadata/hash 反复整文件读取（mtime+size 命中复用，单�?refresh 只读变更文件）✓；外部编�?删除/Unicode-CJK 文件�?bookmark join 有测试覆盖（wiki_index 12 测试 + index_integration_tests 6 测试）✓；不引入数据库、不改用�?wiki 内容、不�?raw/sources/ 不可变规�?✓。验证（review fixes 后重跑全套）：npm run test 318 通过、lint 0 警告、cargo check 干净无警告、cargo test --lib --no-default-features 422 通过（Batch 4 新增 18 �?Rust 测试：wiki_index 12 + index_integration_tests 6）、无 console.log（src �?src-tauri/src）�?*Windows �?Rust 测试需 --no-default-features**（gui feature + cdylib crate-type �?Windows 触发 STATUS_ENTRYPOINT_NOT_FOUND，已�?gotchas）。本轮未推进 Batch 5�?
[2026-07-06] P0 Batch 3/首屏 bundle 拆分 �?�?App shell 的静态依赖图拆开，Dashboard 首屏不再�?Graph/Sigma、Milkdown、Markdown renderer、Readability 买单。改动：(1) src/components/app/AppShell.tsx �?ChatView/ExportsView/GraphView/ImportView/LintView/SettingsView/WikiView/AgentView �?React.lazy（DashboardView 保留静态作为默认首屏），view dispatch 外包 <Suspense fallback={<ViewFallback/>}>；把静态的 articleToMarkdown/extractArticleFromHtml 改成 requestTextImportPreview �?URL 分支�?`await import("../../lib/readability")`，只有真正做 URL 导入时才加载 @mozilla/readability�?2) src/components/app/RightContextPanel.tsx �?第一�?build 发现 PageChatPanel 静�?import �?ChatView �?MessageBubble/StreamingBubble，�?RightContextPanel �?AppShell 静态引入，整条 ChatView→MessageContent→react-markdown 链（和共享的 496KB markdown-renderer chunk）被拽进启动图；改成 lazy 加载 PageChatPanel（仅 Wiki "ask AI" 助手模式挂载）�?3) src/features/wiki/WikiView.tsx �?WikiEditor �?React.lazy，Milkdown/ProseMirror 只在进入 edit 模式时加载（read/preview 永不加载）�?4) 新增 src/components/app/ViewFallback.tsx �?共享的紧�?shell �?Suspense fallback（居�?spinner + i18n 文案，role="status"，无布局抖动/装饰性视觉）；en.json + zh-CN.json �?shell.view.loading key。Build 证据（fresh npm run build）：主入�?bundle �?~1.93MB（index-Dmu_vTi_.js 基线）降�?314.99 kB / gzip 89.83 kB（index-wuMKAKsx.js）。已验证四大重路径全�?async-only（不�?index.html modulepreload 里）：GraphView 221.62 kB（Sigma+graphology+ForceAtlas2+Louvain）、WikiEditor 347.55 kB（Milkdown/ProseMirror�? 独立 WikiEditor-*.css 16.64 kB（nord 主题）、readability 36.02 kB、共�?markdown-renderer chunk lib-CiyRAvPn.js 496.00 kB（react-markdown + remark-gfm/math + rehype-katex/highlight + KaTeX + highlight.js，只�?ChatView/PageChatPanel �?WikiView/MarkdownReader 触达）。DashboardView 故意留在入口包。关键决策：(1) 不为"干净"做大重构，纯 view-level lazy boundary�?2) markdown renderer 不在 WikiView 内部再拆一层（read 是默认模式，再拆会每次开 wiki 都先转圈），�?ChatView/WikiView �?AppShell 边界�?lazy 自然把共�?renderer 推到 async chunk�?3) 双审查后追加 ErrorBoundary（见下段）。审查：跑了两个子代理（A 共享上下文、B 全新上下文），A#S1/B#4（lazy 回归测试）与 B#2（WikiView MarkdownReader 进一步拆分）经论证后**有意推迟**——前者因 jsdom �?lazy 解析快于 1000ms 默认超时�?fallback 先出�?断言脆，�?module-graph 测试需侵入�?vi.mock，权威真值应�?fresh build �?index.html modulepreload 列表（CI bundle-budget 检查已�?plan �?Defer 名单里）；后者因 WikiView 已在 AppShell 边界 lazy（满足首屏标准），共�?lib-CiyRAvPn chunk 已满�?首次开 Chat/Wiki 才加�?renderer"，再拆只�?开�?Wiki 立刻编辑不读"这一罕见场景却给每次读页�?spinner，UX 不划算�?*两位审查者的头号 finding 都是"React.lazy 没配 ErrorBoundary"**——已采纳：新�?src/components/app/ViewErrorBoundary.tsx（最�?class ErrorBoundary：getDerivedStateFromError + componentDidCatch �?console.error），三处�?Suspense 同址包裹（AppShell dispatch �?key=activeView 让导航重置边界、RightContextPanel PageChatPanel、WikiView WikiEditor），错误�?role="alert" + i18n "重新加载" 按钮�?window.location.reload()，en/zh-CN �?shell.view.loadError + shell.view.retry；同时删�?ViewFallback 里没用上�?label prop。动机：开发期 Vite 直连 /src 模块在内存必成功，打包后 tauri:// 加载 Rollup async chunk �?Windows 杀�?部分更新下可�?reject，无边界�?React.lazy 抛错冒到根、整�?shell 白屏（已�?gotchas）。验证（review fixes 后重跑全套）：npm run test 318 通过、lint 0 警告、cargo check 6.54s、tsc -b 干净、无 console.log（src �?src-tauri/src）、fresh build �?bundle 315.05 kB / gzip 89.85 kB（ErrorBoundary �?+~0.06 kB，四大重路径仍全 async-only）。观察（未处理，超出 Batch 3 范围）：settingsStore chunk 171.75 kB �?modulepreload（App.tsx 静�?import useSettingsStore），未来批次若想再瘦首屏可考虑延后 settings bootstrap 或拆 store。无视觉/UX 重设计，Codex 紧凑 shell 保持。本轮未推进 Batch 4-5�?
[2026-07-06] P0 Batch 2/waitForTaskTerminal 永久等待修复 �?重写 src/lib/waitForTaskTerminal.ts，消除所�?promise 永不 settle"路径：新�?{ pollMs 默认 1s, timeoutMs 默认 10min } 选项；递归 setTimeout 轮询 get_task 作为丢事�?监听器注册前终�?瞬�?get_task 失败的兜底；deadline setTimeout 超时后以类型�?WaitForTaskTerminalTimeoutError (code "TASK_WAIT_TIMEOUT", taskId, timeoutMs) reject；cleanup() �?resolve/reject/timeout 三条路径都释放全部监听器 + 两个定时器；监听器注册失败保持非致命（browser-dev 兼容，轮询是权威兜底）。轮询路径补�?next.id === taskId 校验（与事件路径对称），pollMs 钳到 >=1 �?setTimeout(0) 饿死宏任务队列。关键决策：(1) 不改调用方——graphStore.runGraphBuild �?AppShell Import preview 已有 try/catch + errorMessage 会把超时 reject 清晰暴露给用户，用户自有�?AppShell.tsx 一行没动；(2) 删掉旧的"保持 pending"测试（它正在固化 bug），换成 5 �?fake-timer 测试覆盖轮询恢复/瞬态失败重�?错误 id �?resolve/类型化超�?清理/事件 resolve 后清理定时器�?3) 两个审查子代理已跑（A 共享上下文、B 全新上下文），无 blocker，修�?B#1(pollMs 钳制)+B#2(taskId 校验+测试)。验证：npm run test 318 通过、lint 0 警告、cargo check 0.68s、tsc exit 0、无 console.log。遗留：调用方暂未按 error.code 分支�?超时重试"友好提示（P0 可接受，后续批次）；本轮未推�?Batch 3-5�?
[2026-07-06] P0 Batch 1/测试可信�?+ Graph jsdom 噪声 �?修复 App.test.tsx 失败�?sidebar 断言（topbar �?Collapse/Expand sidebar 按钮是用户未提交改动里故意删的，现在只能通过�?splitter 拖到 SIDEBAR_COLLAPSE_THRESHOLD 以下折叠）；�?src/test/setup.ts 里把 HTMLCanvasElement.prototype.getContext 重写为返�?null，消�?jsdom 反复打印�?"Not implemented: HTMLCanvasElement.prototype.getContext" 噪声（这是环境设置不是全局�?console；GraphView �?try/catch 会落�?"canvas unavailable" 占位）；新增回归测试用非枚举标记属�?__silencedJSDOMCanvasNoise + 端到�?fallback 文本 + GraphView 真实 console.warn 断言三重保护 �?关键决策�?1) 不用 vi.spyOn 去断言"噪声没了"，因�?jsdom �?virtualConsole→runner stderr 路由，console.error/stderr.write/emit �?spy 都抓不到（已�?gotchas）；(2) 不全局 suppress console.warn/error，改�?stub 从源头消除并保留 GraphView 真实 warn�?3) 跑了两个审查子代理（A 共享上下文、B 全新上下文），有效问题已修。验证：npm run test 314 通过零噪声、lint 0 警告、cargo check 0.62s、tsc exit 0、无 console.log。遗留：本轮未推�?Batch 2-5�?
[2026-07-06] P0 performance/stability fix planning �?Created `docs/fixes/2026-07-06-p0-performance-stability-progress.md` as the ongoing repair plan/progress log for the audit's urgent items: test reliability, task terminal wait timeout, first-screen bundle splitting, wiki index/cache MVP, and Graph reducer hot path optimization �?Key decision: implement in five small verifiable batches, preserve the dirty worktree/user-owned changes, and defer broad AppShell/command/service refactors until after P0.

[2026-07-06] Performance/complexity audit report �?Wrote `docs/audits/2026-07-06-performance-complexity-audit.md` from the first-principles审查，覆盖首�?bundle、全量扫描、Graph 渲染、Chat/Task 流、Tauri command/service/store 复杂度，并给�?P0/P1/P2 治理计划 �?Key decision: 先修测试可信度、任务终态等待、首屏拆包、wiki index、Graph reducer 热点；暂不做大重构，后续�?use-case 下沉�?controller/hook 拆分逐步瘦身�?
[2026-07-05] Code-level detail audit �?5 个对抗式子代理并行审查（提示�?Lint/Chat-skill、UI 微观、导入管线、性能/屎山、Karpathy 对照），产出 `docs/audits/2026-07-05-code-level-detail-audit.md`，与同日产品策略审计互补 �?关键决策：聚焦代码细节（提示词硬度、Lint 覆盖、Chat 是否�?skill、UI 滚动/对齐/溢出、导入缺陷、性能/屎山），每条发现�?file:line 证据。三大系统级问题�?1) BYOK �?Agent 提示�?能力严重不对等（compile 6 步骤 Agent �?BYOK 无；Chat Agent �?Read/Grep/Glob 全库�?BYOK 只看 6×1200 字符片段，UI 不告知）�?2) 后端无内存索�?+ 同步 IO 阻塞 IPC（scan_wiki/search/get_graph/git checkpoint 每次全量读盘+SHA-256，没 spawn_blocking）；(3) Chat citations 是检�?Top-N 透传，无 claim 级对齐核验。评分：提示�?Skill 5/10、导�?6/10、性能 4/10、代码健�?7/10、UI 7.5/10。Karpathy 对照：nashsu Ingest 流程更具体（读源→讨论→更新 10-15 页→index→log），且用本地 HTTP API �?Agent 查询，本项目可借鉴�?Chat 封装�?wiki-query skill + 暴露只读 127.0.0.1 HTTP 端点�?BYOK tool-calling。性能代理首版超时，重派窄范围版完成。遗留：未跑 tauri dev 复现 UI 缺陷、后端错误模�?Secret/Task 服务未深审（前次已覆盖）�?
[2026-07-05] Product/adversarial audit �?Wrote first-principles user-perspective audit comparing local implementation against Karpathy LLM Wiki, nashsu desktop app, agent-skill implementations, and Obsidian plugin patterns �?Key decision: prioritize time-to-first-value, BYOK retrieval trust, import transparency, task/checkpoint visibility, and purpose/schema productization before adding broad new feature surface.

[2026-07-05] Chat convenience frontend - Added typed convenience edit metadata, store IPC methods, Chat toolbar/message controls, and Security revocation UI - Key decision: frontend only toggles global authorization and calls backend commands; filesystem writes and rollback remain backend-owned.

[2026-07-05] Chat convenience command integration - Added all-Agent convenience invocation profiles, authorized write-intent Chat routing, per-turn checkpoint/audit/rollback handling, and keep/rollback commands - Key decision: default Chat remains read-only; direct project writes require backend authorization plus write intent and use Git HEAD as the rollback boundary.

[2026-07-05] Chat convenience Git helpers - Added ignored-file baseline support for audit and rollback - Key decision: Chat integration can preserve pre-existing ignored files while treating new ignored Agent artifacts as added changes and removing them during rollback.

[2026-07-05] Chat convenience Git helpers - Hardened changed-size accounting, untracked diffs, and scoped rollback after review - Key decision: audit changed size uses HEAD/worktree byte estimates instead of Git numstat line counts; rollback uses scoped restore/clean so parent Git repos are not reset outside the project folder.

[2026-07-05] Chat convenience Git helpers - Added Git changed-file DTOs, structured status/numstat parsing, diff-since-HEAD, rollback-to-HEAD cleanup, and Git-to-audit conversion tests - Key decision: service APIs stay on ProjectContext/BackendError while rollback runs only from the project root cwd.

[2026-07-05] Chat convenience audit �?Tightened write intent routing so ordinary questions containing add/update/delete stay read-only �?Key decision: write keywords require an explicit request cue unless they begin the command.

[2026-07-05] Chat convenience audit �?Fixed mixed imperative/write intent classification so commands that also request explanation remain write actions �?Explicit command prefixes now win before read-only cues.

[2026-07-05] Chat Convenience Models/Audit - Added deterministic chat intent classification, convenience edit DTOs, audit rules, and AppState service registration - Key decision: command routing integration intentionally deferred to the chat command integration task.

[2026-07-05] Settings/Chat convenience authorization - Implemented global-only Chat convenience authorization model, SettingsService methods, Tauri commands, and settings store IPC helpers/tests - Key decision: project authorization is keyed by project id plus deterministic root-path fingerprint and is stored only in global machine settings.

[2026-07-05] Chat convenience mode implementation plan - Wrote task-by-task plan for global local authorization, backend audit/rollback service, all-Agent convenience invocation, Chat command integration, frontend controls, settings revocation, and verification workflow - Key decision: implementation should preserve existing read-only Chat as default while adding direct-project write flow only for authorized write-intent turns.

[2026-07-05] Chat convenience mode design - Wrote approved design spec for project-level Chat convenience mode with real-project Agent writes, per-turn Git checkpoints, post-write audit, hard-violation rollback, soft-violation keep/rollback prompts, global local authorization, and last-edit rollback - Key decision: prioritize convenience by supporting all installed Agents with best-effort constraints, while preserving hard stops for delete/raw/config/external-conflict cases.

[2026-07-05] Chat/Agent wiki context bugfix - Fixed Agent Chat workspace and page Ask context: Chat now routes only verified read-only project agents (Claude/Codex), lets them read wiki/ from the current project, sends Claude prompts via stdin, keeps Codex ephemeral/read-only/ignore-rules, and includes full pinned page bodies for page-level Ask - Key decision: unsupported Agent CLIs (OpenClaw/Hermes) are filtered out of project-root Chat until a verified read-only profile exists; BYOK continues to use inline context.

[2026-07-05] batch-05 Project/Task/Health UX implementation - Implemented task timeline sorting, durable Lint history restore, native directory picker/new-project parent path, compact project launch actions, enriched recents, and review fixes - Key decision: Lint history persists in `.app/lint-history.json` + `.app/lint-reports/`; launch opens only native-picked folders and skips missing recents on bootstrap.

[2026-07-05] batch-04 export workflow preview implementation - Implemented shared Rust HTML export resolver, open-in-browser command, row preview/actions, source preview mode, workspace focus/maximized preview, and review-driven focus/keyboard/error-code fixes - Key decision: React only calls typed Tauri commands; export HTML paths are validated in Rust; cargo test export remains blocked by local Windows loader 0xc0000139 while cargo check --lib --tests passes.

[2026-07-05] batch-03 knowledge/chat/bookmarks implementation - Implemented app-owned wiki/export bookmarks, sidebar favorites, Chinese local-search normalization, pinned-page chat retrieval, Wiki Ask AI panel, pinned citation UI, integration tests, and review fixes - Key decision: user favorites stay in `.app/bookmarks.json`; Chat remains local retrieval plus Agent/BYOK answer flow; export/sidebar/page-chat async paths guard against project or page switches.

[2026-07-04] graph/dashboard batch-02 implementation - Implemented graph cache read-through recovery, event-driven graph rebuild state, pure graph render styling, graph legend/inspector empty states, and dashboard graph overview - Key decision: GraphData schema stays unchanged; get_graph resolves live wiki state through GraphService::resolve; stale cached layouts are recomputed on the client and persisted through the existing save_layout path.

[2026-07-04] Workbench Shell/Layout/Theme follow-up - Removed manual sidebar collapse button, kept splitter-driven icon rail collapse/expand, fixed single-line splitter boundaries, redesigned preset palettes, and fixed collapsed-sidebar drag-out regression - Collapse state now derives from sanitized sidebar width; legacy collapsed snapshots migrate to the icon rail width.

> 每完成一个重要进度节点，按时间倒序在此追加一条记录（最新在上）�?> 格式：[YYYY-MM-DD] 模块/任务 �?完成内容摘要 �?关键决策/遗留问题�?
[2026-07-04] Workbench Shell/Layout/Theme �?Implemented resizable panes, collapsible sidebar, compact project switcher, and preset reading themes �?Layout preferences stay in localStorage; color theme preset is a global app setting.
[2026-07-04] docs/fixes/batch-05-plan �?产出 Project / Task / Health UX 详尽实现计划 �?新增 `docs/fixes/plan/plan-batch-{05}.md`，覆盖任务日志时间线排序、Lint 历史持久化与恢复、项目初始页三入口、Native 目录选择、新建项目父目录语义、recent project 属性补全、WHEN-THEN 验收标准和执行校验；关键决策：任务抽屉默认按执行时间排序，Lint 历史写入 `.app/lint-history.json` + `.app/lint-reports/`，启动页不再接受手填路径，React 仍只通过 typed Tauri commands 触发文件系统行为�?
[2026-07-04] docs/fixes/batch-04-plan �?产出 Export Workflow Preview 详尽实现计划 �?新增 `docs/fixes/plan/plan-batch-{04}.md`，覆盖导出记录行预览、内联文件操作、预览最大化 focus mode、浏览器打开 HTML、后端路径校验、验收标准和验证流程；关键决策：React 仅触�?typed Tauri commands，导�?HTML 路径�?Rust 服务统一校验并使�?direct-argv 打开，最大化预览恢复进入前的右侧面板状态�?
[2026-07-04] docs/fixes/batch-03-plan �?产出 Knowledge Interaction / Chat / Bookmarks 详尽实现计划 �?新增 `docs/fixes/plan/plan-batch-{03}.md`，覆�?`.app/bookmarks.json` v2 资源收藏模型、Wiki/HTML 收藏接线、中文自然问法本地检索修复、Wiki 页面 Ask AI 右侧栏、验收标准和执行校验；关键决策：用户收藏仅写应用私有状态，frontmatter `starred` 保留兼容显示但不进入侧栏精选，Chat 检索仍为本地关键词规范化而非向量/LLM 搜索�?
[2026-07-04] docs/fixes/batch-02-plan �?产出 Graph Dashboard Visuals Reliability 详尽实现计划 �?新增 `docs/fixes/plan/plan-batch-{02}.md`，覆盖图谱缓�?read-through 恢复、graphStore 状态机、纯渲染视觉规则、图�?Inspector 交互、Dashboard 图谱概览、WHEN-THEN 验收标准和执行校验；关键决策：保�?`GraphData` schema 不变，`get_graph` 改走 `GraphService::resolve`，Dashboard 范围依据 02 标题、审计和设计稿收敛为只读图谱概览�?
[2026-07-04] docs/fixes/batch-01-plan �?产出 Workbench Shell Layout Theme 详尽实现计划 �?新增 `docs/fixes/plan/plan-batch-{01}.md`，覆盖可调整面板、可折叠侧栏、紧凑项目切换器、内置配色主题、验收标准和执行校验；关键决策：布局偏好仅写 `localStorage["llm-wiki-desktop.layout.v1"]`，配色预设作为全局设置持久化，不写入项�?`.app/settings.json`�?
[2026-07-04] docs/fixes/theme-presets �?修正 01 规范条目 D 的配色主题语�?�?将“用户自由挑�?输入配色”收敛为“从内置预设配色主题中选择”，并在 `docs/fixes/01-workbench-shell-layout-theme.md` 写入配色参考链接；技术方案同步改�?`ColorThemePresetId`、静�?preset token map、设置页主题预设列表�?Markdown preview，不再提�?hex/RGB/HSL 输入或高级色板覆盖。终验：`npm run test`、`npm run lint`、`npm run build` �?console.log 扫描均通过；`npm run build` 仅保留既�?Vite large chunk 警告�?
[2026-07-04] docs/fixes/review-fix �?完成规范文档双审查后的收口与终验 �?根据共享上下文审查和 fresh review 反馈，修正导出规范拆分导致的 15 条清单计数偏移，补充导出最大化需�?shell �?focus mode、浏览器预览使用 direct-argv opener、面板布局持久化用 `localStorage`、HTML 收藏�?`exportRecordId` 为主键、打开已有项目和打开文件夹为项目共用目录选择器；同时�?exports 测试 fixture 改为类型校验�?`ProjectSummary`，避�?`as never` 掩盖类型漂移。终验：`npm run test`、`npm run lint`、`npm run build` �?console.log 扫描均通过；`npm run build` 仅保留既�?Vite large chunk 警告�?
[2026-07-04] docs/fixes/spec-grouping �?�?15 �?UI/BUG/功能/优化清单生成 5 份独立规范文�?�?读取 Spec 基本文档�?`docs/fixes/00-codebase-audit.md` 后，按代码边界整合为 workbench shell/theme、graph/dashboard、knowledge interaction/chat/bookmarks、export workflow、project/task/health UX 五组，写�?`docs/fixes/01-workbench-shell-layout-theme.md`、`02-graph-dashboard-visuals-reliability.md`、`03-knowledge-interaction-chat-bookmarks.md`、`04-export-workflow-preview.md`、`05-project-task-health-ux.md`；每个条目包含需求概述、现状分析、方案设计、UI/交互、验收标准、风险注意事项和实施步骤。关键决策：收藏 UI 统一�?`.app/bookmarks.json` 作为用户私有 app 状态，frontmatter `starred` 仅保留兼容信号；Chat “约束先�?”先做本地检�?query normalization，不引入向量库或普通搜索模型调用；新建项目路径语义定为“父目录 + 项目名”。遗留：本文档为规格输出，未实现功能代码�?
[2026-06-29] cross-cutting-fe/loop P0+P1 �?�?本轮收敛 �?cross-cutting 前端四条 scope 全部 verified 并通过双子代理审查（接 cross-cutting-be 2026-06-21 �?verified 的后端）。①P0-1 ConfirmationDialog 检查点如实显示：AppShell 原硬编码 `checkpointExists={false}` �?`checkpointHash != null`（后�?compile 冲突已透传 checkpoint_hash，TS 接口早有字段）；compile 冲突显示 available，lint/chat/import 显示 not created yet。②P0-2 Dashboard 首屏 Agent 检测：WorkspaceView �?effect——projectId 变化时跑 refreshCapabilities �?agentRoute（守 hasTauri），首屏不再误显"unconfigured"；refreshCapabilities 加陈旧写入守卫（探测在途切项目则丢弃，�?A 慢探测覆�?B �?agentRoute）。③P1-1 import 预览�?250ms 轮询：抽 `src/lib/waitForTaskTerminal.ts`（订�?task://completed|failed|cancelled + race-safe get_task，已 terminal 短路，settled 清监听），AppShell.requestImportPreview 用它替换 while+setTimeout(250)，统一事件驱动。④P1-2 destructive 按钮 variant：Button cva �?danger variant，ConfirmationDialog �?`variant=destructive?danger:secondary` �?className 覆盖，视�?1:1、语义对齐。双子代理审查（A 共享上下�?/ B 零偏见）�?�?BLOCKER、B 独立确认 P0-1/P0-2/P1-1 测试非假阳性；合并�?3 项（commit `f1d0bff`）：refreshCapabilities 陈旧写入守卫、waitForTaskTerminal get_task 返回 null �?`next.status` 崩溃守卫、FE-P0-1 undefined 字段回归测试。关键决策：(a) waitForTaskTerminal 接收 task 对象而非 taskId——保�?started �?terminal 则不进循�?语义、省一�?get_task、App 集成测试不需 mock get_task/listen（先 taskId 入参�?App.test 挂起，回溯改签名）；(b) checkpointHash `!= null` �?null+undefined 都判 false�?c) P1-2 严格 scoped"视觉不变"，danger hover 同色既有 bug �?roadmap 不在�?loop 改。终验：npm test 163/31 全绿、npm lint 0 warning、无 console.log、未�?src-tauri/ �?UI-Frontend-design/�?*遗留（非�?loop scope�?*：danger hover 无反馈、graphStore �?250ms 轮询（graph 板块，可复用 waitForTaskTerminal 收敛）、useProjectStatus �?refreshCapabilities 双路探测（深层架构单数据源）、waitForTaskTerminal 无超时（backend 静默丢事件才触发，与原轮询等价）、托盘运行时语言切换需重启窗口（src-tauri/平台 blocked）。commits：`310f4b6`→`4575f68`→`32e4a9a`→`e9a240e`→`f1d0bff`�?
[2026-06-29] lint-fe/loop P0+P1 �?�?本轮收敛 �?lint 前端壳七�?scope 全部 verified 并通过双子代理审查（消�?lint-be �?verified �?error �?/ apply_lint_fixes / lint-ignore）。①P0 批量自动修复 CTA：顶�?自动修复 (N)"主按�?�?`LintBatchConfirmDialog`（提�?Git 检查点）→ 收集 safe 路径 `read_wiki_page` hash �?`apply_lint_fixes`（一次检查点）→ applied/skipped(�?reason)/needsConfirmation 分类提示 + high-risk 待确�?banner（点选进既有内联确认流）。②P1 摘要卡四宫格 `LintSummaryCards`（error/warning/info/passed，跟�?mode 计数；passed 反推 localReport 未触发规则）。③P1 已通过�?`LintPassedSection`（绿�?check badge 行）。④P1 模式分段 `mode` store + `.seg`（全�?本地/Agent 深度，aria-pressed�? list/summary �?mode 过滤。⑤P1 issue 卡片三列网格 + tags(severity/issueType/source/可自动·高风险) + 内联修复/查看详情按钮；修 `var(--error)`→`var(--danger)` token bug（原引用不存在的 CSS 变量）；卡片�?`div[role=button]` 避免嵌套 button。⑥P1 修复方案 `check-row` radio（应用修�?忽略本次�? `add_lint_ignore` 接线 + 重跑。⑦P1 安全检�?checkbox：检查点/提交 = checked+disabled（硬边界，如实反映不可关数据安全）；重编�?= 真接 `start_wiki_compile`（勾选且修复成功后触发）；偏�?localStorage 持久化（settings_service �?lint 偏好为后�?blocked，FE 兜底）�?*双子代理审查合并修复**：BLOCKER——单�?safe 修复�?hash：后�?`write_missing_frontmatter_fix` �?safe 修复**强制** expected_hash（缺�?`LINT_FIX_HASH_REQUIRED`），�?`applyFix` �?null �?safe 单条修复必失败；�?`applyFix` �?expectedHash 入参、`handleApplyFix` �?safe 先取 `meta.hash`（两子代理独立命中同根因）。SHOULD-FIX：批�?skipped �?reason、忽略后清选中、mode 切换�?notice、seg `role=tablist`→`role=group`+aria-pressed。终验：`npm run test`(152,30 files)+`npm run lint`(0 warning)+`tsc --noEmit` 全绿、无 console.log；浏览器预览确认 app 干净启动（lint 视图 Tauri 门控，组件级验证�?jsdom）�?*遗留（非�?loop scope�?*：B1 settings_service 持久�?lint 安全偏好；P2 详情�?rightpanel__meta/行级 diff/Agent 建议卡、状态栏 lint 状态、列表方向键 a11y、本地规则开关�?
[2026-06-29] lint/loop P0+P1 �?�?本轮收敛 �?lint 后端三条 scope 全部 verified 并通过双子代理审查。①P0 severity 分级：DeadLink/IndexDrift `Warning`→`Error`（PRD-LINT-001，摘要卡 error 计数有数据支撑），MissingFrontmatter 保持 Warning（commit `b2689be`）。②P0 批量自动修复：新�?`apply_fixes_batch`+`apply_lint_fixes` 命令——一�?`create_scoped_checkpoint` 覆盖全部 safe（MissingFrontmatter）路径后逐条写盘（共�?checkpoint hash，回归测试断言 2 safe�? commit），high-risk（DeadLink/IndexDrift）收�?`LintBatchConfirmation{issue, pending_action}` 交命令层注册 confirmation_registry，前端逐条走既�?`apply_lint_fix(confirm=true, action_id)` 确认；safe �?hash 提前 skip、路径越�?fail-fast、per-item 失败收集不整批中止；抽取 `write_*_fix`+`resolve_checkpoint` 让单�?批量共享 transform（commit `cdc6f17`）。③P0/P1 lint-ignore 持久化：`.app/lint-ignore.json`，key=`(path, rule)`，`run_local_lint` 过滤被忽�?issue；损�?缺失文件回空不崩（mirrors bookmarks reader）；add/remove/list 命令（commit `ab8b77a`）。双子代理审查（A 共享上下�?+ B 零偏见）合并修复（commit `8460329`）：�?*�?bug** index-drift 确认 id 原为 `lint-index-drift-{path}` �?target，batch 处理 wiki/index.md �?ghost 链接�?id 全碰撞、registry last-write-wins 只留最后一条（regen �?holistic 所以结果偶然正确但 id/展示错乱）→ id �?target 去重；②防御�?add_ignore �?`..` 路径（path 是纯 key �?fs sink，但守边界）。驳�?B 的另两条 BLOCKER：B3（clean-file 回滚�?HEAD "无效"）误报——HEAD �?per-path `git checkout` 目标可正确回滚未提交写；S1（ignore 波及 agent issue）误报——agent issue �?`parse_agent_issues`→`.app/lint-reports/` 不经 `run_local_lint`，过滤只作用 local。终验：`cargo check --lib --tests` 全绿 0 warning（我方）、`cargo clippy --lib --tests` 我方 0 warning（余 3 warning �?app_state/export_service/import_service 范围外）、`npm run test`(146,30 files)+`npm run lint` 全绿；`cargo test` 运行期受 Windows runner DLL 入口�?0xc0000139 阻塞（环境问题，�?gotchas 2026-06-29/06-23），�?`cargo check --lib --tests` 为代码正确性闸门�?*遗留（非�?loop scope�?*：前端集成（批量按钮/ignore UI/summary 卡接 error 计数）、P2 项、既�?`regenerate_index` �?fs 读与 dead-link �?target id 碰撞。commits：`b2689be`→`cdc6f17`→`ab8b77a`→`8460329`�?
[2026-06-29] exports/loop P0+P1 �?�?本轮收敛 �?exports 板块三条 scope 全部 verified 并通过双子代理审查。Item 4 已生成列表表格化（commits `2a6250a`）：`ExportsView` 记录列表�?`<ul>` 改为 `.table`（文件名/类型/�?时间/执行路径/状�?操作），成功�?`badge--success`+预览+打开位置，失败行 `badge--danger`+查看日志（开任务抽屉�?重试；补 zh-CN/en 表格/状�?路由文案 + 成功/失败两行布局测试。双子代理审查（A 共享上下�?+ B 零偏见）合并修复（commit `13dfe46`）：①blocker 取消态误判——`run_export_task` spawn 闭包原仅�?`status==Cancelled`，但 `Cancelling->Failed` 是合法迁移，用户取消的导出会被误�?Failed 并落记录；改为取消令�?`is_cancelled` + `Cancelling/Cancelled` 窗口双判。②major 浏览加载态——ExportDialog 源页浏览 `pages===null` 过载且误显表刷新文案，改专用 `browseLoading` �?+ `exports.dialog.browseLoading` 文案；DEFAULT_TEMPLATE 提常量。③major 预览竞态——终端处�?effect �?`finishedId` 去重（`processedTerminalRef`）且仅精确匹�?`taskId` 才自动预览（去掉 `fresh[0]` 错条回退）。终验：`npm run lint` 全绿、`npm run test` 146 通过�?0 files）、`cargo check --lib --tests` 全绿；`cargo test` 运行期受 Windows runner DLL 入口点加载失败阻塞（环境问题，见 gotchas 2026-06-29）。commit 序列：`1596b9e`→`e956c4f`→`df474be`→`2a6250a`→`13dfe46`�?
[2026-06-29] exports/loop P0+P1 �?Item 3 新建导出对话框（commit `df474be`）�?新增 `ExportDialog.tsx`（仿 RunAgentDialog 结构：类�?segmented / 源页 input+scan_wiki 可搜索浏�?/ 模板下拉 default-serif·modern-sans·editorial-magazine / 执行路径 auto·agent·byok / 4 选项，embedCss 锁定勾选反映自包含硬约束）；ExportsView 顶栏内联�?Generate 换为「新建导出」按钮开弹窗，submit 透传 ExportPrefs，任务成功且勾�?openPreview 时自动加载预览；移除 store 中已无消费者的 selectedType/sourcePath 字段；补 zh-CN/en 文案 + 弹窗挂载测试（类型切换→Generate 启用/禁用验证）。验证：`npm run test`(145,30 files)+`npm run lint` 全绿。待办：Item 4 列表表格�?失败徽章+重试�?
[2026-06-29] exports/loop P0+P1 �?Item 2 前端类型+store 透传（commit `e956c4f`）�?`types/export.ts` �?`ExportContentOptions` + `DEFAULT_EXPORT_OPTIONS`，`StartExportRequest`/`RegenerateExportRequest` 加可�?`template`+`options`；`stores/exportStore.ts` 增可�?`ExportPrefs`（route/template/options）参数透传�?invoke，WikiView �?4 参调用向后兼容。验证：`npm run test`(144,30 files)+`npm run lint` 全绿。Item 1 后端（DTO+prompt 注入模板/选项+失败记录持久化，commit `1596b9e`）此前已完成，`cargo check --lib --tests` 全绿；`cargo test` 运行期受运行�?app �?exe + Windows runner DLL 入口点阻塞，收尾�?app 后终验。待办：Item 3 新建导出对话框、Item 4 列表表格�?失败徽章+重试�?
[2026-06-24] frontend/UI comprehensive polish �?完成全前端细节审计与响应�?可访问性修整：移除 1120px 最小宽度与重复视图说明，右侧上下文栏和启动页信息栏支持收起且窄屏默认关闭；工具栏、设�?导出/Lint/Chat/Wiki/冲突对比�?1180/980/820/760px 分级重排；统一按钮防溢出、主�?token、弹�?Escape/焦点圈定/焦点恢复、图标按�?aria-label、状态栏自适应；清理无数据占位与重�?Settings 文案。新增按钮、CSS 合约和弹窗交互回归测试，并以 1440/980/760px、英�?中文、启动页/项目�?抽屉/新建项目弹窗截图逐项验收。计划与设计文档�?`docs/superpowers/specs/2026-06-24-ui-polish-design.md`、`docs/superpowers/plans/2026-06-24-ui-polish.md`；保留既�?Vite �?chunk 警告作为非本任务阻塞项�?
[2026-06-24] graph/loop P0+P1 �?双子代理审查修复（commit `67ad2f7`）�?�?graph 板块 P0+P1 成果�?CLAUDE.md 强制的双子代理审查（A 共享上下文逻辑审查 + B 零偏见盲点审查），合�?3 blocker/major + �?nit 并修复：�?`graphExport.buildGraphSvg` 镜像 type/degree/search 筛选（sigma nodeReducer 只影响渲染不�?graphology 数据，否则导出画出已隐藏节点）；bbox 用循环替�?`Math.min/max(...spread)` 防大图栈溢出。② `GraphView` �?`registerActions`/`exportSvg` 闭包�?`useGraphStore.getState()` 取最新态，项目切换/数据重载后不再陈旧；zoom 对退�?camera ratio（NaN/Infinity）兜底。③ `legendEntries` 社区调色按原�?community id 索引（与 `baseColorFor` 一致，原按排序序号导致图例色块对不上画布）；type 计数减去 degree 阈值过滤掉的节点（图例真正跟随画布）。④ `GraphInspector` 邻居列表�?type+degree 可见性过滤（点击不再选中不可见节点）；度数滑块上限取数据真实 `maxDegree` 而非硬编�?20；导�?PNG 按钮去掉 `disabled={!node}`（导出整图不依赖选中节点）。测试：export 10(+3)、legend 7(+2)、neighbor 4、store 1。验证：`tsc -b` + `npm run test`(130,27 files) + `npm run lint` 全绿，无 console.log，未�?`src-tauri/`与`UI-Frontend-design/`。至�?graph 板块 P0+P1 本轮真正收敛�?
：①画布视觉对齐设计�?`app.css §32`�?2px 网格+中心辐射+圆角+边框，`.graph-canvas` �?styles.css 实现，容器重构让 sigma canvas 填充卡片）。②左上纵向悬浮缩放控件 `GraphCanvasControls`（拆出原顶部横条 zoom/fit/reset，设计稿 `.graph-controls` 5 按钮列）；顶部横�?`GraphControls` 精简�?seg(着�?+search+rebuild+exportSvg+counts。③左下图例 `GraphLegend`（colorMode 动态：type 6�?计数 / community top8+其它 / plain 静态；隐藏类型 dim+计数归零，纯函数 `legendEntries`）。④右上信息�?`GraphInfo`（mono，缩�?1/camera.ratio 订阅 `camera.on('updated')`、选中 label、度数）。⑤类型 checkbox 筛�?+ 度数阈值滑块：graphStore �?`typeFilter:Set`+`degreeThreshold`+`toggleTypeFilter`/`setDegreeThreshold`；nodeReducer 统一 hidden 语义（type/degree/search 非匹配均 `hidden=true`，去掉原搜索 `DIM+hidden` 里永远看不到�?DIM）；Inspector 筛选区 6 checkbox+计数+range 0-20。⑥选中节点相邻列表（`graphNeighbors` 纯函数，degree 降序，前6+view-all，type badge�?图谱状态段（nodes/edges/communities(Louvain)/layout�?操作段（openPage/exportPng/recomputeLayout）。⑦SVG/PNG 导出 `graphExport`（纯函数 SVG 构建，bbox 归一化到 1200 viewBox，选中节点 teal 描边环，XML 转义；PNG �?Image+canvas.toDataURL；浏览器 Blob `a[download]` 下载，零后端改动守「不自动�?src-tauri」）。关键决策：(a) 导出走浏览器下载而非 Tauri save dialog，图谱导出是用户产物非知识库内容、不触发 Git 检查点、零 capability 改动�?b) Inspector 需触达 graphology 实例（在 GraphView refs 里），通过 `registerActions({exportPng,recomputeLayout})` 把闭包发布进 store，Inspector �?store 调用，GraphView 卸载时清空；(c) 搜索语义修正顺手做（roadmap P2）——非匹配�?hidden �?DIM。纯函数单测 16 例（export 7/legend 5/neighbor 4�? store toggle/clamp 1 例。验证：`tsc -b`+`npm run test`(125,27 files)+`npm run lint` 全绿，无 console.log，未�?`src-tauri/`与`UI-Frontend-design/`。遗留（�?roadmap 未动手）：键盘可达、搜索去抖、状态栏图谱行、节�?label 字号/edge token 校准（roadmap §3-4 P2）�?
[2026-06-24] import+compile/sources-as-extracted-originals �?让导入后的「原文」在 `wiki/sources/` 直接可见、可读，编译回归 Karpathy「只生成引用原文的派生页」模�?�?设计文档：`docs/superpowers/specs/2026-06-24-sources-as-extracted-originals-design.md`（已�?spec review + 用户批准）。后端：�?`import_service::confirm_import` 新增 `promote_extracted_to_sources`——确认时把每篇确认源的可读文本逐字写成 `wiki/sources/<clean-name>.md`（带 `type: source` frontmatter），clean-name �?CJK 安全清洗 + `-2` 碰撞后缀；文本来源双路：PDF/DOCX 等走暂存 `raw/extracted/*.md`（确认后删除暂存），�?`.md` 导入�?`raw/sources/` 里的归档原件（这类导�?*没有** `extracted_text_path`，原件本身就是文本，�?gotchas）。`source-index.json` 记录提升后的 `wiki/sources/` 路径。② `compile_service` �?`wiki/sources/` 做三层防护：`validate_manifest` �?删均拒绝（`COMPILE_PROTECTED_PATH`）、`apply_confirmed_manifest` 顶部同款守卫、`manifest_from_workspace` 在文件收集与删除计算里都跳过该子树、`is_safe_wiki_markdown` 兜底拒绝；`extracted_markdown_files` 同时接纳 `wiki/sources/` �?legacy `raw/extracted/`，`populate_workspace` 只复�?legacy `raw/extracted/`（避免与 `copy_tree(wiki)` 双重注入）。③ `provider_prompt`/`compile_prompt` 重写为引用模型：派生页跨源综合、双向引用（frontmatter `sources:` + `> Sources:` 行）、禁�?`wiki/sources/`。④ 5 �?`schema.md` 模板 + `wiki-ingest`/`wiki-lint` SKILL 同步更新。前端：`AppShell.confirmImportPreview` 确认后调 `wikiStore.scan()` 刷新树（即使不编译也立即可见）。测试：新增 `tests/sources_promotion.rs`�? 个集成测试覆盖提�?碰撞/CJK/三层防护/双向枚举/frontmatter 守卫）；更新 `import_service` �?`App.test.tsx` 既有断言。双子代理审查后修复：导入检查点 scope 补上提升后的 `wiki/sources/` 页（Git 安全边界）、`apply_confirmed_manifest` 显式 `COMPILE_PROTECTED_PATH`、`split_frontmatter` 加「首行非 `---` 或内容不�?YAML 则整篇当正文」守卫防�?`---` 分隔线被误判。验证：`cargo check` + 3 个集成测试套件全绿（25 项）、`npm run test`(108)/`npm run lint` 通过。遗留：`cargo test --lib` 仍因既有 DLL 加载问题无法执行（集成测试可跑，�?2026-06-23 记录）；PDF→md 内图片在阅读器内可能不渲染（既有局限，非回归）�?
[2026-06-24] compile/agent-write-permission（第二轮�?�?修复放行 Edit/Write 后编译仍只出 3 脚手架页的问题；两层根因叠加 �?上一轮加�?`--allowedTools` 后，新一次真实编译仍触发 `COMPILE_EMPTY_OUTPUT`。读 task 日志定位到第二层根因�?a) `--allowedTools` 只预授权权限、不改默认工具集，且 `claude --print` 默认工具集是 `[Bash,Edit,Read]`（无独立 Write，Edit 用空 old_string 即可建文件）�?b) 真正的失败是 prompt 问题——`compile_prompt` 只写 "Update wiki/index.md, wiki/overview.md, and wiki/log.md"，弱模型（用户走 glm-5.2）照字面只编辑这 3 个文件、不创建任何 entity/source 页；Bash 被禁后它也不可靠地回退�?Edit 批量建页。修复：�?`compile_prompt`/`provider_prompt` 重写，显式要求「为每个 extracted 源在 wiki/sources/ 建一�?+ �?schema �?entity/concept/synthesis 页，只动 index 三件套算失败」；�?经用户确认（安全 vs 可靠性权衡，用户选放行），把 `Bash` 加回 `--allowedTools`，因为批量建页依�?heredoc，并�?prompt 附加「shell 命令只能作用�?workspace 内、不得影响外部」的软约束（项目层安全已由隔�?workspace + 校验 manifest + Git 检查点兜底；系统层风险用户已知悉接受）。验证：用真�?claude + 最终参数跑完整编译，产�?6 �?source �?+ 10 �?entity �?+ 更新 index/overview/log�? denial / 0 retry。已�?clippy/lint/test(108)。遗留：BYOK 路由同源风险未加守卫；stream-json �?permission_denial/错误 subtype 未解析�?
[2026-06-23] compile/agent-write-permission �?修复导入后编译只产出 3 个脚手架页（index/log/overview）、chat 查不到导入文章的 bug �?根因：编译拉起的 `claude` CLI 跑在 `--permission-mode dontAsk` + sandbox `--settings`，�?sandbox �?auto-allow `Bash`、不�?`Edit/Write`，dontAsk 又把未显式授权的工具一律静默拒绝；agent 读源、规�?23 页后每次写盘都被拒，仍以 exit 0“成功”结束，留下�?wiki。Windows �?sandbox 直接不支持（日志 "sandbox disabled"），但该 bug 跨平台。修复：`agent_service::invocation` Claude 分支新增 `"--allowedTools=Edit Write Read Bash"`（`=` 绑定，因�?flag 变参会吞�?prompt 位置参数）。已用真�?`claude --print` 复现：dontAsk �?allowlist→写盘失败；�?allowlist→exit 0 且在根目录与子目录均创建文件成功。次要修复：`generate_manifest` Agent 分支�?`manifest_from_workspace` 后加守卫——manifest 只含 3 脚手架页�?extracted 输入非空时，返回 `COMPILE_EMPTY_OUTPUT` 错误而非静默套用�?wiki，把 agent 写盘失败从“静默成功”变成可见错误。已�?`cargo check`/`cargo clippy`/`npm run lint`/`npm run test`(108)，双子代理审查已采纳（去�?Bash）。遗留：BYOK 路由同源静默风险未加守卫；stream-json �?`permission_denial`/错误 subtype 未解析；`extracted_markdown_files` 在该路径被二次调用（亚毫秒冗余）——均记为后续。（注：Bash 后经第二轮排�?用户确认重新放行，见 2026-06-24 记录。）

[2026-06-23] compile+chat/agent-spawn �?修复 Windows 下导入后编译/聊天�?“batch file arguments are invalid�?无法启动 Agent �?根因�?CVE-2024-24576：`Command` 直接 spawn `claude.cmd` 时，含特殊字符的 prompt 参数会被 `cmd.exe` 安全转义层拒绝；改为解析 npm shim 转发目标（`.exe` 优先，否�?`node`+脚本）直�?spawn 真正可执行文件，并加 `CREATE_NO_WINDOW`。新�?`resolve_spawn_target`/`resolve_cmd_shim` �?Windows 回归测试；独�?Rust 脚本已复现并验证修复。遗留：`cargo test --lib` 的测试二进制近期被链�?GUI DLL（comctl32/dwmapi/...）导�?`STATUS_ENTRYPOINT_NOT_FOUND` 无法加载，属既有问题、与本次修复无关，待单独排查�?
[2026-06-23] import+chat/reliability �?修复 Markdown 导入预览自产物被误判为重复、默�?Agent 配置分裂�?Chat 失败静默；补�?Rust/React 回归测试 �?`raw/extracted/` 仅作为导入预览产物从既有源哈希扫描中排除，保�?legacy `raw/*` 源目录兼容；`.app/agent-config.json` 作为默认 Agent 真源并同�?settings 镜像，UI 不再把首个已安装 CLI 冒充默认项，已配置但启动失败�?Agent 仍可见；Chat 终�?reload 后保留任务错误�?
[2026-06-23] chat-BE/P0 �?完成 chat 后端 P0 流式 + route（P0-1..P0-4 verified）�?新增 `task://stream-output` 通道 + `StreamDelta`（emit-only 不落盘）；`LlmService::complete_streaming`（reqwest bytes_stream + per-provider SSE/NDJSON 解析 + is_cancelled）；`run_task_streaming_with_delta`（object-safe `&dyn Fn` 回调，真循环单份）；chat 双路 emit delta + 抽出�?`decide_route` + `ChatMessage.provider`。决策：流式 delta 不落盘（终态整�?answer �?chats/*.json，FE 终�?reload 幂等兜底）。验证：fmt/clippy/npm test(105)/npm lint 全绿；⚠�?cargo test 执行被环境级 0xc0000139 阻塞（base 同样失败，见 gotchas），代码正确性以编译+clippy+单测齐备代理。遗留：chat-FE 消费 channel + 逐字呈现 + badge�?
[2026-06-22] agent/loop 里程�?�?�?本轮 P0+P1 全部完成 �?阻塞修复（agent 挂起 `fa18266`、compile 空输入诊�?`0b585bd`、cancel 幂等�?`15deb07`�? UI 重写（AgentView 四宫�?BYOK �?CLI �?任务行内进度/取消 `bacbc01`、RunAgentDialog `bacbc01`、AgentRightPanel `5399651`、TaskLogDrawer terminal 三段式日�?复制/清空/全屏+进度 aria `95b3628`）。新增样式族：`.cli-row/.ingest-card/.sumcard--provider/.task-row/.agent-section/.terminal*/.dialog*/.toggle/.select`。共 6 commits。关键决策：(1) `cancel_task` �?terminal task 改为 idempotent 返回（不�?Err），避免 task 快�?Failed 后用户点取消触发误导�?toast�?2) RunAgentDialog �?html-* 单页 skill 改为�?exports 视图让用户挑源页，project-report 才直�?run�?3) 右面板安全边�?4 checkbox 渲染�?disabled invariant（CLAUDE.md 硬规则），未�?Settings 字段�?4) `list_skills` 后端命令缺，右面�?Skill 清单用已知列表代替，html-project-report 标缺模板 warn�?05 FE tests + 315 Rust lib tests + clippy `-D warnings` + fmt + tsc + lint 全绿。遗留：用户需�?`npm run tauri dev` 实测确认 GUI �?dialog/取消/terminal 行为生效（本会话有用户的 tauri dev 占用 1420，未做浏览器预览）�?
[2026-06-22] agent/backend �?编译空输入诊断化 + 取消任务幂等�?�?用户报：确认导入后编译卡�?COMPILE_INPUT_EMPTY、取消任务无响应。根因：(1) `extracted_markdown_files` 的空错误无论原因都返回同一�?"No extracted Markdown under raw/extracted"，用户无从判断是「未确认导入�?「image-only 源无文本�?「extracted 文件被写空」；(2) `cancel_task` 对任�?terminal 状态返�?Err，task 快�?Failed 后用户点取消只会触发 "cannot cancel" toast，看起来像「取消坏了」。修复：(1) 新增 `compile_input_empty_error` helper，三 stage（`no_confirmed_imports` / `no_extracted_markdown` / `extracted_files_empty`）分别带 hint+details，既�?message 仍含 `raw/extracted` 以保回归；新�?`compile_reports_when_confirmed_sources_have_no_extracted_markdown` 测试覆盖 image-only 分支�?2) `cancel_task` �?terminal task 改为返回当前快照（idempotent），�?`test_cancel_already_completed_task_fails` 重写�?`test_cancel_already_completed_task_is_idempotent` 覆盖 Succeeded/Failed/Cancelled 三态�?15 Rust lib tests + clippy `-D warnings` + fmt + 103 FE tests + lint 零警告全绿�? commits `0b585bd` / `15deb07`。遗留：取消语义改了但没�?`CancellationRegistry` 核心（仍按既定边界）；用户需�?`npm run tauri dev` 实测确认两条修复�?GUI 下生效�?
[2026-06-22] import/drag-drop �?修复桌面文件拖拽无响应并补齐系统多文件选择 �?主窗口显式启�?`dragDropEnabled`，capability 授权 event listen/unlisten �?dialog open，注册失败不再静默；“本地文件”卡片改为系统多选并直接进入预览，Windows/CJK 路径原样传递。新增配置、路径归一化与组件集成测试；同时隔离项目创建测试临时目录并同步 Agent fake �?`--bare` 能力声明，消除全�?Rust 测试失败�?03 FE tests、lint、build�?14 Rust lib + 9 MVP + 7 contracts、clippy `-D warnings`、fmt 全绿；默�?GUI `cargo test` 仍受本机 WebView2 `0xc0000139` 启动阻断�?
[2026-06-22] agent/backend �?修复 Agent 编译/HTML/对话全挂�?�?根因：程序化 Claude 调用没隔离宿�?`~/.claude` 配置，用户装�?MCP 插件（claude-mem/zotero�? SessionStart hooks �?GUI 进程 spawn 上下文里 init 卡住，`--print` 永远到不了模型。修复：4 �?Claude invocation builder（compile stream-json + chat/lint/html-export text profile）统一�?`--bare`（skip hooks/plugin sync/MCP/auto-memory/CLAUDE.md auto-discovery）；`invocation_supported` 检测同步要�?`--bare`，旧 CLI 降级 Failed 而非静默挂。shell 实测 17s/$0.197 �?8.4s/$0.007、glm-5.2 认证不变、无 MCP/hook 噪声。新�?`claude_invocations_isolate_from_user_session_state` 回归守卫（遍�?4 builder 断言 `--bare`）�?14 Rust lib tests + 6 agent_service tests + clippy `-D warnings` + fmt + 103 FE tests + lint 零警告全绿�? commit `fa18266`。关键决策：`--bare` �?auth 收窄�?`ANTHROPIC_API_KEY`/`--settings`，对 OAuth/订阅用户不兼容——但 `--print` 后台自动化本就该�?API key 模型，且 BYOK 是兜底硬约束。遗留：用户需�?`npm run tauri dev` 实测确认 GUI spawn 下也生效；右面板/四宫�?Dialog �?P0/P1 UI 重写待下一轮�?
[2026-06-22] 导入/提取/编译 �?修复 Tauri v2 原生拖拽 payload 与延迟卸载；TXT/HTML/CSV/PDF/DOCX/PPTX/XLSX 在预览期输出 `raw/extracted/*.md`，保�?DOCX 标题/列表/表格、PPTX slide/bullet、XLSX/CSV Markdown 表格，图片仅归档；compile 只消�?`source-index.json` 中已确认且非空的 Markdown，空输入�?`COMPILE_INPUT_EMPTY`，已�?Wiki 内容变化一律进入确认流 �?OCR/MinerU 仍留在编译期 Agent/Skill�?4 MiB 文件/OOXML 总展开上限�?096 entry 上限、XLSX �?shared-string 边界�?extracted 外部编辑保护已补齐。双子代理审查无 Critical�? �?Important �?listener 测试缺口均处理；96 FE tests�?13 Rust lib + 9 MVP + 7 contracts、lint、build、clippy `-D warnings`、fmt 全绿；默�?GUI `cargo test` 仍受本机 WebView2 `0xc0000139` 启动阻断，无 GUI 全套已通过�?[2026-06-22] import/fe �?Import 前端�?P0+P1 重写�?loop 单轮）�?�?`import.html` �?ImportView �?Tab+单行输入重写为「卡片网格（本地文件/文件�?URL/剪贴�?+ 2 禁用项）+ 文件表（checkbox/图标/名称摘要/类型/大小/页数字数/目标路径/状�?预览�? 底部确认条（无损保留文案 + Git 检查点 checkbox + 导入后编�?checkbox + 取消/确认�? 右面板（选中文件/提取预览/归档规则/冲突记录）」四区。新�?`importStore`（zustand）跨组件共享预览/选中/确认/对话�?检查点/编译开关。dlg-url �?dlg-folder 独立对话框；后者调 `preview_open_folder_as_project` 接既�?pendingAction 流，模板/策略控件 disabled（后�?execution plan 决定）。底部条透传 `createCheckpoint` �?`confirm_import_preview`；`compileAfterImport` 条件触发 `startCompile`。拖拽走 `getCurrentWebview().onDragDropEvent`（无需 dialog 插件）。i18n en/zh-CN 全量补齐。双子代理审查（A 共享上下�?+ B 全新上下文）合并修复 4 项真�?bug：AppShell 切项目时�?`importStore.reset()` 防选中/预览泄漏；冲突列�?key �?idx 防碰撞；替换源按�?`replacementPaths[path]?.trim() ?? ""` �?undefined 崩；dlg-folder 模板/策略控件 disabled + 后端决定文案防「伪造参数」�? commit（foundational + rewrite）。BLOCKED：原生多选文件选择器（需 tauri-plugin-dialog）、PDF/Office 实际解析（后�?Unsupported）、imageCount/tableCount（SourceMetadata 缺字段）、URL 图片落盘——均�?roadmap 不动手�?0 FE test + lint 零警�?+ �?console.log 全绿�?[2026-06-21] import/审查修复 �?双子代理审查（A 共享上下�?+ B 全新上下文）合并修复 5 项：BLOCKER XLSX 数值单元格被误读为共享字符串（�?cell `t="s"` 判定，仅 shared 才按索引解析）；SHOULD-FIX `.xls` 提示拼成 `.xlx`（改显式 match 映射）；SHOULD-FIX XLSX inline string 丢失（`<is><t>` 现在输出）；SHOULD-FIX zip bomb/OOM（`ensure_entry_size` 64MiB 上限，四个读取器接入）；SHOULD-FIX `ConfirmedImport.checkpoint_hash` 序列化对称（�?`skip_serializing_if`，None→null，对�?PendingAction 约定）。新�?3 回归测试（数值不误读 / inline string / .xls 提示）。未改项（加�?PDF 消息、新仓库首检查点整树、preview 泄漏 registry、CJK word count）记 roadmap�?95 lib + 7 task8_contracts + 90 FE + lint + clippy(-D warnings) 全绿�?[2026-06-21] import/P0-3 �?导入确认 Git 检查点参数 + 编译链路 �?`ConfirmImportRequest` �?`create_checkpoint: bool`（`#[serde(default)]` 向后兼容），`ConfirmedImport` �?`checkpoint_hash: Option<String>`。为 true 时在归档+�?import-conflicts 后用 `create_scoped_checkpoint`（paths = 归档文件[排除 Skip/LinkToExisting] + source-index.json + import-conflicts.json）创�?scoped 提交。边界处理：新仓库先 `initialize_repository`（初始提交已含归档，返回�?hash）；已有仓库有新归档→scoped 提交；无变更→None。「导入后编译」不后端串接（前�?checkbox 依次�?confirm �?start_wiki_compile），后端只暴�?checkpoint_hash。核心逻辑�?`AppState::create_import_checkpoint`（非 gui-gate 可测）�? 新测试（归档+index 提交 / 新仓库自动初始化 / 无变更返�?None / 已有仓库 scoped 提交）通过�?92 lib + 7 task8_contracts + 90 FE + lint + clippy(--no-default-features, -D warnings) 全绿；GUI 编译通过�?[2026-06-21] import/P0-2 �?「打开文件夹为项目」后端命令链�?�?新增 `preview_open_folder_as_project(path)` 命令（dlg-folder 入口），复用 `project_service.open_project`：普通文件夹→NeedsConfirmation 并注�?pending action（含 execution plan，可�?`confirm_pending_action` 确认）；已有 wiki 文件夹→Opened 且不�?Git/registry/recents 副作用（纯预览）。核心逻辑�?`AppState::preview_folder_as_project`（非 gui-gate，可�?`--no-default-features` 测试），命令层薄包装�? 新测试（普通文件夹注册可确�?action / 已有项目返回 Opened �?pending / CJK 文件名安全）通过�?88 lib + 7 task8_contracts + 90 FE + lint + clippy(--no-default-features, -D warnings) 全绿；GUI 编译通过。gui-feature clippy 21 个预存错误在其它命令模块，stash 基线验证一致，非本 loop 范围�?[2026-06-21] import/P0-1 �?PDF/Office 文本解析适配器落�?�?ExtractionService �?PDF/DOCX/PPTX/XLSX 不再返回 Unsupported：pdf-extract �?PDF（按页）、zip+quick-xml �?OOXML（docx w:t / pptx a:t / xlsx sharedStrings+cells），文本�?raw/extracted/ 并带 word/page count；扫描件/无文本层 �?Failed 路由 OCR 到编�?Agent（不在导入层判断图片价值，守硬约束）；旧式 .doc/.ppt/.xls �?Failed 带转换提示。依�?zip �?--no-default-features --features deflate 规避 Windows 原生 DLL。新�?6 测试 + 替换 2 �?Unsupported 测试�?0 项通过�?85 lib + 7 task8_contracts + 90 FE + lint + clippy(-D warnings) 全绿。commit f9a3153。遗留：图片资产落盘�?P1 单独条目，不在本轮�?[2026-06-21] �?收敛 �?wiki 前端�?P0+P1 全部完成�? commits：`84bf074` reader/frontmatter/prose、`e619637` Milkdown toolbar、`b551aa8` page lifecycle、`9279f0b` three-way conflict、`8ebd007` HTML preview、`0150c8b` right-panel/citation/backlinks、`45babcc` 双重人工审查修复。最终验�?90 tests + lint 全绿，无 console.log，未修改 UI-Frontend-design/ �?src-tauri/。�?关键决策：复�?wiki-BE �?export/graph/navigation stores；冲突覆盖前创建 Git checkpoint 并使�?currentHash；外部浏览器打开因无后端命令唯一 blocked。双审子代理�?usage limit 不可用，�?CLAUDE.md 回退两轮人工审查并修复草稿误丢、跨页预览、错误不可见、diff 高亮、reference citation、目录推�?6 项有效问题；P2/其它板块未触碰�?
[2026-06-21] wiki-fe WIKI-FE-06 �?右侧补齐操作区（HTML/卡片/图谱/复制 wikilink）、引�?反链元数据计数、citation 编号卡片、逐页反链次数与查看全部；正文 `[1]` / `[^1]` 渲染为可定位 citation-ref。commit `0150c8b`；全�?87 tests + lint 通过。�?关键决策：反链次数从现有 wikilinks 确定性统计；操作通过 typed stores 复用 export/graph/navigation，不引入 ad hoc 事件协议；P2 编辑历史未触碰�?
[2026-06-21] wiki-fe WIKI-FE-05 �?Wiki �?read/edit/preview 第三态、四模板选择器、后�?export 任务联动、生成后自动读取预览、sandbox iframe、重新生�?打开位置/复制路径。commit `8ebd007`；清�?3 组陈�?Vitest worker 后全�?84 tests + lint 通过。�?关键决策：复用现�?exportStore 与已交付后端命令，不新增生成管线；“外部浏览器打开”因无后端命令标 blocked 并记 roadmap，其余第三态已 verified�?
[2026-06-21] wiki-fe WIKI-FE-04 �?`FILE_HASH_MISMATCH.details.baselineContent/currentHash` 接入三路 Markdown Diff（打开�?baseline / 磁盘 current / editor-Agent incoming），提供保留当前、使用传入、手动合并三选项；覆盖写前调用既�?`create_git_checkpoint`，再�?currentHash 乐观锁保存。commit `9279f0b`；全�?82 tests + lint 通过。�?关键决策：不新增后端命令；若磁盘再次变化则保�?dialog 并刷�?current/hash，绝不静默覆盖�?
[2026-06-21] wiki-fe WIKI-FE-03 �?接�?wiki-BE `create_wiki_page` / `rename_wiki_page` / `request_delete_wiki_page` / `confirm_pending_action`；树顶栏新增创建入口，文件行新增重命�?删除菜单，创�?重命名表单支�?CJK 与路径标准化，重命名和删除均�?ConfirmationDialog。commit `b551aa8`；全�?80 tests + lint 通过。�?关键决策：前端不写文件或伪�?checkpoint；rename 确认后由后端创建检查点并批量重�?wikilink，delete 直接展示后端 PendingAction，取消也回传 registry�?
[2026-06-21] wiki-fe WIKI-FE-02 �?Milkdown 增加 28px 格式工具条：加粗/斜体/H2/链接/行内代码/引用/撤销/重做，全部调用本�?7.21.2 command API，带 tooltip、aria-label、focus-visible �?editor loading 禁用态。commit `e619637`；全�?75 tests + lint 通过。�?关键决策：工具条�?`useEditor` 放在同一 MilkdownProvider 上下文，`onMouseDown` 保留编辑器选区；额�?`npm run build` 暴露现有跨板�?DTO 类型错误，已�?gotchas，本 loop 不越界修复�?
[2026-06-21] wiki-fe WIKI-FE-01 �?frontmatter 从裸 YAML `<pre>` 改为容错的两列卡片，并将设计�?`.prose` 全套排版、wikilink pill �?missing 状态迁移到 `.wiki-prose`；新�?2 个渲染回归测试。commit `84bf074`；全�?74 tests + lint 通过。�?关键决策：只做展示级扁平 YAML 解析，复�?未知行保留为可读文本，不引入序列化依赖也不改写源文件�?
[2026-06-21] �?收敛 �?wiki 后端 P0+P1 全部完成（只�?src-tauri/）�? commits：`4956217`（W1+W2+W4 create/rename/delete 命令 + rewrite_wikilinks + apply_page_delete �?checkpoint）、`7ed2b2a`（W3 FILE_HASH_MISMATCH baselineContent）、`9a0f17d`（双子代理审查修复）。W1+W2+W4：create CreateNew �?checkpoint；rename 前置 create_checkpoint（批量替换命中硬边界�? 全仓 `[[old]]`→`[[new]]` 重写（含 alias/anchor/CJK/嵌套括号�? 快照回滚；delete �?PendingAction(referenced_by 预览，排除自�?→confirm �?apply_page_delete（re-verify hash + pre HighRiskOperation + post FinalResult �?checkpoint + 失败快照恢复）。W3：file_store verify_write_mode mismatch 分支�?baselineContent（仅 UTF-8，binary 省略），改一�?6 调用点（compile×2/chat/lint×3/wiki save）全受益。双子代理审查（A 共享上下�?/ B 全新上下文）合并修复：①apply_page_delete 失败�?unstage 不恢复工作树→改为快�?恢复；②rename_page 无回滚→改为快照源页+所有引用页+恢复；③create_page page_type �?escape→YAML 注入→route through yaml_escape_scalar（含换行）；④FILE_HASH_MISMATCH binary 文件 from_utf8_lossy �?U+FFFD 垃圾→改为仅 UTF-8 才附 baseline；⑤find_pages_referencing 含自链→排除；⑥死代�?out.push_str(&body[..0])。验收：Rust lib 280 passed�?4 审查驱动新测�? cargo check(gui) compiles / 前端 72 tests / lint clean / �?println!/dbg!/console.log。�?关键决策：①apply_page_delete 镜像 import_service::apply_source_delete 两段 checkpoint 契约（pre 安全�?+ post 捕获删除为真实变更），返�?pre checkpoint.commit_hash.is_some()；②Windows GUI 命令模块 #[cfg(feature="gui")] 跑单�?STATUS_ENTRYPOINT_NOT_FOUND 崩溃，delete 逻辑下沉 lib-available SearchService；③双子代理均独立发�?rename/delete 回滚缺失（A 从设计意图一致性、B �?fresh-eyes 失败模式），证明审查有效。遗留（P2，已�?roadmap）：rewrite_wikilinks 不跳代码�?不识别路径式 [[dir/old]]/引用重写非原子写（与 extract_wikilinks 同源）；前端文件树新�?重命�?删除 UI 接线仍缺�?
[2026-06-21] wiki-be W3 �?`file_store::verify_write_mode` OverwriteIfHashMatches mismatch 分支新增 `details.baselineContent`（`fs::read` + `String::from_utf8_lossy`，binary/�?UTF-8 省略）。commit 7ed2b2a。�?关键决策：改 `verify_write_mode` 一处，6 �?`OverwriteIfHashMatches` 调用点（compile×2 / chat / lint×3 / wiki save）全受益——wiki save_page（PRD-READ-004 外部编辑场景）与 compile 合并冲突（PRD-WIKI-004）两条路径都覆盖，无需分别�?compile_commands / save_page。baseline 读失败（binary 文件）时 mismatch 错误仍正常返回，只是省略 baseline 字段。新�?`file_hash_mismatch_surfaces_disk_baseline_for_three_way_diff` 测试断言 baselineContent==磁盘文本 + currentHash 仍在。验收：276 backend tests + 72 npm tests + lint 全绿�?
[2026-06-21] wiki-be W1+W2+W4 �?新增 create/rename/delete wiki page 三命�?+ `rewrite_wikilinks` + `apply_page_delete` �?checkpoint 契约 + 路径安全/CJK 测试。commit 4956217。�?关键决策：①apply_page_delete �?checkpoint-before-remove"对已跟踪干净文件 created=false（git status 无变更可提交）；改为镜像 `import_service::apply_source_delete` 两段契约——pre-delete `HighRiskOperation` scoped checkpoint（安全网，HEAD 保护 before 态）�?remove_file + 失效 graph-cache �?post-delete `FinalResult` scoped checkpoint（捕获删除为真实变更），返回 `checkpoint.commit_hash.is_some()`；失�?unstage 回滚。②rename 前置 `create_checkpoint(HighRiskOperation)`（全量），因"文件移动+批量引用重写"命中 CLAUDE.md 硬边�?批量替换操作前必�?Git 检查点"。③Windows GUI 命令模块 `#[cfg(feature="gui")]` 跑单测会 STATUS_ENTRYPOINT_NOT_FOUND 崩溃，delete 逻辑下沉�?lib-available `SearchService::apply_page_delete`，`execute_wiki_page_delete` 命令层仅薄包装。④`rewrite_wikilinks` 大小写无�?target 匹配，保�?alias/anchor/嵌套括号。验收：275 backend tests（含 21 search_service + 20 markdown_utils 新测�? 72 npm tests + lint 全绿，无 console.log/println!/dbg!。遗留：W3 FILE_HASH_MISMATCH baseline 透传（本 loop 下一项）�?
[2026-06-21] �?收敛 �?cross-cutting 后端 P0+P1 全部完成（只�?src-tauri/）。P0-1 i18n 5 prompt 点语言注入（chat/compile/export/lint service + compile_commands::provider_prompt，确定性字段保持英文）+ 新增 `utils/i18n.rs`；P0-2 `PendingAction.checkpoint_hash: Option<String>`（`#[serde(default)]` None→null 不省略，配合前端 `!== null`），compile 冲突登记时填�?commit_hash；P0-3 托盘 Show/Hide/Quit+tooltip �?settings.language 本地化（新增 `settings_service::read_language`）；P1-1 BYOK 流式进度：新�?`tasks/byok_progress.rs::poll_with_progress` 统一 compile/chat/lint/export 四处（取�?100ms 轮询 + 进度 �?s append "{verb}�?，`Result<Result<T,E>, ByokCancelled>` 双层 `??` 解包）。双子代理审查（A 共享上下�?/ B 全新上下文）合并修复 M1（Agent 路由 compile_prompt �?"keep frontmatter keys/paths/headings in English" 守卫�? M2（循环前首次取消检查）。检查全绿：Rust lib 259 passed / clippy(lib) clean / cargo check(gui) compiles / 前端 72 tests / lint clean。�?关键决策：①语言注入走参数注入而非内部�?SettingsService，避�?host-state-dependent 测试 flaky；②checkpoint_hash �?`#[serde(default)]` + �?skip_serializing_if 确保 None→null；③BYOK 进度用独�?helper 抽出避免 4 处重复内联；④双子代理均独立发现 M1（Agent 路由缺守卫），证明审查有效。遗留（非本 loop 范围）：前端 `PendingAction` TS 接口�?`checkpointHash` 字段 + ConfirmationDialog 渲染；托盘语言切换需重启窗口生效；full-gui clippy 21 �?pre-existing needless_borrow（HEAD 上即有，非本 loop 引入）�?
[2026-06-21] �?收敛 �?shell+dashboard+启动�?板块 P0+P1 全部完成。P1-7..11 落地：创�?useProjectStatus hook（git_status+detect_agents+list_llm_providers module-cache）→ RightContextPanel �?Git branch/HEAD/pending/per-agent dotstatus rows+进度�?disk；BottomStatusBar �?agent·version+git branch·HEAD+git clean+语言；LeftSidebar Agent foot（claude·version+ChevronRight�? Lint 橙色 badge；TopBar 项目切换 dropdown menu+返回总览+语言顺序中左+macOS ⌘K�?2 tests �?/ lint 绿。关键决策：①共�?hook 避免 3x CLI probe 重复；②git_status 已有 IPC 返回 branch/head/hasChanges 不新增后端；③disk/last-compile �?IPC �?placeholder 降级�?
[2026-06-21] shell+dashboard+启动�?(P0+P1 大部�? �?重写 DashboardView（health-row/统计六宫�?主题分布�?活动时间�?快速操作四象限�? ProjectStartView（三�?launch：顶�?nav/hero/filterbar/projgrid+quickaction/右侧 agentmini+BYOK+templateside/底部状�?新建项目 dialog--wide�? 移植设计组件 CSS �?src/styles.css �?关键决策：①托盘 close 拦截其实已接线（lib.rs:84-100），回改�?roadmap 文档而非改代码；②Dashboard 数据�?wikiStore.tree.pages + taskStore.tasks 派生，不新增 IPC；③Dashboard scan �?hasTauri 守卫避免 jsdom 误触�?mock；④启动�?Agent 检测复�?recentProjects[0] 上下文（不改 detect_agents 后端签名）；⑤语言切换直连 i18next+localStorage�?2 tests 全绿，lint 全绿。剩�?P1-7..11（TopBar/LeftSidebar/RightContextPanel/BottomStatusBar 补全）留给下次唤醒�?
[2026-06-21] Goal 收尾：任务错误可见性与通知点击路由 �?TDD 修复 `fetchTasks`、项目任务恢复和日志读取失败�?store 静默吞掉的问题，错误现在由调�?UI 显示 toast；日志轮询使用稳�?callback，避免翻译函数引用变化触发重�?effect。系统通知携带 `taskId/eventType`，点击后唤醒并聚焦主窗口、打开对应任务抽屉，确认等待任务可直接回到 Diff/确认流。按共享上下文和新鲜上下文各做一轮人�?review（review subagent 额度不可用），未发现新的 Critical/Important 代码问题；完整检查全绿：前端 20 files/72 tests、Rust lib 247、lint 0、无 console.log、build、fmt、clippy，GUI `cargo check --offline` 通过。�?关键决策/遗留：真实打包环境仍需人工验收 Windows/macOS/Linux 通知点击、托盘关窗、系统文件选择器、Agent/BYOK；自动更新缺少发布端点与签名公钥，保留明确“未配置更新源”状态，不伪造下载�?
[2026-06-21] 原始资料替换/删除安全闭环 �?TDD 新增 `.app/source-index.json`，导入确认时记录每个 `raw/sources/`/`raw/assets/` 原件对应�?`raw/extracted/` 产物；Import 页可列出来源并发起替�?删除，但后端只登�?destructive `PendingAction`，确认时重新校验项目 capability、路径范围、原�?替换�?hash 与文件类型，先建 Git 高风险检查点，再以备�?恢复事务更新原件、提取物和索引，最后提交结果。取消、注册失败、状态漂移或事务失败会清理仅属于新版本的暂存提取物，不删除与旧版本共享的产物；确认成功后自动启动可取消的 Wiki compile task，落实“确认后批量更新相关 Wiki”。完整检查全绿：前端 19 files/70 tests、Rust lib 247、lint 0、无 console.log、build、fmt、clippy，GUI `cargo check --offline` 通过。�?关键决策/遗留：未建立 artifact 索引的旧项目来源拒绝自动替换/删除，避免猜测关联文件后误删；需要人工验收系统文件选择器与真实 Agent/BYOK 编译结果�?
[2026-06-21] 图谱布局线程与现�?Wiki Git 基线 �?ForceAtlas2/Louvain 的拓扑着色保持不变，但无缓存布局改用依赖自带 Web Worker supervisor，React 卸载/重建�?kill worker 与清�?timer，运行中定期刷新 Sigma�? 秒收敛窗口后持久化有限坐标；�?Worker 环境保留 80 轮兼容回退，避免桌�?WebView 被同步布局卡住。另修复已识�?Wiki 项目�?`.git` 时“打开成功、首次编译才失败”：`open_project` 在注�?capability 前自动初始化仓库并建立基线提交，普通文件夹仍走显式初始化确认。检查清单全绿：前端 19 files/69 tests、Rust lib 242、lint 0、无 console.log、build、fmt、clippy，GUI `cargo check --offline` 通过。�?关键决策/遗留：Git 对已识别 Wiki 是透明安全基础，不要求普通用户理解；更新源未配置仍属发布配置阻断，原始资料替�?删除因缺�?extraction artifact 索引尚需设计成不会留下陈旧提取物的完整链路，不能只做表面删除按钮�?
[2026-06-21] 编译冲突三路解决闭环 �?TDD 新增 `keep_current/use_generated/manual_merge` 后端解析：保留当前会过滤冲突路径但继续应用无争议候选；使用生成版本沿用�?Git checkpoint+当前 hash 保护的覆盖；手动合并必须为每个冲突（含删除候选）提交且只提交一�?Markdown，缺�?重复/越界路径均拒绝。新增只�?conflict details IPC 返回 current/generated 内容，专用双�?Diff 弹窗提供取消、保留当前、使用生成版本、手动编辑与应用；后�?action 在策略和状态校验成功前不消费，失败可重试，最终仍�?backup/restore、checkpoint HEAD、hash preflight、结果提交。检查清单全绿：前端 19 files/69 tests、Rust lib 242、lint 0、无 console.log、build、fmt、clippy，GUI `cargo check --offline` 通过。�?关键决策/遗留：取消表示放弃本次编译；“保留当前”保留所有冲突文件但提交无争议生成结果，符合三路选择且不静默丢弃安全候选。ForceAtlas2 worker 化与真实更新源继续审计�?
[2026-06-21] 文件夹导入解析后台任务化 �?修复目录归档预览会递归展开、但 ExtractionService 旧调用只解析“目录本身”而使内部文件保持 pending 的跨层缺口；`preview_import` 现在创建可取�?`import` 项目任务，worker 稳定递归展开文件，逐文件提取并写进�?日志，把完整预览原子持久化到 `.app/import-previews/{taskId}.json`，成功后由受 project/task/type/status 校验�?`get_import_preview` 读取。前端打开任务抽屉、跟踪终态、跨项目时丢弃旧响应；失�?取消可见。检查清单全绿：前端 18 files/68 tests、Rust lib 240、lint 0、无 console.log、build、fmt、clippy；GUI `cargo check --offline` 通过。�?关键决策/遗留：URL/剪贴板是 5 MB 有界单条文本预览，保留同步快速路径；本地文件/文件夹批量解析必须走后台任务。ForceAtlas2 前端布局 worker 化、三路编译冲突与更新源仍待继续审计�?
[2026-06-21] 图谱首次构建后台任务�?�?TDD �?`build_graph` 从同步返回图数据改为创建 `graph_build` 项目任务，worker 使用启动时捕获的 ProjectContext 扫描 Wiki、写进度/日志、生�?`.app/graph-cache.json` 并持久化结果；`get_graph` 只读取最后一次完整缓存，不再�?IPC handler 中隐式执行昂贵重建。前端无缓存时自动启动任务、打开任务抽屉、轮询终态并加载缓存；手动重建也走同一路径，失�?取消显示明确错误。检查清单全绿：前端 18 files/68 tests、Rust lib 240、lint 0、无 console.log、build、fmt、clippy；新�?GUI command 另经 `cargo check --offline` 通过。�?关键决策/遗留：拓扑扫�?构建已不阻塞 WebView 且可取消；前�?ForceAtlas2 80 次布局仍在渲染线程同步执行，需继续评估 worker 化；Import 文件夹批量解析仍是同�?IPC�?
[2026-06-21] URL/剪贴板导入闭环与批量确认一致�?�?�?TDD 接�?Import 三种来源 tab：本地路径、URL、剪贴板 Markdown；URL �?Rust 后端抓取，拒绝非 HTTP、localhost、私�?链路本地/保留地址，DNS 解析后固定公�?IP，禁自动重定向，限制 20 秒与 5 MB，再由前�?Readability 提取正文并转换为�?source_url/title/author、链接和图片引用�?Markdown；URL/剪贴板内容由后端命名并写入项目内 `.app/import-staging/`，进入现�?preview→confirm→compile 契约，URL 归档�?`raw/sources/links/`，确认成功清�?staging。另修复批量确认的部分落盘：现在先复验全部源/hash/目标，再复制；写入失败回滚本批已复制文件。检查清单全绿：前端 18 files/67 tests、Rust lib 240、lint 0、无 console.log、build、fmt、clippy，另�?GUI `cargo check --offline` 通过。�?关键决策/遗留：网页图片以绝对 URL Markdown 引用保留，尚未下载为本地资产；URL 抓取/Readability 当前是有界同步用户操作，文件夹批量解析与图谱首次构建仍需继续 TaskService 化�?
[2026-06-21] 人工复核：错误可见性与确认弹窗可达�?�?TDD 修复导入不存在路径被错误计为可归档、全局搜索/导入 IPC 失败静默、任务取消失败无提示；TaskLogDrawer 取消错误现在显示 error toast，并移除非法 button 嵌套。确认弹窗进入时聚焦取消按钮、Escape 可取消、Tab 焦点留在弹窗内，风险/检查点/预览/路径/操作文案全部接入中英 i18n；同步清理过时搜索注释。两�?review subagent 均因额度限制未产出结果，依仓库规则改做共享上下文与新鲜上下文两轮人工复核。检查清单全绿：前端 17 files/64 tests、Rust lib 236、lint 0、无 console.log、build、fmt、clippy 均通过。�?关键决策/遗留：保持取消为弹窗首焦点，避免高风险操作默认聚焦确认；继续审计 URL/剪贴板导入、Import/Graph 长任务、三路冲突处理和更新源接入，尚未宣称全量 review 完成�?
======================================================================
[2026-06-21] 安全确认、全局搜索与真实导入入�?�?密钥状�?API/Provider UI 只显示“已配置”，不再返回或渲染末四位；Lint 高风�?fix �?Chat 覆盖保存改为后端 `ConfirmationRegistry` 存储一次性执行计划，确认必须提交 backend-issued actionId，伪�?`confirmHighRisk/allowOverwrite=true` �?action 会返�?`CONFIRMATION_REQUIRED`，确认使用后端保存的 project/issue/path/hash，取消会消费并撤销 action，写盘仍强制 Git checkpoint+hash。顶�?Ctrl/Cmd+K/Enter 接通纯本地 `search_wiki`（keyword/filter DTO，绝不调用模型），结果弹层可打开 Wiki 页面。Import 前端移除非标�?`File.path` 假入口，改为可实际使用的本地绝对文件/文件夹路径；后端对目录稳定排序递归展开、支�?CJK/Unicode 嵌套文件并跳�?symlink，新增真实目录单 sourcePath 测试。补 KaTeX/highlight 样式；同步修�?Task14 QA �?52/227/8 �?CSV unsupported 描述。检查清单全绿：前端 60、Rust lib 235、lint 0、无 console.log、build、fmt、clippy 均通过�?�?关键决策：当前依赖集中没�?Tauri dialog 插件，因此先用明确路径输入修复不可用入口，不擅自加新原生依赖；原生文件夹选择器仍属人�?UX 增强。遗留：URL/剪贴板入口尚未接 UI，Import/Graph 同步工作尚未全部 TaskService 化；继续审计�?[2026-06-21] 后台任务跨项目契约二次收�?�?复审发现�?TDD 修复组合竞态：A→B→A �?`recover_tasks` 现在跳过同进�?live task，不再把 Running 覆盖�?Failed/替换 token；`set_active_project` 返回全局任务列表，前端不再因�?B �?A 任务隐藏。新�?`create_project_task`，compile/chat/deep-lint/export �?command 首次可信 resolve 后把 `ProjectContext.root` 原子绑定�?task，并�?context 直接交给 worker，彻底移除依�?React effect 同步�?ambient root；创建时持久化失败会阻止 worker 启动，后续状态持久化失败向调用方返回错误，JSON 改复�?FileStore 临时文件+fsync+rename 原子写。ProjectRegistry 保留本会话内所有“用户明确打开过”的可信 id↔root capability，以支持切换后旧任务/确认继续；前�?project scope 仍只显示当前项目并阻止迟到响应，同项�?agentRoute 元数据刷新不再误�?invalidate。settings scope 在异步语言切换后再次校验，reset 保留全局语言/主题但清项目 provider/agent；Lint/Export `list_tasks` 补齐必需 request DTO。新�?A→B→A、显�?task root、同项目元数据刷新、前端全局任务恢复契约测试；检查清单全绿：前端 59、Rust lib 235、lint 0、无 console.log、build、fmt、clippy 均通过�?�?关键决策修正：上一条“单一活动 UI capability”经组合审查证实会破坏旧项目 waiting confirmation，现改为会话内已打开项目能力集；任意未打开路径与伪�?id/root 仍被拒绝。遗留：`create_task` 通用低层命令仍用于无项目/测试任务，产品后台入口全部使用显�?root �?`create_project_task`；继续审计高风险确认与输�?搜索入口�?[2026-06-21] 项目切换竞态与后台任务持久化修�?�?双审查后�?deferred promise 复现并修复自�?reopen 覆盖用户显式选择；ProjectStart 初始化期禁用重复提交并正确显示结构化 BackendError，确认操作也纳入 busy/error；切换时递增 project scope epoch、统一 reset wiki/chat/graph/lint/export/settings/navigation，所有项�?store 丢弃�?scope 的迟到异步响应。ProjectRegistry 改为单一活动 UI capability，打开 B 后旧 A DTO 无法再发起新 IPC，但已启�?worker 持有�?ProjectContext 不受影响。TaskService 不再在切换时清任�?token：跨项目任务保持可见可取消，每个 task 绑定自身 root，确�?A 任务在切�?B 后仍持久化到 A；持久化 envelope 包含 task+logs，queued/running/progress/log/result/error/terminal 变更均即时写 `.app/tasks/`，重启将中断态标�?failed 并恢复日志，同时兼容旧版�?BackendTask JSON。新�?bootstrap 竞态、wiki 迟到响应、旧 capability 撤销、跨项目任务可见/可取�?落盘隔离、非终�?日志恢复测试。检查清单全绿：前端 57、Rust lib 234、lint 0、无 console.log、build、fmt、clippy 均通过�?�?关键决策：项目切换不等于取消后台任务；新 UI 命令只授权当前项目，已启动任务继续使用启动时捕获的可�?context。遗留：重启只会恢复本次打开项目磁盘上的任务历史，未打开项目不会主动全盘扫描（符合本地项目边界）；后续继续修复真实输入入口、全局搜索与高风险确认统一�?[2026-06-21] Project context 安全边界与冷启动入口 �?TDD 修复所有项目级 IPC 信任 UI 自报 `projectRootPath` 的越界风险：新增后端 `ProjectRegistry`，仅允许�?`create_project` / `open_project` 确认并注册的 projectId↔canonical root 组合，所有项目级 commands 统一�?registry 解析，未�?ID、A 项目 ID+B 项目路径、`..`/别名路径均拒绝；长任务在创建 task 前先校验上下文，避免产生孤儿任务。前端移除硬编码假项目，新增打开/新建/最近项目启动页，启动时�?`list_recent_projects` �?`open_project` 注册后端上下文，切换项目返回启动页；语言启动偏好改存全局 localStorage，避免未注册项目阶段调用 `get_settings`。新�?3 �?Rust registry 测试�? �?App 入口测试�? �?projectStore 启动顺序契约测试；检查清单全绿：前端 55、Rust lib 232、lint 0、无 console.log、build、fmt、clippy 均通过�?�?关键决策：registry 只在当前进程内存中保存可信映射，重启必须重新 open；项目文件仍�?Markdown/JSON/本地文件，无数据库。遗留：当前启动页采用可跨平台复制粘贴路径输入，原生目录选择器与 Import 的真实文件选择统一在后续输入入口批次接入；双审查正在进行，若发现有效问题将在本条后续的新里程碑记录修复�?[2026-06-21] 全规格审�?�?�?CLAUDE→PRD→SPEC→APP_flow→TECH_STACK→BACKEND_STRUCTURE→FRONTEND_GUIDELINES→IMPLEMENTATION_PLAN→progress/gotchas→MVP QA 顺序完成 Task 1-14 逐项对照，并�?3 条独立只读审计证据链复核后端安全、前�?设计�?E2E 契约 �?已确认的高风险缺口包括：IPC 信任 UI 自报 projectRootPath、启�?新建/打开项目闭环不可达且默认显示假项目、真实文件选择依赖不存在的 File.path、全局搜索/URL/剪贴板入口未接、Lint/Chat 高风险确认绕�?backend-stored PendingAction、任务恢复不持久化运行�?日志、Import/Graph 长任务未 TaskService 化、文件夹导入未递归；基线为前端 52、Rust lib 229、MVP 集成 9 测试通过，但 cargo fmt --check 失败�?QA 文档仍记录旧 8/227/CSV unsupported。修复按 docs/superpowers/plans/2026-06-21-spec-audit-remediation.md �?8 �?TDD 执行；遗留：本条仅记录审计里程碑，未宣称缺口已修复�?======================================================================
[2026-06-21] Bug 修复 �?claude/codex Agent �?App 内显�?"program not found" �?根因双层�?1) App 进程 PATH 可能不含 `%APPDATA%\npm`（GUI 启动/IDE shell 继承�?PATH 与新开终端不同），即使新开终端 `where claude` 成功，App �?`where` 仍可能失败；(2) 即便 `where` 成功，Windows `where claude` 输出�?*无扩展名 bash shim 排第一**、`claude.cmd` 排后面，�?Rust `Command::new(无扩展名路径)` �?`CreateProcess` 无法运行�?shim �?spawn 失败 �?detect �?Missing。修复：agent_service.rs `find_executable` 重写——`where`/`which` 成功�?*优先�?`.cmd`/`.bat`/`.exe` �?*（CreateProcess 能经 std 内部 cmd.exe 路由运行 .cmd）；新增 `%APPDATA%\npm` 直接探测兜底（PATH 缺失时的 well-known 位置）；新增 `resolve_program()` 把裸程序名解析成全路径后传给 `Command::new`，已有路�?�?Windows 原样返回。三�?spawn 点（run_with_timeout / run_capture / run_task_streaming）全部改�?`resolve_program(program)`。新�?2 个单测：`find_executable_falls_back_to_npm_global_dir_off_path`（Windows，临�?APPDATA+�?claude.cmd，断言兜底解析 .cmd）、`resolve_program_passes_through_paths_and_non_windows_bare_names`（全平台，已限定路径原样返回、不可解析裸名原样返回而非空串）。检查清单全过：cargo --lib 229 passed�?2）、agent_service 5/5、npm test 52、lint 0、vite build 通过、无 console.log。遗留：真实浏览器内 Agent 检测需人工 `npm run tauri dev -- --features gui` 重启验收�?======================================================================
[2026-06-21] Bug 验证 �?WebView2 / DLL 入口点阻断（STATUS_ENTRYPOINT_NOT_FOUND 0xc0000139）已确认修复并清�?gotchas �?核查结果：Cargo.toml `[features] default=["gui"]` + tauri 系依�?optional、lib.rs Tauri import �?run() 全部 `#[cfg(feature="gui")]`、build.rs `tauri_build::build()` gated、task_events.rs EventBus::Tauri 分支 gated —�?feature gate 完整落实。`cargo test --lib --no-default-features --offline` 实测 **227 passed / 0 failed**，测试二进制不再�?PE 加载阶段崩溃，DLL 阻断已不再是阻断点。顺带修复一个挡路的无关借用错误（agent_service.rs:563 `String::from_utf8_lossy` 临时 Cow 未绑�?let �?.lines() 借用，E0716）。已�?gotchas.txt 移除两条 DLL 相关记录（保留联网超�?--offline 条目，那是独立坑）。遗留：�?7 �?Task 14 进度记录里「真�?Agent/BYOK 生成需人工浏览器验收」措辞已过时，DLL 不再阻断，仅 GUI 运行态需人工验收�?[2026-06-21] Task 14 �?End-to-End MVP 流程验证（集成测�?+ 多格�?fixture + QA 文档�?�?交付 4 项：(1) src/test/fixtures/project-fixtures.ts 多格式验证矩阵（11 �?SourceFileType 各一：pdf/docx/pptx/xlsx/csv/md/txt/html/url/image/unknown + 剪贴�?MD），parser 缺口（pdf/office/image）显式标 `unsupported`/`failed` 不静默丢，附 assertEveryFormatCovered 防新增类型漏覆盖�? 个单测覆盖矩阵�?2) src-tauri/tests/mvp_flow.rs 9 个集成测试覆�?IMPLEMENTATION_PLAN §16 步骤 3-6 四循环：①project→wiki（create_project 骨架页齐全→import MD/TXT/CSV 在项目外 staging �?scan_existing 把源判成自身重复→preview 归档�?→confirm→CSV/TXT/MD 各自 ExtractionStatus 断言→apply_confirmed_manifest �?baseline hash 让核心页覆盖+新概念页创建→关键词搜索命中→graph resolve 首建后缓存命中）②sample-wiki（复�?wiki/wiki/ �?0 页切片到 temp 项目**不在原库�?*，copy_markdown_slice_inner �?current_dst lockstep 下降避免双嵌套，逐路径断言扫描不丢页）③AI-assisted fakes（FakeAgentRunner→claude 检�?Installed；BYOK provider 持久�?+ **�?set 真实 key �?SecretService::memory 再断言 settings.json 不含 sk-ant/api_key/bearer/token 等任意大小写秘密字段**——原版只断言不含 key 但从没存�?key，是空真断言；chat build_retrieval_context 真实本地检索返回诚�?citations→假 assistant 答案 save_answer_to_wiki �?wiki/queries/{slug}.md �?[[wikilinks]]→parse_agent_issues 解析 fenced JSON（issueType �?snake_case duplicate_topic）→export build_export_prompt+write_html+append_record �?exports/html/）④safety（compile 冲突不静默覆盖：外部�?index �?apply_manifest 返回 conflict �?*三核心页都断言�?CANDIDATE body 落盘**；confirmed apply 状态漂移→CONFIRMATION_STATE_MISMATCH；chat 覆盖双向：未确认→FILE_ALREADY_EXISTS 内容存活 / 确认+正确 hash+git→checkpoint commit 记录；lint fix **双向**：wiki/ 外路径→LINT_FIX_PATH_OUT_OF_SCOPE / wiki/ �?MissingFrontmatter Safe 修复→Applied+checkpoint+frontmatter 落盘；git checkpoint 真改动产 commit hash）�?3) docs/qa/mvp-acceptance.md QA 证据（命令输出、fixture 路径、sample 计数、显�?parser 限制清单：pdf/office/image 延后�?compile Agent/Skill，CSV/md/txt/html/url 提取）�?4) TDD：先写失败测试再迭代到全过�?*�?review（A 共享上下�?B 全新零偏见）共识修复**：BLOCKER 空真密钥边界断言（先�?key 再验文件不含）；sample 扫描 ≥copied 过松（骨架页膨胀总数，改为逐路径校验且�?copy helper 双嵌�?bug）；CSV 状态原本写�?Unsupported 但实�?Extracted（CSV �?md/txt 共享文本分支）——同步修�?fixture+QA 文档，不再把 CSV �?parser 缺口；applied 返回值丢弃→断言含概念页路径；冲突测试仅�?index→补�?overview/log 无候选落盘；新增正向 lint fix 测试（原只有负向越界）。检查清单全过：npm test 52 通过、lint 0、build 通过、cargo --test mvp_flow 9 通过、cargo --lib 227 通过、无 console.log。遗留：sample-wiki 性能目标 200-500 页受限于样本�?~10 页（切片 cap 50 已代表，全规模由样本自身验证）；真实 Agent/BYOK 生成需人工浏览器验收；temp 目录在断言失败时不清理（用 uuid 唯一名仅磁盘占用非测试失败，未引 Drop guard 避免越界）；Loop3 fake Agent 只经 detect_agents 验证检测（service 层无独立 agent-compile 入口，compile �?apply_manifest）�?[2026-06-20] Bug 修复 �?"大量按钮点击无反�?根因与修�?�?根因：四类按钮在 shell 脚手架阶段只渲染了标�?样式�?*从未�?onClick**（非 IPC 报错、非 hasTauri 静默，是纯无 handler）：①TopBar「切换项目�?TopBar.tsx:28) ②工作区头部 9 视图×2 �?次操作按�?(AppShell.tsx:237) ③LeftSidebar「最近页面」用了硬编码假数据数组且�?onClick (LeftSidebar.tsx:67) ④WikiView 星标按钮 (WikiView.tsx:124)。修复：TopBar→导航到 dashboard；AppShell 新增 runViewAction(slot) 分派器，�?activeView 调用对应 store 动作（wiki startEdit/reload、chat createSession/saveAnswer、lint runLocalLint/startDeepLint、export startExport/openFolder、graph rebuild/load、agent refreshCapabilities、navigation），新增 requireTauri() 在浏览器态弹 toast（可见降级，不再静默 no-op）；LeftSidebar 最近页面改为消�?wikiStore.recentPages（openPage 时去�?最新在�?上限 8，点击开页）；WikiView 星标�?wikiStore.toggleBookmark（无页时 disabled）；新增 toastStore + Toaster 组件�?s 自动消失、role=status aria-live=polite）。后端：新增 toggle_bookmark IPC（models/wiki.rs ToggleBookmarkRequest/Response、search_service.toggle_bookmark �?resolve_project_path + strip_prefix(wiki_dir) 双重路径�?+ 文件存在性校验、wiki_commands、lib.rs 注册），并修�?project_service 两处 bookmarks.json 初始化格式（原写对象 `{bookmarks:[]}` �?reader `Vec<String>` 不一致，改为�?`[]`，否则新项目书签永不持久）。双 review 后修复（�?reviewer 共识）：chat saveAnswer 误传 targetPath=""（应为省略让后端派生 slug）、LeftSidebar 最近页面点击在浏览器态需 hasTauri 守卫+toast（补齐同类可见降级）、Toaster 关闭按钮 aria-label i18n。检查清单全过：47 前端测试通过、lint 0、tsc 干净、vite build 通过、无 console.log、search_service 12 �?Rust 单测通过（含 toggle_bookmark 持久�?round-trip + 拒绝缺失/越界路径）。新增测试：appShellActions.test.tsx（dashboard 主操作跳 import、chat 主操作浏览器态弹 toast）、wiki.test.tsx recentPages 去重测试。遗留：import 头部「添加来源�?settings 头部「保存」无文件对话�?表单可达，改�?toast 引导到视图内控件（未引入 Tauri dialog 插件，避免越界）；图谱「适配视图」头部按钮映射为 load（视图内 fit 工具仍是 GraphControls 主入口）�?[2026-06-20] 测试修复 �?225 �?Rust 单元测试全部通过（原 202 pass / 23 fail�?�?修复 8 类共 23 个失败测试：1) extract_wikilinks 尾部 `]` 切片偏移 bug（`body[start..j]`→`body[start..j.saturating_sub(1)]`，j 指向第二个闭合括号）�?) count_words 预期值错误（2+3�?+4）；3) snippet_for_query 截断长查询词（snippet_end �?`start+radius.max(query.len())`）；4) extraction 测试 output_dir 未创�?`raw/extracted/` 子目录；5) lint IndexDrift �?DeadLink 解析不一致（统一�?build_target_lookup）；6) Windows NTFS 大小写不敏感导致 PathCase 测试无法创建 React.md+react.md 共存文件�?) GlobalSettingsFile 派生 Default 语言字段为空串；8) task_service 测试 Queued→Succeeded 非法状态迁移。关键决策：IndexDrift 改为�?resolution-key lookup 而非精确路径匹配，使 index.md �?`[[agent]]` 能正确解析到 `wiki/concepts/agent.md`；PathCase �?cfg!(not(target_os="windows")) 条件化；GlobalSettingsFile 改为手动 impl Default 调用 default_language()。遗留：无�?[2026-06-20] 环境修复 �?WebView2 DLL 阻断 `cargo test` 问题修复 �?根因：Windows 11 Insider build 26200 内核�?System32 DLL (10.0.26100.xxxx) 版本不一致，导致 Tauri 依赖链拉入的 GUI/COM DLL (ole32/user32/dwmapi �? 中某入口点缺失，test binary �?PE 加载阶段 0xC0000139 崩溃。诊断：集成测试 binary task8_contracts �?import GUI DLL 可正常跑 (5/5 pass)，lib test binary 额外 import 8 �?GUI DLL。修复：Cargo.toml 新增 [features] default=["gui"]，tauri/tauri-plugin-notification/tauri-build 改为 optional；lib.rs / task_events.rs / build.rs / main.rs �?Tauri 相关代码�?#[cfg(feature = "gui")] gate。结果：cargo test --lib --no-default-features 可跑�?202 pass / 23 fail (失败均为已存 test fixture 依赖 wiki/ 样本数据问题)；cargo build --features gui 完整编译不受影响。遗留：cargo tauri dev 需�?-- --features gui�?3 个失败测试需后续补齐 test fixture。：补齐 settings model / commands / service / Zustand store / SettingsView，接�?OS secret 状态、主题持久化、防 FOUC、语言初始化与 8 分组设置页，并在双审查后修正关窗行为、未配置 provider �?secret 清理可见性、更新面板占位误�?�?关键决策：全局设置写用户配置目录、项目设置继续写 .app/settings.json，secret 严格留在 OS 凭据存储，关闭行为按全局 settings 实时读取执行�?[2026-06-20] Task 12 �?HTML/卡片/概念�?项目报告导出（skills/html-* 驱动�?�?models/export.rs（ExportType→skill_folder 映射 html-beautiful-read/html-knowledge-card/html-concept-map/html-project-report，requires_source 区分单页 vs 项目级；ExportRoute/Status/ExportRoutePreference�?[derive(Default)] + #[default] Auto 规避 clippy derivable_impls），ExportRecord camelCase�? 个请�?DTO）。services/export_service.rs 重写（原 2 �?stub）：build_export_prompt 单页型经 SearchService::read_page 嵌源�?body，项目型(ProjectReport/无源 ConceptMap)�?scan_wiki 摘要+purpose.md；build_output_relative_path �?`exports/html/<slug>-<compact-stamp>.html`（compact_timestamp �?chrono %Y%m%d-%H%M%S 无冒�?Windows 安全），**ProjectReport 忽略源路径强制用 skill folder 名做 slug**（否则历史列表里项目报告被命名成单页会误导），defense-in-depth 断言 starts_with exports/html/ 且无 `..`；extract_html �?```html/``` 围栏 + **trim_trailing_prose 砍掉 `</html>` 后模型追加的「Explanation:…」散�?*（rfind case-insensitive，找不到则原样返回避免误截合法内容）；write_html �?ProjectContext::resolve_project_path 路径安全�?+ FileStore::write_text_absolute；append_record/list_records �?`.app/exports.json`。services/agent_service.rs 新增 html_export_invocation（text 输出 profile �?lint/chat invocation）。commands/export_commands.rs：start_export/regenerate_export（→run_export_task→run_export），复用 lint �?resolve_route/select_provider/WorkspaceGuard RAII/task helpers；Agent �?create_export_workspace+html_export_invocation+run_task_streaming 捕获 stdout，BYOK �?secret_service.get+llm_service.complete+tokio::select 100ms 取消轮询�?*EXPORT_OUTPUT_INVALID 同时接受 `<html` �?`<!doctype html`**；reveal_in_file_manager 跨平�?explorer /select,/open -R/xdg-open。templates/skills/html-*/{SKILL.md,template.html}�?×2）：SKILL.md 要求输出完整 standalone HTML（仅内联 CSS/SVG，无外部资源/脚本），template.html 纯样式（reading 调色板对�?UI-Frontend-design token）。前端：types/export.ts、exportStore（Zustand，sortedRecords �?useMemo 规避 gotchas �?Maximum update depth）、ExportsView（两�?grid minmax(0,1fr)+420px�? 类型 chip+可选源输入+Generate/Cancel，行：类型图�?标题/类型徽章/route/outputPath mono 11px/createdAt，Eye/RefreshCw/FolderOpen IconButton，镜�?LintView �?list_tasks/get_task/openDrawer 终�?useEffect 刷新+�?runningTaskId）、HtmlPreviewPane�?*iframe srcDoc + sandbox="" �?allow-scripts**，静态预览阻断脚本注入）、AppShell �?exports 分支+overflow-hidden 集合（activeView 已被�?9 视图分支收窄�?never，通用 else 分支模板串改 cast `activeView as string`）、i18n en/zh-CN exports.* 全量键。Rust 单测：skill_folder 映射、serde round-trip�?*templates_carry_no_schema_or_lint_directives（收紧为 !schema.md && !wiki-lint && !lint rule，原�?!lint 过松�?*、prompt 含源 body、extract_html 剥围�?留裸 HTML/**�?trailing prose/case-insensitive 闭合标签**、write_html 拒绝 `..` 越界+建文件、record round-trip、list 缺省空�?*build_output_relative_path ProjectReport 忽略源路�?*。双 review 后修复（A 共享上下�?B 全新零偏见）：O1 trailing prose 砍除；O6 ProjectReport slug；N1 模板隔离测试收紧；EXPORT_OUTPUT_INVALID 接受 doctype�?*跳过** C1�?app/*.json 并发，与现有 .app 约定一致）、C2/C3（标�?低风险）、其�?cosmetic。检查清单全过：cargo build --tests + clippy -D warnings + fmt --check 干净�?4 前端测试通过、vite build 通过、无 console.log。遗留：真实 Agent/BYOK 导出生成�?sigma/lint �?WebView2 DLL 阻断需人工浏览器验收；.app/exports.json 写入未加文件锁（与现�?.app/*.json 一致，v1 可接受）；concept-map/project-report 的输出质量取决于模型，本机无法跑真实生成�?======================================================================
[2026-06-20] Task 11 �?Lint 本地规则/Agent 深度 Lint/修复�?�?models/lint.rs（camelCase struct + snake_case 枚举：LintSeverity/Source/IssueType(8 本地+6 agent)/Fixability，LintIssue 含确定�?id `{issue_type}:{path}[:{target}]`�?*LintFixOutcome �?struct+discriminator 字段而非 `#[serde(tag)]` 枚举**规避 gotchas.txt:62 �?tagged-enum 字段泄漏�? �?serde 测试）。lint_service.rs：run_local_lint **纯确定性不调模�?*，复�?SearchService::scan_wiki �?WikiPageMeta（wikilinks/tags/sources/aliases），死链解析复用 graph_service �?stem/title/alias 查表逻辑（复制为私有 fn 避免跨耦合），emit 8 条规则（死链�?1-based body 行号 range、孤立页、缺 frontmatter、index 漂移、空页、重复文件名、路径大小写冲突、缺资源；index.md/log.md/overview.md 结构页免孤立/frontmatter 检查）；build_deep_lint_prompt �?purpose.md+schema.md+逐页摘要+要求 SKILL 输出 ```json 块；parse_agent_issues 提取 fenced JSON（无 fence 时裸 `[...]` 回退**仅在 serde 解析通过时才采纳**，避免散文里的方括号让每次深�?Lint 误败）；apply_fix **先校�?issue.path 必须�?`wiki/` 开头且�?`..`**（defense-in-depth，防 agent/前端构造的 `.app/settings.json` 路径）→ MissingFrontmatter=Safe/DeadLink=HighRisk/IndexDrift=HighRisk/其余返回 LINT_FIX_NOT_AUTO。Safe 与已确认 HighRisk 修复**都先 create_scoped_checkpoint(HighRiskOperation) �?write_markdown_checked(OverwriteIfHashMatches)**，checkpoint 失败�?GIT_CHECKPOINT_FAILED 不吞错，写后失效 graph-cache.json + 追加 wiki/log.md。HighRisk 未确认时返回 NeedsConfirmation + PendingAction（inline 确认，不走全局 ConfirmationRegistry，保�?lint 自包含）。agent_service.rs 新增 lint_invocation（text 输出 profile �?chat_invocation）。commands/lint_commands.rs：run_local_lint 同步；start_deep_lint 可取消后台任务（Agent 优先+BYOK 兜底，BYOK �?tokio::select 100ms 轮询 is_cancelled），**WorkspaceGuard RAII �?Err/取消时也清理 temp 工作�?*（happy-path 单独清理会泄漏），报告落 `.app/lint-reports/<task_id>.json`；get_deep_lint_report 读回；apply_lint_fix 委派。templates/skills/wiki-lint/SKILL.md（frontmatter name:wiki-lint，判 6 维度，只输出 ```json 数组）。前端：types/lint.ts、lintStore（Zustand，applyFix→applied 刷新本地 lint；高险返 needs_confirmation→fixConfirm 内联确认，确认面板挂载时�?read_wiki_page �?live hash �?confirmHighRisk；runLocalLint 清空 fixStatus 防陈旧徽章）、LintView（两�?minmax(0,1fr)+360px，工具栏 Run local/Deep lint/Cancel + 计数，左 LintIssueList �?severity×source 分组+Lucide 图标，右 LintIssueDetails �?path/message/target/evidence/suggestedAction/fixability 徽章/检查点说明 + 高险 before/after 预览内联确认）、AppShell �?lint 分支 + overflow-hidden 集合、i18n en/zh-CN 全量 lint.* 键。Rust 单测 13 个（clean vault 无问题�? 条规则各一、prompt �?purpose+页列表、parse_agent_issues 解析 fenced、safe fix �?checkpoint+失效缓存、高险未确认�?pending 不写盘、index drift 确认后重生、strip_wikilink alias 形态）。双 review 后修复（�?reviewer 共识）：(1) apply_fix �?wiki/ 路径边界校验�?agent/前端写盘越界�?2) agent issue id �?type �?path 碰撞→按 (type,path) 计数器加 `:N` 后缀�?3) temp 工作�?RAII 守卫防取�?出错泄漏�?4) 裸方括号 JSON 回退�?serde 通过才采纳；(5) runLocalLint 清空 fixStatus�?6) confirmHighRisk 返回 outcome，视图仅�?applied 时刷新本�?lint（去掉一直为真的误导守卫）。检查清单全过：cargo build --tests + clippy -D warnings + fmt 干净�?2 前端测试通过、vite build 通过、无 console.log。遗留：真实 Agent/BYOK 深度 Lint �?sigma 一样受 WebView2 DLL 0xc0000139 阻断需人工浏览器验收；strip_wikilink �?alias 文本本身�?`[[target|` 的极端嵌套未做正则级鲁棒（概率极低，单测覆盖�?�?alias+CJK 目标已足）；regenerate_index 会丢�?index.md 手写结构（PendingAction 已警告用户，v1 可接受）；range.line �?body 行号不含 frontmatter（前端按绝对行显示会偏移，cosmetic）�?======================================================================
[2026-06-20] Task 10 �?Chat 会话/检�?引用/存盘 �?新增 ChatService（`.app/chats/{id}.json` 原子 JSON 持久化，create/rename/delete/list/load 全通；list 对损坏文�?eprintln 后跳过不崩启动，load 返回 CHAT_PARSE_FAILED recoverable）。检索上下文复用本地 SearchService（关键词/标签/类型/来源，不调模型）：retrieve_with_excerpts �?top-6 命中 + ~1200 字节摘要 + purpose.md，拼成系统前言+purpose+逐页 `### title (path)` 摘要+最�?8 轮历�?最新问题；**citation = 实际喂给模型的检索页（诚实模型），不从模型输出里解析**。send_chat_message 作为可取消后台任务（TaskType::LlmRequest）：�?append 用户消息→build_retrieval_context→resolve_route（Agent 优先+BYOK 兜底，复�?compile �?usable_agent/select_provider 逻辑）→Agent �?`chat_invocation`（claude `--print --output-format text`、codex `exec -` stdin、openclaw `agent --message`、hermes `--prompt`�? `run_task_streaming`�?*返回类型改为 Result<String>**，捕�?LogLevel::Info �?stdout 行作为答案文本，compile 调用点丢弃该 String 不受影响�? BYOK �?LlmService.complete 包在 tokio::select �?100ms 轮询 is_cancelled→append 助手消息（content+citations+route+task_id）→Succeeded。存答案�?`wiki/queries/<slug>.md`：frontmatter type:query/title/created/session/sources（源路径�?yaml_scalar 防注入）+ Question/Answer/Sources 区段；新建页直接写并失效图缓�?追加 log.md�?*覆盖既有页绝不静�?*——无 allow_overwrite 返回 FILE_ALREADY_EXISTS �?currentHash；有 allow_overwrite+匹配 expected_hash �?*先建 scoped Git checkpoint �?OverwriteIfHashMatches**（checkpoint 失败�?GIT_CHECKPOINT_FAILED 不吞错）。前端：chatStore（Zustand，watch sendTask 终态后 reload **sendSessionId** 而非当前活动会话，避免用户中途切走被拽回；saveAnswer FILE_ALREADY_EXISTS→overwriteRequest 内联确认，confirmOverwrite 带回�?targetPath+hash 不退化成�?slug）；ChatView�?20px 会话列表+线程+composer，路由徽章取最近助手消�?route，运行态脉�?Open logs，每条助手消息「Save to wiki�?内联覆盖确认）；RightContextPanel chat 分支渲染 CitationPanel（点�?setActiveView("wiki")+openPage）。新�?9 �?Rust 服务测试（含 save_answer_overwrite_requires_allow_flag_then_hash_matches、retrieval_context_assembles_citations_purpose_and_bounded_history、list_sessions_skips_corrupt�? chatStore/chatView 前端测试。双 review 后修复（�?reviewer 共识）：(1) confirmOverwrite 丢了 targetPath 会落到别�?slug→改回传 request.path�?2) append 前再加一�?is_cancelled 检查消除「生成完到落盘」窗口的取消竞态；(3) sources YAML 未引用→�?yaml_scalar�?4) create_scoped_checkpoint �?`.ok()` 吞错→转 GIT_CHECKPOINT_FAILED�?5) ChatComposer Enter 发送加 `!event.nativeEvent.isComposing` �?IME（CJK 回车确认候选字误发）；(6) reloadActive 改用 sendSessionId 防切走竞态。检查清单全过：cargo build --tests + clippy -D warnings + fmt 干净�?4 前端测试通过、vite build 通过、无 console.log。遗留：真实 Chat/模型调用�?sigma 一样受 WebView2 DLL 0xc0000139 阻断需人工浏览器验收；BYOK 取消�?HTTP 响应延迟�?00ms 轮询间隔）非�?abort，慢 provider 会滞后（follow-up）；run_task_streaming stdout/stderr 通道关闭竞态极小概率丢尾行（follow-up）；前端路由目前硬编�?auto �?UI 强制 Agent/BYOK（Task 10 范围外）�?[2026-06-20] Task 9 �?Wiki Graph 构建/缓存/sigma 视图 �?后端 GraphService：scan_wiki 产出 WikiPageMeta �?一页一节点（type �?frontmatter/dir），边来�?[[wikilinks]]（大小写无关 stem/title/alias 查表�? 共享标签共现（MAX_TAG_GROUP_FOR_EDGES=64 封顶防爆），全部 relation="related"，权重合并、去重、避免自环、度数统计；content_hash（SHA-256 over sorted path:hash）做缓存新鲜度，�?`.app/graph-cache.json`（FileStore 原子写），损坏缓存降级为重建；resolve 命中即返回（cached=true），layout 缺失或未覆盖全部节点�?layout_stale=true。Louvain 社区 + ForceAtlas2 布局在前端跑（BACKEND_STRUCTURE §19 允许前端或后端），算完通过 save_graph_layout 回写（hash 不匹配则 no-op）。前端：sigma.js v3 WebGL 渲染器包 try/catch（jsdom/headless �?canvas 时显示占位），节�?reducer 做搜索淡�?悬停邻居高亮/选中高亮，边 reducer 做悬停高亮；GraphControls 工具栏（type/community/plain 配色、搜索、缩�?适应/重置布局/重建）、GraphInspector 在右侧面板展示选中节点（经 RightContextPanel graph 分支接入，非死代码）。关键决�?修复（双 review 后）：persistLayout �?Number.isFinite 过滤 ForceAtlas2 在退化图（单孤立节点/重合点）产生�?NaN/Infinity，否�?JSON.stringify(NaN)=null 会污染缓存；layout_stale 扩展为「缺失或未覆盖全部节点」让部分缓存触发重算而非半缓存半随机；GraphInspector 复用 types/graph �?PAGE_TYPE_COLORS 去重；邻居计数改 Set 以防未来平行边。已记录坑：sigma �?import 期引�?WebGL2RenderingContext，jsdom 必须�?setup.ts 打桩否则导入即崩（连带拖�?App.test）。检查清单全过：cargo clippy -D warnings + fmt 干净（顺手修�?5 处与 Task9 无关的旧 lint：ProjectTemplate derive Default、extraction strip_prefix、file_store 折叠 if + 删除未用 base 参、project_service or 替代 or_else、import_service vec!→数组、task8 测试 LlmService::default()→LlmService）�?9 前端测试通过、vite build 通过。遗留（未做，MVP 范围外）：get_graph 每次都跑 scan_wiki 全量哈希，未来可�?mtime 清单做廉价增量；save_layout �?rebuild 无互斥锁，极端竞态下可能丢布局（非数据损坏）；浏览器实测仍�?WebView2 DLL 阻断，sigma 真实渲染需人工验收；Task 10 未实现�?[2026-06-20] Task 8 �?Agent Detection、BYOK Provider �?Wiki Compile �?新增四类 CLI 的可替换 ProcessRunner 检�?3 秒超�?非交互沙箱协议，五类 Provider 请求�?OS credential/fake backend，隔离候选工作区、Git checkpoint+HEAD/文件哈希、PendingAction 候�?diff、失败回滚和限定路径结果提交；接�?Agent/Provider/Import compile UI、任务日�?取消、中�?i18n。双 review 后修复冲突部分落盘、确认失败卡 Running、Provider 端点串用/URL 夹密钥�?01/429 分类、Agent 删除识别、临时目录清理与 search/graph stale 刷新。未实现 Task 9 图谱构建。Rust 测试目标可编译且 Task 8 集成测试实际运行通过；完�?cargo test 仍受本机 WebView2 0xc0000139 阻断�?[2026-06-20] Task 7 Step 4 纠偏 �?Milkdown WYSIWYG �?前一版本�?textarea 偷换�?Task 7 Step 4 明确要求�?Milkdown/ProseMirror WYSIWYG 编辑器（IMPLEMENTATION_PLAN.md:541 + 技术栈锁定），属越权范围裁剪，已纠正。安�?@milkdown/kit @milkdown/react @milkdown/preset-gfm @milkdown/plugin-listener @milkdown/theme-nord（均�?7.21.2，peer deps �?react 放开 *，兼�?React 19）。WikiEditor.tsx 重写：MilkdownProvider + useEditor 装配 commonmark/gfm/history/listener 插件，listener.markdownUpdated 把序列化 markdown 回喂 onDraftChange→store.draft；沿用既有乐观并发保存（expectedHash + FILE_HASH_MISMATCH→conflict），Ctrl/Cmd+S 触发保存。关键坑�?1) useEditor deps 不能�?live draft，否则每次击键重�?ProseMirror 丢光标——改�?useState(draft) 只在挂载时取一�?seed�?2) WikiView �?WikiEditor �?key={selectedPath} 保证切页�?remount 重置 seed�?3) .use() 签名�?MilkdownPlugin | MilkdownPlugin[]，每�?preset 本身是数组，�?.use(commonmark).use(gfm).use(history).use(listener) 链式而非打包传数组；(4) theme-nord �?CSS �?exports 子路�?@milkdown/theme-nord/style.css，不能写 /lib/style.css。src/styles.css �?.wiki-editor .milkdown 作用域覆�?nord 主题色，对齐 app token。wikilink [[X]] 不是合法 markdown 语法，在 WYSIWYG 里以字面方括号文本呈现但往返无损（存盘 bytes 不变），解析为可点链接仍�?MarkdownReader 负责；如需在编辑器内也渲染为链接，要写 remark 插件，留作后续。检查清单全过：lint 0�?2 测试通过（新�?Milkdown 挂载冒烟测试）、vite build 通过。遗留：浏览器实测仍不可行（vite �?Tauri IPC、tauri dev �?WebView2 DLL 阻断�?claude/ 禁改无法�?launch.json）；ProseMirror �?jsdom 只能验证挂载不抛异常，真实编辑体验需人工浏览器验收。冲�?diff 三选一对话框（设计�?wiki.html:821 �?baseline/current/agent 合并 UI）未实现——当前冲�?UX �?banner + Reload/Cancel（非静默覆盖、用户显式抉择，已满�?acceptance「instead of silent overwrite」），完�?3-way diff dialog 属设计稿金标，待用户决定是否单开任务�?[2026-06-20] Task 7 review-fix pass �?针对 Task 7 自查 review �?4 项修复。P2（真修）：wikiStore.save 成功后用 refreshed.meta 更新 tree.pages 对应项（不再等下�?reload），保存�?title/tags 后树、类型过滤、反链立即新鲜；补回归测�?test refreshes tree page meta after save�?1 测试全过）。P4（设计对齐）：WikiTree 新增 fileIconFor——index.md→List（accent 色）、log.md→History、其余→FileText，对�?wiki.html 设计稿的差异化文件图标。P1（标注）：search_wiki 命令�?rustdoc 说明它是顶栏全局搜索（⌘K）的后端基建，wiki 树过滤是独立的客户端过滤、不调用此命令；UI 接线留给后续搜索任务，命令现在就暴露以稳定后端契约（不新建无设计稿对应的搜索 UI，避免越�?Task 8）。P3（不修）：WikiTree section 标签已是 0.08em �?CLAUDE.md；RightContextPanel �?0.06em �?Task 5/6 遗留、约束禁止改，仅记录不一致。检查清单全过：lint 0�?1 前端测试通过、vite build 通过、cargo build --tests 通过、cargo fmt --check 干净�?[2026-06-20] Task 7 �?Wiki �?渲染/编辑/搜索 �?后端新增 models/wiki.rs（WikiPageType 推断、WikiPageMeta/WikiTreeNode/WikiPageContent/Save 请求响应 DTO，camelCase serde）、models/search.rs（SearchRequest/Result/Response）；utils/markdown_utils.rs（手�?frontmatter 解析，支�?inline/block list、大小写不敏�?key、unquote；extract_title �?H1→frontmatter→文件名；extract_wikilinks 手扫 [[Target|Alias#anchor]] 去重大小写不敏感、strip anchor；count_words 跳过 code fence；snippet_for_query）；services/search_service.rs 完整实现 scan_wiki（构建扁�?pages + �?+ 加载 .app/bookmarks.json）、read_page、save_page（expected_hash None→CreateNew / Some→OverwriteIfHashMatches 乐观并发，成功后 invalidate graph-cache.json + append wiki/log.md）、search（type/tag/source 过滤 + 关键词评�?title 100/tags 40/sources 30/aliases 30/content 10，单次读盘）。新�?4 �?IPC 命令 scan_wiki/read_wiki_page/save_wiki_page/search_wiki 接入 lib.rs。前端：types/wiki.ts；features/wiki/ �?wikiStore（scan/openPage/startEdit/cancelEdit/save/reload，FILE_HASH_MISMATCH→saveState conflict）、WikiTree�?60px、过滤输�?+ 类型 pills + 递归行）、MarkdownReader（react-markdown + remark-gfm/math + rehype-katex/highlight；自定义 urlTransform 放行 wikilink:// scheme、自定义 a 解析 stem/title/alias→path，缺失→wikilink--missing）、WikiEditor（textarea 源码编辑�?+ 保存状态徽�?+ 冲突 banner）、RelatedPagesPanel（元数据 dl + tags + 反向链接 + sources）、WikiView（双�?+ 44px 头部面包�?模式切换/星标）；接入 AppShell（wiki 视图�?padding overflow-hidden�? RightContextPanel（wiki 模式渲染 RelatedPagesPanel）。i18n en/zh-CN wiki.* 全量键�?0 前端测试通过（含 wikilink 解析、alias、缺失、保存冲突、re-read 失败不掩盖成功、navigate-mid-save 不覆盖）。关键决策：(1) 不引�?Milkdown，用 textarea markdown 源码编辑器（与设计稿编辑窗一致，Milkdown 是重型依赖，留待后续 WYSIWYG 任务）；(2) react-markdown 新增依赖（remark/rehype 插件已预装，�?React 渲染器）�?3) save_page 不接�?GitService checkpoint——与 file_commands.rs::save_file（Task 5/6）一致，单页交互式编辑走乐观并发 hash guard + 原子 tmp �?+ log.md append，destructive/batch（删�?批量替换/Agent 自动修复）才�?Git，属既定范围不发散；(4) Tauri wiki 命令为同步阻塞，样本库数百页扫描足够快，与现�?scan_project 等命令同模式，长任务可取消规则针�?Agent/import/lint 而非秒级扫描。双 review 修复：wikiStore.save �?selectedPath 守卫（导航中途不覆盖�? re-read 失败不掩盖成功保存；preprocessWikilinks alias �?strip #anchor；WikiTree section 标签 tracking 0.06em�?.08em 对齐设计。检查清单全过：lint 0 warning�?0 测试通过、vite build 通过、cargo build --tests 通过、cargo fmt --check 干净、无 console.log。遗�?阻塞：本�?cargo test 仍因 WebView2 DLL�?xc0000139）无法执行，Rust 仅编译验证；KaTeX CSS + highlight.js 主题�?import（结构渲�?OK，公�?代码块样式待后续美化任务）�?[2026-06-19] Task 6 review-fix pass �?修复 review 发现的两项问题：MAJOR-1 跨项目任务污染（set_project_root 恢复新项目任务前未清旧项目内存缓存，list_tasks 会混入两个项目任务）�?新增 CancellationRegistry::clear()，set_project_root 在恢复前清空 tasks HashMap + cancellation registry；补回归测试 test_set_project_root_clears_previous_tasks（切项目�?list 为空、旧 token 不再可达�?AtomicBool 未被误取消、关项目 None 也清空）。MINOR-2 useTaskEvents 在非 Tauri 环境每次挂载无谓发起 10 �?listen() �?reject �?�?hasTauri() 守卫�?taskStore 同源。检查清单全过：lint 0 warning�?0 前端测试通过、vite build 通过、cargo build --tests 通过、cargo fmt --check 干净、无 console.log。遗留：新增 Rust 测试�?WebView2 DLL blocker�?xc0000139）无法本地执行，仅编译验证�?[2026-06-19] Task 6 �?可取消后台任�?�?实现 TaskService 完整生命周期（queued/running/waiting_for_confirmation/cancelling/cancelled/succeeded/failed，稳�?UUID、append-only 日志、validate_transition 状态机）、CancellationRegistry（Arc<AtomicBool> 共享 token）、EventBus 枚举（Tauri/Noop/TestCapture，避�?trait �?dyn-compatible）�?0 �?TaskService 单测 + 4 �?cancellation 单测。新�?7 �?IPC 命令（create/list/get/cancel/get_logs/remove_completed/set_active_project）。前端：TaskLogDrawer（紧凑列�?日志面板+进度�?取消按钮�?s 轮询非终态日志）、TaskActivityButton、taskStore（applyBackendEvent 事件 reducer）、useTaskEvents hook（订阅全�?task://* 频道 �?实时更新 store + OS 通知，项目切换时恢复持久化任务）、i18n（en/zh-CN 任务状�?类型/通知文案）。Tray：托盘菜单（Show/Hide/Quit�? 关闭最小化到托�?+ 左键单击恢复窗口；Tauri notification 插件接入。持久化：terminal transition 自动�?.app/tasks/<id>.json，set_active_project 恢复�?force-fail 中断态任务（�?WaitingForConfirmation）。关键决策：移除冗余�?Rust NotificationService（dead code），通知统一走前端事件驱�?+ i18n；事件总线是后端通知边界。双 review 修复：前端事件订阅缺失、set_result 补发事件、remove_completed 清理 JSON+token、recover 覆盖 WaitingForConfirmation、tray icon unwrap 守卫、taskStore �?hasTauri 守卫避免浏览�?测试环境误调 invoke。遗�?阻塞：本�?cargo test �?WebView2 运行�?DLL 缺失（STATUS_ENTRYPOINT_NOT_FOUND 0xc0000139）无法执行，仅靠 cargo build/check 验证；真实后�?worker spawn 与取消轮询留待后续任务接入具体业务流程�?[2026-06-19] Task 5 review hardening �?根据�?review 反馈加固导入确认边界：confirm 阶段重新校验源文�?hash、分类路由与 Rename 状态，拒绝篡改 archivedPath、预览后源文件变化、以及覆盖既有归档目标；ExtractionService �?MD/TXT/CSV/HTML 提取文本持久写入 raw/extracted/；前端确认成功后清空 preview 并补 confirm IPC 回归测试 �?遗留：PDF/DOCX/PPTX/XLSX 真实 parser �?TaskService 可取消化仍需后续任务完成�?[2026-06-19] Task 5 review fixes �?修复导入预览到确认链路：前端 ImportView 接入 preview_import/confirm_import_preview IPC；ImportFileEntry 增加 sourcePath；confirm_import_preview 在确认时复制源文件到 ProjectContext 校验后的 raw/sources �?raw/assets 归档路径，并�?FileStore 写入完整 .app/import-conflicts.json；精确重复统一�?Skip/LinkToExisting，不再覆盖既有归档目�?�?已补前端 IPC 回归测试与后端确认归档测试；遗留：项目根仍来自请�?DTO，后台可取消任务化需后续架构收口�?[2026-06-19] Task 5 深度审计与优�?�?8 项修复：1) extract_html_title 重写支持带属性标�?+ 偏移�?bug 修复�?) is_valid_url 限制 http/https 协议（拒�?file:///javascript:/data:/ftp:）；3) file_hash_fast 失败回退改用唯一标识 !nohash: 防误判重复；4) ImportView 硬编�?10+ 字符串全�?i18n�?) allow_duplicates 标志�?preview_import 中生效；6) extract_batch 错误保留 BackendError.code�?) classify_file/target_archive_dir/deterministic_rename 提取为自由函数消除跨服务耦合�?) scan_existing 改为 hash→path 映射，ExactDuplicate 冲突填充 existing_path �?新增 4 测试（allow_duplicates 行为、URL 协议限制、HTML title 属�?多行属性）�?1 Rust + 8 前端测试全过，lint/build/fmt 全绿，gotcha 追加 extract_html_title 索引偏移陷阱�?[2026-06-19] Import preview pipeline / Task 5 �?实现导入预览管道：文件分类（11 �?SourceFileType）、归档路由（raw/sources/ 子目�?+ raw/assets/）、SHA-256 哈希冲突检测（ExactDuplicate 跳过 + NameCollision 确定性重命名）、ExtractionService（MD/TXT/CSV/HTML 直接提取；PDF/DOCX/PPTX/XLSX 返回 Unsupported 不中�?batch）、URL 验证工具、@mozilla/readability 前端适配器、ImportView 完整预览 UI（文件列�?+ 状态图�?+ 元数�?+ 文本预览 + 冲突横幅 + 确认编译闸门）、parser-adapters.md 架构文档 �?关键决策：confirm_import_preview �?compile 前置闸门（按钮在�?preview 或确认中禁用）；Extractor trait 接口已设计但 binary format adapters 留待后续任务�?2 个新 Rust 测试 + 87 全量通过�? 前端测试通过，lint/build/fmt 全部通过
[2026-06-19] Git safety / Task 4 �?实现 Git-backed safety：FileStore 增加 SHA-256 哈希校验写入、Git 初始�?状�?checkpoint/diff 后端服务与命令、普通文件夹初始化确认动作改为后端存储并执行、前端接入确认弹窗与项目状态更�?�?关键决策：确认动作快照路径集合与文件内容哈希，执行前重新校验；已有脏 Git 仓库在文件迁�?写入前创建高风险操作 checkpoint，完成后再创建最终结�?checkpoint�?[2026-06-19] UI design alignment �?完整对齐 UI-Frontend-design/ 文件夹设计：字号/密度/组件高度对齐 app.css token、@fontsource 打包三款字体（Inter/JetBrains Mono/Source Serif 4）、左侧栏重构为三区段（主视图/工作�?最近页�?+ Agent 脚）、右侧面板重构为项目信息（路�?索引状�?执行路径/背景任务）、CLAUDE.md �?AGENTS.md 设计对齐原则重写为全文件夹参考（HTML 结构 + CSS token + 图标 + 交互行为）�?关键决策：设计对齐不�?CSS，页面组件拆分与 DOM 层级�?dashboard.html 为准；右侧面板不再依�?AppView prop，改为项目状态驱动；Settings 入口仅保�?TopBar 齿轮图标�? 前端测试 + 31 Rust 测试全过，lint/build 通过�?[2026-06-19] Project lifecycle / Task 3 �?实现项目生命周期（create/open/scan/list-recent/remember-recent）�? 套模板（general/research/reading/personal-growth/business �?purpose.md+schema.md）、FileStore 原子写、普通文件夹 PendingAction（确认前不移动文件）、Dashboard 项目摘要 UI（Task 3 范围）�?关键决策：open_project 宽松判定 wiki 项目以兼容样例库�?Obsidian�?obsidian 当外部目录）；RecentProjects 用原子写 + 规范化去�?+ 上限 20；scan_project 命令对路径做 canonicalize。Review 修复：archive_target_for �?links/、count_files 只计文件、PendingAction preview 递归枚举嵌套文件（避免低估后续迁移量）、scan_project 路径规范化�?1 Rust + 6 前端测试全过，lint/build 通过。残留：RecentProjects 读错时静默降级（非关键状态）、GraphState 空缓存靠字符串哨兵判定、Agent 路线硬编�?Unconfigured（待 Task 5+）�?[2026-06-19] Frontend shell / Task 2 - Built Codex-like desktop shell split into top bar, left navigation, workspace outlet, right context panel, and bottom status bar with i18n labels and mutable project/task status stores - Review findings fixed; npm test, lint, build, console-log scan, and diff check completed.
[2026-06-19] Backend contracts / Task 1 �?Added backend error model, ProjectContext path boundary, confirmation and task DTOs, missing service skeletons, and time utility �?Full cargo tests remain blocked by Cargo dependency resolution timeout; lightweight Rust metadata/format checks pass.
[2026-06-19] Implementation planning �?Created root IMPLEMENTATION_PLAN.md from SPEC requirements �?Plan decomposes MVP into agent-ready milestones with safety gates, file ownership, checks, and acceptance criteria.
[2026-06-19] 应用脚手�?�?建立 React/Vite 前端目录、Tauri/Rust 后端模块骨架、基础测试与配置文�?�?仅搭建文件结构和占位边界，未实现真实业务流程�?[2026-06-19] 项目初始�?�?初始化仓库文档基线与 CLAUDE.md / AGENTS.md，建�?progress.txt �?gotchas.txt 记录机制�?[2026-06-23] chat-FE/P0-1 �?消息渲染富化（Markdown+avatar+time+route badge+citation角标+卡片，MessageContent.tsx 复用 remark/rehype 管线，styles.css �?.msg*/.chat-prose/.seg）�?�?verified（lint 0 warning/test 105 pass�?[2026-06-23] chat-FE/P0-2 �?流式 UI（useChatStream app 级订�?task://stream-output，chatStore streamingText 累积，ChatView StreamingBubble 逐字+cursor，终�?reload 幂等）�?�?verified（lint/test 全绿�?[2026-06-23] chat-FE/P1-1 �?route segment 切换器（Auto/Agent/BYOK 三态，ChatView session toolbar，inline 重命�?删除，左�?60）�?�?verified（lint/test 全绿�?[2026-06-23] chat-FE/P1-2 �?右面板补 引用/原始资料(blocked)/执行路径(partial)/操作 四段，CitationPanel 内联�?RightContextPanel chat 分支 �?�?verified（lint/test 全绿�?[2026-06-23] chat-FE/收敛 �?chat 前端�?P0+P1 全部交付（P0-1 消息富化/P0-2 流式UI/P1-1 segment切换/P1-2 右面板四段）�?1 文件，lint 0 warning/test 105 pass/�?console.log；blocked: P1-3 原始资料(rawRefs) + timing/tokens（后端未交付�?
[2026-06-24] settings/loop P0+P1 �?SET-MODEL 落地（commit `2ad7bd5`）�?扩展 Settings 数据模型覆盖全部 P0+P1 字段（Rust `models/settings.rs` + TS `types/settings.ts` + `settingsStore.ts` 默认值与 density/font 副作�?+ `styles.css` data-density 间距缩放）。关键决策：①全部新字段 `#[serde(default)]`，旧 .app/settings.json / 全局 settings.json 无新字段仍可反序列化（向后兼容，已加测试 `legacy_settings_file_without_new_fields_still_deserializes_with_defaults`）。②沿用既有全局/项目切分边界——跨项目 UI/托盘/通知/更新偏好归全局 `GlobalSettingsFile`，per-project Agent/Git/上下文行为归项目 `ProjectSettingsFile`（密钥仍只进钥匙串，PRD-SET-002）。③`CloseBehavior` 加设计稿第三�?`Ask`；新增枚�?`DensityPreference`/`StartupBehavior`/`AgentOutputLanguage`/`UpdateFrequency`/`NotificationClickBehavior`/`SystemNotificationPrefs`，每个带 `as_str()`。④`ProjectSettingsFile` 因含 `f64 temperature` �?`Eq`（保�?`PartialEq`）。验证：`cargo build --lib` + `cargo test --no-run`（二进制编译通过；运行崩于本机已�?0xc0000139 loader 级环境问题，�?gotchas，与代码无关�? `cargo clippy --lib --tests`（仅旧有 2 warning，非本项�? `cargo fmt --check`（settings.rs 干净�? `tsc -b` + `npm run test`(130) + `npm run lint` 全绿，无 console.log。账�?SPEC/plans/settings.md �?→verified�?
[2026-06-24] settings/loop SET-PROVIDERS �?LlmProviderSettings 重写为设计稿 apikey-row（commit 待提交）�?PRD-SET-001 落地。后�?src-tauri/src/commands/llm_commands.rs: status_with_secret 助手统一回填 has_secret+secret_mask（SecretService::mask �?位，永不回显全文，PRD-SET-002）；list/save/store 经此助手；新�?check_ollama_reachable 异步命令�?{base_url}/api/tags�?s 超时，返 OllamaReachability �?OLLAMA_UNREACHABLE）；test_llm_provider �?complete 探活。lib.rs:122 注册。前�?LlmProviderSettings.tsx 全量重写：每 provider 一�?apikey-row（icon/name+hint/badge/末位 model），Anthropic 深底白字 A / Ollama cpu+badge--warn 服务未运�?/ Custom link 图标；hint 展示 model·mask·钥匙串；编辑面板常显�?provider.test.tsx 契约（API key input 常在、行内含 model、Test provider 按钮、role=status、saved）；Ollama 接实时服务态，未运行禁�?Test；密钥清除并入此页。styles.css �?.apikey-row 5列网�?32px 1fr auto×3)+.badge--success/--outline 变体(设计稿预设但此前�?。i18n �?25 provider.* key（en/zh-CN 对齐）。关键决策：①编辑面板常显而非设计稿的行内展开——保 provider.test.tsx 2 测契约不改测试，SettingsView onTestProvider 包装器仍注入 contextWindow；②contextWindow 硬编�?32_000（沿用原实现，SettingsView 包装器覆盖为 settings.contextWindow）；③不�?tauri-plugin-updater，更新检查用已有 reqwest（见 SET-UPDATES）。守边界：API Key 仍只�?keyring，UI 仅显 mask，项目文件不存密钥；不动 UI-Frontend-design/；不动样本库。验证：npm run test(130 pass,27 files)+npm run lint(0 warning) 全绿�?console.log；cargo build --lib+cargo clippy --lib --tests（仅旧有 2 warning HEAD 已存非本项）+rustfmt --check（本�?2 文件干净；cargo fmt 报的 diff 全在 import_service.rs/tests/sources_promotion.rs 先前未提交工作）。未做浏览器预览：SettingsView 需 Tauri 运行�?已开项目，浏览器无法渲染；渲染契约由 2 provider 单测覆盖。账�?SPEC/plans/settings.md �?→verified�?
[2026-07-07] Chat citation provenance and retrieval planner v1 — Implemented model-grounded `[S#]` citations, retrieval diagnostics, BYOK/Agent prompt split, index/pinned/keyword budgeted retrieval planning, and source-id citation clicks — Citations persisted to chat JSON and wiki/queries now come only from parsed model markers; retrieval hits stay diagnostics; no graph expansion, DB, vector DB, Import, CompilePlan, or Lint changes.

[2026-07-09] Logo transparent cutout - Removed the white background from the approved square book-page + knowledge-graph mark, regenerated Tauri icon outputs from the transparent source, and updated the in-app top-left brand logo to use the same transparent asset with dark-theme inversion - Key decision: keep the selected mark shape unchanged while treating white as background only.

[2026-07-09] Logo exterior-only cutout correction - Reprocessed the approved logo so only the outer connected white background is transparent while the book page interior and lower page slot remain opaque white; regenerated Tauri icons and synced the in-app top-left logo asset - Key decision: preserve internal white page surfaces as part of the mark, not as removable background.
[2026-07-12] Import v2 final A2 safety fixes — Made commit cancellation typed and durably account every unprocessed decision, implemented backend-derived collision-free CreateNew Wiki targets, and stripped URL credentials, secret query parameters, and fragments before session persistence — Key decision: cancellation is never a successful task result, CreateNew accepts no frontend path, and only normalized public URL locators reach project files.
[2026-07-12] Import V2 Task 5 modern Office routing — Added explicit DOCX/XLSX/PPTX primary/MarkItDown/qualified-Oxide/Agent route attempts, typed fallback records and structural quality floors; added a staging-only JSON-RPC MarkItDown runner, release verification, and fail-closed platform qualification — Key decision: repository fixtures validate contracts only; Office Oxide remains disabled without independent qualification and placeholder manifests cannot be published as measured release artifacts.
[2026-07-12] Import V2 File Ingestion Task 6 — Added isolated legacy DOC/XLS/PPT conversion pack contracts, validated OOXML cache handoff, qualified Office Oxide fallback, and Agent eligibility — LibreOffice is release-supplied only; absent local binaries are reported honestly and never installed at runtime.
[2026-07-12] Import V2 Web foundation and WeChat — Added public-only URL contracts, two-phase DNS/connection SSRF policy, bounded manual-redirect fetching, offline Readability pack contract, domain routing, isolated browser sessions, and challenge-aware WeChat extraction — Key decision: signed resource URLs stay in non-serializable request memory while every persisted HTML/Markdown locator uses the stripped public form.
[2026-07-12] Import V2 Web connector and release gates — Added Zhihu, Bilibili with reused subtitle/ASR routing, gated Xiaohongshu/X connectors, stable web recovery/error contracts, capability registration, frozen security fixtures, and a fail-closed QA matrix — Key decision: local source and fixture success does not release unsigned zero-size placeholder packs; Stage 1 awaits signed payload/cross-platform/manual evidence and Stage 2 remains disabled.
[2026-07-12] Import V2 Web dual-review correction — Forced phase-two production routes closed, replaced query secret denylisting with public-key allowlisting, removed secret-bearing Debug surfaces, stripped remote image URLs from persisted lite-pack artifacts, and corrected QA claims — Key decision: current branch is an incomplete fail-closed foundation, not a Stage 1 release; production fetch/extract wiring and sandboxed signed payloads remain blockers.
# [2026-07-13] Import V2 Migration Task 1 — Frozen migration DTOs, validation invariants, and hostile clean/ambiguous/corrupt/Unicode fixture corpus — Key decision: persisted paths are project-relative, links require evidence, and invalid/uncertain records fail closed for later unmanaged/conflict handling.
# [2026-07-13] Import V2 Migration Task 2 — Added bounded read-only legacy inventory scanning with no-follow file reads, deterministic fingerprints, corruption warnings, and immutable raw/wiki evidence — Key decision: scanner output is evidence only; it never creates migration state or follows symlinks/reparse points.
# [2026-07-13] Import V2 Migration Task 3 — Added deterministic correlation planner and documented truth table for stable IDs, hashes, URLs, conflicts, and unmanaged records — Key decision: fuzzy title/name/time evidence is non-authoritative and contradictory or case-colliding identities never auto-link.
# [2026-07-13] Import V2 Migration Task 4 — Added canonical dry-run JSON/Markdown reports with grouped outcomes, confirmation boundary, rollback statement, redacted diagnostics, and explicit untouched legacy/content paths — Key decision: dry-run reports are derived in memory and are not project writes until apply is explicitly confirmed.
# [2026-07-13] Import V2 Migration Task 5 — Added confirmation-bound apply with project/V2 identity drift checks, Core mutation lock/preflight, Git/no-Git rollback boundary, CAS/no-follow transaction writes, cancellation, recovery resume, and idempotent applied-report convergence — Key decision: only V2 index and migration report may be written; legacy index, raw/wiki, old logs/history remain evidence.
# [2026-07-13] Import V2 Migration Task 6 — Exposed scan/plan/apply/status/resume commands with typed camelCase DTOs, shared Core preflight/lock boundaries, durable cancellable TaskService execution, and TypeScript invoke mirrors — Key decision: dry-run commands remain synchronous read-only DTO calls while apply/resume use the existing background task protocol.
# [2026-07-13] Import V2 Migration Task 5 follow-up — Bound inventory project identity and exact V2 index fingerprint into plans, hardened repeat-apply confirmation ordering, and added persisted status projection — Key decision: idempotence never bypasses explicit confirmation or stale-inventory checks.
# [2026-07-13] Import V2 Migration Task 7 — Added bounded legacy task/import-history read-only projection with original IDs/timestamps, malformed-entry warnings, secret/log omission, and destructive actions disabled — Key decision: legacy history remains evidence and is never rewritten into V2 history or routed through V2 mutations.
# [2026-07-13] Import V2 Migration Task 8 — Added typed readiness verifier, non-mutating `check:import-v2-cutover` gate, machine-readable blocked evidence, license/provenance checks, platform matrix, and rollback/soak checklist — Key decision: incomplete or unrun package/platform evidence remains a blocker and is never inferred as passed.
[2026-07-13] Import V2 Migration Task 9 — Added release-bound activation metadata, explicit readiness/rollback confirmation, activation read API, and guards on legacy import mutation paths — Key decision: activation writes only `.app/import-v2-migration/activation.json`; rollback remains prior-release based and V1/V2 dual-write toggles are not exposed.
[2026-07-13] Import V2 Migration Task 10 — Inventoried legacy mutation deletion candidates (`src-tauri/src/commands/import_commands.rs`, `src-tauri/src/commands/file_commands.rs`, `src-tauri/src/services/import_service/source_actions.rs`, `src-tauri/src/services/import_service/source_catalog.rs`, plus their command registrations/tests) and deferred removal — Key decision: no approved soak window exists, so initial activation keeps legacy code and only enforces a runtime mutation guard.
[2026-07-13] Import V2 Migration Task 11 review fix — Preserved the apply/resume checkpoint outcome in the durable task summary instead of discarding `MigrationApplyResult` — Key decision: operators can now distinguish Git-checkpointed apply from explicit release-based no-Git rollback while affected paths remain limited to new V2 metadata.
[2026-07-13] Import V2 Migration Task 11 — Focused migration/activation Rust integration suites passed; manual Review A (design, safety, Core integration, rollback) and Review B (unexpected writes, matching, recovery, dual-write, licensing, tests) were completed and fixes rerun — Key decision: release delivery remains blocked by missing frontend dependencies, incomplete package/platform/license evidence, and the intentionally failing read-only cutover gate.
[2026-07-14] Import V2 frontend gate correction — Migration readiness no longer blocks V2 session creation or source/URL/preview/queue workflows; only final commit remains disabled while activation is pending — Keep the no-V1-write safety gate and explain the partial availability in the migration notice.
[2026-07-20] Import V2 WeChat image localization and migration notice polish — Localized bounded WeChat article images into staging assets with Markdown/snapshot reference replacement and changed the migration advisory to a compact hoverable warning icon — Key decision: image failures remain non-fatal with coverage/warning evidence, while migration remains accessible without dominating the import surface.
[2026-07-20] Import V2 WeChat image verification — Focused frontend tests (11) and WeChat Rust tests (2) passed; the unified check passed frontend tests, lint, build, console scan, and GUI Rust check, then stopped on the existing missing `turndown` dependency in browser-runtime-lite tests — Key issue: repository-wide green status still requires restoring that unrelated capability-pack dependency.
[2026-07-22] Windows capability-loader stack overflow fix — Moved signed capability discovery and verification onto a named 8 MiB startup worker, replaced recursive runtime inventory traversal with a bounded iterative walk, made Debug development packs explicitly replace matching production engines, and preserved structured panic diagnostics — Key decision or open issue: invalid or missing packs remain non-fatal readiness states; only loader infrastructure failure blocks startup, and the original 1 MiB-stack SenseVoice regression plus Release compilation now pass.
[2026-07-22] Windows startup stack-overflow verification — Completed the post-review Release build, real signed SenseVoice test under `RUST_MIN_STACK=1048576`, full unified project check, and an 8-second hidden GUI executable smoke run — Key decision or open issue: the executable remained alive without `STATUS_STACK_OVERFLOW`; `npm run check` passed with 100 frontend files/661 tests, 37 Node capability tests, 5 Python tests, lint/build/console/GUI and all Rust suites.
[2026-07-23] Bilibili API evidence precedence fix — Reproduced the user's two failed BV imports from persisted task/session records, made successful public Bilibili API evidence override unreadable HTML-edge bytes, and made URL engine-output failures surface as web issues instead of file parse failures — Key decision or open issue: videos without public subtitles now enter explicit local-ASR authorization/capability recovery; the reported BV1QZKH6vEe2 no longer produces IMPORT_V2_ENGINE_OUTPUT_INVALID in the live Rust regression.
[2026-07-23] Bilibili import failure recovery hardening — Retained subtitle/ASR recovery across later provider failures, enabled metadata-only previews without media, preserved login/removal/challenge results, terminalized replaced waiting tasks, and revalidated redirected platform assets — Key decision or open issue: the reported IMPORT_FILE_PARSE_FAILED path is eliminated; transient network failures remain distinct fetch errors and the unified check is next.
[2026-07-23] Bilibili import failure review closure — Merged shared-context and fresh-context review findings, preserved explicit provider failures without blocking valid fallbacks, and completed the final regression run — Key decision or open issue: `npm run check` passed with 100 frontend files/661 tests, 47 Node capability tests, 5 Python tests, lint/build/console/GUI and all Rust suites.
[2026-07-24] Graph node manipulation — Added Sigma-native node dragging, empty-canvas selection clearing, drag-state feedback, one-shot layout persistence, and focused interaction tests — Key decision or open issue: pointer moves update graphology directly without React/Zustand churn or ForceAtlas2 restarts; unified check and dual review are next.
[2026-07-24] Graph drag review fixes — Merged both required reviews and added a 3px drag threshold, synthesized-stage-click suppression, bbox-aware Fit, and a serialized latest-wins layout save queue with regression tests — Key decision or open issue: focused graph tests pass with 3 files / 13 tests; the unified check must now be rerun from the beginning.
[2026-07-24] Graph node manipulation review closure — Completed Obsidian/Sigma research, merged both independent reviews, fixed drag threshold/click timing/Fit bounds/save ordering, and passed the full repository gate — Key decision or open issue: node moves stay on the graphology/WebGL path and only the latest serialized layout snapshot persists; `npm run check` passed with 103 frontend files / 680 tests, 60 Node capability tests, 5 Python tests, lint/build/console checks, GUI Rust compile, and all Rust tests.
[2026-08-12] Decision Gate H Batch H2 protected Agent candidate bridge — Added unreachable authenticated Claude/Codex lint transports, layout-authoritative bounded candidate leases, the narrow LintRepair manifest/classification profile, and owner-neutral reuse of the existing Update Wiki review/Diff helpers — Key decision or open issue: both Agent routes remain disabled until H3; H2 classifies delete, unexpected overwrite, and baseline conflict for later confirmation but never applies project changes.
