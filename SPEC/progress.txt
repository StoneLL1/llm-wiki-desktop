# 进度记录 (progress.txt)

> 每完成一个重要进度节点，按时间倒序在此追加一条记录（最新在上）。
> 格式：[YYYY-MM-DD] 模块/任务 — 完成内容摘要 — 关键决策/遗留问题。

======================================================================
[2026-06-19] Task 6 review-fix pass — 修复 review 发现的两项问题：MAJOR-1 跨项目任务污染（set_project_root 恢复新项目任务前未清旧项目内存缓存，list_tasks 会混入两个项目任务）→ 新增 CancellationRegistry::clear()，set_project_root 在恢复前清空 tasks HashMap + cancellation registry；补回归测试 test_set_project_root_clears_previous_tasks（切项目后 list 为空、旧 token 不再可达但 AtomicBool 未被误取消、关项目 None 也清空）。MINOR-2 useTaskEvents 在非 Tauri 环境每次挂载无谓发起 10 个 listen() 被 reject → 加 hasTauri() 守卫与 taskStore 同源。检查清单全过：lint 0 warning、10 前端测试通过、vite build 通过、cargo build --tests 通过、cargo fmt --check 干净、无 console.log。遗留：新增 Rust 测试因 WebView2 DLL blocker（0xc0000139）无法本地执行，仅编译验证。
[2026-06-19] Task 6 — 可取消后台任务 — 实现 TaskService 完整生命周期（queued/running/waiting_for_confirmation/cancelling/cancelled/succeeded/failed，稳定 UUID、append-only 日志、validate_transition 状态机）、CancellationRegistry（Arc<AtomicBool> 共享 token）、EventBus 枚举（Tauri/Noop/TestCapture，避免 trait 非 dyn-compatible）、20 个 TaskService 单测 + 4 个 cancellation 单测。新增 7 个 IPC 命令（create/list/get/cancel/get_logs/remove_completed/set_active_project）。前端：TaskLogDrawer（紧凑列表+日志面板+进度条+取消按钮，2s 轮询非终态日志）、TaskActivityButton、taskStore（applyBackendEvent 事件 reducer）、useTaskEvents hook（订阅全部 task://* 频道 → 实时更新 store + OS 通知，项目切换时恢复持久化任务）、i18n（en/zh-CN 任务状态/类型/通知文案）。Tray：托盘菜单（Show/Hide/Quit）+ 关闭最小化到托盘 + 左键单击恢复窗口；Tauri notification 插件接入。持久化：terminal transition 自动写 .app/tasks/<id>.json，set_active_project 恢复并 force-fail 中断态任务（含 WaitingForConfirmation）。关键决策：移除冗余的 Rust NotificationService（dead code），通知统一走前端事件驱动 + i18n；事件总线是后端通知边界。双 review 修复：前端事件订阅缺失、set_result 补发事件、remove_completed 清理 JSON+token、recover 覆盖 WaitingForConfirmation、tray icon unwrap 守卫、taskStore 加 hasTauri 守卫避免浏览器/测试环境误调 invoke。遗留/阻塞：本机 cargo test 因 WebView2 运行时 DLL 缺失（STATUS_ENTRYPOINT_NOT_FOUND 0xc0000139）无法执行，仅靠 cargo build/check 验证；真实后台 worker spawn 与取消轮询留待后续任务接入具体业务流程。
[2026-06-19] Task 5 review hardening — 根据双 review 反馈加固导入确认边界：confirm 阶段重新校验源文件 hash、分类路由与 Rename 状态，拒绝篡改 archivedPath、预览后源文件变化、以及覆盖既有归档目标；ExtractionService 将 MD/TXT/CSV/HTML 提取文本持久写入 raw/extracted/；前端确认成功后清空 preview 并补 confirm IPC 回归测试 — 遗留：PDF/DOCX/PPTX/XLSX 真实 parser 与 TaskService 可取消化仍需后续任务完成。
[2026-06-19] Task 5 review fixes — 修复导入预览到确认链路：前端 ImportView 接入 preview_import/confirm_import_preview IPC；ImportFileEntry 增加 sourcePath；confirm_import_preview 在确认时复制源文件到 ProjectContext 校验后的 raw/sources 或 raw/assets 归档路径，并用 FileStore 写入完整 .app/import-conflicts.json；精确重复统一为 Skip/LinkToExisting，不再覆盖既有归档目标 — 已补前端 IPC 回归测试与后端确认归档测试；遗留：项目根仍来自请求 DTO，后台可取消任务化需后续架构收口。
[2026-06-19] Task 5 深度审计与优化 — 8 项修复：1) extract_html_title 重写支持带属性标签 + 偏移量 bug 修复；2) is_valid_url 限制 http/https 协议（拒绝 file:///javascript:/data:/ftp:）；3) file_hash_fast 失败回退改用唯一标识 !nohash: 防误判重复；4) ImportView 硬编码 10+ 字符串全迁 i18n；5) allow_duplicates 标志在 preview_import 中生效；6) extract_batch 错误保留 BackendError.code；7) classify_file/target_archive_dir/deterministic_rename 提取为自由函数消除跨服务耦合；8) scan_existing 改为 hash→path 映射，ExactDuplicate 冲突填充 existing_path — 新增 4 测试（allow_duplicates 行为、URL 协议限制、HTML title 属性/多行属性），91 Rust + 8 前端测试全过，lint/build/fmt 全绿，gotcha 追加 extract_html_title 索引偏移陷阱。
[2026-06-19] Import preview pipeline / Task 5 — 实现导入预览管道：文件分类（11 种 SourceFileType）、归档路由（raw/sources/ 子目录 + raw/assets/）、SHA-256 哈希冲突检测（ExactDuplicate 跳过 + NameCollision 确定性重命名）、ExtractionService（MD/TXT/CSV/HTML 直接提取；PDF/DOCX/PPTX/XLSX 返回 Unsupported 不中断 batch）、URL 验证工具、@mozilla/readability 前端适配器、ImportView 完整预览 UI（文件列表 + 状态图标 + 元数据 + 文本预览 + 冲突横幅 + 确认编译闸门）、parser-adapters.md 架构文档 — 关键决策：confirm_import_preview 是 compile 前置闸门（按钮在无 preview 或确认中禁用）；Extractor trait 接口已设计但 binary format adapters 留待后续任务；22 个新 Rust 测试 + 87 全量通过，8 前端测试通过，lint/build/fmt 全部通过
[2026-06-19] Git safety / Task 4 — 实现 Git-backed safety：FileStore 增加 SHA-256 哈希校验写入、Git 初始化/状态/checkpoint/diff 后端服务与命令、普通文件夹初始化确认动作改为后端存储并执行、前端接入确认弹窗与项目状态更新 — 关键决策：确认动作快照路径集合与文件内容哈希，执行前重新校验；已有脏 Git 仓库在文件迁移/写入前创建高风险操作 checkpoint，完成后再创建最终结果 checkpoint。
[2026-06-19] UI design alignment — 完整对齐 UI-Frontend-design/ 文件夹设计：字号/密度/组件高度对齐 app.css token、@fontsource 打包三款字体（Inter/JetBrains Mono/Source Serif 4）、左侧栏重构为三区段（主视图/工作流/最近页面 + Agent 脚）、右侧面板重构为项目信息（路径/索引状态/执行路径/背景任务）、CLAUDE.md 与 AGENTS.md 设计对齐原则重写为全文件夹参考（HTML 结构 + CSS token + 图标 + 交互行为）— 关键决策：设计对齐不只 CSS，页面组件拆分与 DOM 层级以 dashboard.html 为准；右侧面板不再依赖 AppView prop，改为项目状态驱动；Settings 入口仅保留 TopBar 齿轮图标。6 前端测试 + 31 Rust 测试全过，lint/build 通过。
[2026-06-19] Project lifecycle / Task 3 — 实现项目生命周期（create/open/scan/list-recent/remember-recent）、5 套模板（general/research/reading/personal-growth/business 的 purpose.md+schema.md）、FileStore 原子写、普通文件夹 PendingAction（确认前不移动文件）、Dashboard 项目摘要 UI（Task 3 范围）— 关键决策：open_project 宽松判定 wiki 项目以兼容样例库与 Obsidian（.obsidian 当外部目录）；RecentProjects 用原子写 + 规范化去重 + 上限 20；scan_project 命令对路径做 canonicalize。Review 修复：archive_target_for 补 links/、count_files 只计文件、PendingAction preview 递归枚举嵌套文件（避免低估后续迁移量）、scan_project 路径规范化。31 Rust + 6 前端测试全过，lint/build 通过。残留：RecentProjects 读错时静默降级（非关键状态）、GraphState 空缓存靠字符串哨兵判定、Agent 路线硬编码 Unconfigured（待 Task 5+）。
[2026-06-19] Frontend shell / Task 2 - Built Codex-like desktop shell split into top bar, left navigation, workspace outlet, right context panel, and bottom status bar with i18n labels and mutable project/task status stores - Review findings fixed; npm test, lint, build, console-log scan, and diff check completed.
[2026-06-19] Backend contracts / Task 1 — Added backend error model, ProjectContext path boundary, confirmation and task DTOs, missing service skeletons, and time utility — Full cargo tests remain blocked by Cargo dependency resolution timeout; lightweight Rust metadata/format checks pass.
[2026-06-19] Implementation planning — Created root IMPLEMENTATION_PLAN.md from SPEC requirements — Plan decomposes MVP into agent-ready milestones with safety gates, file ownership, checks, and acceptance criteria.
[2026-06-19] 应用脚手架 — 建立 React/Vite 前端目录、Tauri/Rust 后端模块骨架、基础测试与配置文件 — 仅搭建文件结构和占位边界，未实现真实业务流程。
[2026-06-19] 项目初始化 — 初始化仓库文档基线与 CLAUDE.md / AGENTS.md，建立 progress.txt 与 gotchas.txt 记录机制。
