{
  "schemaVersion": 1,
  "purpose": "Batch A frozen expected-red witnesses. These observations document known defects and must be converted to positive green contracts by B-E/F, never skipped.",
  "fixtures": [
    "native_legacy_missing_tasks",
    "native_legacy_missing_sources",
    "obsidian_restricted_cjk",
    "obsidian_enabled_state_only",
    "import_scale_100",
    "import_scale_1000",
    "import_scale_10000"
  ],
  "observations": [
    "layout resolver accepts a legacy native shape while strict registry rejects it",
    "NativeLegacy is Repairable while repairAvailable is false",
    "file discovery invokes its control-plane callback once per file",
    "SessionStore::update_item reloads and rewrites the complete session"
  ],
  "baseline": {
    "discoveryCallbacksFor100Files": 100,
    "operationTasksFor10000Items": 10000,
    "frontendStorePublicationsFor10000TerminalItems": 10000,
    "strictNativeForLegacyTaskRoot": false,
    "legacyRepairAvailable": false,
    "sessionUpdateUsesFullLoadAndSave": true
  },
  "witnesses": [
    { "file": "project_layout_authority_contract.rs", "test": "legacy_native_layout_inspection_agrees_on_a_repairable_missing_task_root" },
    { "file": "project_layout_authority_contract.rs", "test": "native_legacy_repairable_state_advertises_a_repair_plan" },
    { "file": "import_v2_file_discovery.rs", "test": "batch_a_expected_red_counts_one_discovery_callback_per_file" },
    { "file": "import_v2_scale_contract.rs", "test": "expected_red_single_item_update_rewrites_every_persisted_item_file" },
    { "file": "import_v2_file_orchestration.rs", "test": "batch_a_expected_red_task_service_creates_one_backend_task_per_item_at_scale" },
    { "file": "../../src/features/import/importScaleContract.test.ts", "test": "records one frontend store publication per terminal item under the current path" }
  ],
  "migration": {
    "batchB": ["unify native inspection", "supply explicit legacy repair plans"],
    "batchC": ["add compatible app-state task roots without content-root guessing"],
    "batchE": ["coalesce discovery events", "incrementally persist item updates"],
    "batchF": ["batch frontend item/task patches and avoid full refreshes"]
  },
  "greenTargets": {
    "maxDiscoveryCallbackInvocationsFor100Files": 10,
    "maxOperationTasksFor10000Items": 1,
    "maxSessionSummaryWritesForOneItemUpdate": 1,
    "maxItemWritesForOneItemUpdate": 1,
    "maxFrontendPublicationsFor10000TerminalItems": 100
  }
}
