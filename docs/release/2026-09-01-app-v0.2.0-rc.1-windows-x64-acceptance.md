# app-v0.2.0-rc.1 — Windows x64 real-machine acceptance evidence

- Candidate: `app-v0.2.0-rc.1` → commit `98cdbc5113859fd8ec52afba28a59f8653faae39`
- Sealed by workflow run [33481891948](https://github.com/StoneLL1/llm-wiki-desktop/actions/runs/33481891948) (all five jobs green; two `desktop-release` environment reviews approved by the owner)
- Public release verified: prerelease, not draft, `targetCommitish` = `98cdbc5`, 9 assets (Windows setup + `.sig`, 2 × DMG, 2 × app updater archive + `.sig`, `CHECKSUMS.sha256`); `releases/latest/download/latest.json` → HTTP 404 (no stable channel touched)
- Execution date: 2026-09-01, owner-approved first platform (decision ⑤; macOS arm64 follows; macOS x64 / Ubuntu 24.04 x64 tracked Pending in [#34](https://github.com/StoneLL1/llm-wiki-desktop/issues/34))
- Host: real Windows 11 x64 machine, standard user session. Paths and vault names below are sanitized; the real values are CJK-with-spaces and were verified at Unicode codepoint level.

## 1. Anonymous download and integrity

- Assets fetched with plain `curl` (no GitHub credentials): HTTP 200; sizes matched the release page exactly (setup 13,853,757 B; `.sig` 436 B; `CHECKSUMS.sha256` 929 B).
- SHA-256 of the downloaded setup and `.sig` both match the published `CHECKSUMS.sha256`.
- Updater signature cryptographically verified against the committed public trust anchor (`src-tauri/tauri.conf.json` `plugins.updater.pubkey`): Tauri minisign **prehashed** mode (blob magic `ED`, key id matches the anchor), Ed25519 signature valid over BLAKE2b-512 of the setup bytes. Trusted comment binds build timestamp and file name.

## 2. Clean install

- Pre-state: two stale test installs of the retired 0.1.0 coordinate (NSIS `0.1.0-rc.2`, MSI `0.1.0`) plus their app-state directories were removed/archived first; registry verified empty across HKCU/HKLM/WOW6432Node.
- Silent install (`setup.exe /S`): exit 0. Registry `DisplayVersion` = **0.2.0-rc.1**, install dir recreated, `llm-wiki-desktop.exe` 49,269,248 B.
- Note: the exe's Windows version resource reports `0.2.0` (VERSIONINFO cannot carry a prerelease suffix); the authoritative version is the registry `DisplayVersion` `0.2.0-rc.1`.
- Authenticode status of both setup and installed exe: `NotSigned` — consistent with the documented `windows-authenticode-not-required` policy; checksum + signature verification above is the trust path.

## 3. Launch and restart

- Launch: process alive, real top-level window (title "LLM Wiki Desktop", HWND observed), fresh first-run state created (`settings.json` only).
- Restart (kill + relaunch): new PID, window present, `settings.json` byte-identical (SHA-256 prefix compared before/after).
- Restart with project: the CJK-named vault created during the journey was restored automatically (page count, import record, project root, and persisted UI language all correct).

## 4. Core journey (real install, UI-driven)

- Created a new native vault with a **CJK + spaces name** via the in-app dialog ("research" template): on-disk layout matches `SPEC/SPEC.md` §5 exactly — `purpose.md`, `schema.md`, `raw/{sources,extracted,assets}/`, `wiki/{index,log,overview}.md` + all nine subfolders, `exports/html/`, `skills/`, `.app/` state files, and an auto-initialized `.git` checkpoint.
- Unicode integrity verified at codepoint level: the name set in the dialog equals the JSON state entry and the real NTFS directory name (no mojibake in persisted data; console display only).
- Paste-import journey: pasted a Chinese markdown document with an explicit CJK source name; staged → confirmed "Import to Source library (1)". Both layers landed:
  - immutable evidence `raw/sources/<sourceId>/<versionId>/{original.bin, source.json, derived/extracted.md, derived/quality.json, metadata/metadata.json}`, `original.bin` contains the pasted text verbatim;
  - readable Source `wiki/sources/local/<CJK title>.md` with correct frontmatter (`sourceId`, `versionId`, `sourceKind: local_text`, `contentHash`, quality gate `TEXT_COVERAGE 1.0 ≥ 0.98 pass`).
- Capability packs view: 13 definitions listed, overall status "Target unpublished" — the designed fail-closed typed state while no stable release carries the signed archives (no crash, no silent fallback). Real capability install/activation is the post-`publish-stable` phase.

## 5. Uninstall and project preservation

- Pre-uninstall inventory: 80 files, per-file SHA-256 manifest.
- Silent uninstall: exit 0; install dir removed; HKCU uninstall entry gone.
- Post-uninstall inventory: **80/80 files byte-identical — zero added, removed, or changed**. Project bytes preserved.
- Observation: global app state directory (settings/recent projects) survives uninstall — consistent with the data-safety-first design; recorded, not a defect.

## 6. Observations (non-blocking)

- First window rendered in Chinese while the persisted fresh-install default is `en`; after the first project creation the UI followed the persisted `en`. The manual language toggle works and persists (`zh-CN`). Possible default-detection/persist-order inconsistency — minor, tracked here.
- UI automation needed `--force-renderer-accessibility` (WebView2 accessibility tree off by default) — an automation note, not a product defect.
- No SmartScreen prompt appeared on the silent install path (no mark-of-the-web on curl-downloaded files); the documented manual-override warning path applies to browser-downloaded runs.

## 7. Row verdict

| Row | Result |
| --- | --- |
| Clean install + launch + restart | **PASS** |
| Prior-version upgrade | Waived (one-time 0.2.0 bootstrap waiver, re-approved 2026-08-31) |
| Uninstall | **PASS** |
| Project bytes unchanged | **PASS (80/80 byte-identical)** |
| Cryptographic/policy evidence | **PASS** (checksum + Ed25519 updater signature + NotSigned policy record) |

Windows x64 candidate row: **complete, no blocker found.**
