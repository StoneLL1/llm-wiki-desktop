Comparing a macOS temporary path lexically can fail even when both paths identify the same file — `os.tmpdir()` may yield `/var/...` while `fs.realpath()` canonicalizes it to `/private/var/...` — When the production contract returns canonical paths, canonicalize the test fixture with `realpath` before equality assertions and keep containment checks on canonical roots.
A lockfile generated on an existing platform can omit optional peer and bundled WASI closure even while local checks pass — Clean Windows, macOS, and Linux runners then fail `npm ci` with EUSAGE for missing `@emnapi/core`/`@emnapi/runtime` or an exact `wasi-threads` version — Pin Node and npm across CI and release, regenerate with that npm and `--include=optional` in a clean worktree, and prove a fresh `npm ci` before pushing.
Pinning Node/npm without executing the pinned runtime can make every CI platform fail after installation — Node 22.17.0 and 22.23.1 require the `--experimental-test-isolation=none` spelling while newer Node accepts an unprefixed alias — Exercise release scripts with the exact pinned runtime, use the oldest supported flag spelling, and do not pass isolation flags to an intentionally older embedded capability runtime unless required.
A source-contract test can pass in a rewritten working tree and fail in a clean Windows checkout — Matching `#[test]\nfn` assumes LF while `core.autocrlf` can produce CRLF — Normalize read source to LF before structural text matching, and include a clean worktree in release validation.
A successful ShellExecuteW check is not enough if Tauri cleanup runs first — cleanup_before_exit invalidates runtime state even when installer launch later reports <=32 — Validate the shell handoff before on_before_exit, then cleanup and exit only on success; lock validate < cleanup < exit in a root test.
A lazy drawer that is conditionally unmounted cannot observe its closed state to restore focus — The parent removed TaskLogDrawer as soon as drawerOpen became false, bypassing its true-to-false effect — Lazy-load on first open but keep the controller mounted afterward, and test focus return through the real AppShell.
GitHub immutable releases can reject rollback-to-draft after publication — Immutability permits deleting the whole release but locks published assets and tags, so a draft-only rollback can leave an unverified stable release public — Attempt draft rollback first, delete the release as the immutable fallback, retire the exposed tag, and make double failure a critical incident.
Windows updater handoff can exit before Rust records a terminal install state, and the pinned vendor currently ignores `ShellExecuteW` failure — The Windows updater launches the installer and immediately calls `process::exit(0)`, so post-`install()` code is unreachable and a failed shell launch is indistinguishable from an interrupted replacement — Persist a phase-aware handoff receipt before calling the updater, reconcile it on startup, and fix/test the `ShellExecuteW <= 32` result in the pinned 4A vendor boundary rather than trusting an in-memory terminal transition.
A retained Windows directory handle that omits delete sharing can block legitimate namespace mutations in newly created descendant directories — File sharing is enforced during child publish while an ancestor capability remains open, causing ERROR_SHARING_VIOLATION in multi-target transactions — Let retained directory capabilities share delete and prove confinement by swapping the lexical parent to a junction while all mutations continue through the original handle.
Checking identity and hash before an atomic exchange is insufficient against same-inode writes — An external writer can modify the inode after the hash read without changing identity — Exchange or quarantine first, recheck identity and hash on the isolated old object, and restore the name on mismatch.
A race test that alternates attack and mutation phases does not test a retained-binding race — Acquire the retained binding before swapping the parent, mutate through it while the link or junction is active, require successful swaps, and drive a real transaction rollback.
A real Windows junction test can become flaky under the full parallel gate when its controller busy-spins around `cmd /C mklink /J` with a short shared deadline — Process scheduling can exceed the local fast-path timing without indicating a confinement failure — Use independent bounded swap/restore deadlines, sleep briefly while polling, and keep round-specific diagnostics plus exact successful-swap counts.
An env-cleared Windows child creates a literal `%SystemDrive%` tree under its working directory — Windows known-folder fallback uses `%SystemDrive%\ProgramData` when `SystemDrive` is absent, so sanitized PowerShell/Agent probes can leak caches into the repository — Preserve only the non-secret `SystemDrive` runtime variable alongside `SystemRoot`/`WINDIR`, keep token variables excluded, and assert the literal fallback tree is never created.
Running Rust tests with default GUI features exits `0xc0000139` even though the repository gate is green — This repository's test contract uses `cargo test --no-default-features`; the default GUI test binary has a Windows loader entry-point mismatch — Reproduce failures with `npm run test:rust` or pass `--no-default-features` before diagnosing product code.
Closing a PowerShell child's console writer is not a deterministic inherited-pipe EOF fixture — The child may keep its stdout handle open even after `[Console]::Out.Close()`, so a reader-first process-reap regression can time out without exercising the intended lock order — Model the ordering explicitly by dropping the captured pipe in the test before entering the non-blocking reap loop.
An Agent route revision that hashes only CLI version and logical path is not an execution identity — Root cause: npm shims can retarget node/scripts, same-path binaries can be replaced without changing version output, and project Markdown can change during final route probing after a deep snapshot was built — How to avoid: attest program, leading args, readable content digests, executable metadata, and PATH generation with bounded pre/post probes outside the cache lock; bind the opaque invocation; and revalidate both snapshot and baseline immediately before spawn or BYOK dispatch.

Pinned Agent contracts need one stable byte representation and cross-surface fixture — Root cause: line-ending normalization or editing the built-in Skill changes the content hash, while duplicated Rust/TypeScript constants can silently drift — How to avoid: force the Skill file to LF, hash the exact `include_str!` bytes, mirror the hash in typed fixtures, and keep a contract test that computes and compares all three values.

Windows canonical paths can contain a `\\?\\` device prefix that recent-project paths do not — Root cause: `Path::canonicalize()` returns an extended-length path while persisted recent roots normally use the ordinary drive/UNC spelling, causing equality-only relocation guards to reject the same directory — How to avoid: normalize separators, trailing separators, case, device prefixes, and `UNC` spelling in every security comparison, then cover both persisted-root and canonical-root forms in tests.
A newly openable folder does not prove that a missing recent project was moved there — Root cause: current recent `projectId` is session-scoped and `canonicalIdentityKey` includes the path, so neither can establish move equivalence — How to avoid: never replace/remove an old recent entry after an arbitrary picker success; first persist a move-stable native identity and compare it in one backend mutation, while compatible folders without such evidence remain manual.
The configured Cargo registry cannot supply new dependencies in this sandbox — Root cause: `crates-io` is replaced by the Tuna registry, and even explicitly approved direct registry access timed out while the local Cargo cache lacked the requested crate — How to avoid: verify a capability-filesystem crate is cached or use a network-capable Windows runner before beginning descriptor-relative/no-follow work; do not leave a new unresolved dependency in `Cargo.toml`.
Vite/Vitest cannot start in this sandbox even without the Tailwind plugin — Root cause: Vite/Rolldown's config loader invokes `ChildProcess.spawn` for Windows realpath resolution and the sandbox returns `EPERM`; Tailwind's native module error is an additional, not sole, blocker — How to avoid: do not claim frontend unit or E2E coverage from this environment; use a Windows runner that permits Vite child-process execution, then run the normal `npm test` / `npm run check` gates.
Default-feature Rust test binary can fail before running with Windows `STATUS_ENTRYPOINT_NOT_FOUND` (`0xc0000139`) even after it compiles — Root cause: the GUI/Tauri test executable cannot load a required runtime entry point in this sandbox image — How to avoid: use default `cargo check` for GUI compile evidence, exercise lib-available services with `--no-default-features`, and report the GUI test binary failure instead of treating it as an application test failure.
Windows `mklink /J` fixture creation can reject a mixed-separator link path — Root cause: `PathBuf::join("wiki/linked")` preserved a forward slash inside an otherwise Windows path, and `cmd` treated the resulting target as invalid — How to avoid: build each Windows path component with separate `join` calls and use the established `cmd /C mklink /J <link> <target>` argument shape.
Recovery is the authority state that must stop mutation access; unreadable or source-empty states need their own workflow prerequisites — Root cause: treating every non-Healthy assessment as untrusted broke valid empty native-project workflow persistence and caused a barrier test to hang after a worker assertion — How to avoid: reserve the resolver-level mutation downgrade for `Recovery`, and let workflow-specific prerequisites handle missing readable sources.
Cargo test filters are substrings, not grouped regular expressions — Root cause: passing `(create_project|remember...)` selected zero tests while still returning exit 0 — How to avoid: run each exact test filter separately or run the enclosing module and verify the reported test count.
Cursor equality is not a stale-request guard — Root cause: a full workflow refresh can legitimately return the same history cursor while superseding an older load-more request, allowing the older response or error to commit afterward — How to avoid: give pagination its own request token, invalidate it on full refresh, and guard success, error, and loading cleanup with the token, project epoch, and initiating identity.
Workflow history identity checks must cover every page — Root cause: validating only the initial overview/history pair still lets a later cursor page merge runs from a replaced same-root project identity — How to avoid: validate project ID, canonical identity key, and identity revision before every initial or paginated history commit, and preserve the existing runs/cursor on mismatch.
Nullable workflow overview state is not a no-project discriminator — Root cause: the backend represents no project with a successful non-null overview containing fixed rows and open-project prerequisites, while frontend `null` only means idle, loading, or failed — How to avoid: model request lifecycle explicitly, render backend no-project data as returned, and keep overview readiness independent from history loading.
Workflow completion recovery must re-enter the coordinator, not only repair Task JSON — Root cause: an exact post-commit recovery can make a task terminal without releasing the project workflow lease, leaving the next queued run stranded — How to avoid: recover the exact terminal result through TaskService and then normalize terminal state through WorkflowCoordinator; test that the next queued run is promoted.
Compatible project facts are not an executable Update Wiki layout contract — Root cause: reusing native ProjectContext paths after a compatible assessment can write the wrong Wiki/Source roots, while a generic Agent process cannot prove workspace-only output isolation — How to avoid: require one authoritative layout-aware runner access check at prepare, start, every external generation boundary, legacy entry, and atomic apply; return a typed prerequisite before task creation until the shared layout/isolation contract exists.
Crash recovery cannot infer this task's success from Source consumption alone — Root cause: an older consumed version, a provisional receipt, or a crash between file writes, final Git commit, and receipt finalization can make registry state look stable when this candidate was not committed — How to avoid: bind schema-v2 consumption to task/candidate/checkpoint, persist an exact journal and final-commit closure, recognize consumption only after matching finalization, and fail closed on any hash, route, scope, HEAD, or index drift.
Apply race backups are CAS evidence, not permission to install the candidate — Root cause: an edit after the last baseline check can be moved into the backup immediately before replacement; merely observing external bytes in the backup would still overwrite them — How to avoid: compare the backup's actual hash to the journal's expected prior inside the same critical section, restore mismatches losslessly, persist conflict/recovery, and cover captured/installed/restoring plus alias states.
Git workflow-runtime filtering must preserve both sides of a rename and use platform case semantics — Root cause: parsing only the destination can make a content-to-runtime `git mv` look clean, while unconditional case folding hides distinct paths on case-sensitive filesystems — How to avoid: retain both porcelain rename paths, compare case-insensitively only on Windows, and build checkpoints from the same content-filtered set used by preparation.
Workflow-state containment cannot rely on lexical `starts_with` — Root cause: a symlink or Windows junction beneath `.app/workflows` can redirect preferences or task state outside the physically owned app-state root — How to avoid: validate the canonical physical app-owned root through FileStore, reject internal and external link/junction aliases, and keep read-only exceptions entirely ephemeral.
Preparation replay must serialize revalidation, consumption, and task creation per token — Root cause: a check-then-consume split permits concurrent double-start tasks, while consuming before a fallible enqueue burns the only recoverable token — How to avoid: hold one server-side per-token lock through fresh fact/route/baseline validation and the created/existing decision, bind only a successfully created task ID, and leave failed attempts retryable.
Persisted workflow preferences are hints, not authorization for quick rerun — Root cause: an editable project JSON file can forge last-run state and bypass the required first confirmation — How to avoid: require matching server-owned terminal run history plus the exact structured preference/fingerprint before marking quick rerun eligible; any baseline, prerequisite, scope, output, or route change revokes it.
Project access downgrade must serialize policy, ownership, and writes together — Root cause: checking `persistence_enabled` or caller-supplied roots outside the snapshot lock permits a late write or a read-only project A task to be persisted under writable project B — How to avoid: keep project policy, task owner-root validation, entry flags, creation, and atomic writes under one persistence lock; only bind truly unowned legacy tasks after a successful explicit write, and test downgrade plus cross-root attempts.
Filesystem architecture guards are weak when they match only module names — Root cause: `Path::canonicalize`, grouped `use std::{fs, ...}`, and aliased filesystem imports perform I/O without containing the literal `std::fs` form — How to avoid: keep canonicalization and OS identity inspection behind FileStore, scan workflow sources for filesystem imports and path I/O methods, and include executable counterexamples that prove each forbidden spelling is detected.
Adding a shared task-status variant can silently split domain semantics even when serde compiles — Exhaustive recovery matches, terminal helpers, persistence fixtures, and visible consumers evolve at different batches — Search every status match when extending the enum, keep only compile/legacy-contract adaptations in the contract batch, and defer producer/recovery/UI behavior together to the owning implementation batch.
Headless Edge launched through PowerShell `Start-Process -ArgumentList` can silently screenshot `ERR_FILE_NOT_FOUND` for a valid local HTML URL, and the error images may even have identical sizes — The argument array is rejoined in a way that is unreliable for this file-URL/headless combination, while Edge may also finish writing the screenshot after the launcher returns — Invoke `msedge.exe` directly with individually quoted arguments, use a unique `--user-data-dir`, add `--allow-file-access-from-files`, and wait briefly for the output file before inspecting it.
An inline absolute menu remains clipped even with a high z-index — Root cause: an ancestor scroll/list container establishes an overflow clip, and stacking order cannot paint descendants outside that clip — How to avoid: render floating UI in a body-level portal or browser top layer, then anchor it with viewport-aware placement; do not treat `z-index` or removing only the nearest `overflow: hidden` as a durable fix.
In-flight request deduplication needs a freshness identity — Root cause: coalescing every active request by resource key can let an event-driven refresh reuse a snapshot captured before that event — How to avoid: let newer task/candidate tokens replace the active request, coalesce only identical refresh identities, and retain epoch guards so the displaced Promise cannot commit.
Physical schema 3 does not prove a Wiki page was born with Source frontmatter — Root cause: V2 migration upgrades index and manifest JSON atomically but intentionally leaves legacy Markdown bytes unchanged — How to avoid: preserve a bounded exact-path asset fallback only for the current version's validated `legacy-import-{version}` migration marker; ordinary malformed modern Source pages must still fail closed.
Focused Rust tests must use the project's feature profile — Root cause: on Windows, a default-feature GUI-linked test binary can fail at process startup with `STATUS_ENTRYPOINT_NOT_FOUND` even though the core test target is valid — How to avoid: mirror `npm run test:rust` with `cargo test --no-default-features` for focused service tests, then rely on the full gate for GUI compilation.
Source frontmatter parse failure is not proof of a legacy project — Root cause: treating missing, malformed, or non-UTF frontmatter as a legacy signal lets a current project downgrade into full manifest scanning — How to avoid: permit the read-only legacy lookup only when both the persisted index and matching manifest explicitly declare schema 2; all current-schema parse failures must fail closed.
`tauri dev` latency has three independent phases — Root cause: the custom npm launcher prepares a large development capability before Tauri starts, Tauri then waits for Vite and Rust compile/link, and application setup blocks on capability integrity verification before readiness — How to avoid: instrument and benchmark preflight, frontend-listen, Cargo-build, process-start, and window-ready separately, with cold and warm capability/cache results kept distinct.
Running the full gate inside the managed sandbox can produce false failures in Vite native bindings, child-process spawn, and Rust tests that write OS settings — Root cause: Windows sandbox restrictions reject Tailwind oxide loading, process creation, or AppData writes even when the code is valid — How to avoid: rerun the same required gate outside the sandbox before classifying those EPERM/access-denied errors as product regressions; the current outside-sandbox full gate passes.
Agent model identity and CLI version are different audit fields — Root cause: storing a CLI version in `model` makes retries and provenance look pinned to a model that the app never selected — How to avoid: record the Agent execution profile/model identity separately from `engineVersion`, and normalize legacy candidate metadata on read.
Stale Source AI workspace cleanup needs an active lease — Root cause: age-only cleanup can race a long-running or resumed task after sleep or a clock jump — How to avoid: skip current-process leased workspaces and unregister the lease on every normal cleanup path.
Hermes profile resolution is not the same as a fixed default `HERMES_HOME` — Root cause: forcing the platform root bypasses sticky `active_profile`, while native Windows uses `%LOCALAPPDATA%\hermes` — How to avoid: let explicit `HERMES_HOME` win; otherwise resolve `active_profile` from the platform root and preserve only the selected OAuth/model/path selectors.
Source Agent capability checks must mirror the exact workflow argv — Root cause: stale assumptions about OpenClaw/Hermes command surfaces can make an installed Agent fail preflight or start interactively — How to avoid: validate the current official one-shot entry and every exact flag together with invocation and fake-help tests.
Claude Code bare isolation and local OAuth/keychain reuse are mutually exclusive — Root cause: bare mode disables the credential channel required by a GUI-launched Source task — How to avoid: use the safe-mode/no-session/no-project-rule Source profile when reusing local Claude login, and test the exact current CLI help flags.
Empty-state title and body can collide with substring-based UI tests — Root cause: a structured empty state introduced a heading that repeated the first words of its explanatory sentence — How to avoid: keep the heading semantically distinct (for example, “No item selected”) while preserving the detailed sentence for accessible context.
Source AI route availability must come from one backend capability contract — Root cause: the dialog treats any configured default Agent as runnable, while `supports_source_ai_agent` rejects Claude/Codex and auto-routing silently falls through to BYOK; the token-set fact guard then confuses ordinary CJK headings and identifier labels with protected names — How to avoid: expose route/provider readiness with reasons from the backend, make the UI consume that contract, preserve immutable structural anchors deterministically, and treat uncertain semantic heuristics as review warnings rather than hard candidate rejection.
Synchronous ImportEngine code must not run directly on a Tokio async worker — Root cause: a sync web adapter created a current-thread runtime and called `block_on` while Tauri's Tokio worker was already driving async tasks; the detached task panic occurred before attempt/error persistence and left item/task records in Extracting/Running — How to avoid: dispatch ImportEngine work through the bounded blocking queue, give adapters an OS-thread runtime boundary when needed, catch `descriptor`/`supports`/`execute` panics, and regression-test durable terminalization.
PowerShell `Measure-Command { npm run ... }` can report exit code 0 after the npm child failed — Root cause: the timing wrapper returns its own successful measurement object instead of propagating `$LASTEXITCODE` — How to avoid: run the npm command directly and use the command runner's wall time, or explicitly capture and re-emit the child exit code.
Windows Node check orchestrators can fail immediately with `spawn EINVAL` when launching `npm.cmd` directly — Root cause: `.cmd` launch behavior varies across Windows Node/process environments even when the same command works in an interactive shell — How to avoid: when already running under npm, invoke `process.execPath` with `process.env.npm_execpath`; keep a narrowly scoped `cmd.exe /c npm run <known-script>` fallback only for direct Node invocation.
The older `PreviewWithoutTranscript` filtering note below is superseded — Root cause: retaining recovery-filter guidance after deleting the variant can imply that metadata-only commit is still a supported fallback — How to avoid: do not reconstruct or filter that action; it is absent from the model, orchestrator, frontend, i18n, and tests, and missing transcript/no-speech must produce no preview, raw output, or Source.
Missing media transcript must fail closed — Root cause: the former `PreviewWithoutTranscript` recovery action could turn metadata into a committable preview and bypass the spoken-content contract — How to avoid: that action is removed as of the 2026-07-24 product baseline; require a parsed subtitle or an actual authorized local-ASR result, otherwise produce no preview, raw output, or Source.
Xiaohongshu non-video notes must not fall back to the generic article contract — Root cause: using the presence of parsed image URLs to choose `image_post` lets a changed or missing image field downgrade a real note to text-only Markdown and bypass required OCR — How to avoid: classify every target-bound non-video XHS note as `image_post`, then fail explicitly when no image evidence can be localized.
Platform media state can be JSON encoded inside a JSON string — Root cause: XHS `mediaV2` may contain playable streams and subtitles as a serialized JSON string, so object-only traversal sees neither and incorrectly treats the note as metadata-only — How to avoid: parse only the named field with a strict byte bound, retain target-ID binding, and validate every extracted asset URL through the same HTTPS/DNS/redirect policy.
Platform-specific evidence requirements must also constrain recovery actions — Root cause: a generic `PreviewWithoutTranscript` action can bypass a platform contract that requires spoken content, even when extraction itself fails closed — How to avoid: enforce the rule in both backend request construction and frontend action filtering, and make XHS transcript completeness depend on successfully parsed subtitles or an actual local-ASR continuation.
Platform asset warnings can be scheme failures, not host failures — Root cause: Bilibili metadata still returns some trusted `i*.hdslb.com` image URLs as plain HTTP, while the hardened asset policy intentionally accepts only HTTPS; the generic warning text says “host” and repeats once per skipped image — How to avoid: inspect the persisted provider evidence before expanding a host allowlist, keep HTTP blocked, and prefer a separately tested HTTPS-upgrade/aggregated-warning path for known image CDNs.
Progress must be wired through the engine selected at runtime — Root cause: the Bilibili import actually selected `builtin.web-bilibili`, while the first download-progress implementation covered only pack remote assets, leaving the real 130 MB media download at the generic “Extracting source” stage — How to avoid: trace the persisted attempt’s route and engine ID, then test progress at that concrete engine boundary rather than an adjacent architecture path.
Bilibili playback CDN trust must follow exact runtime evidence — Root cause: the player API can return HTTPS media from `*.edge.mountaintoys.cn` on a non-default port, not only `*.bilivideo.com`; an incomplete allowlist rejected the media before download/ASR began — How to avoid: allow only the exact verified suffix with label-boundary matching, require HTTPS, revalidate every redirect before the request, and retain DNS/private-address checks.
Capability process exit and stdout delivery are separate events — Root cause: the OS can report a short-lived child as exited before the stdout reader thread has parsed and queued its terminal JSON-RPC response, and extra progress lines widen that race — How to avoid: after observing exit, drain the bounded output channel for a short fixed deadline before declaring that the process produced no result, and keep a delayed-reader regression test.
Import progress must use one task-wide scale — Root cause: writing ASR `0..100` subprogress directly after extraction `1/4`, then returning to validation `3/4`, makes honest progress visibly jump backward; stale events and retry inheritance can cause the same symptom in frontend state — How to avoid: map download, ASR, validation, and completion into disjoint monotonic ranges, consume the task store's freshness-checked snapshot, and clear progress whenever a different task ID is explicitly bound.
Capability progress does not reach Import items automatically — Root cause: a pack process returned only one terminal JSON-RPC result, while frontend task events and persisted `ImportItem.progress` were separate state paths; even a live `TaskProgress` update could leave the queue and inspector showing an indeterminate stale item — How to avoid: emit bounded typed progress notifications, bridge them into `TaskService`, explicitly bind each returned task to its mapped item, merge subsequent task progress into that item, and deduplicate recovered versus locally recorded batches by task ID.
Development-pack refresh must distinguish source-managed runner files from reusable native payload — Root cause: checking only four executables allowed missing tokens, qualification data, or runtime libraries to loop forever on the refresh path, while checking the old runner against its stale manifest would unnecessarily force a 455 MB restage — How to avoid: validate every non-runner file in the prior manifest, require all core payload declarations, refresh runner files atomically from source, keep the prior manifest through qualification, then reinventory and re-sign only after qualification succeeds.
Lazy UI regression tests need a full-suite timing budget — Root cause: the graph canvas fallback resolved in under 500 ms alone but repeatedly crossed a 5-second assertion timeout while 99 other Vitest files ran concurrently, producing a false logic failure — How to avoid: keep the semantic assertion, use a bounded suite-load margin for lazy chunks/effects, and confirm suspected flakes both alone and in the complete check.
Long-form SenseVoice input must be segmented before inference — Root cause: the offline SenseVoice CLI accepted a 38:54 WAV as one sequence and ONNX self-attention attempted a 24.2 GB allocation, then returned an empty JSON result with exit code zero — How to avoid: decode to bounded short chunks, recognize bounded batches, tolerate silent chunks, rebase token times onto the source timeline, and validate aggregate byte/token/chunk limits.
Development capability freshness cannot be keyed only by version — Root cause: `prepare-sensevoice-dev` treated an installed native/model payload as current even after runner source changes, and its refresh path redownloaded a large source archive before deciding it only needed to re-sign existing files — How to avoid: persist a deterministic runner-source fingerprint, atomically refresh the installed runner when it changes, qualify/reinventory/re-sign it, and fetch native/model sources only when the payload is actually missing.
ASR output validation must follow each declared pack layout — Root cause: the orchestrator accepted only staging-root `.sensevoice-output-*` directories while the supported Whisper pack writes `runtime-temp/asr-output-*`, causing a valid completed transcript to be rejected after authorization was consumed — How to avoid: allow only the explicitly supported temporary layouts, retain canonical staging containment and non-link checks, and regression-test every accepted layout plus near-miss paths.
Rust canonical Windows paths are not portable capability locators — Root cause: `Path::canonicalize()` emits a `\\?\` namespace path while Node resolves the staging root as a drive path, so `path.relative()` treats a legitimate ASR input as outside staging and raises `IMPORT_ASR_POLICY_BLOCKED` — How to avoid: validate the canonical path in Rust, but hand capability-produced artifacts to the next engine through the protocol's staging-relative `chainedInput`.
Bilibili DASH tracks are ASR inputs, not preservable original videos — Root cause: DASH audio/video tracks are separate streams, so treating an audio `m4s` fallback as the original media can save an incomplete file and falsely satisfy transcript requirements — How to avoid: keep progressive media and temporary ASR media as separate typed fields, require an actual Local ASR continuation, and fail closed when preserve-original has only DASH tracks.
Bilibili page metadata and player evidence arrive in different payloads — Root cause: the browser extractor required one response to contain both the target video ID/title and subtitle/media fields, so target-bound player XHRs with `subtitle_url` or DASH streams were observed but discarded — How to avoid: anchor player responses by BV/AV query identity, merge them with the page metadata payload, persist both as evidence, and reject responses bound to recommended videos.
Platform pages can show login prompts beside valid target data — Root cause: broad page-text classification can mistake a reusable login CTA or stale shell for a blocking auth state even when the requested note payload is present — How to avoid: first extract and anchor structured data to the target ID, and classify page-wide captcha/login/removal text only when no usable target payload exists.
High-image-count platform posts can exceed the browser pack notification cap — Root cause: Xiaohongshu extraction permits up to 100 ordered images while the generic JSON-RPC reader previously accepted only 32 remote assets, causing otherwise valid large posts to fail before localization — How to avoid: keep the bounded remote-asset ceiling above the platform image maximum plus media/subtitle slots and regression-test both 100 accepted and the hard upper bound rejected.
Upstream ASR archives can multiply local disk use — Root cause: the Windows sherpa payload duplicated DLLs under `bin/` and `lib/`, bundled unused 700+ MiB CUDA copies, and the development flow retained a second 1.2 GiB source cache — How to avoid: qualify a CPU-minimal development pack, remove duplicate/unused providers, discard the reproducible large source cache, and measure the final ignored directory.
Signed capability inventories must use bytewise path ordering — Root cause: locale-aware `localeCompare` produced an order that differed from Rust's deterministic string order, so a genuinely signed and qualified pack was rejected — How to avoid: sort manifest and executable paths with explicit `<`/`>` comparison and verify the prepared pack through the production Rust resolver.
Windows archive-directory rename can fail with EPERM — Root cause: antivirus or file handles can block renaming an extracted Node runtime even when all files are readable — How to avoid: fall back to a bounded recursive copy, remove partial destinations on failure, and validate the expected runtime executable before reusing cache entries.
Pinned Node 22.17 does not support `--test-isolation=none` — Root cause: the development pack runs its own pinned Node rather than the workspace Node version — How to avoid: invoke the pack's focused runner suite with portable `node --test`; keep workspace-only flags out of the preparation path.
Windows Tailwind native binding and Vite startup can fail before tests — Root cause: the installed `@tailwindcss/oxide-win32-x64-msvc` binary reports invalid UTF-8 and Vite separately hits `spawn EPERM`, so `npm run check` may stop at Vitest initialization or time out even when the repository diff is documentation-only — How to avoid: record the exact startup diagnostics, do not reinstall dependencies without user approval, and separately run lightweight Markdown/diff validation while reporting the unified-check blocker.
Project-scope guards must not discard backend task facts — Root cause: treating a stale project result as wholly irrelevant can suppress the global task-store upsert together with the stale drawer/toast/UI commit — How to avoid: always persist returned task snapshots and cross-project task events globally, then gate only project-local session state, automatic drawer opening, and toasts by project key/epoch.
Session mutation revisions are reconciliation signals, not success filters — Root cause: dropping a successful mutation response whenever another mutation increments a shared revision can leave backend state invisible and skip required follow-up work — How to avoid: replace the session only for the latest revision, but refresh the canonical session and continue scoped follow-up actions when a successful response is superseded.
Opaque URL-reference tests must not use the OS keyring — Root cause: `ImportV2Service::default()` stores test-only web targets in global credential storage, where environment state can surface as `IMPORT_V2_URL_REFERENCE_EXPIRED` — How to avoid: construct import services that call `store_web_target` with `SecretService::memory()` so each test owns an isolated credential map.
Security-bound capability output prefixes apply to test doubles too — Root cause: an ASR integration fixture returned a legacy `.fixture-asr-output` workspace after the orchestrator began accepting only direct-child `.sensevoice-output-*` paths — How to avoid: make fake runners obey the same signed-runner output contract so boundary tests verify behavior instead of bypassing it.
Platform bootstrap payloads often contain both the requested post and recommendations — Root cause: recursively taking the first object with title/media fields can import a sidebar or feed recommendation instead of the URL target — How to avoid: derive the platform content ID from the final redirected URL, scope extraction and retained API evidence to a matching nested object, and fail closed when that target is absent.
URL evidence is not always a full `<!doctype html>` document — Root cause: specialized extractors can persist `<article>` fragments and JSON may start with a UTF-8 BOM, so strict prefix detection bypasses gzip and commit-boundary redaction — How to avoid: classify any UTF-8 markup fragment plus valid BOM-trimmed JSON as URL snapshot evidence and regression-test both forms.
Ad-hoc Rust target directories can break the frontend lint gate — Root cause: Cargo verification used `src-tauri/target-*` directories while ESLint ignored only the default `src-tauri/target`, so generated Tauri JavaScript was treated as application source — How to avoid: ignore both `src-tauri/target/**` and `src-tauri/target-*/**`, then rerun the unified check from the beginning.
Platform image posts must not be hard-gated on OCR — Root cause: classifying every non-video platform image as a temporary OCR input made ordinary XHS/Douyin/Bilibili image imports fail when the optional OCR pack was absent — How to avoid: preserve image evidence independently and create temporary OCR inputs only after the explicit `enable_ocr` recovery action.
Capability runner and orchestrator temporary layouts can drift — Root cause: media/ASR/OCR inputs moved from the legacy `runtime-temp` tree to unique direct-child staging workspaces while cancellation and restart recovery still cleaned only the old tree — How to avoid: keep one tested allowlist of direct-child temporary prefixes, reject symlinks/reparse points, and invoke the same cleanup contract on failure, cancellation, and session recovery.
RapidOCR's unconstrained OpenCV dependency can silently drop Intel macOS support — Root cause: OpenCV 5 no longer publishes an x86_64 macOS wheel even though Windows, Linux, and Apple Silicon resolve — How to avoid: pin OpenCV 4.12.0.88 and dry-run the hash lock for all four target triples with source builds disabled.
RapidOCR 3.8.1 can fail with OmegaConf 2.0.6 before inference — Root cause: RapidOCR assigns a pathlib Path when Global.model_root_dir is null, but that OmegaConf release accepts only primitive values — How to avoid: always pass Global.model_root_dir as a signed-pack string and exercise real engine initialization in release qualification.
Workflow-dispatch inputs must never be interpolated into secret-bearing `run` scripts — Root cause: GitHub expression substitution happens before the shell parses the script, so quotes/newlines can become code and exfiltrate signing secrets — How to avoid: pass inputs through environment variables, validate strict identifiers, protect the job with an environment/default-branch gate, and contract-test all run blocks.
Signed capability versions must be immutable at both archive and manifest layers — Root cause: an archive digest alone does not identify an already extracted same-version directory, so stale signed bytes could be silently accepted — How to avoid: embed `manifestSha256` in the install catalog, verify it for staged and existing installs, reject version collisions, and never publish release assets with overwrite/clobber semantics.
Capability ZIP hashes cannot live inside the ZIP's own signed manifest — Root cause: the manifest bytes affect the archive digest, so `manifest.archiveSha256 == sha256(zip containing manifest)` is a cryptographic fixed-point requirement that normal release tooling cannot satisfy — How to avoid: schema v2 keeps archive digest/sizes in the application-embedded catalog and signs the extracted file inventory in the manifest.
Capability extraction must restore every signed helper executable, not just the main entrypoint — Root cause: ZIP extraction creates ordinary files and Chromium/FFmpeg helper binaries lose Unix execute permission — How to avoid: include a sorted executable path list in the signed manifest, validate it against the file inventory, and chmod only those paths after signature and hash verification.
Bilibili HTML-only extraction is unreliable — Root cause: Bilibili pages are client-rendered/risk-controlled and CDN playback URLs commonly require the page Referer — How to avoid: use the public view/player API with WBI signing for full BV/AV URLs, send the page origin on API/CDN requests, and keep browser capability as the fallback for private/short-link cases.
Bilibili API playback can be unavailable independently of metadata — Root cause: WBI/player responses may fail or expose no progressive stream even when public metadata succeeds — How to avoid: keep metadata/subtitle extraction usable when media lookup fails, but fail preserve-original or ASR-required imports with a typed media-unavailable issue instead of a false-success preview.
Browser fallback must run before anonymous platform HTML routes — Root cause: an installed browser capability cannot reuse the persistent connector profile if the generic HTTP route consumes the URL first — How to avoid: order platform URL routes as browser capability, then verified provider parser, then anonymous generic fallback; keep browser prefetch disabled for the browser route.
ASR recovery actions must match the actual capability route — Root cause: a legacy media.subtitle label and Bilibili-only authorization action were reused for all platform media errors — How to avoid: map missing media transcription to `asr-whisper` / `media.asr` and do not expose platform-specific authorization actions for other platforms.
Page fetch policy cannot discover binary media after the response arrives — Root cause: a `Page` fetch rejects audio/video before content-type inspection — How to avoid: select `WebFetchContent::Media` from a verified direct-media URL before fetching, then stage the bytes only in durable assets when the user chose preserve-original.
URL media imports can appear successful while original media is unavailable — Root cause: protected platform CDN URLs may require the persistent browser connector profile, while the generic Rust fetcher has no browser-cookie context — How to avoid: use the browser capability route for authenticated media capture and keep the raw HTML/API snapshot plus a typed media-fetch failure when the CDN cannot be fetched anonymously.
Chat can appear to hang when search-result snippet generation panics on CJK text — Root cause: `snippet_for_query` computed byte offsets from a visual radius and sliced inside a UTF-8 code point, aborting the worker before it could publish a terminal task state — How to avoid: clamp snippet start/end to `str::is_char_boundary` and keep a CJK/emoji regression test.
Same-text or cross-scope drafts can be cleared by a late accepted send — Root cause: content equality is not draft identity, and an async response may finish after scope migration — How to avoid: track scope lineage plus a per-scope revision, and only clear unchanged drafts owned by the submission.
Chat cancellation must not publish a terminal state before cleanup — Root cause: the task API can mark Cancelled while the worker still owns the Chat send gate and may need to report partial edits — How to avoid: return Cancelling for running Chat tasks, let the worker finish cleanup, emit the final error/task event, then transition to Cancelled.
Convenience rollback can overwrite an external edit on an affected path — Root cause: Git HEAD/checkpoint equality does not prove the file is still the Agent output — How to avoid: persist post-Agent hashes and refuse rollback when any affected path differs.
Chat session deletion can race a background send when invoked from another window — Root cause: UI disabling does not protect the Tauri command boundary — How to avoid: hold the same global Chat send gate through assistant persistence and reject deletion while it is held.
Scoped Chat rollback must not use whole-worktree Git clean/restore — Root cause: convenience-agent changes can overlap with unrelated user edits — How to avoid: audit the exact changed paths and restore/remove only those paths, preserving ignored baselines.
Cancellation can still race a Chat answer write if checked before the session lock — Root cause: task cancellation and session persistence use independent locks — How to avoid: re-check cancellation inside an atomic session append predicate while holding the session mutation lock.
Chat generation can overlap after a session switch even when the current composer looks idle — Root cause: `generating` is scoped by `sendSessionId === activeSessionId`, but the store owns only one global `sendTaskId`/stream slot, so a second send replaces the first task's UI owner — How to avoid: model Chat runs by task and session (or explicitly disable every composer while one global run exists), and clean stream buffers from durable task lifecycle events.
Discovery completion events can be missed while a Tauri listener reconnects — Root cause: source rows are created only after the durable scan task persists inputs, so an event-only UI never refreshes the session — How to avoid: poll the scoped task snapshot with a bounded timer and let the existing terminal reconciliation own the session refresh.
Scrollable agent output needs a per-pane pinned state — Root cause: Chat, Wiki Ask AI, and TaskLogDrawer have independent scroll containers, so one global follow flag either steals the user’s position or misses new output — How to avoid: track near-bottom state on each output pane, follow only while pinned, and show Back to latest only after an upward scroll.
Explicit task replacement cannot share list-race merge semantics — Root cause: background list fetches must preserve a newly-created active task, while project/recovery snapshots must remove stale terminal tasks — How to avoid: use replacement semantics for explicit snapshots and merge semantics only for race-prone list fetches.
Agent structured output can cross task/IPC and UTF-8 boundaries — Root cause: CLI events may arrive before send_chat_message returns, while HTTP byte chunks may split a CJK code point — How to avoid: buffer deltas by task id until binding, merge activity snapshots idempotently, and decode incomplete UTF-8 bytes incrementally.
Structured Agent stderr is not a safe transcript — Root cause: CLIs may print tool arguments, JSON diagnostics, or environment details outside stdout event frames — How to avoid: persist only a fixed diagnostic marker for structured runs; parse visible text and safe activity metadata separately.
Recovery actions were exposed before the state machine could execute them — Cancelled/Skipped/Paused items showed Retry but claim_item_for_run accepted only Queued/Failed, and ASR authorization had no UI path — Keep recovery actions typed end to end, test every visible action against a valid transition, and preserve terminal task snapshots when IPC returns stale queued data.

A task can finish or request confirmation before its IPC creation response resolves — Root cause: task events and IPC responses are independent timelines — How to avoid: reconcile pending work against global task snapshots and confirmation_requested events, preserve newer terminal/waiting states, and never start queued work from a failed session refresh.
Import source buttons looked idle while a durable discovery task was still scanning — The workflow ended the mutation in the command-creation `finally`, before the task reached a terminal state — Keep the add-path mutation open until the matching task event completes, and expose task progress from the global task store in the Import page.

Optimistic selection can be overwritten by a stale project/session response — Selection is intentionally changed before IPC returns, so rollback and response commits must use the captured project key and session epoch — Guard both optimistic rollback and server replacement with `isScopeCurrent`, and disable the checkbox while the item action is pending.

Native PDF routes were treated as capability-pack inputs — built-in PDF execution received a staged path with the original source identity, then failed its own hash check — Only materialize inputs for non-builtin capability engines.
Task creation returned before the item/session task binding was visible — Clearing item pending immediately after `startItems` re-enabled Start/Cancel during queued/running work — Keep item pending keyed by returned task until a terminal event, and release only untracked or terminal task results.

Commit button disabled only after React rerender — A fast double click can enter the async confirm callback twice before the disabled prop updates — Guard `confirm` from the synchronous store snapshot before creating the commit task.

WeChat verification HTML was imported as article text - the specialized connector existed but was not registered, so the generic HTTP engine accepted a 200 challenge page - Register domain-specific engines before relying on generic extraction and classify challenge pages as user-wait states.
Import task progress used a fixed four-stage total — Rendering 1/4…4/4 as completion percentage misrepresented long extraction/fetch work — Treat stage progress as indeterminate until the backend supplies a measured bounded metric; reserve percentages for source/page/byte counts.

File discovery had no session items until the scan task completed — Queue statistics displayed 0/0 while the task already knew how many files were found — Surface discovery task counts in the header and queue during the scan.
The scan task could finish before `addPaths` registered its pending map — A later terminal event then had no session-refresh owner and the source mutation could stay busy — Preserve any newer terminal task already in the global task store when the IPC response arrives and settle from that snapshot.

Batch dismiss cleared task IDs while work was still active — The Import surface lost its only batch-level cancel affordance while backend tasks continued — Permit dismiss only after all tracked tasks are terminal; active work must stay visible and cancellable.
Queue rows contain checkboxes and action buttons — unconditional row Enter/Space handling intercepts child controls and listbox semantics imply unsupported arrow-key behavior — Use a plain list/listitem model and handle row keys only when the row itself owns focus.
History load failure looked like an empty history — The initial list request was swallowed and null page rendered the same empty state — Keep loading, error, and empty states distinct and provide an explicit retry.
Backend recovery actions can exist without frontend IPC support — Rendering every enum as a button creates dead controls — Only expose actions with a real handler (currently task log), and keep unsupported parser/OCR/skip actions as an explicit contract gap.
Late task snapshots can arrive after terminal events — Replacing the task store wholesale lets queued/running IPC data regress a completed task and freeze batch progress — Merge snapshots by task id with timestamp/terminal-state precedence, and cover setTasks after an event with a regression test.
An empty task snapshot is not always authoritative — A startup fetch can race task creation and briefly return no rows — Preserve in-memory active tasks on empty snapshots, while still allowing an empty terminal-only list to clear after cleanup.
V2 history files were being projected as legacy and omitted from the V2 list — Root cause: the presentation command returned an empty V2 entry array and the legacy adapter scanned the same directory — How to avoid: classify persisted records by stable V2 shape and keep V2/legacy pagination in one cursor space.
History inspection reused the recovery-oriented session command — Root cause: one get-session IPC path mixed bootstrap recovery with presentation reads — How to avoid: keep recovery commands explicit and expose a read-only historical session command for details/preview flows.
Historical detail drifted after a later import — Root cause: the UI loaded the current mutable session instead of the batch’s durable result evidence — How to avoid: persist a batch-scoped session snapshot and require history detail reads to carry the batch identity.
Offset pagination repeated or skipped records when new history arrived — Root cause: page boundaries were positions in a changing list — How to avoid: sort by a deterministic key and use an opaque cursor with a request high-water mark.
Connector chips claimed support from hard-coded labels — Root cause: frontend availability was disconnected from registered engines and installed capability packs — How to avoid: return typed platform readiness from backend route/capability state and localize only the presentation reason.
History snapshot was corrected with a second post-commit write — Crash-boundary tests then observed an extra journal cycle and recovery semantics diverged — Update the durable history snapshot in the same successful item transaction before serializing the history target.
History preview metadata pointed to mutable staging while the session snapshot looked immutable — A later session refresh or staging cleanup could change what the history dialog displayed — Copy the committed Markdown into a batch-scoped history-preview directory in the same transaction and verify its full hash on read.
Three-way merge UI had all evidence panels but no user-owned merged buffer — Apply merged reused Agent Markdown and NeedsMerge remained uncommittable — Give conflicts an editable merged buffer, bind it to the displayed Wiki hash, and persist an item-level commit decision before refreshing the session.
Import batch state was coupled to the currently visible workspace and treated missing task records as active — Leaving Import cleared the only batch context, while task-store cleanup made batches impossible to dismiss — Keep session/batch state scoped to project rather than active view, count missing tasks as unavailable, and never cancel a task that is no longer present.
History actions had generic labels and result actions stopped at the detail dialog — Repeated buttons lose their target context for keyboard/screen-reader users, and “open result” required a second click — Add localized contextual aria labels and route a historical result directly to the Markdown preview when evidence exists.
Unified checks can pass frontend build while the Rust integration suite fails on a host-owned AppData temp file — The test harness writes `.settings.json.tmp` under `%APPDATA%`, which may be denied by the desktop sandbox or host ACL — Record the exact path and error, verify Import V2/library/integration subsets separately, and do not attribute the failure to unrelated import changes without reproducing it in a writable test profile.
Import batch context stored as one in-memory task-id array — Parallel starts overwrite cancellation/progress context, and session taskId may be written after task creation — Persist a batchId on every child task, aggregate by batch, and cancel by durable task identity.
Task registry hydration races with Import recovery — Missing task snapshots can look unavailable before the restart recovery list arrives — Gate batch dismissal on explicit project task hydration and only then classify missing tasks as unavailable.
Session refresh can return after a newer user mutation — A late getSession response can overwrite a fresh selection or newly added source — Capture a mutation revision at refresh start and apply only when it is still current.
Waiting-for-confirmation was treated as one semantic state — preview-ready tasks and login/capability waits share the backend task status but require different user guidance, and pending item locks otherwise never receive a terminal event — Clear pending on the task_updated boundary, classify with the durable item status, and exclude all waiting tasks from cancellation while preserving the specific next action.
Queued items remained bound to interrupted pre-spawn tasks — Persisting task identity before worker spawn improves restart continuity but leaves a stale binding if recovery marks the task failed — During recovery, release failed/missing bindings for queued items before allowing a new retry task.
An old async page response could be safely ignored but leave the UI stale — Mutation revision guards dropped the response without requesting a newer snapshot, and history pagination lacked the current request generation — Reissue a scoped refresh after a raced session response and validate pagination responses against the active request generation.
A task can finish or request confirmation before its IPC creation response resolves — Root cause: task events and IPC responses are independent timelines — How to avoid: reconcile pending work against global task snapshots and confirmation_requested events, preserve newer terminal/waiting states, and never start queued work from a failed session refresh.
Markdown imports can disappear before parsing when discovery validates only a UTF-8 prefix — Root cause: legacy-encoded files are rejected during identification before the decoder sees them — How to avoid: classify text by extension, decode the full byte snapshot with explicit encoding support, and persist/display scan skip details.
[2026-07-18] 现象 — `npm run check` 的 97 个 Vitest 文件并发运行时，`App.test.tsx` 图谱 fallback 用例连续两次在 5–6 秒超时，但同一用例单独运行约 0.7 秒即通过。 — 根因 — 全套测试负载下 lazy Graph/fallback 的到达时间超过该用例固定等待窗口，属于 suite-load-sensitive 时序不稳定；不是 Chat 文档变更导致。 — 规避做法 — 先单独重跑确认功能路径，再检查 lazy import/fake timers/等待条件，避免只增加固定 timeout；在修复前完整检查必须如实报告为失败，不能用单测通过替代统一 gate。
Cancellation can still race a Chat answer write if checked before the session lock — Root cause: task cancellation and session persistence use independent locks — How to avoid: re-check cancellation inside an atomic session append predicate while holding the session mutation lock.
Scoped Chat rollback must not use whole-worktree Git clean/restore — Root cause: convenience-agent changes can overlap with unrelated user edits — How to avoid: audit the exact changed paths and restore/remove only those paths, preserving ignored baselines.
Platform route registration is not platform extraction — Root cause: a built-in route can still be backed by generic HTML while signed provider/API capability packs are absent — How to avoid: expose route readiness from verified capability availability and keep generic HTML as a documented fallback with fixture tests.
Platform image or subtitle assets can leak remote URLs into the final Markdown — Root cause: rendering platform JSON first created external Markdown links, and extract-only mode skipped localization entirely — How to avoid: always replace remote image/media references with local assets or explicit non-retained markers, persist usable subtitles as extracted artifacts, and keep platform asset hosts on a closed allowlist.
XHS short links can fail after browser redirect — Root cause: routing, browser platform detection, and page confinement only recognized xiaohongshu.com — How to avoid: treat xhslink.com as the same platform, permit verified platform-host redirects, and keep asset-host DNS checks fail-closed.
[Browser fallback must run before anonymous platform HTML routes — Root cause: an installed browser capability cannot reuse the persistent connector profile if the generic HTTP route consumes the URL first — How to avoid: order platform URL routes as browser capability, then verified provider parser, then anonymous generic fallback; keep browser prefetch disabled for the browser route.
ASR recovery actions must match the actual capability route — Root cause: a legacy media.subtitle label and Bilibili-only authorization action were reused for all platform media errors — How to avoid: map missing media transcription to `asr-whisper` / `media.asr` and do not expose platform-specific authorization actions for other platforms.
Browser packs must not receive `import-web-target:*` as their navigation URL — Root cause: secure references are intentionally opaque outside Rust, but Playwright needs the one-shot request URL — How to avoid: resolve the reference immediately before spawning the browser process while keeping the public locator separate for persistence.
Allowlisted platform CDN hosts must not be continued by Chromium — Root cause: route-time DNS validation followed by `route.continue()` permits a fresh resolver decision — How to avoid: abort browser asset requests, emit typed remote-asset notifications, and fetch them through the Rust pinned HTTP client.
Platform classifier hosts are broader than safe cookie-navigation hosts — Root cause: wildcard subdomains can receive shared domain cookies from a persistent profile — How to avoid: keep broad classification for recognition but use an explicit first-party navigation allowlist and pin every allowed redirect host.
Short-link login may authenticate on a different first-party origin — Root cause: `context.cookies([sourceUrl])` only observes cookies applicable to the short-link host, while the redirect target may own the login cookie — How to avoid: inspect the explicit platform navigation host set and filter domain cookies before writing the OS backup.
Vitest cannot start when Tailwind Oxide's Windows native binding is corrupted — Root cause: the checked-in/current `node_modules` binary is not valid UTF-8 and Vite also receives `spawn EPERM` during config externalization — How to avoid: repair/reinstall the platform-specific dependency before treating `npm run check` as a code regression; keep Rust, TypeScript, lint, console, and browser-policy checks separate for diagnosis.
Borrowing an import item from an inline `load_session(...)` result fails with E0716 — Root cause: the session temporary is dropped at the statement boundary while `capability_for_item` still borrows the item — How to avoid: bind the loaded session first, then borrow from `session.items`.
Cookie backup must validate both cookie name and an approved navigation-domain scope — Root cause: checking a cookie domain against itself made any third-party cookie with a platform auth name eligible for backup — How to avoid: compare normalized cookie domains only against exact approved platform hosts or their parent domains, then validate the auth-cookie name separately.
Subtitle candidate URLs are not transcripts — Root cause: browser capability marked any discovered subtitle URL as verified before Rust fetched and parsed it, allowing malformed JSON/ASS tracks to skip ASR — How to avoid: emit local-ASR fallback whenever authorized and let the Rust parser decide whether the first usable subtitle exists.
Platform signed query keys must be redacted consistently in browser and Rust snapshots — Root cause: XHS `xsec_token` was not covered by the sensitive-key lists — How to avoid: keep browser/runtime and persisted-artifact redaction key sets synchronized and test platform-specific signed URLs.
Raw source snapshots must not be rewritten during remote-asset localization — Root cause: replacing `asset://` markers in the source artifact mixed derived Markdown paths into immutable evidence — How to avoid: localize markers only in the candidate; sanitize capability text artifacts once at the trust boundary before commit.
Browser fallback must not accept a platform article with no platform evidence — Root cause: a Readability fallback reported full text coverage even when no media, subtitle, or usable platform image was extracted — How to avoid: return a typed structure-changed result so the platform-specific route gets a chance to run.
Subtitle fetches must reject HTML and require a valid timeline — Root cause: broad `text/*` acceptance plus a permissive line parser treated login/error pages as transcripts — How to avoid: allow only known subtitle MIME types and require a parsed timestamp before declaring transcription ready.
Root-level Cargo target directories can break the unified lint — Root cause: ESLint scans a repository-root `target/`, including generated Tauri JavaScript, when an ad hoc `CARGO_TARGET_DIR` points there — How to avoid: use the normal `src-tauri/target` or an already ignored external target path, and remove only verified task-generated caches before rerunning `npm run check` from the beginning.
Capability source manifests are not installable release artifacts — Root cause: placeholder hashes, empty target triples/catalog/trust store, and absent native runners/models cannot satisfy signature or runtime-integrity verification — How to avoid: keep runtime fail-closed and publish per-target browser/SenseVoice/OCR ZIPs from release CI with exact inventories, trusted public-key signatures, license evidence, and catalog metadata before claiming first-use installation works in production.
Sherpa accelerator selection can silently fall back while exiting successfully — Root cause: `sherpa-onnx-offline --provider=cuda` may log `Fallback to cpu!`, return valid output, and exit 0 when the binary lacks a usable CUDA provider — How to avoid: inspect bounded stderr for the explicit provider fallback signal, rerun with CPU, and record only the provider actually used.
Capability runners writing through a fixed intermediate directory inside a user-visible staging tree can follow a pre-created symlink/junction outside the tree — The staging root is validated, but a reusable child directory remains replaceable by another local process — Create each random output directory atomically and directly under the resolved staging root; never reuse an attacker-nameable intermediate path.
Capability wrapper timeout is shorter than the runner's own staged timeouts — Long CPU ASR is killed by the Rust parent even though FFmpeg and sherpa are still within policy — Store timeout per PackSpec and make the outer ASR timeout exceed the sequential decode plus inference maxima.
Top-level Python wheel license metadata omits redistributed native-library terms — opencv-python bundles FFmpeg/Qt-family LGPL components and Shapely bundles LGPL GEOS — Inspect staged wheel license evidence and include redistributed native terms in the aggregate manifest, accepted-license list, NOTICE, and SBOM.
Capability routing advertises extensions the runner rejects — Valid imports are selected for a pack and then fail after installation — Keep registered extension lists identical to the runner's tested decoder allowlist, and add common web containers such as WebM/Opus to ASR routing.
Security-bound capability output prefixes apply to test doubles too — Root cause: an ASR integration fixture returned a legacy `.fixture-asr-output` workspace after the orchestrator began accepting only direct-child `.sensevoice-output-*` paths — How to avoid: make fake runners obey the same signed-runner output contract so boundary tests verify behavior instead of bypassing it.
Platform bootstrap payloads often contain both the requested post and recommendations — Root cause: recursively taking the first object with title/media fields can import a sidebar or feed recommendation instead of the URL target — How to avoid: derive the platform content ID from the final redirected URL, scope extraction and retained API evidence to a matching nested object, and fail closed when that target is absent.
Security-bound capability output prefixes apply to test doubles too — Root cause: an ASR integration fixture returned a legacy `.fixture-asr-output` workspace after the orchestrator began accepting only direct-child `.sensevoice-output-*` paths — How to avoid: make fake runners obey the same signed-runner output contract so boundary tests verify behavior instead of bypassing it.
Platform bootstrap payloads often contain both the requested post and recommendations — Root cause: recursively taking the first object with title/media fields can import a sidebar or feed recommendation instead of the URL target — How to avoid: derive the platform content ID from the final redirected URL, scope extraction and retained API evidence to a matching nested object, and fail closed when that target is absent.
Windows 下 `git worktree prune` 可能无法删除指向不存在目录的管理记录 — Git 返回 Invalid argument — 对已核实为 prunable 的 `.git/worktrees/<name>` 目录做定点清理，并再次用 `git worktree list` 验证
Windows Tauri startup can overflow before the first window appears — Root cause: signed capability discovery and hashing ran synchronously on the GUI main thread, whose development executable reserves a 1 MiB stack — How to avoid: perform deterministic startup loading on the named 8 MiB capability worker and keep a 1 MiB-stack regression using a real signed development pack.
Capability runtime trees must not be traversed recursively — Root cause: a deeply nested or hostile installed pack can consume the loader thread stack even after increasing its reserve — How to avoid: use a bounded iterative directory walk, reject symlinks and special files, and enforce entry-count and depth limits before hashing.
Bilibili HTML success must not outrank parsed API evidence — Root cause: the page edge can return compressed or anti-bot bytes with a successful HTTP status, and the engine previously decoded those bytes even after the public JSON API had produced a valid video document — How to avoid: prefer validated Bilibili API evidence for exact BV/AV targets, keep HTML only as fallback, and test against unreadable page bytes plus a live public BV.
Recoverable provider errors can be lost during fallback — Root cause: a later route's generic validation or structure error overwrote an earlier subtitle-unavailable result that carried the required local-ASR action — How to avoid: retain actionable recovery errors separately from the last diagnostic error, prefer them only after all providers fail, and regression-test mixed failure sequences.
Sigma node dragging makes later layout resets appear clipped or strangely scaled — Dragging needs a fixed `customBBox` so graph-to-viewport normalization does not shift under the pointer, but leaving that box installed freezes the old extent — Keep the custom box during manual dragging, then call `renderer.setCustomBBox(null)` before any full ForceAtlas2 recompute.
Sigma emits `mousemovebody` before incrementing `draggedEvents` — Calling `preventSigmaDefault()` in a custom node-drag move handler stops camera movement but also bypasses Sigma's click-suppression counter, so mouseup may be followed by a synthetic node/stage click — Disable the camera for drag ownership, let real drag moves reach Sigma's counter, keep a pixel threshold, and suppress only the immediate post-drag stage click as a picking-buffer safeguard.
Import V2 visual states can silently lose semantic color — Root cause: the import stylesheet references `--success`, `--success-text`, and `.import-v2-item-status` tone classes without defining the tokens or selectors — How to avoid: keep semantic tokens and state-component selectors in the same CSS contract test, and verify ready/warning/failed states in both the matrix and queue.
Fast graph layout saves can finish out of order and persist stale coordinates — Fire-and-forget Tauri invokes have no completion ordering, even when mouseup events are ordered — Serialize saves and coalesce pending work so only the newest pending full-position snapshot runs after the active request.
SenseVoice can report an engine failure after FFmpeg successfully creates Windows ASR shards — Root cause: the session/item UUID hierarchy plus the full media SHA and model ID pushed shard WAV paths beyond the legacy 260-character Win32 boundary; sherpa-onnx's wave reader then reported the existing file as missing — How to avoid: place ASR shards in a short, capability-private workspace (or shorten bounded shard identifiers) and regression-test the native runner with absolute WAV paths on both sides of the Windows path-length boundary.
Structured CLI contracts must be parsed at their real final-output field — Root cause: Claude `--json-schema` returns the candidate object in `structured_output`, while a generic stream parser that only reads text/result silently hands an empty string to Source validation — How to avoid: pair CLI schema flags with parser coverage for structured final events, use Codex `--output-last-message`, and regression-test transport parsing before domain validation.
Long-running structured BYOK generation cannot reuse an interactive non-streaming completion contract — Root cause: a full candidate can reach the 120-second response boundary and an OpenAI-compatible provider may return text without a JSON-output constraint — How to avoid: stream the bounded background request with a long-task timeout, request provider-native JSON output where supported, preserve cancellation between chunks, and validate the assembled candidate once.
Agent detection contracts can regress after adding a required invocation flag — Root cause: fake CLI `--help` fixtures still advertised the previous capability set, so detection correctly changed from Installed to Failed even though runtime code was valid — How to avoid: update every fake help fixture and run detection contract tests whenever `help_supports_invocation` gains a required flag.
Repository text search can fail even when `rg.exe` is present — Workspace execution policy can deny launching the bundled executable — Fall back to PowerShell `Select-String`, and enumerate mixed file/directory paths one at a time before piping them into the search.
Memory-only workflow persistence flags can accidentally disable legacy explicit snapshots — Root cause: applying `persistence_enabled` inside public `persist_task` changes its established explicit-write contract, while the flag is only meant to suppress automatic `persist_current_task` writes — How to avoid: gate automatic persistence at the caller and keep explicit `persist_task(id, root)` authoritative; test both ephemeral workflows and legacy explicit persistence.
Persisted record identifiers are untrusted path input even when the surrounding JSON schema is valid — Root cause: recovery can deserialize a hostile `task.id` and later reuse it to derive an atomic rewrite path outside `.app/tasks/`; symlinked state or candidate directories create the same boundary failure — How to avoid: require canonical UUID text equal to the snapshot filename, validate canonical directory containment before every write, and canonicalize candidate references before restoring confirmation state.
A first `capability_release` link failure need not be a persistent Rust-test failure — Root cause: one parallel full-gate attempt received an unexpected `link.exe` error, but a later standalone run completed 906 Rust tests and a fresh full gate passed — How to avoid: rerun the complete Rust suite with a realistic multi-minute link budget before classifying a Windows linker error as an application or toolchain blocker; only the final `npm run check` exit status is release-gate evidence.
Graphify AST extraction fails with WinError 5 in the workspace sandbox — Graphify uses local subprocess workers that the sandbox can reject even though scanning itself starts — Re-run the identical code-only extraction with narrowly approved elevated execution; it remains local and writes only graphify-out/.
Graphify's inline Python AST extraction emits BrokenProcessPool traces on Windows — `multiprocessing` cannot respawn an inline `<stdin>` main module — The extractor falls back to a correct sequential pass; use Graphify's CLI or a guarded script when clean parallel output matters.
An empty Update Wiki changed-source scope has two distinct UI meanings — Root cause: the backend uses an empty changed-source request to auto-detect changes, while a user explicitly clearing all visible sources means invalid input; collapsing both into the same array state either dead-ends Full-to-Changed or accepts an unintended empty selection — How to avoid: track the auto-detect intent separately from the structured scope, clear that intent on manual selection changes, and cover both paths with preparation regressions.
Responsive hooks can fail only in broad test suites when `matchMedia` stubs omit listener methods — Root cause: some legacy or minimal MediaQueryList doubles expose `matches` without either `addEventListener` or `addListener` — How to avoid: read the initial match synchronously, feature-detect both listener APIs, and allow a static no-listener fallback while keeping runtime change coverage in focused tests.
An 820px sidebar can hide labels yet retain its persisted desktop width — Root cause: the root element's inline `--sidebar-w-current` value outranks a narrow media-rule token assignment — How to avoid: make the stress-breakpoint custom-property override authoritative and verify the actual sidebar `clientWidth` in the real WebView, not only label visibility.
Agent lint stream failures must terminate the process tree before candidate cleanup — Root cause: reader errors, invalid UTF-8, output overflow, or a parent exit with descendant-held pipes can otherwise leave an Agent mutating after the task reports failure — How to avoid: count raw stdout and stderr bytes, surface every reader error, drain trailing events under the same cancel/deadline, require a structured terminal event, and kill the process tree on every failure path.
Layout-defined repair Source protection cannot trust request readOnlyRoots or a hardcoded wiki/sources snapshot — Root cause: native raw/extracted Markdown and compatible Source/Mixed roots may sit outside the Wiki root, while raw originals and backend-reserved roots must never enter an Agent-writable candidate — How to avoid: derive Source inputs and write scope from ProjectLayout, bind the request to that authority, copy only protected Markdown context, and compare each protected hash before manifest generation.
Packaged WebView2 command counting cannot assume Tauri invoke is monkey-patchable — Root cause: Tauri v2 exposes `window.__TAURI_INTERNALS__.invoke` as a non-writable, non-configurable property, so a source-free CDP wrapper cannot observe backend command counts — How to avoid: use an explicitly approved build-time/backend trace counter for packaged IPC evidence, or mark the metric Pending; never report an unobservable count as zero.
A Vitest case can time out while its final `waitFor` still owns the same deadline — Root cause: an outer default 5-second test budget and an inner 5-second `waitFor` overlap, so a valid 20-remount lazy-route contract can be killed before its assertion reports — How to avoid: keep the assertion and per-wait deadline intact, but give the outer integration test a larger explicit budget that covers setup plus the bounded waits.
Tauri updater checks can retain an unbounded duplicate manifest after a bounded preflight — Root cause: upstream tauri-plugin-updater 2.9 performs its own `response.json()` fetch without a response-size hook and stores `raw_json` on the returned Update — How to avoid: use the pinned local plugin patch that streams with `max_manifest_bytes`, compare its result to the independently bounded preflight candidate, and clear `raw_json` before registration.
Tauri updater installation is not a normal returning mutation on every platform — Root cause: on Windows a successful `Update::install` launches the installer and exits the process, so post-call cleanup and in-memory terminal state are unreachable — How to avoid: keep the install IPC unregistered until the backend has persisted the Batch 4B dirty/task/restart authorization state, and perform all required durable bookkeeping before calling install.
Linux-only child-process syscalls can pass Windows gates and fail the first macOS compile — `libc::prctl(PR_SET_PDEATHSIG)` was guarded by broad `cfg(unix)` in duplicated Import launchers — Route child setup through the shared cross-platform process-lifetime helper, which limits `prctl` to Linux/Android while preserving Unix process groups and Windows job semantics.
`CommandExt::pre_exec` hooks can deadlock or corrupt the child after `fork` even when the Rust compiles — the hook runs in a restricted post-fork environment where allocating/locking operations (including constructing owned error strings) are unsafe — keep the closure async-signal-safe and return allocation-free `io::Error::from_raw_os_error(...)` values
The Unix watchdog protects an attached process tree but is not a formal zero-instruction-window parent-death primitive — macOS has no `PDEATHSIG`, so an application crash after `spawn` succeeds but before the immediately following watchdog `attach` remains a narrow inherited limitation — keep the calls adjacent, test abrupt owner exit after attachment, and use an already-armed external supervisor before launch if a future contract requires absolute zero-window ownership
Graphify can finish Windows AST extraction and then fail the rebuild on `.graphify_labels.json.sig` with `EINVAL` — the watch rebuild path can reject its own signature path after backing up curated output, leaving dirty graph artifacts without a completed current graph — report the extraction and rebuild separately, preserve pre-existing graph changes, and never claim the graph refresh passed unless the final rebuild exits zero
