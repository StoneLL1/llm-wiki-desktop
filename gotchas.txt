Discovery completion events can be missed while a Tauri listener reconnects — Root cause: source rows are created only after the durable scan task persists inputs, so an event-only UI never refreshes the session — How to avoid: poll the scoped task snapshot with a bounded timer and let the existing terminal reconciliation own the session refresh.
Recovery actions were exposed before the state machine could execute them — Cancelled/Skipped/Paused items showed Retry but claim_item_for_run accepted only Queued/Failed, and ASR authorization had no UI path — Keep recovery actions typed end to end, test every visible action against a valid transition, and preserve terminal task snapshots when IPC returns stale queued data.

A task can finish or request confirmation before its IPC creation response resolves — Root cause: task events and IPC responses are independent timelines — How to avoid: reconcile pending work against global task snapshots and confirmation_requested events, preserve newer terminal/waiting states, and never start queued work from a failed session refresh.
Import source buttons looked idle while a durable discovery task was still scanning — The workflow ended the mutation in the command-creation `finally`, before the task reached a terminal state — Keep the add-path mutation open until the matching task event completes, and expose task progress from the global task store in the Import page.

Optimistic selection can be overwritten by a stale project/session response — Selection is intentionally changed before IPC returns, so rollback and response commits must use the captured project key and session epoch — Guard both optimistic rollback and server replacement with `isScopeCurrent`, and disable the checkbox while the item action is pending.

Native PDF routes were treated as capability-pack inputs — built-in PDF execution received a staged path with the original source identity, then failed its own hash check — Only materialize inputs for non-builtin capability engines.
Task creation returned before the item/session task binding was visible — Clearing item pending immediately after `startItems` re-enabled Start/Cancel during queued/running work — Keep item pending keyed by returned task until a terminal event, and release only untracked or terminal task results.

Commit button disabled only after React rerender — A fast double click can enter the async confirm callback twice before the disabled prop updates — Guard `confirm` from the synchronous store snapshot before creating the commit task.

WeChat verification HTML was imported as article text - the specialized connector existed but was not registered, so the generic HTTP engine accepted a 200 challenge page - Register domain-specific engines before relying on generic extraction and classify challenge pages as user-wait states.
Import task progress used a fixed four-stage total — Rendering 1/4…4/4 as completion percentage misrepresented long extraction/fetch work — Treat stage progress as indeterminate until the backend supplies a measured bounded metric; reserve percentages for source/page/byte counts.

File discovery had no session items until the scan task completed — Queue statistics displayed 0/0 while the task already knew how many files were found — Surface discovery task counts in the header and queue during the scan.
The scan task could finish before `addPaths` registered its pending map — A later terminal event then had no session-refresh owner and the source mutation could stay busy — Preserve any newer terminal task already in the global task store when the IPC response arrives and settle from that snapshot.

Batch dismiss cleared task IDs while work was still active — The Import surface lost its only batch-level cancel affordance while backend tasks continued — Permit dismiss only after all tracked tasks are terminal; active work must stay visible and cancellable.
Queue rows contain checkboxes and action buttons — unconditional row Enter/Space handling intercepts child controls and listbox semantics imply unsupported arrow-key behavior — Use a plain list/listitem model and handle row keys only when the row itself owns focus.
History load failure looked like an empty history — The initial list request was swallowed and null page rendered the same empty state — Keep loading, error, and empty states distinct and provide an explicit retry.
Backend recovery actions can exist without frontend IPC support — Rendering every enum as a button creates dead controls — Only expose actions with a real handler (currently task log), and keep unsupported parser/OCR/skip actions as an explicit contract gap.
Late task snapshots can arrive after terminal events — Replacing the task store wholesale lets queued/running IPC data regress a completed task and freeze batch progress — Merge snapshots by task id with timestamp/terminal-state precedence, and cover setTasks after an event with a regression test.
An empty task snapshot is not always authoritative — A startup fetch can race task creation and briefly return no rows — Preserve in-memory active tasks on empty snapshots, while still allowing an empty terminal-only list to clear after cleanup.
V2 history files were being projected as legacy and omitted from the V2 list — Root cause: the presentation command returned an empty V2 entry array and the legacy adapter scanned the same directory — How to avoid: classify persisted records by stable V2 shape and keep V2/legacy pagination in one cursor space.
History inspection reused the recovery-oriented session command — Root cause: one get-session IPC path mixed bootstrap recovery with presentation reads — How to avoid: keep recovery commands explicit and expose a read-only historical session command for details/preview flows.
Historical detail drifted after a later import — Root cause: the UI loaded the current mutable session instead of the batch’s durable result evidence — How to avoid: persist a batch-scoped session snapshot and require history detail reads to carry the batch identity.
Offset pagination repeated or skipped records when new history arrived — Root cause: page boundaries were positions in a changing list — How to avoid: sort by a deterministic key and use an opaque cursor with a request high-water mark.
Connector chips claimed support from hard-coded labels — Root cause: frontend availability was disconnected from registered engines and installed capability packs — How to avoid: return typed platform readiness from backend route/capability state and localize only the presentation reason.
History snapshot was corrected with a second post-commit write — Crash-boundary tests then observed an extra journal cycle and recovery semantics diverged — Update the durable history snapshot in the same successful item transaction before serializing the history target.
History preview metadata pointed to mutable staging while the session snapshot looked immutable — A later session refresh or staging cleanup could change what the history dialog displayed — Copy the committed Markdown into a batch-scoped history-preview directory in the same transaction and verify its full hash on read.
Three-way merge UI had all evidence panels but no user-owned merged buffer — Apply merged reused Agent Markdown and NeedsMerge remained uncommittable — Give conflicts an editable merged buffer, bind it to the displayed Wiki hash, and persist an item-level commit decision before refreshing the session.
Import batch state was coupled to the currently visible workspace and treated missing task records as active — Leaving Import cleared the only batch context, while task-store cleanup made batches impossible to dismiss — Keep session/batch state scoped to project rather than active view, count missing tasks as unavailable, and never cancel a task that is no longer present.
History actions had generic labels and result actions stopped at the detail dialog — Repeated buttons lose their target context for keyboard/screen-reader users, and “open result” required a second click — Add localized contextual aria labels and route a historical result directly to the Markdown preview when evidence exists.
Unified checks can pass frontend build while the Rust integration suite fails on a host-owned AppData temp file — The test harness writes `.settings.json.tmp` under `%APPDATA%`, which may be denied by the desktop sandbox or host ACL — Record the exact path and error, verify Import V2/library/integration subsets separately, and do not attribute the failure to unrelated import changes without reproducing it in a writable test profile.
Import batch context stored as one in-memory task-id array — Parallel starts overwrite cancellation/progress context, and session taskId may be written after task creation — Persist a batchId on every child task, aggregate by batch, and cancel by durable task identity.
Task registry hydration races with Import recovery — Missing task snapshots can look unavailable before the restart recovery list arrives — Gate batch dismissal on explicit project task hydration and only then classify missing tasks as unavailable.
Session refresh can return after a newer user mutation — A late getSession response can overwrite a fresh selection or newly added source — Capture a mutation revision at refresh start and apply only when it is still current.
Waiting-for-confirmation was treated as one semantic state — preview-ready tasks and login/capability waits share the backend task status but require different user guidance, and pending item locks otherwise never receive a terminal event — Clear pending on the task_updated boundary, classify with the durable item status, and exclude all waiting tasks from cancellation while preserving the specific next action.
Queued items remained bound to interrupted pre-spawn tasks — Persisting task identity before worker spawn improves restart continuity but leaves a stale binding if recovery marks the task failed — During recovery, release failed/missing bindings for queued items before allowing a new retry task.
An old async page response could be safely ignored but leave the UI stale — Mutation revision guards dropped the response without requesting a newer snapshot, and history pagination lacked the current request generation — Reissue a scoped refresh after a raced session response and validate pagination responses against the active request generation.
A task can finish or request confirmation before its IPC creation response resolves — Root cause: task events and IPC responses are independent timelines — How to avoid: reconcile pending work against global task snapshots and confirmation_requested events, preserve newer terminal/waiting states, and never start queued work from a failed session refresh.
