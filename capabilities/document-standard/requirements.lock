# Release-input lock. CI resolves this exact direct requirement and its
# docx/xlsx/pptx transitive wheels into a per-target, hash-locked wheelhouse.
# The distributable pack MUST NOT be published until verify-import-pack.ps1
# validates the generated wheelhouse lock, archive digest, SBOM and licenses.
markitdown[docx,xlsx,pptx]==0.1.0
